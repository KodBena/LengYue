# Appendix (part 2 of 3) — verbatim commissions and reports

Continuation of `audit-spa-history-lessons-2026-06-10-appendix-p1.md`:
main-run distillers, merge, and the 36 adversarial verifier verdicts.
The `[SHARED: ...]` factoring conventions and shared prompt segments are
defined in part 1 §0. License: Public Domain (The Unlicense).

## §2 Main run — distillers and merge

### §2 · distill:correctness

**Commission** (the following + `[SHARED: EVIDENCE-JSON]`):

````
You are a distiller in a retrospective review of the LengYue frontend (Vue 3 + TS SPA, /home/bork/w/omega/frontend; today undefined). Twelve evidence miners produced the findings JSON below. Your job: distill candidate REFACTORING OPPORTUNITIES / lessons-learned guidance through ONE lens. You may verify details in the repo (read-only; ADR-0002 read-fully discipline applies to any doc you cite beyond the findings). Propose at most 12 candidates for your lens; each must be grounded in at least one finding (name the source miner), be ADR-consistent (or explicitly name the ADR deviation it would require), respect ADR-0009 (no unsubstantiated perf claims — frame perf as hypothesis + required measurement), and honestly estimate cost and risk. Do NOT propose undoing documented by-design disciplines (rolling-archive migrations, committed generated wire types, the dispatch ledger) unless the evidence shows the discipline itself failing. Mark likelyAlreadyFiled with the todo-DB item id when a finding overlaps a filed item.

YOUR LENS: CORRECTNESS: silent-failure surfaces, trust-boundary gaps, state-machine fragility, places where the type system could police what review currently polices, recurring bug shapes that a structural change would prevent.
````

**Candidates returned (verbatim):**

````json
{
 "candidates": [
  {
   "id": "branded-path-types",
   "title": "Brand the two variation-path shapes (RootToLeafPath / RootToCurrentPath) so position-vs-path confusion is a compile error",
   "lesson": "Functions that act at a position but read global cursor/active-path state caused four real bugs in one arc (d62b51d, ec4cb3d, 59365c8, 87f7dfc — git-narrative). The class is policed today only by warning docstrings and reviewer memory, and the match postmortem's recommended call-site audit and branded-type intensification were never executed (postmortems). The codebase has twice proven this exact retrofit is cheap: proxy v1.0.21 identity branding and the 2026-06-08 RawKey/EnrichedKey arc (817 tests green, zero runtime change).",
   "currentState": "Fixes plus warning docstrings live at usePlayFromPosition.ts:224-236; 16 files under frontend/src still call getActiveVariationPath (verified at HEAD); no todo-DB item exists (verified by keyword SELECT). The e2e harness gap ec4cb3d named (fixtures only ever ran from fresh boards where current == leaf) is also unaddressed per the miners.",
   "proposal": "Brand the two path shapes as distinct types minted by getPath / getActiveVariationPath respectively, thread them through path-consuming APIs (the 16-file list is the worklist), and require an explicit named conversion where a site genuinely wants either. Add at least one test fixture with pre-existing forward variation so path-consuming APIs are exercised where current != leaf. File the work as a todo-DB item first per the SSOT discipline.",
   "axis": "correctness",
   "evidence": "git-narrative finding 4 (four cursor-proxy bugs); postmortems finding 4 (audit unexecuted, brands unbuilt, 16-file count); worklog-current finding 3 (five match-arc recommendations all dangling). 16-file usage re-verified at HEAD this session.",
   "cost": "1-2 sessions: mostly mechanical type threading plus a 16-site intent audit; test fixture is small.",
   "risks": "Brand erosion via casts if call sites are awkward (mitigate with the cast-hygiene lint candidate); a few sites may legitimately accept either path shape and need a named union or conversion rather than a forced choice.",
   "likelyAlreadyFiled": ""
  },
  {
   "id": "engine-subscription-union-narrowing",
   "title": "Narrow the KataGoResponse union at the subscription boundary; fail loudly on the error variant",
   "lesson": "A cast at a trust boundary erases the discriminated union the proxy deliberately ships (v1.0.13 AnalyzeResponse | MetadataResponse split). Error packets then flow silently down the happy path — the precise silent failure ADR-0002 forbids — and the swallow is invisible because the type system was told not to look.",
   "currentState": "analysis-service.ts:646 and :836 cast subscriber payloads `res as KataAnalysisResponse` (verified at HEAD); per mech-conformance, katago-client.ts:101-104 forwards error packets to per-id subscribers unconditionally, so onAnalysisUpdate processes mis-typed error responses, telemetry.recordPacket gets undefined turnNumber, and the error is swallowed on that path.",
   "proposal": "One discriminant-checking function at the subscription seam: narrow to the analysis variant, route KataErrorResponse to a loud structured-error surface (throw or the ApiError-style typed channel), delete both casts. Small diff; aligns SPA-side union handling with the proxy's discriminated-union contract.",
   "axis": "correctness",
   "evidence": "mech-conformance finding 4 (full trace from katago-client contract through onAnalysisUpdate). Both cast sites re-verified at HEAD this session.",
   "cost": "Small: one boundary helper, two call-site edits, a unit test feeding an error packet.",
   "risks": "Failing loudly will surface engine errors that were previously invisible — that is the point, but the presentation (toast vs console vs status surface) needs a deliberate choice so the loudness is usable, not just noisy.",
   "likelyAlreadyFiled": "silent-coercion-protocol-boundaries-audit"
  },
  {
   "id": "cast-hygiene-lint-and-typed-capability-metadata",
   "title": "Mechanize the cast-justification rule (ban bare `as any`) and type the per-capability proxy metadata",
   "lesson": "The 'a cast needs a justification or it doesn't ship' rule is review-only and held at roughly 50% in a 32-site sample — the corpus's own repeated lesson is that prose disciplines decay and only mechanical gates arrest recurrence (postmortems finding 1). Meanwhile new proxy wire fields enter the SPA via scattered local casts, defeating the hand-maintained-mirror discipline at exactly the trust boundary it exists to guard.",
   "currentState": "224 cast sites; sampled bare `as any` in engine/sgf-loader.ts:83/89, katago-client.ts:133, useReviewSession.ts:266 (mech-conformance). Capability metadata is typed Record<string, Record<string, unknown>> and available_value_bindings is read via casts at capability-injection.ts:203 and AnalysisControls.vue:48 (verified at HEAD), despite the learned-vf dispatch instructing a typed extension (docgraph-dispatch).",
   "proposal": "ESLint rule banning bare `as any` (CI already gates lint since 2026-06-01), with the project's existing escape hatch: a comment-adjacent justification or a brand()/helper-wrapped cast — the proxy's three-documented-casts mypy --strict precedent is the model. Pair with typed per-capability metadata interfaces (e.g. AdaptiveReevaluateCapabilityMeta carrying worst_quantile, extra_visits, available_value_bindings) so wire drift is a compile error at the single mirror site. Adopt at warn, burn down, ratchet to error.",
   "axis": "correctness",
   "evidence": "mech-conformance finding 3 (~50% conformance sample, worst offenders named); docgraph-dispatch finding 3 (untyped capability metadata, dispatch instruction unmet). Capability cast sites re-verified at HEAD this session.",
   "cost": "Lint rule cheap; the burn-down is the real cost (~120 bare sites extrapolated from the sample) — bounded by adopting at warn with a per-touch ratchet. Typed metadata interfaces are a small pure type-level change.",
   "risks": "A blanket ban can force awkward contortions at genuinely-untypeable seams — the documented-cast escape hatch must be real, or the rule trains people to write justifications that justify nothing. SFC template casts were unaudited by the miner, so the population estimate has error bars.",
   "likelyAlreadyFiled": ""
  },
  {
   "id": "keyed-cache-brand-at-construction",
   "title": "Make branded keys a construction-time rule for new keyed caches, not a periodic sweep",
   "lesson": "Composite keys over heterogeneous provenance is a proven recurring class: the analysis ledger's palette-swap stranding, its same-day twin in stability-trajectory-store (found only because the first fix's author looked), per-board single-slot maps, and KnobDomain before that. The branded-key + single-mint-site fix retrofits cheaply and turns wrong-key reads into compile errors; the gap is that discovery of each instance was reactive.",
   "currentState": "The 2026-06-08 brand arc closed known instances and a follow-up audit found no third instance of the dangerous class (worklog-current). But the rule lives nowhere enforceable, and the same week introduced a new stringly discriminator — the r:/e: configHash prefix on the persistence path — one week after stringly contracts were banned via G1 (git-narrative finding 12, low confidence on harm, single parse boundary today).",
   "proposal": "Codify in frontend/CLAUDE.md plus the pre-merge checklist: any new keyed cache mints a branded key whose declaration names the dependency set of the value (so a key leg the value doesn't depend on is visible at declaration). One bounded audit confirming the r:/e: prefix is parsed in exactly one place and cannot collide with legacy hashes — promote to a typed/versioned record (the v1/v2 bundle-union precedent) if either fails. Optionally a best-effort lint flagging Map keys built by template-literal concatenation of >1 variable.",
   "axis": "correctness",
   "evidence": "git-churn finding 2 (the twin-bug arc with commit ids); git-narrative findings 2 and 12; worklog-current finding 9 (brand retrofit cost: 817 tests green, zero runtime change).",
   "cost": "Doc rule + checklist line + prefix audit: small (hours). Lint heuristic: medium and likely noisy — optional.",
   "risks": "The corpus's own evidence says a prose-only rule will decay (postmortems finding 1); without at least the checklist anchor this candidate reproduces the failure it names. The lint heuristic will have false positives needing an annotation escape.",
   "likelyAlreadyFiled": ""
  },
  {
   "id": "blind-mode-ui-state-owner",
   "title": "Give blind-mode UI state an owner: snapshot/restore instead of unconditional writes (live preference-clobbering defect)",
   "lesson": "The multi-writer-no-owner shape the project already paid to learn (card-tree slot: per-writer gate called 'the band-aid' and superseded by an owner one day later) has a live near-twin in review flow. Per-writer fixes recur; owners stick.",
   "currentState": "Verified at HEAD: store.session.ui.showMoveSuggestions has four writers — useReviewSession.ts:282 (loadCard sets false for blind mode), :556 (finishCard sets true), :629 (endSession sets true), lib/keybindings.ts:217 (user toggle). finishCard/endSession write true unconditionally rather than restoring the pre-review value, so a user who keeps suggestions off has them force-enabled by every completed or ended review. treeExpanded is flipped at :283 and never restored at all.",
   "proposal": "A blind-mode owner inside the review session: snapshot the prior values of both fields on loadCard, restore them on finishCard/endSession (and on abort paths), mirroring the slot-ownership pattern from fb0159a. Decide explicitly what a mid-review user toggle means for the restore (probably: user toggle updates the snapshot). One unit test against the review-state fakes.",
   "axis": "correctness",
   "evidence": "arch-ergonomics finding 8 (writer enumeration, board-scope.md lesson lineage); git-churn finding 3 and work-status finding 5 (the owner-not-gates pattern record). All four writers re-verified at HEAD this session.",
   "cost": "Small: localized to useReviewSession plus one test.",
   "risks": "Restore semantics vs mid-review toggles need one deliberate decision; abort/teardown paths (closeBoard during review) must also restore or the snapshot leaks — the existing abort-controller registry is the place to hook. Per the standing memory, this >1-writer fix should get the out-of-frame hack-rationalization review before being called done.",
   "likelyAlreadyFiled": ""
  },
  {
   "id": "hydration-rebind-audit-and-barrier",
   "title": "Execute the deferred module-scope-vs-hydration audit and name the barrier idiom",
   "lesson": "One-shot binding of module-scope state against persisted/server truth races hydration. The audit that would have swept the class was recommended in the PBO postmortem, deferred in its worklog, never filed — and the class then recurred twice (tags-fetch hydration race, tenancy instance-cache leak), each fixed reactively. This is the only recurring failure shape the postmortem miner found still without a mechanical or structural net.",
   "currentState": "No todo-DB item (verified by keyword SELECT; tags-fetch-hydration-race is closed, covering one instance only). Fix precedents exist and are good: immediate-true rehydrate watchers (useQeubo/useAppBootstrap), disjoint boot write targets plus the persisted-profile 'user-authored data only' invariant (5b57d25), and the IDENTITY_SCOPED_CACHES registry. useAppBootstrap carries the repo's highest per-file fix ratio (5 fixes in 10 touches — git-churn).",
   "proposal": "File the item, then run the bounded audit: enumerate module-scope and component-instance state derived from persisted or server truth (analysis ledger, thumbnail caches, board-card-trees, forest roots, query telemetry per the postmortem's own candidate list); each instance must rebind reactively, register in IDENTITY_SCOPED_CACHES, or sit behind an explicit whenHydrated() barrier. Classify remaining profile fields against the user-authored-vs-server-derived invariant. Optional mechanization: a best-effort lint flagging store.profile reads inside one-shot bootstrap callbacks.",
   "axis": "correctness",
   "evidence": "postmortems finding 2 (audit named, never executed, class recurred); worklog-current finding 1 (deferral trail, registry template); git-churn finding 5 (useAppBootstrap fix ratio, 5b57d25 resolution standard).",
   "cost": "Medium: one bounded audit session plus small per-instance fixes; the lint heuristic is extra and optional.",
   "risks": "Enumeration can miss instances (mitigate with a grep over module-scope mutable declarations in services/composables as the audit's mechanical first pass); the lint is best-effort syntactic with named gaps, per the project's own guard convention — it must not be sold as complete.",
   "likelyAlreadyFiled": ""
  },
  {
   "id": "migration-silent-noop-guard",
   "title": "Make migration no-ops loud: post-migration leaf assertions for new migration bodies",
   "lesson": "Migrations are the one surface where ADR-0002 fail-loud is structurally absent: a hand-walked blob path that misses is indistinguishable from an absent field. The 47→48 F-optimizer retirement migration walked out.settings?.knobs instead of out.profile?.settings?.knobs, silently did nothing, stamped v48, and required 48→49 purely as a repair migration — a silent failure that reached persisted user blobs.",
   "currentState": "Migration bodies hand-walk optional-chained paths over untyped legacy blobs; per-migration unit tests exist (frontend/tests/unit/store/migrations.test.ts) but a fixture written by the same author shares the same wrong path assumption, so the test passes vacuously. The append-only rolling-archive discipline itself is sound and not in question.",
   "proposal": "For new migration bodies only (shipped bodies stay frozen): a small assertion helper — if the pre-image contains the targeted leaf, assert the post-image reflects the transformation, throw otherwise (loudness level 1). Alternatively or additionally, the composition-level invariant test the 2026-04 miner sketched: hydrating any legacy blob through migrate + updateFromRemote + immediate save persists the same key set as a clean migration of that blob. Check whether the existing suite already covers the composition before filing.",
   "axis": "correctness",
   "evidence": "worklog-current finding 8 (the 47→48/48→49 incident); worklog-2026-04 finding 5 (composition gap hypothesis); git-narrative finding 6 (retirement-path defect framing). Does not touch the documented rolling-archive design — it adds loudness inside the discipline.",
   "cost": "Small-medium: one helper plus a convention line; the composition test is one fixture-driven test.",
   "risks": "Fully typed accessors over historical blob shapes are impossible (old shapes predate current types) — the assertion form is the honest scope; over-eager assertions could reject genuinely-absent optional leaves, so the guard must condition on pre-image presence, exactly as the incident shape suggests.",
   "likelyAlreadyFiled": ""
  },
  {
   "id": "store-write-ownership-lint",
   "title": "Fix the mutator-bypass writes and mechanize store write-ownership with a writer-enumeration lint",
   "lesson": "The mutator discipline holds where review looks (zero structural store.boards writes outside the store) and frays where it doesn't: a board field is mutated via an unjustified as-any in a service, bypassing the boardsVersion bump that SyncService's debounced persistence keys on — a write that silently fails to persist. store.engine has a de facto owner the discipline never names, and knownTags needed a real bug (tag-mutation-ssot) before getting its chokepoint. The project's own recorded lesson is slots get owners, not per-writer patches.",
   "currentState": "Verified at HEAD: analysis-service.ts:463 `if (board) (board as any).maxVisitsTarget = visits` (the field is already optional on BoardState, so even the cast is unnecessary); 29 direct store.engine.* write sites in analysis-service vs one named mutator, with the disconnect-reset block duplicated at two sites (arch-ergonomics). Un-mutatored persisted-profile collection writes exist in useQeubo, useLocale, scenarioContext (mech-conformance).",
   "proposal": "Three steps in increasing size: (1) route the :463 write through mutateBoard and delete the cast; (2) name an engine-subtree owner — either extract an engine-connection module or document analysis-service as owner — collapsing the duplicated reset block; add named mutators for persisted profile collections (pinned bookmarks pattern). (3) Mechanize: an ESLint writer-enumeration rule for assignments to store.boards/store.engine/store.profile paths outside their owners, following the existing import-boundary and clear-needs-ownership lint precedents, with ADR-0001's sanctioned template UI-toggle exception carved out explicitly.",
   "axis": "correctness",
   "evidence": "arch-ergonomics findings 1 and 2 (write sites, persistence-miss mechanism, duplication); mech-conformance finding 7 (profile-collection writes, knownTags two-writer history); ADR-0001 Revisit #3 names exactly this drift class per the adr-triggers miner. The :463 site and 29-write count re-verified at HEAD this session.",
   "cost": "Step 1 trivial; step 2 small-medium (seam move in a 1181-line hot service — minimal-touch, not a rewrite); step 3 medium (AST member-assignment matching).",
   "risks": "The lint must encode ADR-0001's documented exception or it fires on sanctioned writes and gets disabled; the engine-owner extraction touches the analysis hot path, so it should ship behind the existing test suite plus a perf-battery null check per the standing memory (framed as regression insurance, not a perf claim).",
   "likelyAlreadyFiled": ""
  },
  {
   "id": "service-import-boundary-fail-closed",
   "title": "Invert the component-to-service ESLint boundary from blocklist to fail-closed",
   "lesson": "An enumerated blocklist is fail-open: the trust boundary silently lapses for every service added after the list was written. This is ADR-0002's posture applied to guard design — the boundary lint exists as 'the absent reviewer', but an absent reviewer who only knows four names by sight waves the fifth through.",
   "currentState": "eslint.config.js:166-179 restricts exactly four services (backend-service, library-service, analysis-service, analysis-persistence-service) from components; sync-service and qeubo-service are absent; any new effectful service is importable from components by default (arch-ergonomics). The eslint header's violation census is also stale (claims four violating components; grep finds none — adr-triggers), and the ADR-0010 Revisit #4 layering tension behind the rule is untracked in the work-status store.",
   "proposal": "Minimal: restrict src/services/** wholesale from src/components/**, exempting the reactive-state modules (analysis-ledger, analysis-config) and type-only imports explicitly — new services are then denied by default. Update the stale rationale-header census in the same change, and file the work-status item for the ADR-0010 #4 reconciliation the adr-triggers miner found missing. The fuller structural resolution (relocating reactive-state modules to a state/ band so the rule needs no exemptions) stays a named follow-up requiring deliberate sign-off, since it is a doc-graph-structural move.",
   "axis": "correctness",
   "evidence": "arch-ergonomics finding 5 (fail-open enumeration, exemption design); adr-triggers finding 5 (stale header census verified by grep, untracked Revisit #4 tension).",
   "cost": "Polarity inversion + header fix: small (one config edit plus a lint run to shake out type-only imports). The state/-band relocation: medium, separate arc.",
   "risks": "Inversion may surface legitimate existing imports the blocklist never saw — each needs an explicit exemption decision rather than a silent grandfather, which is some one-time review work; the relocation option changes documentation structure and triggers the doc-graph regeneration duty.",
   "likelyAlreadyFiled": ""
  },
  {
   "id": "enrichment-merge-null-validation",
   "title": "Validate, don't coerce, at the enrichment merge boundary (mergeRecords inner-null sharp edge)",
   "lesson": "A sharp edge named in the adaptive-deeper postmortem four weeks ago is still unguarded: mergeRecords skips top-level null/undefined values per key, but a non-null inner object whose fields are null-bearing replaces a populated entry wholesale. ADR-0002 Rule 4's calibration is validate-and-reject over silent coercion, and this is a documented silent-overwrite surface on the analysis data path.",
   "currentState": "Verified at HEAD: analysis-ledger.ts mergeRecords (~line 185) has exactly the guard shape the postmortem flagged. The post-stratification mergeEnrichment is documented as additive last-writer-wins with no visit gate by an accepted, recorded tradeoff — that decision is not in question here.",
   "proposal": "At the merge boundary, detect inner enrichment values that would replace a populated leaf with a null-bearing object and reject or loud-warn (warn-first, calibrated against real adaptive packet shapes, then promote to throw once the false-positive rate is known). Keep the no-visit-gate tradeoff as documented; this closes only the silent-overwrite case the postmortem named.",
   "axis": "correctness",
   "evidence": "postmortems finding 5 (§5.5 paper trail, current guard shape at analysis-ledger.ts:185-195, documented mergeEnrichment tradeoff). Guard shape re-verified at HEAD this session.",
   "cost": "Small: one validation pass inside an existing helper plus a unit test with a null-bearing packet fixture.",
   "risks": "No observed incident in four weeks, so urgency is low; a too-strict rejection could drop legitimate proxy packets during adaptive deepening — hence warn-first calibration rather than an immediate throw.",
   "likelyAlreadyFiled": ""
  },
  {
   "id": "outbound-wire-mappers",
   "title": "Close the outbound ACL gap: domain-to-wire mappers instead of snake_case construction in composables",
   "lesson": "The ACL rule — wire shapes appear only in the ACL, no other module sees snake_case — is contradicted on the outbound leg, and a rule that documentation itself contradicts erodes the authority of all sibling rules. The inbound mappers exist precisely because hand-assembled wire shapes drift silently; the outbound leg has the same exposure with less review attention.",
   "currentState": "useMinting.ts:108-154 builds the snake_case grading_parameter payload as Record<string, any> with a brand-erasure cast at :46; types.ts:2115-2130's own comment documents that no inverse-mapper exists for the create flow and names the composable as the construction site (mech-conformance). The promised gradingParameter.data known-keys ledger note was never written (docgraph-dispatch), so the keys under data are also undocumented.",
   "proposal": "Add outbound domain-to-wire mappers in backend-service.ts symmetric to the mapToReviewCard family; type the known data keys (gamma, default_visits, analysis_config) with an explicit open-keyed escape field for the rest; retire the Record<string, any>. Write the promised known-keys section into docs/wire-schemas.md in the same arc, since the mapper is its natural enforcement point. If instead the project decides the outbound exception is sanctioned, amend frontend/CLAUDE.md to say so — the contradiction is the unacceptable state, not either resolution.",
   "axis": "correctness",
   "evidence": "mech-conformance finding 5 (rule text vs construction site, with line cites); docgraph-dispatch finding 1 (the unfulfilled known-keys ledger promise and its dispatch trail).",
   "cost": "Small: one mapper + types + the wire-schemas section; the doc edit triggers no structural doc-graph change if it stays within the existing file.",
   "risks": "data is genuinely open-keyed today, so the mapper cannot be fully closed-typed — typing the known keys with a named escape is the honest scope; coordinating the wire-schemas note means touching a backend-shared contract doc, so the backend side should see the PR.",
   "likelyAlreadyFiled": "gradingparameter-opacity-typing"
  },
  {
   "id": "vue-lifecycle-footgun-guards",
   "title": "Mechanize the two expressible Vue footguns: boolean gate-prop defaults and module-intent state in <script setup>",
   "lesson": "Five separate investigations paid for Vue semantics traps, and the two costliest share a signature: correct-looking code whose scope or default silently differs from author intent, latent until a second consumer with a different lifecycle arrives. An omitted boolean prop casts to false (4756c30's active gate broke the intermission chart despite an 'other consumers safe' claim); 'module-shared' caches declared in <script setup> are actually per-instance (the MiniBoardCanvas texture flash). The corpus's own meta-finding is that prose records of such traps do not stop recurrence — only mechanisms do.",
   "currentState": "The lessons live scattered across ~5 worklogs (worklog-current finding 7); the open thumbnail-render-lifecycle-consolidation item covers the texture/render-resource instance class but not the boolean-prop class; no lint or checked reference exists for either shape.",
   "proposal": "Two best-effort guards plus one doc consolidation: (1) ESLint or test convention requiring explicit defaults on boolean props of shared components whose name reads as a gate (active/enabled/visible), plus an omitted-prop rendering test for opt-out gates; (2) a lint or convention forbidding module-intent mutable state (top-level let/Map/Set) declared inside <script setup> — require a plain <script> block or a separate module file, making the per-instance/module distinction syntactically visible; (3) fold the remaining footguns (v-memo under churning shallowRef, container-query self-styling, structuredClone vs reactive Proxy) into one checked frontend/CLAUDE.md section. Each guard ships verified against the original defect, per the project's RCA-minted-guard practice.",
   "axis": "correctness",
   "evidence": "worklog-current finding 7 (five investigations enumerated with worklog cites); git-narrative finding 5 (69810e2 boolean-prop defect contradicting 4756c30's safety claim; 463a15e per-instance scope); work-status finding 5 (the open consolidation item as anchor).",
   "cost": "Small-medium per guard; the doc section is an hour. The script-setup rule is the harder AST shape.",
   "risks": "Both lints are heuristic: the gate-prop name match and the module-intent inference will produce false positives needing an annotation escape — ship as best-effort syntactic with named gaps (the project's established guard posture), not as complete coverage. The boolean-prop class may partially overlap what strict vue-tsc template checking already polices; check that before writing the rule (adr-triggers finding 7 flags this as unassessed).",
   "likelyAlreadyFiled": "thumbnail-render-lifecycle-consolidation"
  }
 ]
}
````

### §2 · distill:auditability

**Commission** (the following + `[SHARED: EVIDENCE-JSON]`):

````
You are a distiller in a retrospective review of the LengYue frontend (Vue 3 + TS SPA, /home/bork/w/omega/frontend; today undefined). Twelve evidence miners produced the findings JSON below. Your job: distill candidate REFACTORING OPPORTUNITIES / lessons-learned guidance through ONE lens. You may verify details in the repo (read-only; ADR-0002 read-fully discipline applies to any doc you cite beyond the findings). Propose at most 12 candidates for your lens; each must be grounded in at least one finding (name the source miner), be ADR-consistent (or explicitly name the ADR deviation it would require), respect ADR-0009 (no unsubstantiated perf claims — frame perf as hypothesis + required measurement), and honestly estimate cost and risk. Do NOT propose undoing documented by-design disciplines (rolling-archive migrations, committed generated wire types, the dispatch ledger) unless the evidence shows the discipline itself failing. Mark likelyAlreadyFiled with the todo-DB item id when a finding overlaps a filed item.

YOUR LENS: AUDITABILITY-FOR-HUMANS: where a human auditor must reconstruct context by hand; oversized/mixed files; stringly-typed contracts; implicit coupling; places where simpler code or a sharper seam would make review and audit cheap. Simpler beats clever.
````

**Candidates returned (verbatim):**

````json
{
 "candidates": [
  {
   "id": "split-types-ts-along-banner-seams",
   "title": "Split types.ts along its own banner seams; retire the half of IDENTIFIERS.md that decays",
   "lesson": "When a file becomes a mixed hub (identifier brands + value objects + knob vocabulary + persisted GlobalStore schema in one 2362-line file), the project compensates with a hand-maintained lookup index (IDENTIFIERS.md) — and the index drifts (arch-ergonomics verified its line citations already wrong). A compensating index treats the symptom; the compiler-checked structure is the fix. Co-change data (git-churn: 90 touches, 46x with defaults.ts, 45 commits touching the types/defaults/migrations triple) shows every feature arc pays the hub tax.",
   "currentState": "frontend/src/types.ts is 2362 lines (verified; +117 in the week before the review per mech-conformance), mixing identifier types, knob vocabulary (~lines 472-803), qEUBO types, AppSettings/UISession, and GlobalStore. frontend/CLAUDE.md itself concedes the mix; IDENTIFIERS.md exists as the workaround and its citations have drifted. ADR-0007's type-catalogue exception says split 'along clean domain seams, not by line count alone' — the file's own section banners name the seams.",
   "proposal": "Split along the existing banners: types/ids.ts (brands + PerBoard), types/knobs.ts, a store-schema module (AppSettings/UISession/GlobalStore, colocated or paired with store/defaults.ts), per-domain value-object modules, wire-type aliases. A barrel re-export keeps the ~hundreds of import sites stable so the diff is mechanical. Re-point or shrink IDENTIFIERS.md to the parts a file split cannot carry. Update FILES.md entries in the same PR (the file-map discipline).",
   "axis": "auditability",
   "evidence": "git-churn finding 1; mech-conformance finding 8; arch-ergonomics finding 4. Verified this session: wc -l = 2362; banner seams and IDENTIFIERS.md drift are the miners' content-level reads.",
   "cost": "Medium: one mechanical PR (barrel keeps call sites stable), plus FILES.md and IDENTIFIERS.md updates. Build-time effect is an unmeasured hypothesis (types erase at runtime; no perf claim per ADR-0009).",
   "risks": "This is a batch split, which contradicts the recorded handled-on-touch policy on refactoring-queue-adr0007 (work-status finding 8) — it needs explicit maintainer sign-off as a named policy deviation, not a silent override. Merge-conflict exposure with in-flight branches is real given the file's churn rate; schedule between arcs.",
   "likelyAlreadyFiled": "refactoring-queue-adr0007"
  },
  {
   "id": "retire-hand-maintained-censuses-in-code",
   "title": "Hand-maintained counts, censuses, and ad-hoc identifiers in comments decay; use stable handles or registries",
   "lesson": "Three independent instances show prose enumerations in code misleading auditors: store/index.ts:331 says 'Four cleanups currently fire' above a list of ten (verified); eslint.config.js:82's rationale header asserts four component violations that greps show no longer exist (adr-triggers); closeBoard docstrings use O12–O15 tags that collide with the archived audit plan's different O12/O14 definitions, a collision flagged in May and grown since (worklog-2026-05). Meanwhile the one comment convention an audit found load-bearing was tagging landing sites with work-status item ids — the only substrate that survived squashed history (audits finding 6).",
   "currentState": "Stale census at store/index.ts:331; stale violation census at eslint.config.js:82-86; O-number collisions across store/index.ts docstrings (verified at lines 373-468) vs docs/archive/notes/resource-ownership-audit-plan.md; no todo-DB item tracks the collision.",
   "proposal": "Fix the three named instances now ('Four'→drop the count; correct or delete the eslint census; add descriptive slugs or work-status ids alongside the O-numbers rather than rewriting the archived plan). Adopt the convention: code comments cite stable handles (work-status item ids, the proven 'Item 30c' pattern) and avoid counts/censuses that a registry, test, or generated report could carry instead.",
   "axis": "auditability",
   "evidence": "arch-ergonomics finding 9; adr-triggers finding 5(b); worklog-2026-05 finding 1; audits finding 6. All three stale instances re-verified by grep this session.",
   "cost": "Small: three localized comment edits plus a one-paragraph convention note in frontend/CLAUDE.md.",
   "risks": "Low. Renumbering O-pairs would touch archive cross-refs — prefer additive slugs over rewriting frozen documents. The convention is policy-only (no mechanization proposed here), so it shares the decay risk it describes; pairing handles with the todo DB is what gives them a checkable anchor.",
   "likelyAlreadyFiled": ""
  },
  {
   "id": "deferral-filing-convention-and-harvest",
   "title": "Worklog 'What's deferred' sections are a shadow tracker; harvest them once and require an item-id-or-declined line",
   "lesson": "Deferrals recorded only in prose reliably evaporate, and the cost is concrete: the module-scope rebinding audit deferred in the 2026-05-17 worklog was never filed and the bug class recurred twice (tags-fetch-hydration-race, tenancy-instance-cache-leak); five match-arc postmortem recommendations, the useStabilityMetrics follow-up named 'the next step', retro roadmap items (KeepAlive contract), and ~15 smaller deferrals are all dangling. By contrast, every deferral that entered the todo DB is accounted for (worklog-2026-04: 'ledgered ones did not dangle'). The SSOT works when used; the leak is at authoring time.",
   "currentState": "Three miners independently enumerated dangling deferrals across 2026-04, 2026-05, and current worklogs plus retros and postmortems (worklog-current findings 1-5; worklog-2026-05 finding 5; worklog-2026-04 finding 1; retros finding 1; postmortems finding 3). The umbrella CLAUDE.md declares the todo DB the canonical record of deferred work, so the current state is a standing SSOT violation in practice.",
   "proposal": "One bounded harvest pass: triage the miners' already-compiled dangling lists into file / drop-with-dated-record / confirm-done. Going forward: every 'What's deferred' or postmortem-recommendation bullet ends with a todo-DB item id or an explicit grep-able 'not-filed: <reason>' marker; same for retro roadmap sections at retro close. Optionally a trivial advisory grep in CI for deferred-section bullets lacking either marker.",
   "axis": "auditability",
   "evidence": "worklog-current findings 1-5 (91 worklogs read end-to-end); worklog-2026-05 finding 5; worklog-2026-04 finding 1; retros findings 1-2; postmortems finding 3. The miners' DB keyword sweeps are the no-item evidence (capped at high-but-not-certain by their own coverage notes).",
   "cost": "The harvest is a day-scale doc+DB pass working from lists the miners already produced. The convention costs one line per bullet.",
   "risks": "Ceremony creep: if filing feels expensive, authors may stop writing deferral sections at all — the 'not-filed: reason' escape hatch must stay genuinely cheap. Keyword-sweep false negatives mean a few 'dangling' entries may already be filed under different wording; the triage pass should check before filing duplicates.",
   "likelyAlreadyFiled": ""
  },
  {
   "id": "doc-graph-dangling-signal-cleanup",
   "title": "De-noise the doc-graph dangling-reference report so the genuine-drift list is reviewable again",
   "lesson": "A drift report that is two-thirds noise by the project's own conventions stops being consulted: 55 of 125 live danglers originate from worklogs the consolidation note classifies as frozen ('do not edit to fix') while the generator treats only docs/archive/** as frozen; 31 more target one deliberately retired hub. Meanwhile real breaks hide in classes the gate cannot see at all — code→doc pointers (ADR-0010:169 cites tests/integration/render-locality/, actual dir is render-count/; MoveSuggestions.vue cites a moved audit path) and missing-on-disk vs outside-node-set are conflated.",
   "currentState": "docs/doc-graph-report.md lists 125 live danglers; CI deliberately does not gate on broken references 'because existing drift would make every PR red' (docgraph-dispatch). The discipline itself (committed graph, freshness gate) is working as designed — this is not a proposal to undo it, but evidence its drift-report half has lost signal.",
   "proposal": "In tools/doc-graph/generate.mjs: (a) align the frozen boundary with the working convention (worklog as frozen or a third bucket); (b) split dangling classes — missing-on-disk vs outside-node-set — and resolve/flag directory links; (c) tombstone handling for retired hubs like deferred-items.md. Once the genuine-action list is small, an advisory ratchet (no NEW live danglers) becomes feasible. Separately, either extend checking to code-comment→doc paths or adopt retrofit-on-touch for them, and fix the two verified stale pointers.",
   "axis": "auditability",
   "evidence": "docgraph-dispatch findings 5, 6, 7 (report + all dispatches + consolidation note read end-to-end by that miner); audits finding 4 (stale ADR-0010 and MoveSuggestions pointers); retros finding 5 (missing-on-disk vs outside-node-set conflation, directory-link skips).",
   "cost": "Small-to-medium tooling change plus one regeneration (a generator change altering report structure should regenerate in the same PR per the committed-artifact discipline).",
   "risks": "Frozen-boundary classification is a judgment the maintainer should confirm — encoding the wrong convention would institutionalize it. Gate history here counsels advisory-first: the pre-merge checklist's merge-blocking framing was tried and retracted, so the ratchet should start as a report section, not a red X.",
   "likelyAlreadyFiled": ""
  },
  {
   "id": "adr-record-hygiene-sweep",
   "title": "Record the fired ADR triggers, fix ADR-0001's now-false mutator description, and strip the committed tool-call artifact",
   "lesson": "The Revisit-when mechanism only works with a sweep cadence: two ADRs whose triggers fired carry in-place records (0005, 0009), two rotted silently (0001's reactivity-hot-spot trigger fired but the ADR still describes a mutateBoard identity-swap that was removed for causing whole-tree re-renders; 0003's file inventory is de-facto superseded by FILES.md). A reader auditing the mutation model today is actively misled by the canonical decision record. Separately, ADR-0010 ends with literal '</content>'/'</invoke>' harness-envelope lines, committed 2026-05-31 and surviving two later edits — tool-generated docs can leak envelopes into authoritative artifacts and nobody notices.",
   "currentState": "Per adr-triggers (all ten ADRs read end-to-end by that miner): ADR-0001 lines 97-107 misdescribe mutateBoard vs store/index.ts:157-171; 9 of 19 ADR-0003 inventory paths have moved while FILES.md carries 215 band tags; ADR-0010's trailing artifact verified by byte dump; todo item adr-effectiveness-audits (open) already wants a periodic per-tenet audit.",
   "proposal": "Four bounded edits following the project's own amendment convention: (1) amend ADR-0001 recording trigger #2 as fired and correcting the mutator-benefits enumeration; (2) amend ADR-0003 to delegate the per-file inventory to FILES.md, keeping only band definitions and the principle; (3) delete ADR-0010's two trailing lines and add a one-line CI grep for '</invoke>|</content>' over docs/; (4) fold a 38-trigger sweep checklist into the existing adr-effectiveness-audits item so this audit has a cadence instead of being a one-off.",
   "axis": "auditability",
   "evidence": "adr-triggers findings 1, 2, 3, 6 — that miner read all ten ADRs end-to-end and verified the artifact bytes and current store code. I have not independently read the ADRs; the executing session must read each amended ADR in full first (ADR-0002 read-fully discipline).",
   "cost": "Small: doc amendments + a one-line CI grep. The trigger-sweep fold-in is a DB description edit.",
   "risks": "Amendments must follow ADR-0005's authoring rules, which neither I nor the adr-triggers miner read end-to-end — the executing session reads it first. Amending ADR-0003 changes what 'canonical band definitions' points at; FILES.md band-tag correctness was not verified by the miner, so delegation should not be read as validation.",
   "likelyAlreadyFiled": "adr-effectiveness-audits"
  },
  {
   "id": "mechanize-cast-justification",
   "title": "Ban bare `as any` via the existing lint host; fix the two load-bearing casts on the analysis path",
   "lesson": "A review-only cast rule decayed to ~50% sampled conformance within weeks of being written down — the same decay pattern every postmortem documents for prose disciplines. Two surviving casts are load-bearing: analysis-service.ts:646/:836 erase the response union so KataErrorResponse flows into the packet path and is swallowed (the silent failure ADR-0002 forbids), and :463 does `(board as any).maxVisitsTarget = visits` — a direct board mutation bypassing mutateBoard, so no boardsVersion bump fires for persistence, with an unnecessary cast since the field is declared optional.",
   "currentState": "224 cast sites; ~17 of 32 sampled are bare (mech-conformance). frontend-ci has gated eslint since 2026-06-01, so the lint host exists. Verified this session: the :463 cast and 4 'See analyzeRange above' twin-sync comments are present.",
   "proposal": "(1) ESLint rule banning `as any` (error severity after a measured zero-or-triaged baseline, following the a75814c adoption pattern); a brand()/justifiedCast helper or comment-adjacent convention for the genuinely hard residue. (2) Fix the named sites: discriminant-narrow at the subscription boundary and fail loudly on the error variant per ADR-0002; route the :463 write through mutateBoard and drop the cast. (3) Record the rule's named gaps in the eslint header per the existing rule-rationale convention.",
   "axis": "correctness",
   "evidence": "mech-conformance findings 3 and 4 (32-site sample, union/contract reads at katago-client.ts:101-104, engine/katago/types.ts:498-501); arch-ergonomics finding 1 (:463 site, ADR-0001 drift-class framing). Cast at :463 re-verified by grep this session.",
   "cost": "Lint rule is small; triaging ~17+ bare sites is a bounded half-day; the two named fixes are small diffs each wanting a regression test (an error-variant packet through the subscription path).",
   "risks": "Some bare casts may hide typings that are genuinely hard to express — forcing bad types is worse than a documented cast, so the escape hatch must exist (the proxy's three-documented-casts precedent). Changing error flow at the subscription boundary alters runtime behavior on a hot path; needs the existing suite plus the new test, and a perf-battery run only if reactivity paths are touched (ADR-0009: no perf claim either way without measurement).",
   "likelyAlreadyFiled": "silent-coercion-protocol-boundaries-audit"
  },
  {
   "id": "extract-shared-analysis-query-builder",
   "title": "Collapse the analyzeRange/analyzeActiveNode near-twins into one pure query builder",
   "lesson": "Two ~200-line near-duplicate methods held in sync by four 'See analyzeRange above for the … rationale' comments make every review a manual two-copy diff, and the verbatim-duplicated clamp logic has no test that would catch divergence. The file's history (8 subject-level fix commits in 40 touches) says reviewers keep paying this tax. The codebase's own posture — pure logic before effectful glue — and the already-extracted pure helpers (winrate-framing, capability-injection) prove the seam works.",
   "currentState": "frontend/src/services/analysis-service.ts:451-671 and :673-858 per arch-ergonomics (file read end-to-end by that miner); 1181 lines total and absent from the ADR-0007 queue (mech-conformance: queue stale in membership). Four cross-referencing twin-sync comments verified by grep this session.",
   "proposal": "Extract a pure buildAnalysisQuery(descriptor) in the engine band returning the wire query from plain args, unit-testable at tier 1 with an explicit divergence test for the clamp logic; the two methods reduce to descriptor resolution + bookkeeping + subscribe. File the work as evidence on refactoring-queue-adr0007 rather than a new item, and note it satisfies handled-on-touch if bundled with the next analysis-service change.",
   "axis": "simplicity",
   "evidence": "arch-ergonomics finding 3 (full-file read, line-cited duplication); git-churn finding 2 (fix-commit concentration); mech-conformance finding 1 (file absent from size queue).",
   "cost": "Medium: behaviour-preserving refactor on the highest-churn service. Existing suite + new builder unit test; leg-by-leg diff review of the two copies during extraction.",
   "risks": "Subtle ordering differences between the two copies could be load-bearing rather than accidental — the extraction must treat each divergence as a question, not noise (the audit-disposition lesson from worklog-2026-05 finding 7). High churn means merge-conflict exposure; coordinate with in-flight analysis work. Perf assumed neutral but unproven — run the before/after battery if the change touches subscription or reactivity paths (ADR-0009).",
   "likelyAlreadyFiled": "refactoring-queue-adr0007"
  },
  {
   "id": "outbound-wire-mapper-symmetry",
   "title": "Add outbound domain-to-wire mappers (or amend the rule), and write the promised gradingParameter.data key ledger",
   "lesson": "A rule the codebase itself contradicts erodes the authority of all sibling rules: frontend/CLAUDE.md says the ACL is the only place wire shapes appear, yet useMinting.ts builds the snake_case grading_parameter payload as Record<string, any> in a composable, and types.ts's own comment documents the missing inverse-mapper. Separately, the dispatch ledger records a promised 'known keys under data' note as 'Not done' — an auditor typing that contract today starts from a grep instead of the promised authoritative list.",
   "currentState": "Verified this session: useMinting.ts:88/:108 carry the Record<string, any> construction with a comment acknowledging the mutation pattern. docgraph-dispatch (all 14 dispatches + wire-schemas.md read end-to-end) confirms no gradingParameter section in wire-schemas.md and the unfulfilled ledger-note promise; todo item gradingparameter-opacity-typing (open) covers only data.analysis_config typing.",
   "proposal": "(1) Add outbound domain→wire mappers to the backend-service ACL, symmetric to the inbound mapToReviewCard family, retiring the Record<string, any>. (2) Write the promised wire-schemas.md gradingParameter.data section enumerating known keys and their owners (gamma backend-read; default_visits, analysis_config frontend-owned). If the mapper is judged not worth it, the ADR-consistent alternative is amending the CLAUDE.md rule to name the sanctioned outbound exception — either way the rule and the code stop contradicting each other. Widen gradingparameter-opacity-typing's description to carry whichever is chosen.",
   "axis": "auditability",
   "evidence": "mech-conformance finding 5 (rule text at frontend/CLAUDE.md:60, construction site line-cited); docgraph-dispatch finding 1 (promise and 'Not done' record, both dispatch files read in full). Record<string, any> sites re-verified by grep this session.",
   "cost": "Small-to-medium: one mapper plus typing the create-flow fields, plus a doc section. The wire-schemas addition is a structural doc change — regenerate the doc-graph in the same PR.",
   "risks": "The mapper forces typing decisions for create-flow fields that may have no domain type yet — that is partly the point, but it can snowball; scope to the grading_parameter payload only. The cheaper amend-the-rule path trades a structural fix for honesty — acceptable, but say so explicitly rather than leaving the contradiction.",
   "likelyAlreadyFiled": "gradingparameter-opacity-typing"
  },
  {
   "id": "typed-capability-metadata-interfaces",
   "title": "Type the per-capability metadata mirror instead of scattering casts at consumption sites",
   "lesson": "The hand-maintained-mirror discipline for proxy wire types only pays when the mirror is typed: capabilities as Record<string, Record<string, unknown>> pushes drift detection out to local casts in capability-injection.ts and AnalysisControls.vue, so a proxy field rename fails silently at runtime instead of loudly at compile time — the exact brand-style migration the proxy already executed on its side (v1.0.21).",
   "currentState": "frontend/src/engine/katago/types.ts types capabilities loosely (the capability-advertisement region around lines 230-260 verified this session); available_value_bindings is read via casts at two sites per docgraph-dispatch; the learned-vf dispatch explicitly instructed extending the typed capability shape; wire-schemas §8 defers AsyncAPI, so this is the intermediate step, not a competing scheme.",
   "proposal": "Define per-capability metadata interfaces at the single mirror site (e.g. AdaptiveReevaluateCapabilityMeta carrying worst_quantile, extra_visits, available_value_bindings; selector metadata likewise), narrow once at the version-response boundary, and delete the consumption-site casts. Pure type-level change; no runtime behaviour at stake.",
   "axis": "auditability",
   "evidence": "docgraph-dispatch finding 3 (dispatch instructions and cast sites line-cited; that miner read all dispatches and wire-schemas.md end-to-end). Capability region of types.ts spot-verified this session.",
   "cost": "Small: one types.ts addition, two cast deletions, no migration.",
   "risks": "Drift moves from invisible to compile-error — which is the goal, but it adds a small mandatory SPA edit per proxy capability change; the mirror comment should name the proxy tag it tracks so the update is mechanical at bump time. Cross-boundary surface: type only what the proxy's tag annotations actually advertise, and flag any ambiguity via dispatch rather than guessing.",
   "likelyAlreadyFiled": ""
  },
  {
   "id": "relocate-app-vue-shared-css",
   "title": "Move App.vue's covert global stylesheet to assets/css; delete the dead selector",
   "lesson": "An unscoped style block in the root component is invisible coupling: at least six other components consume classes defined only in App.vue (.dark-input, .deck-dropdown, .settings-section, .toolbar-btn-sm, …), so an App.vue CSS edit silently restyles distant surfaces — the inverse of locally-reviewable change. The dead .tab-padding-sr rule surviving a month past its worklog deferral note is the same shadow-tracker failure in miniature.",
   "currentState": "Verified this session: .tab-padding-sr still defined at frontend/src/App.vue:624 with zero consumers (per worklog-2026-05's grep and arch-ergonomics' sweep); App.vue is 718 lines with a ~193-line style section against the ~150-line section budget arch-ergonomics cites from ADR-0007.",
   "proposal": "Move the shared chrome classes to assets/css (the contraction option frontend/CLAUDE.md already lists, per the miners), delete .tab-padding-sr, and leave App.vue's style block owning only App-local rules. Optionally add a grep-level dead-selector advisory to close the orphan-CSS class. The larger App.vue script extraction (board-move routing policy) is a separate, bigger candidate already covered by refactoring-queue-adr0007's handled-on-touch posture.",
   "axis": "auditability",
   "evidence": "arch-ergonomics finding 6 (full App.vue read, consumer grep across src/**/*.vue); worklog-2026-05 finding 4 (the deferral provenance and dead rule). Dead rule re-verified by grep this session.",
   "cost": "Small mechanical CSS relocation plus a click-through of the six consuming surfaces.",
   "risks": "Cascade-order can change when CSS moves between a component block and a global sheet — visual smoke per consumer is the verification, not build-green. Any value edits made while moving must respect the theme-token rules (no surface-1 backgrounds; read theme.css first per the standing memory). No perf implication asserted.",
   "likelyAlreadyFiled": "refactoring-queue-adr0007"
  },
  {
   "id": "services-boundary-deny-by-default",
   "title": "Invert the component-to-service lint from enumerated blocklist to deny-by-default",
   "lesson": "A boundary enforced by an enumerated blocklist of exactly four services is fail-open: sync-service and qeubo-service are absent, and any future effectful service is importable from components until someone remembers to extend the list. The stale rationale header (claiming four violating components that greps show resolved) demonstrates the list is not being maintained — the rule needs to be structural, not enumerated.",
   "currentState": "frontend/eslint.config.js:166-179 lists four services (per arch-ergonomics' full read of the config); the header census at :82-86 is stale (verified this session). The ADR-0010 Revisit #4 layering tension (reactive-state modules living in services/) is documented in three places but tracked in no work-status item (adr-triggers).",
   "proposal": "Near-term: restrict src/services/** wholesale and exempt the reactive-state modules (analysis-ledger, analysis-config) explicitly, so new services are denied by default; correct the header census while there. File the ADR-0010 #4 tension as a work-status item so 'when there is bandwidth' has an owner. Fuller resolution, as ADR-0010 itself suggests per the miners: relocate the reactive-state modules to a state/ band, making the rule structural ('components read state, composables orchestrate effects, services perform them') — that step interacts with ADR-0003's band definitions and should follow its amendment path.",
   "axis": "auditability",
   "evidence": "arch-ergonomics finding 5 (config read end-to-end, ACL classification of qeubo-service from types.ts); adr-triggers finding 5 (no-tracking-item DB queries, stale census greps).",
   "cost": "Polarity inversion is a small config change; run lint over the tree first to find legitimate exemptions before adopting at error severity. The state/ relocation is a medium structural move (imports, FILES.md band re-tags, doc-graph regeneration if docs move).",
   "risks": "An incomplete exemption list breaks CI on legitimate imports — measure before enforcing, per the project's own rule-adoption pattern (a75814c measured 0 hits first). The relocation half changes the band map and should not ride along silently with the lint change; two PRs.",
   "likelyAlreadyFiled": ""
  },
  {
   "id": "refs-rows-link-diagnoses-to-items",
   "title": "Link open items to their paid-for diagnoses with refs rows; retitle items to their residuals",
   "lesson": "An open item whose description says 'cause unclear' while a written diagnosis sits in docs/notes/audit/ forces the next investigator to re-derive an analysis the repo already paid for — the exact hand-reconstruction cost this lens targets. The same store also shows title-vs-residual drift (items whose work is substantially shipped but whose titles still describe the whole arc), which makes title-level dedupe by future sessions actively misleading. The contrast case proves the cheap fix works: nav-during-range-query-perf carries a refs row to its audit and is immediately reconstructable.",
   "currentState": "pv-hover-jank-range-query (open, 'cause unclear') and many-boards-open-slowness (open) have zero refs rows while perf-audit-nav-and-pv-hover-2026-05-27.md diagnosed both and its planned fixes verifiably landed (audits finding 2). nav-during-range-query-perf and i18n-string-sweep carry residual-only descriptions under arc-wide titles (work-status finding 9). spa-board-scope-consistency-audit appears substantially shipped but open (work-status finding 2) — same signature, needs a maintainer status check. Both item ids confirmed open this session.",
   "proposal": "(1) Add refs rows pointing the two perf items at the 05-27 audit. (2) Re-profile both symptoms before building any lever — the dropped rb3-packet-receive-chunking item records exactly why: a pre-refactor attribution re-measured at ~99ms vs the attributed ~2.35s (ADR-0009 applied to planning). Then close or re-describe. (3) Norm: an item created near a diagnosis doc gets a refs row at filing; on next touch, retitle open items to their residuals. (4) Surface the spa-board-scope-consistency-audit status question to the maintainer rather than closing it unilaterally — the audit doc may record deliberate residuals.",
   "axis": "auditability",
   "evidence": "audits finding 2 (refs-less items vs landed fixes, line-cited); work-status findings 2, 3, 9 (full items/refs/labels dump; rb3 lesson quoted verbatim from the DB).",
   "cost": "Minutes per refs row and retitle. The re-profiles are the real cost: two bounded perf-battery runs per the existing capture protocol.",
   "risks": "Near-zero for the refs rows. Re-profiling may invalidate the items' current descriptions — that is the desired outcome, not a regression. Closing spa-board-scope-consistency-audit without reading the audit doc end-to-end would violate the read-fully discipline; it is flagged here as a question, not a closure.",
   "likelyAlreadyFiled": ""
  }
 ]
}
````

### §2 · distill:ergonomics

**Commission** (the following + `[SHARED: EVIDENCE-JSON]`):

````
You are a distiller in a retrospective review of the LengYue frontend (Vue 3 + TS SPA, /home/bork/w/omega/frontend; today undefined). Twelve evidence miners produced the findings JSON below. Your job: distill candidate REFACTORING OPPORTUNITIES / lessons-learned guidance through ONE lens. You may verify details in the repo (read-only; ADR-0002 read-fully discipline applies to any doc you cite beyond the findings). Propose at most 12 candidates for your lens; each must be grounded in at least one finding (name the source miner), be ADR-consistent (or explicitly name the ADR deviation it would require), respect ADR-0009 (no unsubstantiated perf claims — frame perf as hypothesis + required measurement), and honestly estimate cost and risk. Do NOT propose undoing documented by-design disciplines (rolling-archive migrations, committed generated wire types, the dispatch ledger) unless the evidence shows the discipline itself failing. Mark likelyAlreadyFiled with the todo-DB item id when a finding overlaps a filed item.

YOUR LENS: ARCHITECTURAL ERGONOMICS: choices that discovered use cases have made wrong or unergonomic; satisfied-but-unacted ADR revisit triggers; seams the codebase keeps fighting; abstractions used once that should be dissolved, and repeated patterns that earned an abstraction.
````

**Candidates returned (verbatim):**

````json
{
 "candidates": [
  {
   "id": "split-types-ts-along-documented-seams",
   "title": "Split types.ts along its documented seams; let the split retire the decaying half of IDENTIFIERS.md",
   "lesson": "A single type hub becomes the co-change coupling point of every feature arc, and a hand-maintained compensating index (IDENTIFIERS.md) decays rather than compensates. When a file needs a lookup index to stay navigable, the structure — not the index — is the thing to fix.",
   "currentState": "frontend/src/types.ts is 2362 lines (verified by wc -l this session), touched in 90 commits, co-changing with store/defaults.ts 46x and forming a 45-commit triple with migrations.ts (git-churn). It mixes identifier brands, the knob vocabulary, qEUBO types, AppSettings/UISession/GlobalStore, and wire shapes (section ranges per arch-ergonomics, who read it end to end); IDENTIFIERS.md's line citations have already drifted (arch-ergonomics). mech-conformance measured +117 lines in one week.",
   "proposal": "Split into types/ids.ts (identifier brands + PerBoard), types/knobs.ts, a store-schema module colocated with defaults.ts (pairing each persisted slice's type with its default), per-domain value-object modules, and wire-shape aliases near the ACL — with a barrel re-export from types.ts so import sites stay stable and the split can land incrementally. Per mech-conformance's end-to-end read of ADR-0007, the type-catalogue exception itself prescribes 'split along clean domain seams, not by line count alone', so this is ADR-consistent in shape.",
   "axis": "ergonomics",
   "evidence": "git-churn finding 1 (co-change hub metrics); mech-conformance finding 8 (growth rate, ADR-0007 exception text); arch-ergonomics finding 4 (section ranges, IDENTIFIERS.md drift, two declared-dead types). Verified this session: wc -l = 2362; section dividers exist (e.g. types.ts:1655, :2113).",
   "cost": "Medium-large: mechanical but wide. Barrel re-export keeps the import-site diff near zero; FILES.md entries and doc-graph regeneration needed if doc cross-references move. Expect a week of merge friction for in-flight branches.",
   "risks": "Contradicts the recorded handled-on-touch policy on refactoring-queue-adr0007 (batch framing was explicitly retired per the work-status miner) — needs maintainer sign-off as a deliberate exception. Circular-import hazards between store schema and domain types must be checked first. Types erase at runtime, so no behavior risk; any build-time effect is an unmeasured hypothesis (ADR-0009).",
   "likelyAlreadyFiled": "refactoring-queue-adr0007"
  },
  {
   "id": "echarts-lifecycle-adapter",
   "title": "One audited ECharts lifecycle adapter instead of per-component renegotiation of the library contract",
   "lesson": "A library boundary whose semantics (setOption merge, notMerge-vs-clear, active gating, dispose) live in scattered call sites generates fix-of-a-fix chains: each chart surface re-learns the same contract, and a 'safe' change at one site regresses another.",
   "currentState": "BaseChart.vue is a 590-line <script setup> with a 2-line template (verified this session: script lines 20-610, template 612-614) — per mech-conformance, the cleanest composable-extraction candidate in the over-budget set. git-churn finding 4: 6 fix commits / 23 touches including a same-day fix-of-a-fix on clear semantics (a1dd749 -> 4c0c6a0) and an active-gating regression (4756c30 -> 69810e2, the omitted-boolean-prop footgun per git-narrative finding 5); CardTreeWidget <-> card-tree-echarts co-changes 7x.",
   "proposal": "Extract the lifecycle contract into one tested adapter/composable (init, themed re-init, setOption merge policy, empty-series-via-notMerge, explicit-default active gating, dispose) with tier-2 tests reproducing the historically regressed cases. BaseChart becomes a thin binding shell. Frame it as the small evidence-backed step toward the filed polymorphic-chart-renderer Port and record it as evidence on that item, not a competitor.",
   "axis": "ergonomics",
   "evidence": "git-churn finding 4; mech-conformance finding 2; git-narrative finding 5 (the prevention-surface defects that enumerate the adapter's test cases). Verified this session: BaseChart's script/template shape.",
   "cost": "Medium: one adapter plus migrating BaseChart and the card-tree imperative site; the regression test fixtures are already enumerated by history.",
   "risks": "Touches a render-path surface, so the before/after perf battery is required even though behavior is intended identical (ADR-0009 — no perf claim asserted here). Over-abstraction risk if the card-tree site's imperative needs diverge; the IO<T> lesson (git-narrative finding 7) applies — every adapter method needs at least two real call sites that spend it.",
   "likelyAlreadyFiled": "polymorphic-chart-renderer"
  },
  {
   "id": "scoped-state-registry-by-default",
   "title": "Scoped state is born into a registry; extend the drain pattern to closeBoard's hand-sequenced purges",
   "lesson": "Hand-enumerated cleanup at mutation sites does not stay complete as state grows — teardown was re-fixed four times before converging on registry-drained-by-mutator plus exact-set completeness test (IDENTITY_SCOPED_CACHES, BOARD_SCOPED_STORE_CELLS). The pattern has earned default status; the residual hand-ordered enumerations are the documented soft spot, and four shipped 2026-06 bugs share the unregistered-scoped-state root shape.",
   "currentState": "closeBoard still hand-sequences ~7 per-board purges with prose ordering constraints, and its docstring enumeration has rotted ('Four cleanups currently fire:' followed by ten items — arch-ergonomics finding 9). The registry covers only the Class A cells by documented judgment in board-scope.md (per arch-ergonomics's end-to-end read). work-status finding 5: tag-mutation-ssot, tenancy-instance-cache-leak, mini-board-texture-scope, tags-fetch-hydration-race all reduce to this class.",
   "proposal": "Extend the registry shape with explicit ordering phases (stop-engine / purge-caches / abort / pre-splice) so per-board owners register {phase, clear} and closeBoard becomes a phase-ordered drain with the existing completeness-test pattern. Adopt the authoring rule: new per-board / per-identity / shared-render state registers at birth, never joins a clear list. Fix the stale 'Four cleanups' docstring immediately regardless.",
   "axis": "correctness",
   "evidence": "git-narrative finding 1 (the four-fix convergence arc); worklog-2026-05 finding 2 (half-registry, half-manual state); arch-ergonomics finding 9 (rotted enumeration); work-status finding 5 (shipped bug cluster).",
   "cost": "Small for docstring + phase scaffold; medium to migrate the seven inline purges with their ordering encoded as data; completeness-test extension is small.",
   "risks": "board-scope.md records the Class B inline ordering as a deliberate judgment — the migration must preserve the ordering constraints exactly, or it converts a documented-correct shape into a subtle ordering bug. Partial overlap with the open thumbnail-render-lifecycle-consolidation item (the shared-render-resource leg); file findings as evidence there rather than duplicating.",
   "likelyAlreadyFiled": "thumbnail-render-lifecycle-consolidation"
  },
  {
   "id": "branded-path-types-and-callsite-audit",
   "title": "Brand the two path shapes (root-to-leaf vs root-to-current) and execute the deferred call-site audit",
   "lesson": "Global cursor/active-path state used as a positional proxy caused four bugs across two layers; the codebase's proven cure for same-shaped-but-different-meaning values is branded types (RawKey/EnrichedKey retrofit; proxy v1.0.21 identity branding), which demonstrably retrofit cheaply. Docstring warnings alone have not closed the class, and the recommended audit was never filed.",
   "currentState": "Fixes plus warning docstrings exist at two call sites in usePlayFromPosition.ts; 16 files still use getActiveVariationPath; the match postmortem's §5b audit and the branded RootToLeafPath/RootToCurrentPath intensification have no todo-DB record (postmortems finding 4, verified against the DB by that miner; worklog-current finding 3 lists all five dangling match-arc recommendations).",
   "proposal": "Mint RootToLeafPath / RootToCurrentPath brands at the two path helpers and thread them through the analysis/match call sites — the 16-file list is the worklist. Functions that act at a position take it explicitly rather than reading global cursor state (git-narrative finding 4's rule). Add a test fixture with pre-existing forward variation, the gap that let the original bugs pass e2e. File the item first so the work is SSOT-visible either way.",
   "axis": "correctness",
   "evidence": "postmortems finding 4; worklog-current finding 3; git-narrative finding 4 (the four-bug history and the test-fixture gap). The key-brand precedent's zero-runtime-change property is the worklog-current miner's report of the 2026-06-08 arc.",
   "cost": "Medium: ~16 files of mostly mechanical type-threading; precedent suggests no runtime change and a green-suite retrofit.",
   "risks": "Brands on arrays lose the brand through slice/concat — mint-site helpers must re-brand, and if cast pressure exceeds value the fallback is the audit-only leg (still worthwhile alone). No ADR deviation; this is ADR-0002 loudness moved to compile time.",
   "likelyAlreadyFiled": ""
  },
  {
   "id": "services-band-reconciliation-deny-by-default",
   "title": "Resolve the ADR-0010 services-band tension structurally: a state/ band plus a deny-by-default service lint",
   "lesson": "The services/ directory now hosts two architectures (effectful singletons vs reactive-state modules) reconciled by an enumerated ESLint blocklist that fails open — a newly added effectful service is component-importable until someone remembers to extend the list. ADR-0010's own Revisit #4 names this tension; it is satisfied-but-unacted and untracked in the SSOT.",
   "currentState": "EFFECTFUL_SERVICE_PATTERN enumerates exactly four services with reactive-state modules exempted by comment (verified this session, eslint.config.js ~166-179); sync-service and qeubo-service are absent from the list (arch-ergonomics finding 5). The config header's violation census is stale and no todo-DB item tracks the reconciliation (adr-triggers finding 5, who queried the DB).",
   "proposal": "Step (a), cheap: invert lint polarity — restrict services/** wholesale and exempt the reactive-state modules by name — and refresh the stale census header. Step (b), structural: relocate analysis-ledger / analysis-config (and stability-trajectory-store) into a state/ band so 'components read state, composables orchestrate effects, services perform them' becomes directory-structural and the exemption list dissolves. File the work-status item for Revisit #4 either way, and amend ADR-0010 to record the trigger acted on.",
   "axis": "ergonomics",
   "evidence": "adr-triggers finding 5 (untracked trigger, stale census — from end-to-end reads of ADR-0010 and eslint.config.js); arch-ergonomics finding 5 (fail-open polarity). Verified this session: the four-entry blocklist and exemption comment.",
   "cost": "Small for (a); medium for (b): file moves, import updates, FILES.md band re-tags, doc-graph regeneration where docs cross-reference the moved modules.",
   "risks": "(b) is wide but mechanical; it must preserve ADR-0010's display-leaf-reads-what-it-displays sanction or the inverted lint fires on sanctioned reads. Acting on a named revisit trigger is ADR-consistent, not a deviation, but the amendment itself must follow ADR-0005's authoring rules (not read by me this session — the amendment author should read it).",
   "likelyAlreadyFiled": ""
  },
  {
   "id": "multi-writer-slots-get-owners-engine-and-blind-mode",
   "title": "Give the remaining un-owned multi-writer slots owners: store.engine and the blind-mode UI pair",
   "lesson": "Per-writer gates on multi-writer slots get re-fixed within days; owners stick (the 3e11c38 'band-aid' superseded one day later by fb0159a's owner). The lesson is mechanized only for the card-tree slot; isomorphic slots remain convention-guarded, and one carries a live preference-clobbering defect.",
   "currentState": "store.engine has ~45 direct write references inside analysis-service.ts with the disconnect-reset block duplicated at lines ~169-192 and ~398-411 (verified this session), against one named store mutator; analysis-service.ts:463 writes a board field via '(board as any).maxVisitsTarget = visits', bypassing mutateBoard with an unjustified, unnecessary cast (verified). showMoveSuggestions has four writers — useReviewSession.ts:282/556/629 and keybindings.ts:217 (verified) — and finishCard/endSession force 'true' unconditionally instead of restoring the user's prior value; treeExpanded is flipped at review start and never restored (arch-ergonomics finding 8).",
   "proposal": "(a) Route maxVisitsTarget through mutateBoard and delete the cast. (b) Extract an engine-connection owner module for the store.engine subtree (collapsing the duplicated reset into one function) or explicitly document analysis-service as the subtree owner and lint other writers. (c) A blind-mode owner that snapshots and restores showMoveSuggestions + treeExpanded. (d) One bounded enumeration pass over remaining >1-writer GlobalStore cells, with the out-of-frame writer-enumeration review the project already practices for such fixes.",
   "axis": "correctness",
   "evidence": "arch-ergonomics findings 1, 2, 8; git-churn finding 3 (owners-stick arc, fb0159a body quote); work-status finding 5 (the institutionalized lesson the live twin contradicts). All writer sites re-verified by grep this session.",
   "cost": "Small for (a) and (c); medium for (b); (d) is a half-day grep-plus-judgment session.",
   "risks": "(c) changes user-visible behavior — force-enabling suggestions after review may be intentional pedagogy, so confirm intent before 'fixing'. (b) must not disturb the activeMode bookkeeping analysis-service threads through query minting. Adjacent but distinct from spa-board-scope-consistency-audit (board-scope cells, largely shipped per the work-status miner).",
   "likelyAlreadyFiled": ""
  },
  {
   "id": "dedup-analyze-range-active-node-pure-builder",
   "title": "Extract a pure buildAnalysisQuery and collapse the analyzeRange/analyzeActiveNode near-twins",
   "lesson": "Two ~200-line near-duplicates held together by 'see analyzeRange above for the rationale' comments are a divergence bug with no test to catch it. The churn data points the same direction the project's stated posture does: fix load concentrates in stateful glue (composables 22%, services 15%) while lib/ absorbed zero fixes — moving decision logic into pure, tier-1-testable modules is the correction-load-reducing move.",
   "currentState": "analysis-service.ts:451-671 vs 673-858 duplicate the first-report/cadence clamp verbatim, plus cacheFlags, capability injection, telemetry registration, and the restart-thunk pattern; five cross-referencing comments stand in for shared code (arch-ergonomics finding 3, from an end-to-end read). Pure helpers already extracted from this file prove the seam works.",
   "proposal": "Extract a pure buildAnalysisQuery(descriptor) in the engine band returning the wire query from plain args, unit-tested at tier 1 including the clamp edge cases; the two methods reduce to descriptor resolution + bookkeeping + subscribe. Golden-query equivalence tests (same inputs => identical query before/after) make the refactor checkable. Coordinate with semantic-clarity-refactors-effect-typing leg (a) — the onAnalysisUpdate multiplexer — since both reshape the same module.",
   "axis": "simplicity",
   "evidence": "arch-ergonomics finding 3; git-churn findings 2 and 6 (analysis-service as fix hotspot: 8 subject-level fix commits / 40 touches; pure-band zero-fix signal, with the miner's own small-denominator caveat).",
   "cost": "Medium. Behavior-preserving, but this is the app's core query path, so the equivalence-test scaffolding is part of the cost, not optional.",
   "risks": "Hidden coupling to instance state inside the duplicated blocks may make the extraction less pure than the miner's read suggests (it could not adjudicate ledger/sync internals). Any perf effect is a hypothesis only — run the perf battery if the hot path's allocation pattern changes (ADR-0009). Same-module overlap with the filed semantic-clarity item argues for one coordinated arc, not two.",
   "likelyAlreadyFiled": ""
  },
  {
   "id": "dissolve-app-vue-global-stylesheet-and-policy",
   "title": "Dissolve App.vue's covert global stylesheet and extract its grading-policy script",
   "lesson": "A root component accretes both shared chrome CSS (silently restyling distant components from an unscoped block) and domain policy (review-state move routing) because it is the path of least resistance — and size budgets declared without a ratchet regrow (the declared 500-line 'bounded steady state' is now 718).",
   "currentState": "Classes defined in App.vue's unscoped style block are consumed with no local definition in at least five other components (verified this session by grep: SettingsTab, ReviewSessionPanel, ForestDirectory, AnalysisTabsEditor, AnalysisControls); .tab-padding-sr at App.vue:624 is dead — definition only, zero consumers (verified; also the month-old dangling deferral worklog-2026-05 finding 4 names). handleBoardMove/handlePastePv carry review-grading gating policy in the component script (arch-ergonomics finding 7); App.vue is 718 lines (verified).",
   "proposal": "Move shared chrome classes to assets/css with one named owner and delete the dead rule; extract useBoardMoveRouting + usePlayVsEngine composables so the SR-gating policy (which guards grading integrity — free play bypassing the N-move discipline) becomes testable against fakes. Record as evidence on refactoring-queue-adr0007, and consider the advisory line-count ratchet adr-triggers finding 4 proposes so regrowth is visible rather than silent.",
   "axis": "ergonomics",
   "evidence": "arch-ergonomics findings 6 and 7 (end-to-end read of App.vue); mech-conformance finding 2 (script-heavy over-budget shape); worklog-2026-04 finding 4 (regrowth from declared steady state); worklog-2026-05 finding 4 (the dead-CSS deferral). Style-class consumers and the dead rule re-verified this session.",
   "cost": "Medium: the CSS move is grep-verifiable; the composable extraction follows the established C2-arc shape, with two hard-won Vue extraction patterns already recorded (worklog-2026-04 finding 6).",
   "risks": "CSS relocation can change cascade order/specificity — a visual pass over the six consumer surfaces is required. The extraction touches the review-grading path; the routing matrix (AWAITING_MOVE / LOADING / IDLE / FINISHED) needs test coverage before, not after, the move.",
   "likelyAlreadyFiled": "refactoring-queue-adr0007"
  },
  {
   "id": "adr-revisit-trigger-sweep-and-amendments",
   "title": "Sweep the ADR revisit triggers; record the two that fired silently (ADR-0001 #2, ADR-0003 #2)",
   "lesson": "Revisit-when clauses only work when paired with a sweep cadence. Two ADRs now actively misdescribe the codebase — ADR-0001's mutator-benefit enumeration describes identity-swap semantics that were removed for documented render reasons, and ADR-0003's file inventory is 9/19 stale and de-facto superseded by FILES.md — while two other ADRs recorded their fired triggers properly. The mechanism is sound; the cadence is missing.",
   "currentState": "Per the adr-triggers miner (end-to-end reads of all ten ADRs plus store/index.ts): ADR-0001 #2 fired with no amendment; ADR-0003's inventory drifted with no delegation; trigger bookkeeping is inconsistent across the set (recorded in ADR-0005/0009, silent in 0001/0003). ADR-0010 additionally carries committed LLM tool-call artifact lines at its tail (adr-triggers finding 1). The open item adr-effectiveness-audits already wants a per-tenet audit cadence.",
   "proposal": "Fold a per-trigger sweep checklist into adr-effectiveness-audits (this review's miners just performed one instance and found two unrecorded firings). Amend ADR-0001 to record the fired trigger and correct the mutator-benefits enumeration; amend ADR-0003 to delegate the per-file inventory to FILES.md, keeping only band definitions and the principle — dissolving the drifting parallel registry. Delete ADR-0010's two artifact lines and add the trivial CI grep for harness-envelope strings under docs/.",
   "axis": "auditability",
   "evidence": "adr-triggers findings 1, 2, 3, 6 — all from that miner's end-to-end ADR reads and DB queries; corroborated by postmortems finding 1's broader prose-vs-mechanism pattern.",
   "cost": "Small: append-only amendments per the project's own convention, a two-line deletion, a one-line CI grep, half a day for the sweep.",
   "risks": "Low. Amendments must follow ADR-0005's authoring discipline (not read by me this session — the author should read it end to end first). Structural doc edits require doc-graph regeneration in the same change per the umbrella CLAUDE.md.",
   "likelyAlreadyFiled": "adr-effectiveness-audits"
  },
  {
   "id": "outbound-wire-mappers-acl-symmetry",
   "title": "Restore ACL symmetry: outbound domain-to-wire mappers, or an explicitly sanctioned exception",
   "lesson": "An inbound-only ACL discovered its missing half when the create flow arrived: composables now construct snake_case wire payloads as Record<string,any>, contradicting the stated 'the ACL is the only place wire shapes appear' rule. A rule the code visibly contradicts erodes the authority of its sibling rules — either implement the missing half or amend the rule; the half-state is the worst option.",
   "currentState": "useMinting.ts builds the grading_parameter payload as Record<string,any> in the composable band (verified this session, lines ~108-130, including a second hand-built variant for specific-palette mints); types.ts's own comment documents 'no inverse-mapper for the create flow' with the composable named as the construction site (mech-conformance finding 5, attributing the rule text to frontend/CLAUDE.md:60).",
   "proposal": "Add outbound mappers to backend-service.ts symmetric to the inbound mapToReviewCard family, typed against a declared GradingParameterData interface — which simultaneously discharges territory of the open gradingparameter-opacity-typing item and the never-written known-keys ledger note (docgraph-dispatch finding 1 proposes the wire-schemas.md section as its home). Alternatively, amend the frontend CLAUDE.md rule to name the sanctioned outbound exception.",
   "axis": "ergonomics",
   "evidence": "mech-conformance finding 5; docgraph-dispatch finding 1 (the unfulfilled known-keys promise and the gradingparameter-opacity-typing scope gap). Payload construction re-verified this session.",
   "cost": "Small-medium: one payload family today; the mapper also retires the Record<string,any> typing.",
   "risks": "The data record's keys are co-owned with the backend (gamma backend-read, per docgraph-dispatch) — typing them is a cross-boundary contract statement that should cite docs/wire-schemas.md and may warrant a dispatch note per the dispatch-ledger discipline rather than silent unilateral typing.",
   "likelyAlreadyFiled": "gradingparameter-opacity-typing"
  },
  {
   "id": "typed-capability-metadata-mirror",
   "title": "Type the per-capability metadata mirror; stop admitting new proxy wire fields via scattered casts",
   "lesson": "The hand-maintained-mirror discipline for proxy wire types only delivers its drift signal if the mirror is actually typed. An open Record<string, Record<string, unknown>> pushed the typing burden to per-consumer casts, so a proxy-side capability field rename now fails silently at N call sites instead of loudly at the one mirror site — defeating the discipline's purpose.",
   "currentState": "capabilities is Record<string, Record<string, unknown>> (verified this session at engine/katago/types.ts:276); available_value_bindings is read via casts at capability-injection.ts:203 and AnalysisControls.vue:48; the learned-vf dispatch explicitly instructed extending the typed capability interface, which did not happen (docgraph-dispatch finding 3, from end-to-end dispatch reads).",
   "proposal": "Declare per-capability metadata interfaces (e.g. AdaptiveReevaluateCapabilityMeta carrying worst_quantile / extra_visits / available_value_bindings) keyed off a capability-name union, parsed and validated once at the version-response boundary — failing loudly on shape mismatch per ADR-0002 — with consumers reading the typed result. This is the intermediate step short of the deliberately deferred AsyncAPI work, not a competitor to it.",
   "axis": "correctness",
   "evidence": "docgraph-dispatch finding 3; composes with the umbrella CLAUDE.md's account of the v1.0.14 capability contract (in context). Cast sites and the open record type re-verified this session.",
   "cost": "Small: a handful of interfaces, one validation site, two cast deletions.",
   "risks": "Over-strict validation could reject benign proxy-side additions — unknown extra keys should pass through. The mirror is a cross-boundary contract; a one-line wire-schemas.md cross-reference (and possibly a dispatch line to the proxy) keeps it honest rather than silently divergent.",
   "likelyAlreadyFiled": ""
  },
  {
   "id": "migration-walking-fail-loud-and-cadence-review",
   "title": "Make migration blob-walking fail loudly; treat the bump-per-feature cadence as a separate, ADR-level question",
   "lesson": "Hand-walked optional-chained paths in migration bodies are the one place ADR-0002 is structurally absent — a wrong path is indistinguishable from an absent field, which already produced a silent no-op migration plus a repair migration. Separately, ~1.4 schema bumps/day means nearly every feature pays the version-bump ceremony, a signal about what is being persisted (the 5b57d25 persisted-vs-derived invariant, generalized), not just about the ledger.",
   "currentState": "The F-optimizer retirement's first 47->48 draft walked out.settings?.knobs instead of out.profile?.settings?.knobs, silently did nothing, and stamped v48; 48->49 exists solely to repair the stamped blobs (worklog-current finding 8). CURRENT_SCHEMA_VERSION reached 59 in ~6 weeks (git-churn finding 7); multiple migrations shipped even where deepMerge already backfilled, 'to keep the schema-version marker honest' (worklog-2026-05 finding 9).",
   "proposal": "Leg 1 (pure ADR-0002 strengthening, no deviation): a typed path-accessor helper for active migration bodies, or a post-migration assertion that a targeted leaf exists whenever the pre-image had it — wrong paths become loud, scoped to the active body only so the archived bodies' append-only freeze is untouched. Leg 2 (explicitly a discipline relaxation — requires a deliberate ADR-level decision before any code): a declared additive-default-backfill class handled by the generic defaults-merge without a version bump, reserving bump+migration for genuine shape changes, paired with a persisted-vs-derived review of profile fields.",
   "axis": "correctness",
   "evidence": "worklog-current finding 8 (the silent no-op and repair pair); worklog-2026-05 finding 9 (eleven bumps in six days; the 'marker honesty' rationale); git-churn finding 7 (cadence and the persisted-vs-derived generalization); git-narrative finding 6 (the retirement-path-as-correctness-surface lesson).",
   "cost": "Small for leg 1 (one helper or assertion plus adoption in the active body). Leg 2 is mostly decision cost: an ADR amendment and one review pass; code change small.",
   "risks": "Leg 2 weakens the version marker's deliberately chosen 'honest' property — it is the one proposal here that is a named deviation from a recorded discipline, and it stands or falls on maintainer judgment; leg 1 is independent and stands alone if leg 2 is declined. Leg 1's assertions must never run against archived migration bodies (rolling-archive discipline is by-design and not in question).",
   "likelyAlreadyFiled": ""
  }
 ]
}
````

### §2.4 · distill:merge

**Commission** (the following + `[SHARED: RAW-CANDIDATES-JSON]`):

````
You are the merge/dedupe judge in a retrospective review of the LengYue frontend (/home/bork/w/omega). Below are candidate refactoring opportunities from three lens-distillers (correctness / auditability / ergonomics), as JSON. Merge semantic duplicates (union the best evidence, keep the sharpest proposal), drop candidates that are weakly evidenced or that merely restate an already-filed todo-DB item WITHOUT adding new evidence or priority signal (keep ones where history adds real signal to a filed item, and say what the new signal is in 'lesson'). Rank by expected (correctness + auditability) gain relative to cost. Return at most 18 candidates, best first, in the same schema (axis = the dominant axis; carry likelyAlreadyFiled forward). You may run read-only checks in the repo or todo DB to settle a dedupe question.
````

**Merged candidates (verbatim — these are the 18 the verifiers received):**

````json
{
 "candidates": [
  {
   "id": "engine-subscription-union-narrowing",
   "title": "Narrow the KataGoResponse union at the subscription boundary; fail loudly on the error variant",
   "lesson": "A cast at a trust boundary erases the discriminated union the proxy deliberately ships (v1.0.13 split), so error packets flow silently down the happy path — the exact silent failure ADR-0002 forbids, made invisible because the type system was told not to look. New signal vs the filed silent-coercion audit (description read in the DB this session): that item is scoped to open-set defaults on closed wire vocabularies (enum/Literal witnesses); this is a different coercion shape — a union-erasing cast with a concretely traced swallow path — so it widens the filed audit's worklist and raises its priority rather than restating it.",
   "currentState": "analysis-service.ts:646 and :836 cast subscriber payloads `res as KataAnalysisResponse` (re-verified at HEAD by two distillers); katago-client.ts:101-104 forwards error packets to per-id subscribers unconditionally, so onAnalysisUpdate processes mis-typed error responses and telemetry.recordPacket receives undefined turnNumber. silent-coercion-protocol-boundaries-audit confirmed open/active in the todo DB this session.",
   "proposal": "One discriminant-checking helper at the subscription seam: narrow to the analysis variant, route KataErrorResponse to a loud structured-error surface (throw or the ApiError-style typed channel), delete both casts, and add a unit test feeding an error packet through the subscription path. Append the union-erasing-cast shape to the filed audit item's description/refs so the audit's sweep covers this class too.",
   "axis": "correctness",
   "evidence": "mech-conformance finding 4 (full trace from the katago-client contract at :101-104 and engine/katago/types.ts:498-501 through onAnalysisUpdate); both cast sites re-verified at HEAD by two independent lenses. DB check this session: the filed audit item's scope does not cover this shape.",
   "cost": "Small: one boundary helper, two call-site edits, one unit test.",
   "risks": "Failing loudly surfaces engine errors that were previously invisible — that is the point, but the presentation (toast vs console vs status surface) needs a deliberate choice so the loudness is usable. Error-flow change on a hot path: run the existing suite; perf battery only if reactivity paths are touched (ADR-0009 — no perf claim either way).",
   "likelyAlreadyFiled": "silent-coercion-protocol-boundaries-audit"
  },
  {
   "id": "multi-writer-slots-get-owners",
   "title": "Give the un-owned multi-writer slots owners: fix the mutator-bypass write, the blind-mode preference clobbering, and the store.engine subtree; then mechanize writer enumeration",
   "lesson": "Per-writer gates on multi-writer slots get re-fixed within days; owners stick (the 3e11c38 'band-aid' superseded one day later by fb0159a's owner). The lesson is mechanized only for the card-tree slot, and two live defects show the class is active: a board-field write that bypasses mutateBoard (so no boardsVersion bump fires and the debounced persistence silently misses it) and review-flow writes that clobber a user preference instead of restoring it.",
   "currentState": "All verified at HEAD this review: analysis-service.ts:463 `(board as any).maxVisitsTarget = visits` (cast unnecessary — the field is optional on BoardState); ~29-45 direct store.engine.* writes in analysis-service vs one named mutator, with the disconnect-reset block duplicated (~:169-192 and ~:398-411); showMoveSuggestions has four writers (useReviewSession.ts:282/:556/:629, lib/keybindings.ts:217) and finishCard/endSession force true unconditionally; treeExpanded is flipped at :283 and never restored; un-mutatored persisted-profile collection writes in useQeubo, useLocale, scenarioContext.",
   "proposal": "Staged: (1) route the :463 write through mutateBoard and delete the cast — trivial. (2) A blind-mode owner in the review session: snapshot showMoveSuggestions + treeExpanded on loadCard, restore on finishCard/endSession and abort paths (hook the existing abort-controller registry; mid-review user toggle updates the snapshot); one unit test against the review-state fakes. (3) Name an engine-subtree owner — extract an engine-connection module or document analysis-service as owner — collapsing the duplicated reset; add named mutators for persisted-profile collections (pinned-bookmarks pattern). (4) Mechanize: an ESLint writer-enumeration rule for store.boards/store.engine/store.profile assignments outside their owners, with ADR-0001's sanctioned template UI-toggle exception carved out explicitly. Run the out-of-frame hack-rationalization review before calling any leg done, per standing practice for >1-writer state.",
   "axis": "correctness",
   "evidence": "arch-ergonomics findings 1, 2, 8 (write enumeration, persistence-miss mechanism, duplication); git-churn finding 3 and work-status finding 5 (the owners-not-gates pattern record, fb0159a); mech-conformance finding 7 (profile-collection writes, knownTags two-writer history); ADR-0001 Revisit #3 names this drift class per adr-triggers. All writer sites re-verified by grep this session by the distillers.",
   "cost": "Step 1 trivial; step 2 small; step 3 small-medium (seam work in a 1181-line hot service — minimal-touch, not a rewrite); step 4 medium (AST member-assignment matching).",
   "risks": "Force-enabling suggestions after review may be intentional pedagogy — confirm intent before 'fixing'. The lint must encode ADR-0001's documented exception or it fires on sanctioned writes and gets disabled. Engine-owner extraction must not disturb the activeMode bookkeeping threaded through query minting; ship behind the suite plus a perf-battery null check (regression insurance, not a perf claim). Adjacent but distinct from spa-board-scope-consistency-audit (board-scope cells).",
   "likelyAlreadyFiled": ""
  },
  {
   "id": "migration-silent-noop-guard",
   "title": "Make migration blob-walking fail loudly: post-migration leaf assertions for active migration bodies",
   "lesson": "Hand-walked optional-chained paths over untyped legacy blobs are the one surface where ADR-0002 is structurally absent — a wrong path is indistinguishable from an absent field. This already produced a silent no-op migration that stamped v48 and required 48->49 purely as a repair migration, and the per-migration unit test passed vacuously because its fixture shared the same wrong path assumption. The bump-per-feature cadence question (~1.4 bumps/day) is a real but separate ADR-level discipline question and is deliberately not bundled here.",
   "currentState": "The 47->48 F-optimizer retirement walked out.settings?.knobs instead of out.profile?.settings?.knobs, silently did nothing, and stamped v48 (worklog-current finding 8). Migration bodies hand-walk paths; per-migration tests exist (frontend/tests/unit/store/migrations.test.ts) but share author-side path assumptions. The append-only rolling-archive discipline itself is sound and untouched by this proposal.",
   "proposal": "For new/active migration bodies only (shipped bodies stay frozen): a small assertion helper — if the pre-image contains the targeted leaf, assert the post-image reflects the transformation, throw otherwise. Optionally add the composition-level invariant test the 2026-04 miner sketched (hydrate any legacy blob through migrate + updateFromRemote + save and compare key sets against a clean migration), checking first whether the suite already covers it. The cadence-relaxation idea (additive-default backfills without a bump) is explicitly out of scope — it weakens the deliberately chosen 'honest version marker' property and needs its own ADR-level decision; this guard stands alone if that is declined.",
   "axis": "correctness",
   "evidence": "worklog-current finding 8 (the 47->48/48->49 incident); worklog-2026-04 finding 5 (composition-gap hypothesis); git-narrative finding 6 (retirement paths as a correctness surface); git-churn finding 7 and worklog-2026-05 finding 9 carry the separate cadence signal.",
   "cost": "Small: one helper plus adoption in the active body and a convention line; the composition test is one fixture-driven test.",
   "risks": "Fully typed accessors over historical blob shapes are impossible — the presence-conditioned assertion is the honest scope; over-eager assertions could reject genuinely-absent optional leaves, so the guard must condition on pre-image presence. Assertions must never run against archived bodies.",
   "likelyAlreadyFiled": ""
  },
  {
   "id": "branded-path-types",
   "title": "Brand the two variation-path shapes (RootToLeafPath / RootToCurrentPath) and execute the deferred call-site audit",
   "lesson": "Functions that act at a position but read global cursor/active-path state caused four real bugs in one arc (d62b51d, ec4cb3d, 59365c8, 87f7dfc), across two layers; the class is policed today only by warning docstrings, and the match postmortem's recommended call-site audit and branded-type intensification were never executed or filed. The codebase has twice proven this exact retrofit is cheap and runtime-neutral: proxy v1.0.21 identity branding, and the 2026-06-08 RawKey/EnrichedKey arc (817 tests green, zero runtime change).",
   "currentState": "Fixes plus warning docstrings live at usePlayFromPosition.ts:224-236; 16 files under frontend/src still call getActiveVariationPath (verified at HEAD); no todo-DB item exists (keyword sweep over the items table re-confirmed this session). The e2e harness gap ec4cb3d named — fixtures only ever ran from fresh boards where current == leaf — is also unaddressed.",
   "proposal": "Mint RootToLeafPath / RootToCurrentPath as branded types at getPath / getActiveVariationPath respectively, thread them through path-consuming APIs (the 16-file list is the worklist), and require an explicit named conversion where a site genuinely wants either. Adopt the rule that functions acting at a position take it explicitly rather than reading global cursor state. Add at least one test fixture with a pre-existing forward variation so path APIs are exercised where current != leaf. File the todo-DB item first so the work is SSOT-visible either way.",
   "axis": "correctness",
   "evidence": "git-narrative finding 4 (the four-bug history and the fixture gap); postmortems finding 4 (audit unexecuted, brands unbuilt, 16-file count); worklog-current finding 3 (all five match-arc recommendations dangling). 16-file usage re-verified at HEAD; DB absence re-verified this session.",
   "cost": "1-2 sessions: mostly mechanical type threading plus a 16-site intent audit; the fixture is small.",
   "risks": "Brands on arrays are lost through slice/concat — mint-site helpers must re-brand; if cast pressure exceeds value, the fallback is the audit-only leg, which is worthwhile alone. A few sites may legitimately accept either shape and need a named union/conversion rather than a forced choice. Composes with the cast-hygiene lint candidate against brand erosion.",
   "likelyAlreadyFiled": ""
  },
  {
   "id": "hydration-rebind-audit-and-barrier",
   "title": "Execute the deferred module-scope-vs-hydration audit and name the barrier idiom",
   "lesson": "One-shot binding of module-scope state against persisted/server truth races hydration. The audit that would have swept the class was recommended in the PBO postmortem, deferred in its worklog, never filed — and the class then recurred twice (tags-fetch hydration race, tenancy instance-cache leak), each fixed reactively. This is the only recurring failure shape the postmortem miner found still without a mechanical or structural net.",
   "currentState": "No todo-DB item (keyword sweep re-confirmed this session; tags-fetch-hydration-race is closed/shipped and covers one instance only). Good fix precedents exist: immediate-true rehydrate watchers (useQeubo/useAppBootstrap), disjoint boot write targets plus the persisted-profile 'user-authored data only' invariant (5b57d25), and the IDENTITY_SCOPED_CACHES registry. useAppBootstrap carries the repo's highest per-file fix ratio (5 fixes in 10 touches).",
   "proposal": "File the item, then run the bounded audit: enumerate module-scope and component-instance state derived from persisted or server truth (analysis ledger, thumbnail caches, board-card-trees, forest roots, query telemetry per the postmortem's own candidate list); each instance must rebind reactively, register in IDENTITY_SCOPED_CACHES, or sit behind an explicit whenHydrated() barrier. Classify remaining profile fields against the user-authored-vs-server-derived invariant. Optional mechanization: a best-effort lint flagging store.profile reads inside one-shot bootstrap callbacks.",
   "axis": "correctness",
   "evidence": "postmortems finding 2 (audit named, never executed, class recurred); worklog-current finding 1 (deferral trail, registry template); git-churn finding 5 (useAppBootstrap fix ratio, 5b57d25 resolution standard).",
   "cost": "Medium: one bounded audit session plus small per-instance fixes; the lint heuristic is optional extra.",
   "risks": "Enumeration can miss instances — use a grep over module-scope mutable declarations in services/composables as the mechanical first pass. The lint is best-effort syntactic with named gaps per the project's guard convention; it must not be sold as complete.",
   "likelyAlreadyFiled": ""
  },
  {
   "id": "cast-hygiene-lint-bare-as-any",
   "title": "Mechanize the cast-justification rule: ESLint ban on bare `as any` with a justified-cast escape hatch",
   "lesson": "The 'a cast needs a justification or it doesn't ship' rule is review-only and held at roughly 50% in a 32-site sample — the corpus's own repeated meta-lesson is that prose disciplines decay and only mechanical gates arrest recurrence. The two load-bearing offenders on the analysis path are carried as their own candidates here (subscription-boundary narrowing; the mutator-bypass write); this candidate is the mechanization that stops the population regrowing.",
   "currentState": "224 cast sites; ~17 of 32 sampled are bare, including engine/sgf-loader.ts:83/89, katago-client.ts:133, useReviewSession.ts:266 (mech-conformance). The lint host exists: frontend-ci has gated eslint since 2026-06-01, and the a75814c rule-adoption pattern (measure a zero-or-triaged baseline first) is established.",
   "proposal": "An ESLint rule banning bare `as any`, adopted at warn against a measured baseline and ratcheted to error after burn-down, with a real escape hatch: a comment-adjacent justification or a brand()/justifiedCast helper — the proxy's three-documented-casts mypy --strict precedent is the model. Record the rule's named gaps in the eslint config header per the existing rule-rationale convention.",
   "axis": "correctness",
   "evidence": "mech-conformance findings 3 and 4 (sample, worst offenders named); postmortems finding 1 (prose-discipline decay pattern); the a75814c adoption precedent. Worst-offender sites re-verified at HEAD by the distillers.",
   "cost": "Rule is small; the burn-down is the real cost (~120 bare sites extrapolated) — bounded by warn-then-ratchet and per-touch cleanup.",
   "risks": "Some casts hide genuinely hard typings — forcing bad types is worse than a documented cast, so the escape hatch must be real or the rule trains people to write empty justifications. SFC template casts were unaudited, so the population estimate has error bars.",
   "likelyAlreadyFiled": ""
  },
  {
   "id": "typed-capability-metadata-mirror",
   "title": "Type the per-capability proxy metadata mirror; validate once at the version-response boundary",
   "lesson": "The hand-maintained-mirror discipline for proxy wire types only delivers its drift signal if the mirror is typed. capabilities as Record<string, Record<string, unknown>> pushes drift detection out to per-consumer casts, so a proxy-side field rename fails silently at N call sites instead of loudly at the one mirror site — and the learned-vf dispatch explicitly instructed the typed extension, which did not happen.",
   "currentState": "capabilities is typed Record<string, Record<string, unknown>> (verified at engine/katago/types.ts:276); available_value_bindings is read via casts at capability-injection.ts:203 and AnalysisControls.vue:48 (verified at HEAD); wire-schemas §8 defers AsyncAPI, so this is the intermediate step, not a competing scheme. No todo-DB item exists (keyword sweep this session).",
   "proposal": "Declare per-capability metadata interfaces at the single mirror site (e.g. AdaptiveReevaluateCapabilityMeta carrying worst_quantile, extra_visits, available_value_bindings; selector metadata likewise), keyed off a capability-name union, narrowed/validated once at the version-response boundary — failing loudly on shape mismatch per ADR-0002, with unknown extra keys passing through. Delete the consumption-site casts. The mirror comment names the proxy tag it tracks so updates are mechanical at bump time; add a one-line wire-schemas.md cross-reference, and flag any contract ambiguity via dispatch rather than guessing.",
   "axis": "correctness",
   "evidence": "docgraph-dispatch finding 3 (dispatch instruction and cast sites, from end-to-end reads of all dispatches and wire-schemas.md); composes with the umbrella CLAUDE.md's v1.0.14 capability-contract account. Cast sites and the open record type re-verified this session by the distillers.",
   "cost": "Small: a handful of interfaces, one validation site, two cast deletions; no migration, no runtime behaviour at stake beyond the new loud rejection.",
   "risks": "Over-strict validation could reject benign proxy-side additions — unknown keys must pass through. Each future proxy capability change gains a small mandatory SPA edit; that is the discipline working, but it should be named at bump time. Cross-boundary: type only what the proxy's tag annotations actually advertise.",
   "likelyAlreadyFiled": ""
  },
  {
   "id": "services-boundary-deny-by-default",
   "title": "Invert the component-to-service ESLint boundary from enumerated blocklist to deny-by-default; file the ADR-0010 Revisit #4 item",
   "lesson": "An enumerated blocklist is fail-open: the boundary silently lapses for every service added after the list was written — sync-service and qeubo-service are already absent, and the stale rationale header (claiming four violating components that greps show resolved) demonstrates the list is not being maintained. This is ADR-0002's posture applied to guard design: the absent reviewer who only knows four names by sight waves the fifth through.",
   "currentState": "eslint.config.js:166-179 restricts exactly four services from components, with reactive-state modules exempted by comment (verified this session by the distillers); the header census at :82-86 is stale. The ADR-0010 Revisit #4 layering tension (reactive-state modules living in services/) is documented in three places but tracked in no work-status item — the keyword sweep over the todo DB this session re-confirmed the absence.",
   "proposal": "Step (a), small: restrict src/services/** wholesale from src/components/**, exempting the reactive-state modules (analysis-ledger, analysis-config) and type-only imports explicitly; run lint over the tree first to find legitimate exemptions before error severity (the a75814c measure-first pattern); refresh the stale census header in the same change; file the Revisit #4 work-status item. Step (b), separate arc requiring sign-off: relocate the reactive-state modules (plus stability-trajectory-store) to a state/ band so the rule becomes directory-structural and the exemption list dissolves — follows ADR-0003's amendment path, FILES.md re-tags, doc-graph regeneration.",
   "axis": "correctness",
   "evidence": "arch-ergonomics finding 5 (fail-open enumeration, from an end-to-end config read); adr-triggers finding 5 (stale census verified by grep, untracked Revisit #4 tension verified by DB query, re-confirmed this session).",
   "cost": "Polarity inversion + header fix: small (one config edit plus a lint shake-out). The state/-band relocation: medium, two PRs, not a rider.",
   "risks": "Inversion may surface legitimate existing imports — each needs an explicit exemption decision rather than a silent grandfather. The relocation must preserve ADR-0010's display-leaf-reads-what-it-displays sanction; any ADR amendment author reads ADR-0005 end to end first.",
   "likelyAlreadyFiled": ""
  },
  {
   "id": "keyed-cache-brand-at-construction",
   "title": "Make branded keys a construction-time rule for new keyed caches, not a periodic sweep",
   "lesson": "Composite keys over heterogeneous provenance is a proven recurring class: the analysis ledger's palette-swap stranding, its same-day twin in stability-trajectory-store (found only because the first fix's author looked), per-board single-slot maps, and KnobDomain before that. The branded-key + single-mint-site fix retrofits cheaply (817 tests green, zero runtime change); the gap is that discovery of each instance was reactive — and the same week introduced a new stringly discriminator (the r:/e: configHash prefix) one week after stringly contracts were banned via G1.",
   "currentState": "The 2026-06-08 brand arc closed known instances and a follow-up audit found no third instance of the dangerous class (worklog-current). The rule lives nowhere enforceable; the r:/e: prefix on the persistence path is a fresh instance of the input shape (low confidence on harm, single parse boundary today).",
   "proposal": "Codify in frontend/CLAUDE.md plus the pre-merge checklist: any new keyed cache mints a branded key whose declaration names the dependency set of the value (so a key leg the value doesn't depend on is visible at declaration). One bounded audit confirming the r:/e: prefix is parsed in exactly one place and cannot collide with legacy hashes — promote to a typed/versioned record (the v1/v2 bundle-union precedent) if either fails. Optionally a best-effort lint flagging Map keys built by template-literal concatenation of more than one variable.",
   "axis": "correctness",
   "evidence": "git-churn finding 2 (the twin-bug arc with commit ids); git-narrative findings 2 and 12; worklog-current finding 9 (retrofit cost evidence).",
   "cost": "Doc rule + checklist line + prefix audit: hours. The lint heuristic: medium and likely noisy — optional.",
   "risks": "The corpus's own evidence says a prose-only rule decays — the checklist anchor (and ideally the lint) is what keeps this from reproducing the failure it names. The lint will have false positives needing an annotation escape.",
   "likelyAlreadyFiled": ""
  },
  {
   "id": "work-status-authoring-hygiene",
   "title": "Harvest the dangling worklog deferrals into the todo DB; add refs rows linking items to their paid-for diagnoses; adopt the item-id-or-declined convention",
   "lesson": "Deferrals recorded only in prose reliably evaporate, at concrete cost: the module-scope rebinding audit deferred in a 2026-05-17 worklog was never filed and the bug class recurred twice; five match-arc postmortem recommendations and ~15 smaller deferrals dangle. Symmetrically, open items without refs to existing diagnoses force re-derivation of paid-for analysis (two perf items have zero refs while a 05-27 audit diagnosed both). The contrast cases prove the cheap fix works: every deferral that entered the todo DB is accounted for, and nav-during-range-query-perf's refs row makes it immediately reconstructable. The SSOT works when used; the leak is at authoring time.",
   "currentState": "Three miners independently enumerated dangling deferrals across 2026-04/05/current worklogs, retros, and postmortems. pv-hover-jank-range-query ('cause unclear') and many-boards-open-slowness confirmed open with no refs rows this session, while perf-audit-nav-and-pv-hover-2026-05-27.md diagnosed both and its planned fixes landed. spa-board-scope-consistency-audit confirmed open despite appearing substantially shipped. Title-vs-residual drift exists on arc-wide titles (nav-during-range-query-perf, i18n-string-sweep).",
   "proposal": "One bounded harvest pass triaging the miners' already-compiled dangling lists into file / drop-with-dated-record / confirm-done. Add refs rows pointing the two perf items at the 05-27 audit; re-profile both symptoms before building any lever (the dropped rb3-packet-receive-chunking item records why: a pre-refactor attribution re-measured at ~99ms vs ~2.35s — ADR-0009 applied to planning), then close or re-describe. Surface the spa-board-scope-consistency-audit status as a maintainer question rather than closing unilaterally. Going forward: every deferral or postmortem-recommendation bullet ends with a todo-DB item id or a grep-able 'not-filed: <reason>' marker; items filed near a diagnosis doc get a refs row at filing; retitle open items to their residuals on next touch. Optional advisory CI grep for unmarked deferral bullets.",
   "axis": "auditability",
   "evidence": "worklog-current findings 1-5 (91 worklogs read end-to-end by that miner); worklog-2026-05 finding 5; worklog-2026-04 finding 1; retros findings 1-2; postmortems finding 3; audits finding 2; work-status findings 2, 3, 9 (full items/refs/labels dump). Item states re-confirmed by DB query this session.",
   "cost": "Day-scale doc+DB pass working from lists the miners already produced; refs rows and retitles are minutes each; the two re-profiles are bounded perf-battery runs; the convention costs one line per bullet.",
   "risks": "Ceremony creep — if filing feels expensive, authors stop writing deferral sections; the 'not-filed: reason' escape must stay genuinely cheap. Keyword-sweep false negatives mean some 'dangling' entries may exist under different wording — check before filing duplicates. Closing the board-scope audit without reading its doc end to end would violate the read-fully discipline.",
   "likelyAlreadyFiled": ""
  },
  {
   "id": "adr-trigger-sweep-and-record-hygiene",
   "title": "Record the two silently-fired ADR triggers (0001 #2, 0003), strip ADR-0010's committed tool-call artifact, and fold a trigger-sweep cadence into the filed audits item",
   "lesson": "Revisit-when clauses only work with a sweep cadence: two ADRs recorded their fired triggers properly (0005, 0009), two rotted silently — ADR-0001 still describes a mutateBoard identity-swap that was removed for documented render reasons (so the canonical decision record actively misleads anyone auditing the mutation model), and ADR-0003's file inventory is 9/19 stale, de-facto superseded by FILES.md. New signal for the filed adr-effectiveness-audits item: this review's miners effectively performed one sweep instance and found the two unrecorded firings plus a committed harness-envelope artifact ('</content>'/'</invoke>' lines) at ADR-0010's tail surviving two later edits — concrete evidence the item's cadence leg is overdue, plus a ready 38-trigger checklist to fold in.",
   "currentState": "Per the adr-triggers miner (all ten ADRs read end-to-end, artifact bytes and store/index.ts:157-171 verified): ADR-0001 lines 97-107 misdescribe the mutator; 9/19 ADR-0003 inventory paths moved while FILES.md carries 215 band tags; ADR-0010 carries the trailing artifact. adr-effectiveness-audits confirmed open/active in the DB this session; its description shows the per-tenet cadence leg still open.",
   "proposal": "Four bounded edits per the project's amendment convention: (1) amend ADR-0001 recording trigger #2 as fired and correcting the mutator-benefits enumeration; (2) amend ADR-0003 to delegate the per-file inventory to FILES.md, keeping band definitions and the principle; (3) delete ADR-0010's two trailing lines and add a one-line CI grep for harness-envelope strings under docs/; (4) fold the 38-trigger sweep checklist into adr-effectiveness-audits so the audit has a cadence instead of being a one-off.",
   "axis": "auditability",
   "evidence": "adr-triggers findings 1, 2, 3, 6 (end-to-end ADR reads, byte-level artifact verification, DB queries); corroborated by postmortems finding 1's prose-vs-mechanism pattern. Neither I nor the distillers beyond that miner read the ADRs — the executing session must read each amended ADR in full first (ADR-0002 read-fully discipline).",
   "cost": "Small: append-only amendments, a two-line deletion, a one-line CI grep, a DB description edit; half a day for the sweep instance.",
   "risks": "Amendments must follow ADR-0005's authoring rules (read it end to end first). FILES.md band-tag correctness was not verified — delegation is not validation. Structural doc edits trigger the doc-graph regeneration duty in the same change.",
   "likelyAlreadyFiled": "adr-effectiveness-audits"
  },
  {
   "id": "outbound-wire-mappers-acl-symmetry",
   "title": "Restore ACL symmetry: outbound domain-to-wire mappers for the grading_parameter payload, or an explicitly sanctioned exception",
   "lesson": "The ACL rule — wire shapes appear only in the ACL — is contradicted on the outbound leg: a composable builds the snake_case grading_parameter payload as Record<string, any>, and types.ts's own comment documents the missing inverse-mapper. A rule the code visibly contradicts erodes the authority of its sibling rules; the half-state, not either resolution, is the unacceptable part. New signal for the filed gradingparameter-opacity-typing item (description read in the DB this session): that item is conditional/future ('tighten if the inner shape stabilizes'), but the evidence here — an active rule contradiction, a brand-erasure cast, and the never-written wire-schemas known-keys note the dispatch ledger records as promised — is grounds to activate and widen it now rather than wait for stabilization.",
   "currentState": "useMinting.ts:108-154 builds the snake_case payload as Record<string, any> with a brand-erasure cast at :46 (re-verified at HEAD), including a second hand-built variant for specific-palette mints; types.ts:2115-2130's comment documents the absent inverse-mapper and names the composable; no gradingParameter section exists in wire-schemas.md (docgraph-dispatch, all 14 dispatches read end-to-end by that miner). gradingparameter-opacity-typing confirmed open/future this session.",
   "proposal": "Add outbound domain-to-wire mappers in backend-service.ts symmetric to the inbound mapToReviewCard family, typed against a GradingParameterData interface carrying the known keys (gamma backend-read; default_visits, analysis_config frontend-owned) with an explicit open-keyed escape field; retire the Record<string, any>. Write the promised wire-schemas.md gradingParameter.data section in the same arc — the mapper is its natural enforcement point — with the backend side seeing the PR and a dispatch note per the ledger discipline. If the outbound exception is instead judged sanctioned, amend frontend/CLAUDE.md to say so explicitly. Widen the filed item's description to carry whichever is chosen.",
   "axis": "auditability",
   "evidence": "mech-conformance finding 5 (rule text at frontend/CLAUDE.md:60 vs construction site, line-cited); docgraph-dispatch finding 1 (the unfulfilled known-keys promise and its 'Not done' dispatch record). Construction sites re-verified by grep this session by the distillers; DB scope gap verified this session.",
   "cost": "Small-to-medium: one mapper family, the typed interface, one doc section. Scope to the grading_parameter payload only.",
   "risks": "data is genuinely open-keyed today — the mapper cannot be fully closed-typed; typing known keys with a named escape is the honest scope. The keys are co-owned with the backend, so typing them is a cross-boundary contract statement — dispatch, don't unilaterally declare. If cross-references are added to wire-schemas.md, the doc-graph structural duty applies.",
   "likelyAlreadyFiled": "gradingparameter-opacity-typing"
  },
  {
   "id": "vue-lifecycle-footgun-guards",
   "title": "Mechanize the two expressible Vue footguns: boolean gate-prop defaults and module-intent state in <script setup>",
   "lesson": "Five separate investigations paid for Vue semantics traps; the two costliest share a signature — correct-looking code whose scope or default silently differs from author intent, latent until a second consumer with a different lifecycle arrives (omitted boolean prop casting to false broke the intermission chart despite an 'other consumers safe' claim; 'module-shared' caches in <script setup> are per-instance, the MiniBoardCanvas texture flash). New signal for the filed thumbnail-render-lifecycle-consolidation item (description read in the DB this session): that item covers the per-instance shared-resource class via lifecycle consolidation only; the boolean-gate-prop class and a syntactic guard for the script-setup scope trap are outside its scope, and the corpus's meta-finding is that prose records of these traps have not stopped recurrence.",
   "currentState": "The lessons live scattered across ~5 worklogs; the filed item covers the texture/render-resource instance class; no lint or checked reference exists for either shape; whether strict vue-tsc template checking already polices part of the boolean-prop class is unassessed (adr-triggers finding 7).",
   "proposal": "Two best-effort guards plus one doc consolidation: (1) ESLint or test convention requiring explicit defaults on boolean props of shared components whose name reads as a gate (active/enabled/visible), plus an omitted-prop rendering test for opt-out gates — first checking what vue-tsc strict already catches; (2) a lint or convention forbidding module-intent mutable state (top-level let/Map/Set) declared inside <script setup> — require a plain <script> block or a separate module file, making the per-instance/module distinction syntactically visible; (3) fold the remaining footguns (v-memo under churning shallowRef, container-query self-styling, structuredClone vs reactive Proxy) into one checked frontend/CLAUDE.md section. Each guard ships verified against the original defect, per the RCA-minted-guard practice.",
   "axis": "correctness",
   "evidence": "worklog-current finding 7 (five investigations enumerated with worklog cites); git-narrative finding 5 (69810e2 contradicting 4756c30's safety claim; 463a15e per-instance scope); work-status finding 5; DB scope check this session.",
   "cost": "Small-medium per guard; the doc section is an hour; the script-setup rule is the harder AST shape.",
   "risks": "Both lints are heuristic — name-match and module-intent inference will produce false positives needing an annotation escape; ship as best-effort syntactic with named gaps (the project's established guard posture), not as complete coverage.",
   "likelyAlreadyFiled": "thumbnail-render-lifecycle-consolidation"
  },
  {
   "id": "scoped-state-registry-by-default",
   "title": "Scoped state is born into a registry; extend the drain pattern to closeBoard's hand-sequenced purges",
   "lesson": "Hand-enumerated cleanup at mutation sites does not stay complete as state grows — teardown was re-fixed four times before converging on registry-drained-by-mutator plus exact-set completeness tests (IDENTITY_SCOPED_CACHES, BOARD_SCOPED_STORE_CELLS), and four shipped 2026-06 bugs share the unregistered-scoped-state root shape. New signal for the filed thumbnail-render-lifecycle-consolidation item: that item covers only the shared-render-resource leg; the closeBoard hand-sequenced purges (with a docstring census that has rotted from 'Four cleanups' to ten actual items) and the birth-registration authoring rule are outside it.",
   "currentState": "closeBoard hand-sequences ~7 per-board purges with prose ordering constraints; its docstring enumeration has rotted (store/index.ts:331, verified). The registry covers only the Class A cells by documented judgment in board-scope.md (per arch-ergonomics's end-to-end read). tag-mutation-ssot, tenancy-instance-cache-leak, mini-board-texture-scope, tags-fetch-hydration-race all reduce to this class (work-status finding 5).",
   "proposal": "Extend the registry shape with explicit ordering phases (stop-engine / purge-caches / abort / pre-splice) so per-board owners register {phase, clear} and closeBoard becomes a phase-ordered drain under the existing completeness-test pattern. Adopt the authoring rule: new per-board / per-identity / shared-render state registers at birth, never joins a clear list. Fix the stale 'Four cleanups' docstring immediately regardless. File shared-render findings as evidence on the thumbnail item rather than duplicating it.",
   "axis": "correctness",
   "evidence": "git-narrative finding 1 (the four-fix convergence arc); worklog-2026-05 finding 2 (half-registry, half-manual state); arch-ergonomics finding 9 (rotted enumeration, verified); work-status finding 5 (the shipped bug cluster).",
   "cost": "Small for docstring + phase scaffold; medium to migrate the seven inline purges with ordering encoded as data; completeness-test extension small.",
   "risks": "board-scope.md records the Class B inline ordering as a deliberate judgment — the migration must preserve those constraints exactly or it converts a documented-correct shape into a subtle ordering bug.",
   "likelyAlreadyFiled": "thumbnail-render-lifecycle-consolidation"
  },
  {
   "id": "split-types-ts-along-banner-seams",
   "title": "Split types.ts along its own banner seams; retire the decaying half of IDENTIFIERS.md",
   "lesson": "When a file becomes a mixed hub, the project compensates with a hand-maintained lookup index — and the index drifts; the compiler-checked structure, not the index, is the fix. New signal for the filed refactoring-queue-adr0007 item (description read in the DB this session): the queue's recorded policy is handled-on-touch with types.ts explicitly parked under the type-catalogue exception at ~2245 lines, but the file is now 2362 (+117 in one week), is the repo's co-change hub (90 touches, 46x with defaults.ts, a 45-commit triple with migrations.ts), and IDENTIFIERS.md's citations have verifiably drifted — evidence the exception no longer fits its own terms, since ADR-0007's exception text itself prescribes splitting 'along clean domain seams, not by line count alone'.",
   "currentState": "frontend/src/types.ts is 2362 lines (verified by wc -l), mixing identifier brands, the knob vocabulary (~472-803), qEUBO types, AppSettings/UISession/GlobalStore, and wire shapes (section ranges per arch-ergonomics's end-to-end read; dividers at e.g. :1655, :2113); IDENTIFIERS.md exists as the workaround and has drifted; refactoring-queue-adr0007 confirmed open/active with the handled-on-touch, no-batch policy.",
   "proposal": "Split along the existing banners — types/ids.ts (brands + PerBoard), types/knobs.ts, a store-schema module colocated with store/defaults.ts (pairing each persisted slice's type with its default), per-domain value-object modules, wire-type aliases near the ACL — with a barrel re-export from types.ts so import sites stay stable and the split lands incrementally. Re-point or shrink IDENTIFIERS.md to what a file split cannot carry; update FILES.md and regenerate the doc-graph in the same PR where cross-references move. Present to the maintainer explicitly as a named deviation from the handled-on-touch policy, not a silent override.",
   "axis": "auditability",
   "evidence": "git-churn finding 1 (co-change hub metrics); mech-conformance finding 8 (growth rate, ADR-0007 exception text from an end-to-end read); arch-ergonomics finding 4 (section ranges, IDENTIFIERS.md drift, two declared-dead types). wc -l and queue policy re-verified this session.",
   "cost": "Medium-large but mechanical: the barrel keeps the import-site diff near zero; FILES.md + IDENTIFIERS.md + doc-graph updates ride along. Build-time effect is an unmeasured hypothesis (types erase at runtime; no perf claim per ADR-0009).",
   "risks": "Contradicts the recorded handled-on-touch policy — requires explicit maintainer sign-off. Circular-import hazards between store schema and domain types need a check first. Merge-conflict exposure with in-flight branches is real given the churn rate; schedule between arcs.",
   "likelyAlreadyFiled": "refactoring-queue-adr0007"
  },
  {
   "id": "doc-graph-dangling-signal-cleanup",
   "title": "De-noise the doc-graph dangling-reference report so the genuine-drift list is reviewable again",
   "lesson": "A drift report that is two-thirds noise by the project's own conventions stops being consulted: 55 of 125 live danglers originate from worklogs the consolidation note classifies as frozen while the generator treats only docs/archive/** as frozen; 31 more target one deliberately retired hub. Meanwhile real breaks hide in classes the gate cannot see — code-comment-to-doc pointers (two verified stale) — and missing-on-disk vs outside-node-set are conflated.",
   "currentState": "docs/doc-graph-report.md lists 125 live danglers; CI deliberately does not gate on broken references because existing drift would make every PR red (docgraph-dispatch). The discipline itself (committed graph, freshness gate) is working as designed — this restores its drift-report half, it does not undo it. Verified stale code-side pointers: ADR-0010:169 cites tests/integration/render-locality/ (actual: render-count/); MoveSuggestions.vue cites a moved audit path.",
   "proposal": "In tools/doc-graph/generate.mjs: (a) align the frozen boundary with the working convention (worklogs frozen, or a third bucket — maintainer confirms the classification); (b) split dangling classes — missing-on-disk vs outside-node-set — and resolve or flag directory links; (c) tombstone handling for retired hubs like deferred-items.md. Once the genuine-action list is small, an advisory no-new-danglers ratchet becomes feasible as a report section, not a red X. Separately fix the two verified stale pointers and decide extend-checking vs retrofit-on-touch for code-comment paths.",
   "axis": "auditability",
   "evidence": "docgraph-dispatch findings 5, 6, 7 (report plus all dispatches plus consolidation note read end-to-end by that miner); audits finding 4 (the two stale pointers); retros finding 5 (class conflation, directory-link skips).",
   "cost": "Small-to-medium tooling change plus one regeneration in the same PR (a generator change altering report structure falls under the committed-artifact discipline).",
   "risks": "Frozen-boundary classification is a judgment the maintainer should confirm — encoding the wrong convention would institutionalize it. Gate history counsels advisory-first: the merge-blocking framing was tried and retracted once already.",
   "likelyAlreadyFiled": ""
  },
  {
   "id": "retire-hand-maintained-censuses-in-code",
   "title": "Stop embedding counts, censuses, and colliding ad-hoc identifiers in code comments; cite stable handles instead",
   "lesson": "Prose enumerations in code mislead auditors as they decay: a docstring saying 'Four cleanups currently fire' above a list of ten; an eslint rationale header asserting violations greps show resolved; O12-O15 docstring tags colliding with an archived audit plan's different O12/O14 definitions. The one comment convention an audit found load-bearing was tagging landing sites with work-status item ids — the only substrate that survived squashed history. Two of the three named instances are already fixed in passing by sibling candidates here (the closeBoard docstring under the scoped-state-registry candidate, the eslint census under the services-boundary candidate); the residual unique work is the O-number collision and the convention itself.",
   "currentState": "Stale census at store/index.ts:331; stale violation census at eslint.config.js:82-86; O-number collisions across store/index.ts:373-468 docstrings vs docs/archive/notes/resource-ownership-audit-plan.md; no todo-DB item tracks the collision (keyword sweep this session found none). All three instances re-verified by grep by the distillers.",
   "proposal": "Add descriptive slugs or work-status ids alongside the O-numbers rather than rewriting the archived plan (additive, never editing frozen docs); let the two census fixes ride with their sibling candidates. Adopt the convention in frontend/CLAUDE.md: code comments cite stable handles (work-status item ids, the proven 'Item 30c' pattern) and avoid counts/censuses that a registry, test, or generated report could carry instead.",
   "axis": "auditability",
   "evidence": "arch-ergonomics finding 9; adr-triggers finding 5(b); worklog-2026-05 finding 1; audits finding 6 (the survives-squash observation).",
   "cost": "Small: localized comment edits plus a one-paragraph convention note.",
   "risks": "Low. The convention is policy-only and shares the decay risk it describes — pairing handles with the todo DB is what gives them a checkable anchor; prefer additive slugs over renumbering anything cross-referenced from frozen archives.",
   "likelyAlreadyFiled": ""
  },
  {
   "id": "enrichment-merge-null-validation",
   "title": "Validate, don't coerce, at the enrichment merge boundary (mergeRecords inner-null sharp edge)",
   "lesson": "A sharp edge named in the adaptive-deeper postmortem four weeks ago is still unguarded: mergeRecords skips top-level null/undefined values per key, but a non-null inner object with null-bearing fields replaces a populated entry wholesale — a documented silent-overwrite surface on the analysis data path, the validate-and-reject calibration of ADR-0002 Rule 4 applied. Verified this session that the filed silent-coercion audit item is scoped to enum-vocabulary coercion and does not cover this merge-boundary shape, so it is not already filed.",
   "currentState": "analysis-ledger.ts mergeRecords (~:185-195) has exactly the guard shape the postmortem flagged (re-verified at HEAD). The post-stratification mergeEnrichment additive last-writer-wins behaviour with no visit gate is an accepted, recorded tradeoff and is not in question.",
   "proposal": "At the merge boundary, detect inner enrichment values that would replace a populated leaf with a null-bearing object and loud-warn first (calibrated against real adaptive packet shapes), promoting to throw once the false-positive rate is known. Closes only the silent-overwrite case the postmortem named; the documented tradeoff stays.",
   "axis": "correctness",
   "evidence": "postmortems finding 5 (§5.5 paper trail, guard shape line-cited and re-verified at HEAD); DB scope check on silent-coercion-protocol-boundaries-audit this session.",
   "cost": "Small: one validation pass inside an existing helper plus a unit test with a null-bearing packet fixture.",
   "risks": "No observed incident in four weeks, so urgency is low — ranked accordingly. A too-strict rejection could drop legitimate proxy packets during adaptive deepening; hence warn-first calibration rather than an immediate throw.",
   "likelyAlreadyFiled": ""
  }
 ]
}
````


---

## §3 Main run — adversarial verifiers (verdicts verbatim)

Each commission = the role text below + `[SHARED: VERIFY-COMMON]` + `[SHARED: CANDIDATE-JSON <id>]`. The two role texts:

**Existence/evidence role text:**

````
You are an adversarial EXISTENCE/EVIDENCE verifier in a retrospective review of the LengYue frontend. Try to REFUTE this candidate refactoring opportunity on factual grounds: does the claimed current state actually hold at HEAD (check the code: wc -l, read the named files/sections, run git log where commits are cited)? Is the load-bearing evidence real and correctly characterized (read cited docs end to end)? Was the issue already fixed? If the candidate survives but details are off, set refuted=false and put the sharpenings in corrections.
````

**Fit/value role text:**

````
You are an adversarial FIT/VALUE verifier in a retrospective review of the LengYue frontend. Try to REFUTE this candidate refactoring opportunity on judgment grounds: (1) Does it fight a binding tenet or documented by-design discipline (read the relevant ADRs under docs/adr/ end to end — at minimum the ones the candidate touches — and the relevant CLAUDE.md sections) without naming the deviation? (2) Is it a duplicate of a filed work-status item (psql -h 192.168.122.1 -d todo, read-only SELECTs over items/refs/labels) — if so name it in alreadyFiled, and refute ONLY if the candidate adds no new evidence or priority signal beyond the filed item. (3) Is the cost/benefit honest — would the refactor plausibly pay for itself in correctness/auditability, per the review's stated tradeoff (small perf loss acceptable only for large auditability gain)? (4) Is the proposal shaped to this codebase's vocabulary (Components/Composables/Services, ACL, bands, branded types) or is it a generic-best-practice import that doesn't compose? Give a priorityHint.
````

### §3 · verify:exist:engine-subscription-union-narrowing

**Verdict (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Every load-bearing factual claim checks out at HEAD (2dc056e). The casts exist bare at analysis-service.ts:646 and :836 with no justification comment (also violating frontend/CLAUDE.md's as-needs-justification rule). katago-client.ts:101-104 forwards error packets to per-id subscribers unconditionally (the :94-99 error intercept does not return). The union at engine/katago/types.ts:498-501 is as cited. The traced swallow is real and verifiable: onAnalysisUpdate passes undefined turnNumber to telemetry.recordPacket (analysis-service.ts:941), and recordPacket (useQueryTelemetry.ts:256-305) additionally treats the error packet's undefined isDuringSearch as a finalized turn (turnsCompleted += 1), which can fire the auto-cleanup at :297-302 prematurely. The todo-DB item silent-coercion-protocol-boundaries-audit is open/active and its description is scoped to open-set defaults on closed wire vocabularies (enum/Literal witness cure) — the union-erasing cast genuinely falls outside it, so this is a widening, not a duplicate. No test covers error packets through the subscription path; nothing at HEAD fixes this. The one material overstatement — that errors were 'invisible'/'silent' end to end — does not refute the candidate; the type-confused downstream processing is real. Corrections carry the sharpenings.",
 "corrections": "1) The 'silent/invisible' framing is overstated: error packets ARE surfaced to the user — katago-client.ts:94-99 fires console.error + callbacks.onError, which analysis-service.ts:196-198 maps to pushSystemMessage('error', ...). The actual silent failure is the ADDITIONAL type-confused flow: the same packet also enters onAnalysisUpdate as a fake analysis response. The risks line 'surfaces engine errors that were previously invisible' should read: the gain is typed narrowing plus correct per-query/telemetry state, not first-time visibility. 2) The swallow is slightly worse than claimed: in useQueryTelemetry.recordPacket (useQueryTelemetry.ts:256-305) an error packet's undefined isDuringSearch is falsy, so turnsCompleted increments and can trigger premature auto-cleanup of the telemetry entry (:297-302) while the query is still live in activeQueries; lastTurnNumber is also set to undefined. 3) 'Delete both casts' undercounts: a third subscription-seam cast exists at usePlayFromPosition.ts:187, but it is GUARDED — lines 180-186 already do the 'error' in res discriminant check and reject loudly. It is the in-repo worked example of the proposed helper (via untyped double-casts); the helper should replace it too rather than leave a second hand-rolled idiom. 4) Both analysis-service casts are bare — no justification comment — so the current state also violates frontend/CLAUDE.md's 'a type assertion (as) needs a justification in a comment or it doesn't ship' rule; additional grounds for the fix. 5) Minor attribution: the proxy v1.0.13 split was AnalyzeResponse | MetadataResponse; the SPA-side union at types.ts:498-501 (KataAnalysisResponse | KataActionResponse | KataErrorResponse) is the frontend's own wire typing — v1.0.13 is the discriminated-union precedent, not the source of the error variant specifically. 6) DB facts confirmed this session: item state=open, disposition=active, scope=both, tier=medium; description names only the open-set-default/enum-witness shape.",
 "alreadyFiled": "",
 "priorityHint": "keep"
}
````

### §3 · verify:fit:engine-subscription-union-narrowing

**Verdict (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Not refuted. Every load-bearing claim verified at HEAD: (1) both casts exist with no justification comment (analysis-service.ts:646, :836), violating ADR-0002 Rule 2 (read end to end) and frontend/CLAUDE.md's type-assertion rule, so the current code — not the proposal — is the documented-discipline violation; (2) KataGoResponse is the discriminated union including KataErrorResponse (engine/katago/types.ts:498-501) and katago-client.ts:101-103 forwards error packets to per-id subscribers unconditionally; (3) the swallow path is concrete and worse than a type smell — an error packet reaches telemetry.recordPacket with turnNumber=undefined, increments turnsCompleted (isDuringSearch undefined → falsy), which can prematurely auto-delete the telemetry entry at useQueryTelemetry.ts:297, fires a misleading 'ponder exhausted' user warning for errored ponder queries (analysis-service.ts:1008-1019), and leaks the activeQueries entry/restart thunk for a query that will never complete. Duplicate check: the filed silent-coercion-protocol-boundaries-audit's DB description (read this session) scopes to open-set defaults on closed wire vocabularies (enum/Literal-witness cure) — a different coercion shape; the candidate adds a traced new shape plus concrete corruption evidence, so it widens rather than restates the audit. Cost/benefit honest: small fix, real correctness/auditability gain, no perf claim asserted (ADR-0009-compliant). Vocabulary fit strong: discriminated-union narrowing at a service-layer trust boundary is the project's own type-driven-design language, and awaitFinalPacket (usePlayFromPosition.ts:179-195) is an in-codebase precedent that already narrows this exact union with an 'error' in res branch and a loud reject — the proposal generalizes existing practice. One framing overstatement (silence claim) is corrected, not refuting.",
 "corrections": "1. 'Error packets flow silently down the happy path' overstates the silence: katago-client.ts:94-99 intercepts every error packet with console.error + callbacks.onError, wired in analysis-service.ts:196-198 to pushSystemMessage('error', …) — the user DOES see the error (ADR-0002 loudness level 4). The genuinely silent failure is the ADDITIONAL flow into onAnalysisUpdate: telemetry-state corruption (undefined turnNumber written to lastTurnNumber; spurious turnsCompleted increment because isDuringSearch is undefined→falsy, enabling premature entry auto-cleanup at useQueryTelemetry.ts:297), a misleading 'ponder exhausted' user warning when the errored query is a ponder (analysis-service.ts:1008-1019, since !undefined passes the final-packet check), and a never-cleaned activeQueries entry + restart thunk for a permanently-failed query. 2. Consequently the proposal's 'route to a loud structured-error surface (throw or ApiError-style channel)' should be re-aimed: user-facing loudness already fires at the transport layer; double-surfacing risks the warning-spam ADR-0002's 'What this tenet does NOT mean' cautions against. The seam's real job is per-query failure semantics — narrow the union, and on the error variant tear down the failed query (activeQueries / restart thunk / telemetry unregister) instead of processing it. The risks section's 'presentation needs a deliberate choice' is partially pre-answered. 3. Sharpening: the helper has an in-codebase precedent — awaitFinalPacket (usePlayFromPosition.ts:179-195) already narrows with an 'error' in res branch and a loud reject; the new helper could also replace that site's `res as unknown as Record<string, unknown>` shape (three call sites, not two; still small). 4. The closed item stringly-typed-api-errors (shipped 2026-06-01, ApiError class, PR #318) is the precedent for the typed-error-channel shape — supporting context, not a duplicate.",
 "alreadyFiled": "",
 "priorityHint": "keep"
}
````

### §3 · verify:exist:multi-writer-slots-get-owners

**Verdict (verbatim):**

````json
{
 "alreadyFiled": "",
 "corrections": "1) Write-count sharpening: \"~29-45 direct store.engine.* writes\" conflates reads and writes — 45 is total `store.engine.` references in analysis-service.ts; direct assignment statements number 18 (lines 169, 170, 175, 184, 192, 209, 273, 297, 328, 341, 347, 370, 398, 399, 400, 408, 411, 980). The duplicated disconnect-reset block is confirmed exactly at :169-192 (onDisconnect callback) and :398-411 (public disconnect()), including a duplicated telemetry sweep. 2) \"One named mutator\" refers to setSelectedModel (store/index.ts:191); the store also owns three named system-message helpers writing store.engine.messages. Strengthening fact: analysis-service bypasses setSelectedModel itself, writing store.engine.selectedModel directly at :192, :297, :408. 3) Persistence-miss mechanism sharpened: it is a trigger-miss, not data corruption — SyncService watches [boardsVersion, activeBoardIndex, profile (deep), session (deep)]; a board write bypassing mutateBoard schedules no sync, but the value rides along whenever any other change fires one. Because query telemetry is module-scope (not in the store), a standalone analyzeRange call can leave the maxVisitsTarget write as the only store change, unpersisted indefinitely until an unrelated mutation. The :463 cast also violates the frontend CLAUDE.md \"every `as` needs a justification comment\" rule (no comment present). 4) The \"persisted-profile collection writes in useQeubo, useLocale, scenarioContext\" list overreaches: only useQeubo's qeuboPinnedBookmarks (:824 assign, :826 push) is a collection write. useLocale.ts:66 writes a scalar (settings.appearance.locale); scenarioContext.ts:125/:234 writes a scalar AND already implements the snapshot/restore owner pattern with documented capture-neutrality — it is the manual form of the desired discipline, not a clean offender. Step 3's \"pinned-bookmarks pattern\" should read as the offender to fix, not an existing good pattern (no pinned-bookmarks mutator exists in the store). 5) Timing: fb0159a (Jun 9 00:58) superseded 3e11c38 (Jun 8 21:21) about 3.5 hours later — \"one day later\" is calendar-true only. 6) Strengthening: showMoveSuggestions is a persisted preference (store.session round-trips via buildPersistencePayload / updateFromRemote deepMerge), so finishCard/endSession's unconditional `= true` (:556/:629) genuinely clobbers persisted user state; treeExpanded (:283) is restored by nothing except the user's manual App.vue:400 toggle. endSession's own comment says \"Restore the default\" — it restores the default, not the prior value. 7) Mechanization claim confirmed precisely: the only writer-discipline lint at HEAD is local/clear-needs-ownership, scoped solely to src/composables/cards/useCardTreeData.ts (eslint.config.js:332-364), and it is a clearer-needs-ownership rule, not writer enumeration; its rationale comment explicitly records the per-writer-flag (isReviewActive) failure and says the rule is configurable for \"a second owned multi-writer slot\". ADR-0001 (read end to end) Revisit #3 names the bypass-the-mutators drift class, and its \"Exception: UI state written directly from templates\" section is the sanctioned-write carve-out step 4's lint must encode, exactly as the candidate's risk note states. 8) Filing hygiene: no duplicate in the todo DB; adjacent open items are spa-board-scope-consistency-audit (active — the candidate already distinguishes it) and engine-connection-lifecycle-logout (future — wants analysisService.disconnect() wired into resetWorkspace under a future deployment model; a step-3 engine-connection extraction should name that item as a beneficiary).",
 "priorityHint": "keep",
 "reasoning": "Attempted refutation on every load-bearing claim; nearly all hold at HEAD (2dc056e). Verified by direct read/grep: the :463 mutator-bypass write with unnecessary cast (maxVisitsTarget is on BoardState at types.ts:278); the sync-service watcher shape that makes the bypass a real persistence trigger-miss; the duplicated disconnect-reset blocks at :169-192 and :398-411; the four showMoveSuggestions writers at exactly the cited lines (useReviewSession.ts:282/:556/:629, keybindings.ts:217) with unconditional force-true in finishCard/endSession; treeExpanded set at :283 and never restored by review code; the existing pendingAnalysisAborts registry the proposal would hook; useQeubo's direct qeuboPinnedBookmarks writes; the clear-needs-ownership lint scoped to the single card-tree slot; commits 3e11c38/fb0159a with the gate-then-owner supersession arc; ADR-0001 (read end to end) Revisit #3 naming the drift class and the template-write exception the lint must carve out. The 1181-line count for analysis-service.ts is exact. Nothing is already fixed, and the todo DB has no duplicate item (closest matches are adjacent, not overlapping). The upstream distiller findings cited as evidence (arch-ergonomics/git-churn/work-status/mech-conformance) are session-internal artifacts I could not read, so I verified their underlying factual content against the tree instead — all of it checks out except the overreaches recorded in corrections (the engine write count conflates reads, and the useLocale/scenarioContext examples are scalars, with scenarioContext already snapshot-restoring). No unsubstantiated perf assertion is present — the perf-battery mention is framed as regression insurance, consistent with ADR-0009 posture. Two live, user-visible defects plus a verified mechanization gap with an in-tree precedent justify keeping priority as-is; the defects are low-severity individually, so no raise.",
 "refuted": false
}
````

### §3 · verify:fit:multi-writer-slots-get-owners

**Verdict (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Could not refute on any of the four grounds. (1) Tenet fit: ADR-0001 (read end to end) explicitly anticipates this drift in Revisit #3 (\"contributors frequently bypass mutateBoard... and produce state bugs\"), and the candidate names that trigger and carves out the documented UI-toggle exception; the proposed lint composes with existing practice rather than fighting the ADR — the shipped card-tree producer-ownership rule (eslint.config.js:332-364) says verbatim \"the rule is configurable, so a second owned multi-writer slot adds another block,\" and ADR-0002's loudness hierarchy ranks build-time enforcement above the code-review level ADR-0001 currently relies on. (2) Not a duplicate: psql search over items/refs found no filed item covering writer-ownership of these slots; spa-board-scope-consistency-audit (open) is the per-board-vs-global scope axis, which the candidate already distinguishes; engine-connection-lifecycle-logout and semantic-clarity-refactors-effect-typing are adjacent-but-distinct. (3) Cost/benefit honest: both headline defects verified live at HEAD — analysis-service.ts:463 writes board.maxVisitsTarget via an unnecessary `as any` (field is optional, types.ts:278) without a boardsVersion bump, and the sync watcher (sync-service.ts:188-197) watches boardsVersion + deep profile/session but NOT store.boards deep, so the write genuinely never schedules persistence; useReviewSession:556/:629 force showMoveSuggestions=true (endSession's own comment says \"Restore the default\", i.e., default-restore not preference-restore) and treeExpanded (:283) is never restored, with session.ui persisted via buildPersistencePayload so the clobber survives reloads. Staged costs are plausible (lint has a worked local-rule precedent). No unmeasured perf assertion — the perf-battery run is framed as insurance, conforming to the ADR-0009 posture. (4) Vocabulary fit: owners-vs-per-writer-gates is fb0159a's own commit language; named mutators, the abort-registry hook (pendingAnalysisAborts exists at useReviewSession:612), and the local-rule extension are all native idioms, not generic imports. Priority: raise — two verified live defects (one a silent ADR-0002-class persistence miss), a one-day band-aid→owner supersession on record, an explicit ADR revisit trigger satisfied, and an existing mechanization seam waiting for exactly this extension.",
 "corrections": "Sharpenings, none refuting: (a) The :463 write executes BEFORE the early-return guards at :464-465, so maxVisitsTarget is mutated even when analyzeRange refuses the query (engine disconnected / empty path) — the fix should move the write behind the guards, not just wrap it in mutateBoard. (b) The persistence miss is bounded, not permanent: buildPersistencePayload serializes store.boards wholesale, so the un-bumped write rides along with the next sync any other trigger fires; the loss window is \"no subsequent sync trigger before session end.\" Still a silent miss per ADR-0002, but state the bound. (c) Count precision: 19 direct `store.engine.X = ...` assignment statements in analysis-service.ts (45 total store.engine.* references) — \"~29-45 writes\" overstates; and analysis-service bypasses the one existing named engine mutator (setSelectedModel, store/index.ts:191) at :192/:408, which strengthens the owner case and should be cited. (d) finishCard's force-true is plausibly deliberate pedagogy (reveal suggestions after the blind attempt); the unambiguous oversight is the treeExpanded never-restore asymmetry — keep the candidate's confirm-intent risk, and note the likely fix shape is snapshot/restore at endSession while preserving the finishCard reveal. (e) session.ui IS persisted (deep-watched + in buildPersistencePayload), so the preference clobber survives reloads — stronger than the candidate states. (f) Step 4 should include an ADR-0001 amendment (append, per ADR-0002 Rule 6's append-don't-silently-edit convention) recording the lint as the Revisit-#3 response, plus doc-graph regeneration if cross-references change. (g) Drop the ADR-0004 invocation in the cost note — that tenet governs partial-visibility editing and explicitly permits substantial rewrites under full visibility; the intended point is just \"seam extraction, not rewrite.\" (h) Cross-reference engine-connection-lifecycle-logout (open, future): resetWorkspace intentionally does NOT reset store.engine today; an engine-subtree owner must preserve that intent and is the natural landing place for that item's future disconnect wiring.",
 "alreadyFiled": "",
 "priorityHint": "raise"
}
````

### §3 · verify:exist:migration-silent-noop-guard

**Verdict (verbatim):**

````json
{
 "alreadyFiled": "",
 "corrections": "1) The \"per-migration unit test passed vacuously because its fixture shared the same wrong path assumption\" claim is factually wrong. No per-migration test for 47->48 (or 48->49) ever existed on any branch (`git log --all -S '47 → 48' -- frontend/tests/` is empty; the retirement commit 9ac3b34 touched no migration test). Per-migration coverage in frontend/tests/unit/store/migrations.test.ts runs 1->2 through 43->44, then jumps to 56->57 — the incident sat inside an uncovered 44->56 gap (12 consecutive migrations with no fixtures). The suite passed because nothing exercised the new migration at all, not because a fixture mirrored the wrong path. The shared-assumption risk is real but is recorded as a hypothesis, Open Question 1 of docs/notes/design/migration-test-rotation-plan.md (\"the bug doesn't surface if they share an upstream defect\"), not as what happened. This correction arguably strengthens the candidate: the in-body assertion guard covers exactly the case where no test gets written. 2) Scope nuance: the broken draft was never committed (no commit anywhere contains `const knobs = out.settings?.knobs`); the author caught it pre-commit within the dev session after it stamped v48 on local/dev blobs (\"before the arc shipped beyond the dev branch\" — worklog 2026-05-25; archived-migrations.ts:2140-2149). The 47->48 body was fixed in place under pre-ship editorial scope; 48->49 catches blobs the broken draft stamped. So \"stamped v48\" is true for the dev blob population only — it never reached users or main. 3) The 47->48/48->49 bodies now live in frontend/src/store/archived-migrations.ts (lines 2085-2180), not migrations.ts; active bodies at HEAD are 57->58 and 58->59 (CURRENT_SCHEMA_VERSION = 59), both hand-walked optional chains with no assertion helper — the proposal is not already implemented. 4) The composition-level invariant test does not already exist: the only migrate-importing test file is migrations.test.ts, and the only updateFromRemote integration test is the narrow hydration-knowntags.test.ts — the proposal's \"check first whether the suite already covers it\" resolves to \"it does not\". 5) Per-migration fixture authoring resumed at 56->57 (commits c158bd2, 5b57d25, fb78e5a each added the matching describe block), so the no-test gap is historical, not current practice — the guard's marginal value is against both the gap recurring and the co-tested-fixture risk the rotation plan names. 6) Cadence figure checks out: 58 bumps (v1->v59) over 2026-04-27 to 2026-06-08, ~42 days, ~1.38/day. 7) Nearest filed neighbor in the todo DB is the open `silent-coercion-protocol-boundaries-audit` (ADR-0002 silent-coercion family) but it targets closed-set wire vocabularies, a different surface — adjacent, not a duplicate.",
 "priorityHint": "keep",
 "reasoning": "The core incident is real and documented at HEAD: archived-migrations.ts lines 2140-2149 and docs/worklog/2026-05-25-katago-f-optimizer-retirement.md (read end to end) both record that the first draft of 47->48 walked `out.settings?.knobs` instead of `out.profile?.settings?.knobs`, silently no-opped, stamped v48, and that 48->49 exists purely as the repair. The proposed guard is not already implemented (migrations.ts read in full — active bodies 57->58/58->59 are presence-tolerant hand-walked optional chains, no assertion helper), the composition-level test does not exist, and no duplicate is filed in the todo DB. The candidate therefore survives on existence/evidence grounds. One load-bearing detail is wrong — there was never a vacuous 47->48 unit test; there was no test at all (coverage gap 44->56) — but that substitutes one support for an equally strong one rather than undermining the proposal, so it goes to corrections, not refutation. The cadence figure (~1.4 bumps/day) verified against git history; the candidate correctly quarantines it as out of scope.",
 "refuted": false
}
````

### §3 · verify:fit:migration-silent-noop-guard

**Verdict (verbatim):**

````json
{
 "alreadyFiled": "",
 "corrections": "(1) The \"per-migration unit test passed vacuously\" claim is factually wrong. No per-migration test for 47→48 ever existed: commit 9ac3b34 (the retirement) did not touch frontend/tests/unit/store/migrations.test.ts, and git log -S across all branches finds no step(44)/step(46)/step(47) test ever added. The actual lapse is discipline drift: per-migration describe blocks stop at 43→44 (line 2462) and resume at 56→57 (line 2515) — steps 44→45 through 55→56 are uncovered — while the test file's own header still claims \"one describe block per migration\" (a small ADR-0002 Rule-6-shaped doc drift worth surfacing in the same change). (2) The headline mechanism would NOT have caught its motivating incident as specified: a presence-conditioned assertion authored by the same author shares the wrong-path frame — the pre-image check at out.settings?.knobs also finds nothing, so the assertion conditions out vacuously. To catch the wrong-path class the guard needs an independent witness: assert against the leaf path the runtime actually reads (defaults.ts / the v-current shape), or rely on realistic fixtures — notably the existing blobWithKnobs test helper encodes the correct profile.settings.knobs path and a routine 47→48 happy-path test reusing it would have gone red. The composition-level invariant test is therefore the load-bearing half of the proposal for the observed incident class; the leaf-assertion helper covers a different sub-class (right path, wrong transformation / partial write) and should be re-weighted accordingly. (3) Unnamed risk: once a shipped body calls the shared assertion helper, the helper becomes a mutable dependency of frozen bodies — editing it later edits frozen behavior. The helper must itself be frozen-once-shipped (or inlined per body) or the frozen-bodies discipline is quietly weakened. (4) Cross-reference docs/notes/design/migration-test-rotation-plan.md (design-note: in-progress): its Open Question 1 already names the co-tested-fixture hazard (\"recommend hand-write\") and Phase 1 is the suite this proposal extends; the honest framing is \"close the drifted Phase-1 per-migration discipline + add the composition test + an independent-witness assertion helper,\" not a wholly new surface. (5) Blast-radius sharpening: the incident was caught by the project author pre-ship (48→49 body comment; worklog), so observed user impact was nil — the structural exposure to persisted workspaces in the wild is the justification, not realized damage. (6) Composition-test coverage check (the candidate's own hedge): the suite's end-to-end walk asserts only a handful of leaves plus the version stamp, not key-set comparison against a clean default blob — so no, not already covered.",
 "priorityHint": "keep",
 "reasoning": "Not refuted. (1) Tenet fit: ADR-0002 read end to end — the candidate is a direct application of the loudness hierarchy to the one surface where a wrong path is indistinguishable from an absent field; it explicitly preserves the frozen-bodies rolling-archive discipline (frontend/CLAUDE.md) and explicitly de-scopes the cadence/honest-version-marker question to its own ADR-level decision, so no unnamed deviation. The migration files' own headers and the migration-test-rotation-plan design note frame migrations as exactly the ADR-0002-with-special-force surface the candidate names. (2) Not a duplicate: read-only sweep of the todo DB (items/refs/labels) finds no item touching the migration corpus; silent-coercion-protocol-boundaries-audit is the nearest open item but targets closed-set wire-vocabulary parsing, a different surface; no refs target migration files. The rotation-plan design note is adjacent prior art, not a filed work-status item, and proposes no runtime guard. (3) Cost/benefit: honest in magnitude (one helper + one fixture-driven test) and the axis is user-persisted-workspace correctness, where the incident is real and verified — but the headline assertion's yield against the motivating bug class is overstated (same-frame vacuity; see corrections), so the value case rests on the composition test plus an independent-witness formulation of the assertion. With the corrections applied the bundle plausibly pays for itself; a small runtime cost in migration bodies for hydrate-time correctness audibility is squarely the review's stated tradeoff. (4) Vocabulary fit: shaped in the codebase's own terms (migration ledger, frozen bodies, Tier-1 tests, loudness hierarchy), not a generic import. Priority: keep — real, cheap, unfiled correctness guard over user data, but the strongest variant is partly a revival of the project's own drifted Phase-1 discipline rather than a new mechanism, and the incident's realized impact was contained pre-ship, so no raise.",
 "refuted": false
}
````

### §3 · verify:exist:branded-path-types

**Verdict (verbatim):**

````json
{
 "alreadyFiled": "",
 "corrections": "(1) Commit hash: the fourth bug is 87fbc3f (\"fix(frontend): project activeMainIndex from the board's own cursor (P2)\", 2026-06-08), not 87f7dfc (a 2026-05-02 docs-dispatch commit). (2) \"Four real bugs in one arc\" → three bugs in the May match arc (d62b51d, ec4cb3d 2026-05-15; 59365c8 2026-05-16) plus a fourth instance of the same class three weeks later in the June board-scope audit arc (87fbc3f, latent, audit-found). This strengthens the candidate: the worklog explicitly deferred the brands \"until a third site of confusion surfaces\" (ADR-0003 N=2 threshold), and the class has since recurred twice — the project's own recorded deferral trigger has fired. Caveat for the final report: 59365c8/87fbc3f are ambient-cursor/aliasing confusions, not strictly getActiveVariationPath-vs-getPath shape confusions; the brands address the path-shape half of the class, the \"take the position explicitly\" rule addresses the rest — the proposal already contains both legs. (3) \"Across two layers\" described the two match-arc bugs (loop termination vs query construction, per ec4cb3d's message); the four bugs collectively span four surfaces (match loop, query construction, store/composable aliasing, analysis projection). (4) Evidence sharpening: \"all five match-arc recommendations dangling\" overcounts — postmortem §5a's docstring tightening is substantially delivered at HEAD (buildAnalyzeQuery's warning comment shipped with ec4cb3d itself, despite §5a's \"not done in this PR\" framing; playEngineMatch's docstring carries the numMoves + cursor-independence contract). The other four (§5b call-site audit, mid-tree regression test, branded path types, §5c rubric formalisation) are verified undone: no brands anywhere in frontend/src, no audit artifact, no test constructs current != leaf (e2e fixtures never navigate off the leaf; usePlayFromPosition has no integration tests; getActiveVariationPath has no direct unit test), no rigor rubric in docs/adr/ or handoff. (5) Precedent attribution: the \"817 tests green, zero runtime change\" figure attaches to dd3b85e (the 2026-06-08 brand-threading follow-up, which says verbatim \"no test/behaviour change\"); the RawKey/EnrichedKey brands themselves were minted in 0792f9a/6cb99d3, which were behaviour-changing fixes — cite dd3b85e for the zero-runtime-retrofit precedent. (6) Verified intact: 16-file getActiveVariationPath count exact at HEAD; warning comment at usePlayFromPosition.ts:223-236; todo DB (88 items) has no covering item — closest is spa-board-scope-consistency-audit (open), a different concern (per-board vs global scope), not a duplicate.",
 "priorityHint": "keep",
 "reasoning": "Adversarial check could not refute the candidate. Verified at HEAD: the RootToLeafPath/RootToCurrentPath brands exist only in the postmortem and worklog (no implementation); the §5b call-site audit was never executed or filed; the 16-file getActiveVariationPath worklist is exact; the e2e fixture gap (current == leaf universal) is still real — no test exercises a pre-existing forward variation with cursor mid-tree; the todo DB has no item covering this work (title+description sweeps over all 88 items). Both cited retrofit precedents are real (proxy v1.0.21; dd3b85e with \"build + 817 tests... no test/behaviour change\"). The factual errors found are peripheral: one garbled commit hash (87f7dfc should be 87fbc3f), the \"one arc\" framing (the fourth bug is from the June board-scope arc), and a five-vs-four count on dangling recommendations. The new facts found mostly strengthen the candidate — the worklog's own deferral trigger for the brands (\"until a third site of confusion surfaces\") has demonstrably fired with 59365c8 and 87fbc3f. No perf claims asserted, so ADR-0009 is not implicated. Priority: keep — correctness-axis, recurring class (latest instance 2026-06-08), unfixed, unfiled; the latest instance was latent rather than user-visible, which tempers a raise.",
 "refuted": false
}
````

### §3 · verify:fit:branded-path-types

**Verdict (verbatim):**

````json
{
 "alreadyFiled": "",
 "corrections": "(1) Bug count: 87f7dfc is the 2026-05-02 frontend session-handoff dispatch (docs-only commit), not a bug fix. The verifiable arc is THREE shipped fixes — d62b51d (iteration counter), ec4cb3d (root→leaf vs root→current query), 59365c8 (match cursor aliased to the user-visible cursor) — all in frontend/src/composables/board/usePlayFromPosition.ts, one composables-layer file. 'Across two layers' is wrong for the fixes; it is the 16-file call-site exposure (re-verified at HEAD) that spans components/composables/engine/services. The deflation is partly offset by a FOURTH, still-latent, documented twin the candidate missed: docs/worklog/2026-05-16-match-cursor-independence.md §Scope records the structurally-identical cursor-conflation bug in playEngineMoves, verified still unfixed at HEAD (only playEngineMatch deep-clones, usePlayFromPosition.ts:565), deferred until the self-play path gets a production UI wire. (2) 'Never executed or filed' under-describes the brand half: the deferral was deliberate and documented with an explicit trigger — postmortem §5b 'not urgent until a third site of this confusion class surfaces'; worklog 2026-05-15 'Premature today per ADR-0003 (N=2 use case is on the threshold; defer until a third site of confusion surfaces)'. Strictly no third misuse of getActiveVariationPath has surfaced; 59365c8 is cursor-aliasing, which a path brand would NOT have caught (only the candidate's explicit-position-parameter rule addresses it). The final report should present the brand leg as a deliberate revisit of a documented ADR-0003-grounded deferral whose threshold is argued to be effectively met (N=2 helper misuses + adjacent cursor-aliasing bug + documented latent twin + two cheap-retrofit precedents postdating the deferral), naming the deviation explicitly. The audit leg (§5b's primary deliverable, recommended unconditionally) and the fixture leg ('recommended if the bug class recurs' — it recurred the next day) carry no such condition and stand cleanly: no audit doc exists, no test mentions playEngineMatch or pre-existing variation, no RootToLeafPath/RootToCurrentPath exists outside the postmortem/worklog. (3) 'All five match-arc recommendations dangling' overstates by one: the §5a docstring leg substantially landed in the fixes themselves (buildAnalyzeQuery root→current warning in ec4cb3d; playEngineMatch cursor-independence contract with 59365c8); the other four legs (audit, regression test, brands, rigor-rubric adoption) are genuinely dangling. (4) The runtime-neutrality claim is substantiated by construction, not an unmeasured perf assertion: the 2026-06-08 key-brand worklog confirms '817 passed, 3 skipped' and explicitly records 'the brands erase at runtime... no perf battery run'. (5) Completeness per house discipline the proposal omits: new brands need IDENTIFIERS.md rows (which already records the related useActivePath.ts:14-15 path:string[] widening as a known erosion the work would intersect), FILES.md entries if a new file is minted, and doc-graph regeneration if doc structure changes — same-PR cadence.",
 "priorityHint": "keep",
 "reasoning": "Not refuted. (1) Tenet fit: branded types are the house style (frontend CLAUDE.md Type-driven design; ADR-0001 keeps readonly on branded ID value objects; the postmortem itself coined these exact brand names). The only friction is ADR-0003's extract-when-second-use-case-exists posture, invoked by the worklog's documented deferral with an explicit trigger — the candidate fails to name that deviation, but this is a correction rather than a refutation because the candidate brings real post-deferral evidence the filed record lacks: the third cursor-class bug (59365c8), the documented still-latent playEngineMoves twin, and two retrofit-cost precedents (proxy v1.0.21; dd3b85e 2026-06-08 key-brand arc) that postdate and undercut the deferral's cost premise. (2) No duplicate: swept the 88-item todo DB over titles, descriptions, and refs (path/brand/variation/cursor/fixture, getActiveVariationPath, RootToLeaf, postmortem/file targets) — nothing filed; the SSOT-visibility gap the candidate names is real. (3) Cost/benefit honest: 16-file worklist verified at HEAD; the 2026-06-08 precedent did comparable brand-threading in one arc with zero runtime change by construction (brands erase), so the review's auditability-vs-perf tradeoff is strictly favorable; the bug class scores rigor-warranted on the postmortem's own four-axis rubric (autonomous + GPU-consuming + low-detectability), and the e2e current==leaf fixture gap is confirmed unaddressed (no test mentions playEngineMatch or pre-existing variation). (4) Vocabulary fit: native — brands with mint-site factories, composables, Tier-3 integration fixture, file-the-item-first. Priority kept rather than raised because the postmortem's own strict trigger (third misuse of getActiveVariationPath) has not fired, the worst consumer is already fixed and docstring-guarded, the latent twin has no production UI wire, and the postmortem grades these follow-ups 'none urgent in isolation'; kept rather than lowered because the class produced three shipped bugs plus one documented latent twin, the unconditioned audit recommendation has been dangling since 2026-05-15 with no SSOT record, and the retrofit is demonstrably cheap and runtime-neutral.",
 "refuted": false
}
````

### §3 · verify:exist:hydration-rebind-audit-and-barrier

**Verdict (verbatim):**

````json
{
 "alreadyFiled": "",
 "corrections": "1) \"Audit never executed\" is overstated at HEAD: its territory has since been covered piecewise. (a) /home/bork/w/omega/docs/notes/consult/opus-consult-2026-06-03-knowntags-fence-and-boot-ordering.md Part 2 is a class-wide sweep of every onMounted write and auth/hydrate-boundary watcher touching shared reactive state (7-row inventory; verdict: exactly one genuine hazard, the knownTags site, fixed structurally in 5b57d25; the six siblings — qeubo bootstrap, analysis-persistence restore, autosave, SyncService save path, the five immediate:true watchers — already mitigated by two convergent patterns). (b) /home/bork/w/omega/docs/notes/audit/audit-spa-board-scope-consistency-2026-06-05.md inventories the module-scope surfaces (board-card-trees, analysis-service maps, analysis ledger, stability-trajectory store, thumbnail caches, pendingAnalysisAborts) with scope/teardown verdicts; its recommendations live under the OPEN item spa-board-scope-consistency-audit. Of the postmortem's §7.3 candidate list, only useQueryTelemetry's reconnect-rebind contract remains unexamined (module-scope singleton Maps confirmed at HEAD in /home/bork/w/omega/frontend/src/composables/useQueryTelemetry.ts; not in IDENTITY_SCOPED_CACHES — though it derives from live socket activity, not persisted/identity truth). The item should be reframed from \"run the audit\" to \"close the audit's residue and name the class\": useQueryTelemetry, plus a single doc that names the failure shape and indexes the existing nets. 2) \"Still without a mechanical or structural net\" is too strong: the hydration-race instance got an enforced-by-shape fix (knownTags moved out of the persisted blob; two writes now target disjoint fields), the ProfileState invariant comment (frontend/src/types.ts:1871), and hydration-boundary integration tests (frontend/tests/integration/hydration-knowntags.test.ts); the identity-flip instance got the IDENTITY_SCOPED_CACHES registry + completeness test + the identity :key remount for instance state. What is missing is a unified class-level name/idiom, not a net for the observed instances. 3) The whenHydrated() barrier proposal collides with a recorded, maintainer-adopted decision: the 2026-06-03 consult explicitly recommends AGAINST a general SyncService.whenHydrated() gate or ESLint rule as disproportionate (\"build the gate only if a second genuine instance of the class appears\" — none has appeared post-fix; no whenHydrated exists at HEAD), and /home/bork/w/omega/docs/worklog/2026-06-03-tags-fetch-hydration-race-fix.md records that as deliberately not done. A filed item must explicitly engage/overturn that decision rather than present the barrier as an open design slot. 4) \"Forest roots\" is not on the postmortem's §7.3 candidate list (that list: analysis-service per-board maps, analysis ledger, board-card-trees, useQueryTelemetry; the worklog deferral adds thumbnail caches); roots was the tenancy bug's surface and is already handled by the identity :key — the registry doc-comment (store/index.ts ~525) explicitly names instance state as unreachable from the registry by design. 5) Churn figure is window-dependent: useAppBootstrap.ts has 20 all-time commits (git log --follow); \"5 fixes in 10 touches\" holds only for its most recent 10 touches and only if the perf-bug commit 254bf82 is counted alongside the 4 fix-prefixed commits (6cb99d3, 5b57d25, be67253, 3c8e59c). \"Repo's highest per-file fix ratio\" was not verified repo-wide. 6) Classification nuance: the tenancy leak is the identity-flip register of the class (the PBO postmortem's §4 substitution test names identity binding as an instance), not a hydration race per se — \"recurred twice\" is defensible but should say the two recurrences are different registers of the shape, with different nets now in place. 7) Confirmed accurate: §7.3 recommendation and the worklog \"What's deferred\" trail; no todo-DB item for the audit (broad keyword sweep over id/title/description); tags-fetch-hydration-race and tenancy-instance-cache-leak both closed/shipped 2026-06-03; immediate:true rehydrate watchers, the user-authored-only invariant, and IDENTITY_SCOPED_CACHES all real at HEAD. Nearest existing item is the OPEN spa-board-scope-consistency-audit (scope/teardown axis, not hydration-rebind) — adjacent, not a duplicate, but the new item should cross-reference it.",
 "priorityHint": "lower",
 "reasoning": "The candidate's evidence trail verifies end to end: the PBO postmortem (read in full) recommends the §7.3 audit; its companion worklog defers it under \"What's deferred\"; the todo DB has no item for it (only tags-fetch-hydration-race, closed/shipped 2026-06-03, covering one instance); both claimed recurrences are real commits/items (5b57d25 tags-fetch; b4df796 / tenancy-instance-cache-leak), each fixed reactively; the cited fix precedents (immediate:true watchers, the persisted-profile user-authored-only invariant at types.ts:1871, IDENTITY_SCOPED_CACHES at store/index.ts:530) all exist at HEAD. So the candidate is not refuted on existence grounds. However, its central framing — the class-sweeping audit \"never executed\" and the shape \"still without a mechanical or structural net\" — is materially stale: the 2026-06-03 Opus consult's Part 2 performed precisely the hydration-axis sibling sweep (every boot-path writer enumerated, one hazard found and structurally eliminated), the 2026-06-05 board-scope audit inventoried the module-scope surfaces, and both observed registers of the class now carry structural nets (enforced-by-shape move-out + hydration-boundary tests; registry + completeness test + identity :key). The only §7.3 candidate untouched by any of these is useQueryTelemetry. Additionally, the proposal's whenHydrated() idiom contradicts a recorded maintainer decision declining exactly that gate as disproportionate. The item survives only as a narrowed residue (telemetry rebind contract + naming/indexing the class + explicitly engaging the anti-gate decision), so priority should be lowered rather than the item dropped.",
 "refuted": false
}
````

### §3 · verify:fit:hydration-rebind-audit-and-barrier

**Verdict (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Core claim survives adversarial check, but two of its planks are overstated and one half of the proposal silently fights a documented by-design decision. (1) Tenet fit: the audit itself composes with ADR-0002 (the postmortem frames the failure shape as fail-loudly at the binding-lifecycle layer and §7.3 recommends exactly this audit); ADR-0004 governs partial-visibility edits and is not fought by a full-visibility audit pass. However, the 'name the barrier idiom' half collides with a documented deliberate-not-done: the 2026-06-03 consult (docs/notes/consult/opus-consult-2026-06-03-knowntags-fence-and-boot-ordering.md, Part 3, adopted in docs/worklog/2026-06-03-tags-fetch-hydration-race-fix.md §Guard) explicitly rejected a general SyncService.whenHydrated() gate AND an ESLint rule as disproportionate, with a named re-trigger: 'build the gate only if a second genuine instance of the class appears.' The candidate does not cite this decision — that is the silent-deviation shape ADR-0002 Rule 6 forbids. Not fatal, because the audit is precisely the instrument that would establish whether the re-trigger holds; but the barrier/lint must be re-sequenced as conditional-on-findings, not offered as an upfront disposition. (2) Not a duplicate: no todo-DB item files the §7.3 audit (items, refs, labels all swept); tags-fetch-hydration-race and tenancy-instance-cache-leak are closed single-instance fixes; spa-board-scope-consistency-audit (open) is the nearest neighbor but covers per-board-vs-global scope coherence, a different axis. (3) Cost/benefit: direction honest (pure correctness/auditability, no perf cost, no unsubstantiated perf assertion per ADR-0009 concerns), but the residual gap is roughly half what the candidate claims — the consult's Part-2 inventory already swept every onMounted write and every auth-state/hydrate-boundary watcher (7 sites, one genuinely racy, fixed structurally), including the analysis-ledger restore path and the qeubo path from the §7.3 list. Mechanical nets partially exist (ProfileState user-authored-only invariant + first hydrate-boundary integration tests; IDENTITY_SCOPED_CACHES registry with a coverage test that fails on silent add/remove; identity :key). (4) Vocabulary fit is genuine (composables, registry, invariant, branded precedents verified: commit 5b57d25 exists and says what the candidate says; useAppBootstrap's '5 fixes in 10 touches' matches the post-rename path history), with one category blur: the tri-valued disposition rule conflates the hydration-order axis with the identity-flip axis — IDENTITY_SCOPED_CACHES clears on identity flip and does nothing for hydration ordering; an instance may need a disposition on both axes. Priority: lower, not drop — filing is cheap and warranted (the postmortem's recommendation has had no owner for three weeks and the consult's re-trigger condition needs an instrument), but the corrected scope is a small residual audit (analysisService per-board maps, useQueryTelemetry reconnect rebind, board-card-trees, thumbnail caches' hydration axis, plus the grep pass), not the class-sweeping arc the candidate's framing implies.",
 "corrections": "1. 'Audit never executed' must be sharpened: a bounded boot-path slice of the §7.3 audit WAS executed 2026-06-03 — the Opus consult's Part-2 sibling inventory (docs/notes/consult/opus-consult-2026-06-03-knowntags-fence-and-boot-ordering.md) swept all onMounted writes and auth/hydrate-boundary watchers (7 sites; one genuinely racy, fixed structurally), covering the analysis-ledger restore path (guarded by boardId-keying + board-existence ordering) and the qeubo path (guarded by re-firing watchers). The genuinely un-audited §7.3 residue: analysisService's per-board maps (interaction-time rebind), useQueryTelemetry's reconnect rebind contract, board-card-trees (postmortem itself rates 'probably safe'), and the thumbnail caches' hydration axis. The audit item should cite the consult as prior art and scope to the residue. 2. 'Still without a mechanical or structural net' is overstated: the hydration axis has the ProfileState 'user-authored data only' invariant + the first hydrate-boundary integration tests (tests/integration/hydration-knowntags.test.ts); the identity axis has the IDENTITY_SCOPED_CACHES registry with a coverage test that fails on any silent add/remove, plus the identity :key. What is missing is audit coverage of the residue, not any net. 3. whenHydrated() and the lint were deliberately declined on 2026-06-03 with the re-trigger 'build the gate only if a second genuine instance of the class appears' (consult Part 3; adopted in the tags-fetch worklog). The proposal must name this decision and demote barrier/lint to conditional-on-audit-findings, per the decision's own terms. 4. 'Recurred twice' chronology is loose: tags-fetch was surfaced 2026-04-27 (predates the PBO postmortem) and sat deferred-as-benign; the tenancy leak is a pre-existing identity-axis sibling found 2026-06-03. The sharper signal the candidate misses: the tenancy leak realized the PBO postmortem §4 substitution test's predicted 'privacy class of bug' (prior-identity state surviving the identity boundary) — a prediction-come-true, which is stronger evidence than 'recurred twice'. 5. The tri-valued disposition rule conflates two axes: IDENTITY_SCOPED_CACHES addresses identity-flip clearing, not hydration ordering; the audit should classify each instance on both axes, and the disposition vocabulary should include the strongest shipped pattern — disjoint write targets / move-out of the persisted blob (the 5b57d25 shape) — which the candidate cites as precedent but omits as a disposition. 6. Minor: '5 fixes in 10 touches' checks out on the post-rename path (4 explicit fix commits + 1 perf-bug fix in exactly 10 commits on frontend/src/composables/auth-app/useAppBootstrap.ts); 'repo's highest per-file fix ratio' was not independently verified and should be stated as the miner's measurement.",
 "alreadyFiled": "",
 "priorityHint": "lower"
}
````

### §3 · verify:exist:cast-hygiene-lint-bare-as-any

**Verdict (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "The candidate's core claims hold at HEAD (2dc056e). The prose rule exists and is review-only (frontend/CLAUDE.md \"Type-driven design\": \"a type assertion (`as`) needs a justification in a comment or it doesn't ship\"); no ESLint rule currently mechanizes it (read frontend/eslint.config.js end to end — no no-explicit-any, no cast rule). The lint host claim is correct: frontend-ci.yml with an `npx eslint .` step landed 2026-06-01 (commit 35c939c), and a75814c (2026-06-01) is a real measure-baseline-then-adopt precedent. Both load-bearing analysis-path offenders verified bare at HEAD: katago-client.ts:133 (`subscribe(query as any, ...)`) and analysis-service.ts:463 (`(board as any).maxVisitsTarget = visits`). The proxy three-documented-casts mypy --strict precedent is confirmed in the umbrella CLAUDE.md (v1.0.21). No duplicate item in the todo DB (searched items title/description for cast/eslint/lint/any). Not already fixed. Details are off in several places (line numbers, population scoping, prior-consideration of the rule) — carried as corrections, not refutation.",
 "corrections": "1) Line numbers: sgf-loader.ts bare `as any` sites are lines 76, 77, 89 at HEAD (not 83/89); useReviewSession.ts site is line 262 (not 266), and it carries a trailing comment (\"// Retain the tab ID\") — intent but not type-soundness, so calling it bare is defensible but should be characterized as thinly-commented rather than uncommented. katago-client.ts:133 is exact. 2) Scope tension between title and cost: `as any` specifically has only ~12 code sites in src (14 grep hits, 2 of which are comment mentions) — a rule banning only bare `as any` has a dozen-site burn-down, not ~120. The ~120 extrapolation applies only to a rule covering ALL unjustified casts, which needs a custom rule (justification-comment detection); the project has the custom-rule precedent (frontend/eslint-rules/clear-needs-ownership.js). The final report should either pick one scope or present a two-stage plan: `as any` first (tiny population, could go straight to error per existing practice), justified-cast custom rule second. 3) Population number: my grep reproduction finds ~203 cast sites in .ts and ~78 in .vue (~281 total, comment lines excluded); 224 is in-range for a different methodology but should be carried as approximate (~200-280). 4) The rule is not unconsidered: the eslint.config.js header (lines 136-138) explicitly defers `@typescript-eslint/no-explicit-any` (~152 sites measured) as a \"warn-as-backlog candidate, not adopted here\" — the candidate should cite that deferral note and frame itself as un-deferring with a burn-down plan. Note no-explicit-any also covers `: any` annotations, broader than `as any`. 5) Warn-then-ratchet is a NEW posture for this config, not the a75814c pattern: every rule adopted so far went in at error on a zero-or-fully-triaged baseline (a75814c: 0 hits; no-floating-promises: 7 sites all resolved pre-adoption), and the header's deferral of warn-as-backlog rules suggests the project has so far declined warn-tier rules. The candidate should acknowledge this rather than presenting warn-then-ratchet as the established pattern. 6) The cited \"mech-conformance\" and \"postmortems\" findings are workflow-internal artifacts, not repo docs; the underlying facts were re-verified directly at HEAD.",
 "alreadyFiled": "",
 "priorityHint": "keep"
}
````

### §3 · verify:exist:typed-capability-metadata-mirror

**Verdict (verbatim):**

````json
{
 "alreadyFiled": "",
 "corrections": "(1) types.ts:276 is the query-side opt-in field (KataGoAnalysisQuery.capabilities); the field the two cited casts actually read — via store.engine.info.capabilities — is the structurally identical advertisement twin at types.ts:485 (KataActionResponse.capabilities). Cite both; the validation-boundary work targets the 485 side. (2) Both consumption-site casts are guarded (Array.isArray/typeof checks follow), so the failure mode is precisely silent degradation, not a crash: a proxy-side rename of available_value_bindings would default the list to [], silently hide the learned dropdown, and silently stop sending value_binding — no loud signal, strengthening the ADR-0002 framing. (3) The dispatch instruction (proxy-to-frontend-learned-vf.md:150) presupposes an AdaptiveReevaluateCapability type that never existed in types.ts; implementing it means introducing the per-capability type, not extending one — the clarifications dispatch's SPA roadmap (lines 225-226) pinned the same extension on KataActionResponse.capabilities and it also did not happen. (4) The proposed validation site already exists: version-probe.ts::parseVersionResponse (lines 106-119) shape-validates the outer dict-of-dicts cast-free via isRecord guards but leaves per-capability metadata as Record<string, unknown>; the proposal layers per-capability narrowing onto an existing cast-free idiom with existing unit-test scaffolding (tests/unit/engine/katago/), making the small-cost estimate credible. (5) 'selector metadata likewise' is near-trivial — wire-schemas §2's example advertises selector as empty {}; adaptive_reevaluate is the only capability with populated advertisement metadata today. (6) The proposal composes with frontend/CLAUDE.md's house rule that a type assertion needs a justification comment or it doesn't ship — capability-injection.ts:203's `as readonly string[]` carries no such justification.",
 "priorityHint": "keep",
 "reasoning": "Every load-bearing claim verified at HEAD: the open Record type exists at types.ts:276 (and its advertisement twin at :485); the casts at capability-injection.ts:203 and AnalysisControls.vue:48 are present verbatim; proxy-to-frontend-learned-vf.md (read end to end) explicitly instructs the typed extension at line 150 and grep confirms AdaptiveReevaluateCapability exists nowhere in frontend src or tests; wire-schemas.md (read end to end) §8 defers AsyncAPI and names drift-induced silent bugs as a lift condition, so the candidate is the intermediate strengthening, not a competitor; version-probe.ts confirms no per-capability validation exists at the boundary today. Todo DB keyword sweeps found no existing item. Not fixed at HEAD. No perf claims asserted. Priority kept rather than raised: the concrete failure mode is silent UI degradation of an [experimental] surface (learned dropdown vanishing), not data corruption — real but bounded.",
 "refuted": false
}
````

### §3 · verify:fit:cast-hygiene-lint-bare-as-any

**Verdict (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "The candidate survives all four refutation probes. (1) Tenet fit: ADR-0002 Rule 2 is verbatim the prose rule being mechanized (\"Type assertions must be justified... `as any` is strongly discouraged; when unavoidable... comment the scope of the unsafety\"), and ADR-0002's own Consequences section names the gap the candidate closes (\"The tenet is a policy, not an enforced mechanism... only code review catches it\"); the loudness hierarchy explicitly prefers build-time lint over review, so this moves the rule up the hierarchy rather than fighting it. frontend/CLAUDE.md's \"a type assertion (as) needs a justification in a comment or it doesn't ship\" confirms the review-only status. (2) Not a duplicate: no open or closed todo item covers cast hygiene or an as-any lint (nearest, silent-coercion-protocol-boundaries-audit, targets wire-vocabulary coercion — a different shape). (3) Cost/benefit is broadly honest (escape-hatch risk and SFC error bars named; warn-then-ratchet matches the recorded adoption discipline) with one real internal scope conflation, correctable not refuting: the title prices a narrow rule (~13 actual bare `as any` sites in src at HEAD) while the burn-down prices the full cast-justification mechanization (~120 extrapolated bare casts of all types, needing a custom justification-adjacency rule). (4) Vocabulary fit is strong and verified: the a75814c measure-baseline-then-adopt precedent is real, the rule-rationale header convention is load-bearing in eslint.config.js, the local custom-rule host exists (eslint-rules/clear-needs-ownership.js), the inline-disable-with-justification escape hatch already has a worked model (vue/no-v-html), and the proxy's three-documented-casts mypy --strict precedent is confirmed in the umbrella CLAUDE.md. One genuine gap: the eslint.config.js header already records no-explicit-any (~152 sites) as deliberately deferred (\"warn-as-backlog candidates, not adopted here\"); the candidate is the \"separate, later decision\" that deferral anticipated, but it must name that it reopens a recorded deferral rather than appearing unaware of it — a correction, not a refutation.",
 "corrections": "1) Scope conflation to fix: the title bans bare `as any`, of which there are only ~13 code sites in frontend/src at HEAD — not \"~120\". The ~120 extrapolation (from 224 total cast sites, ~17/32 bare) is the population of bare casts of ALL types (`as Foo` without a justification comment), which is what the prose rule actually governs and which requires a custom justification-adjacency rule (the eslint-rules/clear-needs-ownership.js local-rule precedent), not a stock ban. Recommend staging: stock ban on `as any` first (near-free, ~13 sites), full justification-adjacency mechanization second, and price each honestly. 2) The proposal must name that it reopens a recorded deferral: frontend/eslint.config.js header lines 134-138 already defer `no-explicit-any` (~152 sites) as \"warn-as-backlog candidates, not adopted here\" — per ADR-0002 Rule 6 (design-time drift surfaces too), the rule-rationale header should record the relationship to that deferral rather than silently overriding it. 3) Line-number drift at HEAD: sgf-loader.ts bare casts are at 76/77/89 (not 83/89); useReviewSession.ts is 262 (not 266; its comment \"Retain the tab ID\" states intent, not cast soundness — still effectively bare); katago-client.ts:133 exact. 4) The escape hatch need not be invented: the config's vue/no-v-html model (inline eslint-disable-next-line + justification) is the established hatch, named twice in the config header; cite it instead of (or alongside) a new justifiedCast helper. 5) Strongest citation is ADR-0002 Rule 2 itself, plus the Consequences admission that only code review currently enforces the tenet — these are sharper warrants than the frontend CLAUDE.md sentence alone.",
 "alreadyFiled": "",
 "priorityHint": "keep"
}
````

### §3 · verify:exist:services-boundary-deny-by-default

**Verdict (verbatim):**

````json
{
 "alreadyFiled": "",
 "corrections": "1) Temporal framing in the lesson is wrong: sync-service (first commit 2026-04-26) and qeubo-service (2026-04-28) PREDATE the blocklist (commit 6893996, 2026-05-31); so does every other unlisted service (newest is stability-trajectory-store, 2026-05-28). No service has been added after the list was written — the absences are authoring-time omissions, not post-list drift. Restate as: \"the enumeration was incomplete from day one (sync-service, qeubo-service, api-client, resource-service — all effectful — were never listed), and its fail-open shape means any future service is unguarded by default.\" The structural critique survives; the drift narrative does not. 2) The stale census has a precise provenance: commit 35c939c (2026-06-01) resolved all five violation sites via new composables (useLibraryPreview, useCardMetadata, useForestStats, useAnalysisPersistence) and added the frontend CI gate, but did not update the eslint.config.js header — stale since that date, ~8 days at review time. 3) Census line range is 81-86, not 82-86 (the sentence starts at line 81); the pattern block is lines 163-179 including the exemption comment, with the const at 166-179 as cited. 4) Lint shake-out under deny-by-default is tiny and known: the only non-exempt component-to-service import at HEAD is AnalysisControls.vue:17's type-only import of AnalysisBundleStorageError from analysis-bundle — and analysis-bundle.ts self-declares \"Pure projection / replay... No side effects, no network\", so it is not an effectful singleton; the exemption decision is type-only allowance and/or pure-module classification. Note the base no-restricted-imports rule cannot exempt type-only imports; @typescript-eslint/no-restricted-imports with allowTypeImports can — the proposal's \"exempt type-only imports explicitly\" implies switching the rule for the component block. 5) Confirmed clean: ADR-0010 Revisit #4 wording matches the candidate (including the relocation path); the three documentation places check out; todo-DB sweep (49 open items, closest candidates refactoring-queue-adr0007 / semantic-clarity-refactors-effect-typing / adr-effectiveness-audits read in full) confirms no tracking item; a75814c is real and is the measure-before-error-severity precedent; stability-trajectory-store's header confirms it is reactive-state in the analysis-ledger shape, consistent with step (b)'s relocation list.",
 "priorityHint": "keep",
 "reasoning": "Every load-bearing claim was checked at HEAD. eslint.config.js read end to end: the blocklist is exactly the four named services (lines 166-179) with the reactive-state exemption comment, and the header census claiming four violating components is provably stale — grep shows zero component imports of the restricted services, and commit 35c939c (2026-06-01) resolved all five sites without touching the config header. ADR-0010 read end to end: Revisit #4 matches the candidate's characterization verbatim in substance. Todo DB queried directly (items table, keyword sweeps plus full open-item listing): no item tracks Revisit #4 or the boundary; the three nearest items cover different concerns. Commit a75814c verified as the measure-first lint-adoption precedent. The candidate survives. The one factual error is the lesson's drift narrative: sync-service and qeubo-service were not \"added after the list was written\" — they predate the blocklist by a month, and in fact no service postdates it; the omissions are authoring-time, which still demonstrates fail-open enumeration but via day-one incompleteness rather than subsequent lapse. That and several sharpenings (provenance of the staleness, the single type-only import a deny-by-default rule would flag, the allowTypeImports mechanism) go to corrections, not refutation.",
 "refuted": false
}
````

### §3 · verify:fit:typed-capability-metadata-mirror

**Verdict (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Not refuted on any of the four grounds. (1) Tenet fit: read ADR-0002, wire-schemas.md, proxy-to-frontend-learned-vf.md, and both CLAUDE.md files end to end. The proposal implements ADR-0002 Rule 4 (ACL validates, not coerces) and removes a Rule-2 violation at HEAD (capability-injection.ts:203 casts wire data to readonly string[] with no justification comment and no element-type check — the subsequent Array.isArray guard checks array-ness only). The opacity it removes is not by-design: unlike analysis_config and overrideSettings, the capabilities docstrings in engine/katago/types.ts carry no \"deliberately opaque\" rationale, and the learned-vf dispatch (binding per its own status section) explicitly instructed the typed extension at types.ts — instructing a type, AdaptiveReevaluateCapability, that grep confirms never existed; the implementation worked around it with casts without flagging the deviation, which the umbrella CLAUDE.md's dispatch discipline forbids. wire-schemas §8's AsyncAPI deferral names hand-maintained mirrors as the operative scheme; typing the mirror strengthens it rather than competing, and keeps §8's lift-condition 2 (drift-induced silent bug) honest by converting would-be drift into compile-time signal. (2) Not a duplicate: full open-item listing plus keyword sweeps over the todo DB found nothing covering this; silent-coercion-protocol-boundaries-audit is the adjacent register (closed-vocabulary enum coercion) not structural metadata typing, and the candidate carries new evidence (the unimplemented binding dispatch instruction) regardless. (3) Cost/benefit honest in direction, slightly understated in cost (see corrections): the validation seam already exists as the pure parseVersionResponse, but the opaque Record type threads through ~10 signature sites in 5 files, including four in usePlayFromPosition.ts the candidate didn't enumerate — which simultaneously strengthens its drift-surface claim (more silent sites than it knew). Essentially no perf cost (probe-time, once per WS open); the gain is compile-time drift detection at a boundary the proxy is actively evolving (v1.0.22–v1.0.27 adaptive arc). (4) Vocabulary fit is native: typed mirror at the engine ACL, capability-name union narrowed once at a pure boundary parser, discriminated-union discipline per frontend CLAUDE.md's type-driven design; no library import proposed, and version-probe.ts's existing isRecord guard style is the in-codebase pattern to extend. No perf assertions made, so ADR-0009 hypothesis-form correction is not needed.",
 "corrections": "(a) Cast-site asymmetry: AnalysisControls.vue:48 is a benign widening cast followed by full runtime validation (Array.isArray + per-element typeof filter); the genuinely dishonest cast is capability-injection.ts:203 — `as readonly string[]` asserts element types never checked, with no justification comment, violating ADR-0002 Rule 2 and frontend CLAUDE.md's cast-justification rule at HEAD. (b) The mirror/consumer surface is larger than stated: the opaque Record<string, Record<string, unknown>> appears at src/types.ts:2078 (store.engine.info.capabilities), engine/katago/types.ts:276 (query side) AND :485 (advertisement side — the actual target; the candidate cites only 276), version-probe.ts:64/106/185, capability-injection.ts:105/170/173/233, and composables/board/usePlayFromPosition.ts:221/314/371/504. Cost line (\"a handful of interfaces, one validation site, two cast deletions\") undercounts the mechanical signature thread-through; still small-tier. (c) The validation seam already exists: parseVersionResponse in version-probe.ts is a pure, unit-tested boundary parser that already shape-validates the outer dict (and currently drops non-record metadata entries silently); the proposal lands as an extension of it, not a new site. (d) Loudness calibration: \"fail loudly on shape mismatch\" must not extend the connection-refusal surface — refusal is contractually reserved for missing delta_analysis (version-probe.ts:184, per the dispatch's Frontend-will-not clause), and whole-field malformation keeps the documented feature-detection-to-null disposition (version-probe.ts:84–98). A known capability with mismatched metadata should degrade that one capability with a level-4/5 surface (pushSystemMessage / console.warn per ADR-0002's loudness hierarchy), not null the advertisement or refuse the connection. (e) Unknown capability NAMES must keep passing through, not just unknown metadata keys: parseVersionResponse currently preserves arbitrary names, and presence checks ('transposition' in advertised) plus forward compat depend on that; the capability-name union types known metadata but must not become a closed parse. (f) Dispatch evidence sharpening: proxy-to-frontend-learned-vf.md:150 names a type (AdaptiveReevaluateCapability) that never existed in types.ts — the binding dispatch presumed a typed per-capability mirror and the deviation was never flagged; that dispatch also still carries Status: Open though shipped, so a status sibling/closure is adjacent doc debt the implementing PR should note. (g) Cross-reference, not duplicate: open active item silent-coercion-protocol-boundaries-audit covers the enum-vocabulary register of the same ADR-0002-at-the-boundary concern; the new item should ref it (and gradingparameter-opacity-typing is the filed precedent for this item shape).",
 "alreadyFiled": "",
 "priorityHint": "keep"
}
````

### §3 · verify:fit:services-boundary-deny-by-default

**Verdict (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Could not refute on any of the four grounds. (1) No tenet fight: the inversion strengthens the documented layering tenet (frontend/CLAUDE.md \"Architectural shape\") rather than fighting it; it preserves the config's deliberate small-targeted-ruleset posture (it stays one rule, polarity inverted) and follows the config's own documented measure-first adoption pattern (a75814c, verified as the error-message-guard commit). Step (b) is not a rogue relocation — it is literally ADR-0010 Revisit #4's own named option (\"relocating reactive-state modules out of services/\"), correctly framed as a separate sign-off-gated arc; ADR-0010's \"no retroactive sweep\" and ADR-0003's \"not a refactoring mandate\" clauses govern component rewrites and Port extraction, not this. (2) Not a duplicate: keyword sweeps over items (title+description) and refs (targets) in the todo DB for eslint/lint/boundary/ADR-0010/reactive-state/relocation/blocklist/restricted-imports returned zero matches; none of the 33 open frontend/umbrella items covers this; refactoring-queue-adr0007 and semantic-clarity-refactors-effect-typing are unrelated. (3) Cost/benefit honest and verified: the census staleness is real (zero current imports of the four restricted services by the four named components), the fail-open gap is real and actually WIDER than claimed (api-client and resource-service are also absent, beyond sync-service/qeubo-service), and the shake-out is measurably tiny — current component-layer service imports are six sanctioned reactive-state sites plus one type-only import from the pure analysis-bundle module. No runtime perf cost; the gain is a fail-closed CI-gated boundary (frontend CI runs eslint . since 2026-06-01). No unsubstantiated perf assertion to correct per ADR-0009. (4) Vocabulary mostly composes (seam, effectful-vs-reactive split, measure-first, FILES.md/doc-graph duties); one slip noted in corrections. Priority: keep, not raise — the boundary holds in practice today (no live violations), so this is preventive guard-hardening plus filing a documented-but-untracked ADR tension, cheap and worthwhile but not an active defect.",
 "corrections": "(a) The fail-open gap is wider than stated: besides sync-service and qeubo-service, the effectful api-client and resource-service are also absent from the blocklist; analysis-bundle (pure projection, per its header \"No side effects, no network\") and query-id (pure brand factory) are absent but non-effectful, so the deny-by-default design must classify them deliberately — AnalysisControls.vue:17 already does a type-only import of AnalysisBundleStorageError from analysis-bundle, which the proposed type-only exemption covers. (b) stability-trajectory-store.ts is a third reactive-state module (same observe-via-getter shape as analysis-ledger, per its header); step (a)'s exemption list should be the full reactive-state class {analysis-ledger, analysis-config, stability-trajectory-store}, consistent with step (b)'s own relocation set — the current eslint header comment names only two. (c) New gap the candidate missed: the layering tenet defines Components as src/components/* AND src/App.vue, but the lint block's glob is src/components/** only — and src/App.vue:30 imports analysisService (a restricted singleton) today, outside the rule's reach. The inversion PR should either widen the glob to include App.vue (one real adjudication: justified exemption for the omnibus wiring file, or composable extraction) or name the App.vue gap explicitly per ADR-0002; leaving it silently uncovered would repeat the fail-open lesson inside the fix. This slightly qualifies the \"small\" cost if scope is made tenet-faithful. (d) Vocabulary fix: \"state/ band\" should be \"state/ directory (layer)\" — bands are ADR-0003's domain-coupling tags (B1/B2/B3), orthogonal to the Components/Composables/Services layer axis; relocated modules keep their B1 band tags, FILES.md entries move paths (re-tag only if coupling changes), and ADR-0003's Band 1 inventory entry naming services/analysis-ledger.ts by path gets refreshed under that ADR's inventory-drift clause (\"the inventory will drift\"), which is the accurate citation rather than \"ADR-0003's amendment path\". (e) Sharpened census fact for the header refresh: the four components named at :82-86 have zero imports of the four restricted services today; the live component-layer imports are BoardTab (ledger+config), BoardWidget (ledger+config), ToolbarEngineMetrics (ledger+config), AnalysisControls (ledger + type-only analysis-bundle).",
 "alreadyFiled": "",
 "priorityHint": "keep"
}
````

### §3 · verify:exist:keyed-cache-brand-at-construction

**Verdict (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Every load-bearing claim verifies at HEAD. The 2026-06-08 brand arc is real: 0792f9a (#361, ledger provenance stratification), 6cb99d3 (#362, trajectory twin — commit message confirms it was found by the first fix's author as a deliberate follow-up), dd3b85e (#363, key-brand tightening). The worklog docs/worklog/2026-06-08-key-brand-tightening.md (read in full) confirms the follow-up audit found no new instance of the dangerous under-keyed-cache class and reports 817 tests passed with type-level-only changes (no runtime change; #362 additionally reports a perf-battery null result). The r:/e: prefix exists (frontend/src/services/analysis-bundle.ts:192-193) and was introduced in 0792f9a on 2026-06-08, exactly one week after the G1 lint landed (a75814c, 2026-06-01). No keyed-cache rule exists in docs/pre-merge-checklist.md (read end to end), no relevant lint exists in frontend/eslint.config.js, and no matching item exists among the 88 rows of the todo DB. The candidate survives; several details need sharpening (G1 scope overstatement, the existing prose brand rule, and the prefix audit being largely answerable already) but none refute the core: the dependency-set-at-declaration rule for new keyed caches exists nowhere, discovery of the class instances was reactive, and a stringly discriminator did ship on the persistence path in the same arc.",
 "corrections": "(1) G1 scope: the candidate says \"stringly contracts were banned via G1\" — G1 (RCA docs/notes/postmortem/rca-discipline-lapses-2026-06-01.md §4, adopted 2026-06-01 in a75814c as eslint no-restricted-syntax) bans reverse-engineering structure from error-MESSAGE strings specifically (.includes/.match/.startsWith/.test on `.message` receivers). The r:/e: configHash prefix is outside G1's letter and would not trip the adopted lint; the irony claim should read \"same species in spirit (structure encoded in a string, reparsed downstream), outside the adopted guard's letter.\" (2) Sharper irony than claimed: the prefix landed in the SAME COMMIT (0792f9a) as the first brand fix, not merely the same week. (3) \"The rule lives nowhere enforceable\" needs nuance: a general branded-identifier prose rule already exists (frontend/CLAUDE.md \"Type-driven design\": brands for confusable identifiers, construction via ACL or dedicated factory, raw string/number does not flow through the domain) and frontend/IDENTIFIERS.md carries an add-a-row-per-new-brand discipline with construction-site soundness notes. What exists nowhere is the candidate's actual novel content — the key declaration naming the DEPENDENCY SET of the value (the under-keying class; the ledger's old composite key could have been branded and still wrong) — plus no checklist line and no lint. (4) The proposed bounded audit is largely already answerable in the safe direction: mint and parse each occur in exactly one function in analysis-bundle.ts (projectLedgerToBundle :226/:231; replayBundleIntoLedger :259-276); repo-wide grep finds no other parse site; the backend stores config_hash as an opaque str (backend/domain/analysis_bundle.py:90, no prefix interpretation); and collision with legacy bare hashes is impossible because legacy values are DJB2 lowercase-hex ((hash>>>0).toString(16), analysis-config.ts:164-171) or the literal 'default' — neither can start with 'r:' or 'e:'. By the candidate's own criterion, neither audit condition fails, so the promote-to-typed-record branch would not trigger; the audit's deliverable is a recorded confirmation, not a migration. The prefix is also already documented in IDENTIFIERS.md's RawKey/EnrichedKey rows (including the r:/e: re-brand site at replay). (5) Supporting precedent for the optional lint: a custom local ESLint rule (clear-needs-ownership, fe4b7eb, 2026-06-09, frontend/eslint-rules/) shows the project-local-rule path is established. (6) The one remaining documented stringly soft spot in this neighbourhood — the trajectory store's `|`-delimited composite key assuming `|`-free NodeIds — is already recorded in IDENTIFIERS.md \"Known erosions\" (documented-not-scheduled), which supports the class concern without being new work.",
 "alreadyFiled": "",
 "priorityHint": "keep"
}
````

### §3 · verify:fit:keyed-cache-brand-at-construction

**Verdict (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Not refuted on any of the four grounds. (1) Tenet fit: the proposal composes with the binding tenets rather than fighting them. ADR-0002's loudness hierarchy names compile-time error as the strongest channel, and the ledger worklog + IDENTIFIERS.md already describe branded keys as exactly that (\"ADR-0002's strongest channel\"); frontend/CLAUDE.md's \"Type-driven design\" section already carries the branded-identifier rule this sharpens (keyed caches + dependency-set naming is new content, an increment not a redundancy). ADR-0005 Revisit-when #3 pre-authorizes absorbing a new discipline by appending a rule, and the 2026-06-01 RCA's synthesis (\"this project's characteristic failure mode is the invisible-at-authoring-time, visible-only-in-aggregate defect, against which its heavy policy discipline is structurally weak and only mechanical nets help\") plus its Open Question 4 explicitly invite this kind of codification. (2) Not a duplicate: no todo-DB item covers a construction-time branded-key rule, the prefix audit, or the lint. Nearest neighbors are distinct: silent-coercion-protocol-boundaries-audit (open) targets closed-set wire vocabularies parsed with open-set defaults, a different defect class; adr-effectiveness-audits (open) is the RCA-named \"adjacent vehicle\" for cross-cutting articulation but is scoped to per-tenet audit cadence + doc reorganization — the codification PR should cross-reference it, not be absorbed by it. (3) Cost/benefit is honest and actually improves on inspection: the retrofit-cost evidence is real and substantiated (817 tests green per the key-brand-tightening worklog; both stratification PRs ran the full-stress perf battery with null results, so no ADR-0009 hypothesis-form correction is needed), the class has bitten twice in one day with user-visible symptoms (blank overlays; stranded stability trajectories), and I executed the proposed bounded audit during verification — both halves pass (see corrections), shrinking the residual work to essentially doc-only hours. (4) Vocabulary fit is native: branded types, single-mint-site factories, IDENTIFIERS.md as the namespace registry, the v1/v2 bundle precedent — this is the codebase's working language, not a generic import. Priority: keep, not raise — the known instances are closed, the 2026-06-08 fan-out audit found no third instance of the dangerous class, and the r:/e: prefix is sound and documented, so the rule pays off only on future keyed caches; but the class's demonstrated recurrence and the hours-scale cost argue against lowering.",
 "corrections": "Four sharpenings to carry into the final report. (a) \"Stringly contracts were banned via G1\" overstates G1. G1 (RCA §4, adopted at error in frontend/eslint.config.js as a no-restricted-syntax rule, rationale comment citing the RCA) bans error-MESSAGE reparse specifically (.includes/.match/.startsWith/.test on a `.message` receiver). The r:/e: configHash prefix is outside G1's scope; no adopted guard covers it. Soften to: the prefix is the same family (structure encoded in a string, reparsed downstream) as the lapse G1 mechanized against — and unlike Lapse 1 it was a deliberate, loudly-documented design choice: the ledger worklog records the resolution (wire schema_version is a backend-gated literal 1; rippling the two-store split through every v2 encoder scheme was the rejected alternative), the prefix note in analysis-bundle.ts documents all three record shapes, and the legacy branch warns loudly per ADR-0002. (b) The proposed prefix audit is already answerable — performed during verification: mint sites only in projectLedgerToBundle (frontend/src/services/analysis-bundle.ts:226,231), parse only in replayBundleIntoLedger (analysis-bundle.ts:259-276), no other reference in frontend/src or frontend/tests; collision is impossible because legacy bare configHash values are hashConfig outputs — (hash >>> 0).toString(16), lowercase hex, or the literal 'default' for undefined (frontend/src/services/analysis-config.ts:164-171) — and neither contains a colon, so neither can start with 'r:' or 'e:'. The conditional promotion to a typed/versioned record will not trigger; reshape the audit deliverable to \"record this conclusion\" (e.g., a line in the analysis-bundle prefix note and/or the IDENTIFIERS.md rows, which already document the prefix split and mark both keys Sound). (c) The risk-section claim that \"the checklist anchor … is what keeps this from reproducing the failure\" over-credits the checklist: docs/pre-merge-checklist.md is explicitly NOT a merge-blocking gate (template-not-gate framing, §7.3 amendment provenance) — it is a template trusted sessions consult. A checklist line is the RCA's G4 shape, and the RCA's own caveat applies verbatim: \"a checklist guarded by memory is weaker than a lint; this is a mitigation, not a fix.\" Only the lint (precedented: G1 and the clear-needs-ownership lint, commit fe4b7eb, both live in eslint.config.js with the measured-in-warn-mode discipline) mechanizes. (d) Minor class-membership trim: KnobDomain belongs to the closest-match/classification family (ADR-0002 Rule 7's codification instances; ADR-0008), not the under-keyed-composite-cache class — citing it dilutes the class definition. The class evidence stands on the ledger + trajectory same-day twins and the brand-hygiene gaps the 2026-06-08 audit closed. Also note for shaping: the natural codification home is an append to frontend/CLAUDE.md's existing \"Type-driven design\" section, and the PR should cross-reference the open adr-effectiveness-audits item (RCA Open Question 4 names it as the vehicle for the cross-cutting meta-shape) and likely file its own small work-status item per the store discipline.",
 "alreadyFiled": "",
 "priorityHint": "keep"
}
````

### §3 · verify:exist:work-status-authoring-hygiene

**Verdict (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Every checkable claim held at HEAD. DB queries confirm: pv-hover-jank-range-query (open, 'cause unclear' verbatim in description) and many-boards-open-slowness (open) have zero refs rows; nav-during-range-query-perf has the contrast refs row to the 05-29 audit; rb3-packet-receive-chunking is closed/dropped 2026-05-29 with the ~99ms-vs-~2.35s correction recorded verbatim in its description; spa-board-scope-consistency-audit is open while PR #357 (the audit doc) merged 2026-06-08. perf-audit-nav-and-pv-hover-2026-05-27.md (read end to end) diagnoses exactly the two unref'd symptoms (Bugs A and C) and its four planned fixes landed as the 2026-05-27-perf-fix1..4 worklogs. The 2026-05-17 pbo-claim-rehydration worklog (read end to end) defers the module-scope rebinding audit per postmortem §7.3; no DB item for it exists; two later scope/rebinding-lifecycle bugs (tags-fetch-hydration-race fixed 06-03, mini-board-texture-scope fixed 06-09 on the thumbnail-cache surface §7.3 explicitly named) substantiate the recurrence claim. The match postmortem (read end to end) carries ~five §5 recommendations, none filed and none quietly delivered (buildAnalyzeQuery's docstring still opens with the exact ambiguous phrase the postmortem flagged; no getActiveVariationPath audit doc; no rigor rubric in ADRs). Not already fixed: the 2026-06-02 liveness audit (read end to end) validated existing items' asserted status only — it did not harvest prose deferrals, add refs rows, or set an authoring convention. No duplicate item exists (harvest/deferral/hygiene searches return only the closed work-status-labels-facet). 91 worklogs in docs/worklog/ matches the cited miner count; DB at 88 items / 49 open; work_status_violations empty.",
 "corrections": "Sharpenings, all strengthening or refining rather than refuting: (1) Both unref'd perf items were filed after the 2026-06-02 migration (neither appears in the 59-item liveness audit), i.e., they record symptoms observed after the 05-27 audit's fixes landed — so the proposed refs rows should frame that audit as a prior, partially-consumed diagnosis, not a fresh one. (2) Concretely in the candidate's favor: the 05-27 audit's Bug C root cause 2 (useEnrichedData/visitVector reactive cascade starving the PV's rAF) was never in the four-fix sequence (fix #3 addressed only C.1) — a diagnosed, undischarged cause exists for the item whose description says 'cause unclear'; later 05-30/31 throttling work may have partially addressed it, which is exactly why the candidate's re-profile-first step is right. (3) The 'recurred twice' claim: tags-fetch-hydration-race (fixed 06-03) is squarely the deferred audit's class and was itself tracked as a DB item before its fix; mini-board-texture-scope (fixed 06-09) is the broader module-scope-vs-per-instance state-lifecycle class rather than strictly 'rebinds from persisted truth on reload', though it hit the thumbnail-cache surface the §7.3 candidate list explicitly named. (4) spa-board-scope-consistency-audit is not a zero-refs example — it has five refs rows including the merged audit doc (PR #357, 2026-06-08); its open question is purely the status/residual one (the description gates a scope-handling decision, and follow-on fixes landed 06-09), so the maintainer-question framing is correct. (5) The 'five match-arc recommendations' count is approximately right (5a x2 docstrings, 5b audit + optional branded path types, 5c rubric); one is partially encoded (numMoves field comment says 'relative to the start') but the recommended buildAnalyzeQuery root-to-current warning is absent and the docstring still opens with the exact ambiguous phrase the postmortem flagged. (6) Scale update: the DB now holds 88 items (49 open), up from 59 at the 06-02 migration; invariant gate clean. (7) Supporting spot-checks: the green-perf retro's container-query deferral WAS filed and closed (de-cq-preview-hide), supporting 'every deferral that entered the DB is accounted for'; the 05-17 worklog's smaller deferrals (structuredClone migration-chain cost, intlify warning suppression) are unfiled, consistent with the '~15 smaller deferrals' direction (that count itself not exhaustively re-verified).",
 "alreadyFiled": "",
 "priorityHint": "keep"
}
````

### §3 · verify:fit:work-status-authoring-hygiene

**Verdict (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Attempted refutation on all four grounds; none hold. (1) Tenets: ADR-0002, ADR-0005, ADR-0009 read end to end. The proposal is these tenets' own register applied at deferral-authoring time — the file-on-defer symmetric of RCA guard G4, composing with G5 (the shipped todo-DB SSOT). The one-time harvest pass does not fight ADR-0005's no-retroactive-rewrite Neutral clause (precedents: Rule 9's one-time pre-SSOT design-note pass; the maintainer-commissioned 2026-06-02 liveness audit); the CI grep is correctly advisory-never-gate per the RCA's G6 calibration and the pre-merge-checklist retraction; the re-profile leg applies ADR-0009 in hypothesis form, citing rb3's drop record accurately (verbatim in the DB). (2) Duplicate: full 88-item dump scanned; not filed. Nearest neighbors are adr-effectiveness-audits (per-tenet audit cadence + doc reorganization — a distinct duty) and the completed 2026-06-02 liveness audit, which validated items already IN the DB; the candidate addresses prose deferrals that never ENTERED it — the inverse direction, so it adds new evidence rather than restating a filed item. (3) Cost/benefit: honest and verified this session — pv-hover-jank-range-query and many-boards-open-slowness are open with zero refs while perf-audit-nav-and-pv-hover-2026-05-27.md (read end to end) diagnoses exactly those two symptoms (Bug A/Bug C) with ranked causes; the 2026-05-17 module-scope-rebinding deferral exists verbatim and matches no DB item, and the bug class demonstrably recurred (mini-board-texture-scope, shipped 06-09, whose own follow-up WAS filed — confirming the contrast claim); match-arc postmortem recommendations 5a/5b/5c confirmed dangling; title-residual drift on i18n-string-sweep and nav-during-range-query-perf confirmed from descriptions. Costs (minutes per refs row/retitle, day-scale pass, bounded perf-battery runs) are plausible; there is no perf loss at all, and the auditability gain addresses what the RCA names as the project's characteristic failure mode. The risks section is unusually honest (ceremony creep, keyword false negatives, read-fully before closing). (4) Vocabulary fit: native — SSOT delegation, refs rows within the closed kind enum, drop-with-dated-record matching rb3's worked shape, the not-filed marker paralleling ADR-0009's explicit-unsubstantiated qualifier. priorityHint raise: it extends the maintainer's own commissioned RCA arc at low cost, and two affected items are user-prioritised perf symptoms.",
 "corrections": "Four sharpenings to carry, none load-bearing against the candidate: (a) The 05-27 audit's Bug C cause #1 (the MoveSuggestions packet-watch guard) already shipped as perf-fix3, so pv-hover-jank-range-query's \"cause unclear\" residual is plausibly the audit's un-acted-on causes C.2 (useEnrichedData/visitVector reactive cascade) and C.3 (ownership normalization alloc); the new refs row should carry that note so a reader doesn't misread the diagnosis as wholly un-acted-on, and the re-profile should target the residual causes. (b) Ref-kind wrinkle: audit docs are currently ref'd under kind 'design-note' (existing precedent on two items); attaching one audit doc to TWO items brushes a pedantic reading of ADR-0005 Rule 9's \"exactly one owning item per design-note ref\". Audit records are not design notes, and the refs kind enum lacks an 'audit' value — an ADR-0002 Rule 7 closest-match seam the harvest PR should name explicitly rather than silently reuse. (c) spa-board-scope-consistency-audit: PR #357 (the audit doc) MERGED 2026-06-08, but the item's description gates a scope-handling decision on the still-open card-metadata-during-review fix — soften \"appearing substantially shipped\" to \"the audit deliverable shipped; the gated decision leg is the plausible open residual\". Maintainer-question framing is correct. (d) Aggregate counts (\"five match-arc recommendations\", \"~15 smaller deferrals\") were spot-checked at the exemplar level only, not exhaustively recounted; and \"the bug class recurred twice\" verifies unambiguously for one recurrence (mini-board-texture-scope) — carry as \"at least once, plausibly twice\" unless the second instance is named.",
 "alreadyFiled": "",
 "priorityHint": "raise"
}
````

### §3 · verify:fit:adr-trigger-sweep-and-record-hygiene

**Verdict (verbatim):**

````json
{
 "alreadyFiled": "adr-effectiveness-audits",
 "corrections": "(1) \"Two silently-fired triggers\" is slightly over-formal. ADR-0001 Revisit-when #2's headline condition (\"performance profiling reveals specific reactivity hot spots\") fired on a plain reading, but its anticipated form (\"deep-proxy access is actually expensive ... shallow projections\") is not what the 2026-05-27/28 audits found — they found O(N) find-walks (fixed by boardsById) and array-dep over-invalidation from the identity swap. The readonly decision itself stands and needs no revisiting; the load-bearing defect is the stale mutator-benefits enumeration at ADR-0001 lines 99-103, best filed under ADR-0005 Rule 8 (wrong in a load-bearing way -> Amendments-header entry), with the trigger framing secondary. (2) Scope the ADR-0001 correction to mutateBoard only: mutateReviewSession still performs the re-wrap (frontend/src/store/index.ts:209). (3) ADR-0003's Revisit-when #2 as written is about band-distribution shift; what actually happened is path-level staleness from a directory reorganization (6 composables moved into composables/{analysis,board,forest,review}/, four components into components/{charts,board,tree}/). Ground the amendment in ADR-0005 Rule 1 (FILES.md, 215 band tags with same-PR cadence, is the de-facto per-file band SSOT) plus ADR-0003's own Negative-clause refresh duty, rather than a literal trigger firing. State staleness as \"roughly half the cited paths\" — exact 9/19 depends on enumeration choices. (4) New fact: the chess-clone item is open/ACTIVE (not future) in the todo DB, so ADR-0003 Revisit-when #1 (\"a second domain adopter materializes\") may also be live; the executing session should check that item before cutting the inventory/port-sizing prose, since \"What a Chess port would actually require\" may be about to become load-bearing. (5) ADR-0010 artifact confirmed at byte level (lines 266-267, after the License section); per Rule 8's spirit a one-line Amendments note alongside the deletion is the conservative shape even though the artifact is a paste defect, not content. (6) Amendments that add cross-references (e.g., ADR-0001 citing the game-scroll perf audit) are structural doc edits — the doc-graph regeneration duty the candidate's risks section names is real and applies.",
 "priorityHint": "keep",
 "reasoning": "Could not refute on any of the four grounds. (1) Tenet fit: the proposal uses the project's own amendment convention (ADR-0005 Rule 8 + Amendments header, precedent set by 0005's and 0009's own recorded firings, both read end to end), and the ADR-0003 delegation is supported by ADR-0005 Rule 1 — two parallel per-file band inventories drifting is exactly the failure mode Rule 1 names. No retroactive-rewrite clause is violated: ADR-0001's misdescription is the \"wrong in a load-bearing way\" case whose prescribed response IS an amendment. (2) Duplicate: adr-effectiveness-audits (open/active, umbrella/large) covers only the cadence leg, which the candidate explicitly proposes folding into rather than duplicating; the three corrective legs are unfiled, and the candidate adds concrete new evidence (two verified unrecorded drift instances + a byte-verified committed artifact + a sweep checklist) — new signal beyond the filed item, so the duplicate rule does not refute. (3) Cost/benefit honest: all factual claims verified (ADR-0001:99-103 vs store/index.ts:153-173; ~half of ADR-0003's cited paths no longer resolve; FILES.md band-tag count exactly 215 as claimed; ADR-0010 tail artifact byte-confirmed; DB state confirmed). No perf claims, so ADR-0009 doesn't bite. Half-a-day estimate plausible; \"append-only\" slightly undersells the ADR-0003 inventory cut and doc-graph regeneration, both named in risks. The auditability gain is genuine: ADR-0001 is a canonical reference every session is routed to under the read-fully discipline, and it currently asserts as a live mechanism one the code deliberately removed — a reader is actively misled, which is the precise failure class the review's tradeoff privileges. (4) Vocabulary fit: speaks in Revisit-when triggers, Amendments headers, bands, FILES.md tags, doc-graph duty, work-status-store folding — native, not imported. Priority \"keep\" rather than \"raise\": real and bounded doc-hygiene corrective folding into an already-filed large item, with no code-correctness or user-facing impact.",
 "refuted": false
}
````

### §3 · verify:exist:adr-trigger-sweep-and-record-hygiene

**Verdict (verbatim):**

````json
{
 "alreadyFiled": "",
 "corrections": "(1) ADR-0001 scope: only the re-wrap bullet (lines 100-103, the `store.boards[index] = { ...board }` benefit) is now false, and only for mutateBoard — mutateReviewSession still identity-swaps (`store.session.reviews[boardId] = { ...review }`, frontend/src/store/index.ts:209). The version-counter, invariant, and grep-target bullets still hold; \"lines 97-107 misdescribe the mutator\" should be narrowed accordingly. The amendment should also fix ADR-0001's Related-section claim that the mutators' \"docstrings reference this ADR\" — mutateBoard's no longer does. (2) Mutator location: frontend/src/store/index.ts (store singular, not stores/), function at lines 153-173, rationale comment 157-170; the comment itself cites a stale path (docs/notes/perf-audit-game-scroll-2026-05-28.md — actual: docs/notes/audit/perf-audit-game-scroll-2026-05-28.md), a small adjacent hygiene fix. (3) ADR-0010 artifact survived THREE later commits (f46d35e, 061cd81, 59aef38), not two; it has been present since creation (5da97f1). (4) ADR-0003: the 9-moved-paths numerator is confirmed exactly (wait-for-analysis, useTreeExpansion, useTreeLayout, useReviewSession, useChartNavigation, BoardDisplay, BoardWidget, TreeWidget, use-move-suggestions; BaseChart.vue, listed bare, is also at components/charts/ now), but the denominator 19 is a counting convention I could not reproduce — I count ~23 concrete file entries; state the ratio as \"9 of the inventory's ~23 file references\". The drift is path-staleness from the composables/components reorg, not demonstrated band misclassification — supports delegation-to-FILES.md but says nothing about band-tag accuracy (candidate's risk note already correct on this). FILES.md band-tag count 215 confirmed exactly (78 B1, 39 B2, 97 B3, 1 B?). ADR-0003's only post-initial edit was a one-line cross-ref path fix (ca871d1), strengthening the never-refreshed claim. (5) The \"two recorded properly (0005, 0009)\" contrast is verified for those two (ADR-0005 Amendments header + \"(Fired 2026-06-01)\" on Revisit-when #2; ADR-0009 \"Amended (2026-06-01)\" section), but whether they are the ONLY recorded firings among ten ADRs is unverified — ADRs 0002/0004/0006/0007/0008 were not read this session. (6) The \"38-trigger checklist\" count is not verifiable from the repo (miner-internal); the five ADRs read here carry 21 numbered Revisit-when triggers; the executing session must re-derive the full count from end-to-end reads. (7) ADR-0001 trigger #2's text says firing \"might or might not motivate revisiting readonly\" — the amendment records the firing and the factual correction; it does not obligate a decision change.",
 "priorityHint": "keep",
 "reasoning": "Every load-bearing factual claim checks out at HEAD. (a) ADR-0010 lines 266-267 are byte-exactly `</content>` / `</invoke>` (od -c verified), committed at creation and never removed across three subsequent commits touching the file. (b) ADR-0001 has exactly one commit (never amended) and still presents the `store.boards[index] = { ...board }` re-wrap as a benefit of the mutator convention, while mutateBoard at frontend/src/store/index.ts:153-173 removed exactly that identity swap with an in-code rationale citing the game-scroll perf audit (Arc 1) — i.e., removed for documented render reasons, and the perf-profiling-reveals-reactivity-hot-spots condition of its Revisit-when #2 factually occurred with no record. (c) ADR-0003's inventory has 9 file references that no longer resolve as written (all moved in the composables/components reorg), its only post-initial edit was a one-line cross-ref fix, and frontend/FILES.md carries exactly 215 band tags — the de-facto-superseded framing holds. (d) ADR-0005 and ADR-0009 do record their fired triggers via the amendment convention, confirming the recorded-vs-rotted contrast for the named pair. (e) The todo DB item adr-effectiveness-audits is open/active with the per-tenet cadence leg explicitly still open, and does not already cover the three concrete corrective edits, so the candidate is an overlap/extension, not a duplicate. Not refuted; details sharpened in corrections (artifact survived three edits not two, the ADR-0001 misdescription is one bullet of the benefits list not the whole section, the 9/19 denominator is unreproducible, and the 38-trigger count is unverified). Priority: keep — real, bounded, auditability-grade doc-integrity work with no runtime impact; cost estimate (small, append-only amendments + two-line deletion + CI grep + DB description edit) is plausible against what I saw.",
 "refuted": false
}
````

### §3 · verify:exist:outbound-wire-mappers-acl-symmetry

**Verdict (verbatim):**

````json
{
 "alreadyFiled": "",
 "corrections": "Five sharpenings; the core finding stands at HEAD. (1) Doc-promise mischaracterization, load-bearing: what the dispatch ledger records as promised-and-not-done is a docs/notes/ ledger note recording the known data keys (frontend reply in docs/dispatch/backend-to-frontend-card-metadata-inline-edit-status.md: \"A small ledger note will be left in docs/notes/ ... recording the known keys under data\"; arc2-consumed dispatch lines 219-223: \"Not done; flagged as the natural companion arc when Path 2 becomes the next move\"). A wire-schemas.md gradingParameter section was never promised near-term — publishing there IS Path 2 itself, explicitly roadmap/deferred \"when the frontend's data shape has stabilised\", with backend mirroring as Pydantic, and the arc2-consumed dispatch closes by saying Path-2 work \"will open its own dispatch arc\". So the candidate's \"never-written wire-schemas known-keys note ... recorded as promised\" conflates the unilateral promise (docs/notes/ note — verified absent by grep over docs/notes/) with the cross-boundary Path-2 deliverable. The proposal's \"write the promised wire-schemas.md section in the same arc\" should be reframed: writing it is deliberately triggering Path 2, a dispatch-first cross-boundary contract change, not late fulfilment of a promise. (2) Name collision: a generated GradingParameterData type already exists at frontend/src/types/backend.ts:1406-1414, shaped {gamma?: number|null} & {[key:string]: unknown} — backend already publishes gamma typed plus an open-keyed escape (landed with PATCH arc 2). frontend/CLAUDE.md forbids duplicating generated declarations, so the proposed hand-authored \"GradingParameterData interface\" must take a different (domain-side) name or extend components['schemas']['GradingParameterData']. Note also the generated CardCreate.grading_parameter is plain {[key:string]: unknown} | null — the create and PATCH wire schemas are asymmetric backend-side, so a frontend mapper alone cannot make the create leg schema-checked end-to-end; another reason the candidate's dispatch-don't-unilaterally-declare risk note is right. (3) The asymmetry is narrower than \"the outbound leg\": the PATCH leg already has an outbound ACL projection (backend-service.ts::updateCardMetadata, lines 191-205, wraps domain gradingParameterData into grading_parameter.data and composes the snake_case body inside the ACL). The rule contradiction is specific to the create flow: createCard (lines 207-210) forwards a payload the composable built. Supporting evidence the candidate missed: readGradingParam's comment (backend-service.ts:63-65) — \"If the backend ever publishes a typed schema for grading_parameter, this helper can be deleted\" — names the same direction from the inbound side. (4) Known-keys inventory is incomplete: useMinting also writes overrideSettings (camelCase key, conditionally included at lines 111/130) into grading_parameter.data; any typed interface or known-keys note must carry it alongside gamma/default_visits/analysis_config. (5) Minor line cites: the brand-erasure cast is at useMinting.ts:51 (its justifying comment ends at :46) and strips the CardId brand on parent_card_id, not anything inside grading_parameter; the Record<string, any> declaration is at :108, second variant :119-132; the file is composables/review/useMinting.ts (the types.ts comment itself cites the stale pre-move path composables/useMinting.ts — small corroborating doc-rot). DB item gradingparameter-opacity-typing verified this session: state=open, disposition=future, scope=both, tier=small, description matches the candidate's quote; it covers the typed-schema half but not the ACL-symmetry half nor the docs/notes promise, so widening (as the candidate proposes) rather than new filing is the right shape — not a duplicate.",
 "priorityHint": "keep",
 "reasoning": "Attempted refutation on every factual leg; all core claims hold at HEAD. Verified by direct read: useMinting.ts (180 lines, read in full) builds the snake_case CardCreatePayload in a composable with grading_parameter: Record<string, any> at :108 and a second hand-built specific-palette variant at :119-132; types.ts:2113-2130 documents the absent inverse-mapper and names the composable; frontend/CLAUDE.md:59-60 carries the exact rule text (\"The ACL is the only place wire shapes appear; no other module sees snake_case\"); docs/wire-schemas.md read end to end (621 lines) — no gradingParameter section among §1-§9; 14 dispatches in docs/dispatch/; both card-metadata dispatches read end to end confirm the Ask-3 Path-1/Path-2 record and the \"Not done\" known-keys-note deferral; psql query against the todo DB confirms gradingparameter-opacity-typing is open/future with the quoted conditional description; createCard still forwards the composable-built payload, so nothing was fixed. The candidate survives. Details that are off (the promised note was docs/notes/, not wire-schemas.md; a generated GradingParameterData already exists; the PATCH leg already has an outbound ACL projection so the contradiction is create-flow-specific; overrideSettings missing from the key inventory; cast at :51 not :46 and it brands parent_card_id) are corrections, not refutations — none undermines the central claim that the ACL rule is visibly contradicted on the create flow and the half-state is real and documented in the code's own comments. No perf claims asserted, so ADR-0009 is not implicated. Priority: keep — evidence is real and the cost/risk framing in the candidate is roughly right; the sharpenings reduce the doc-side scope (the unilateral piece is a docs/notes note; the wire-schemas section is a dispatch-gated Path-2 trigger) but also strengthen the code-side case (generated type already half-exists, making the typed mapper cheaper than the candidate assumed).",
 "refuted": false
}
````

### §3 · verify:fit:outbound-wire-mappers-acl-symmetry

**Verdict (verbatim):**

````json
{
 "alreadyFiled": "gradingparameter-opacity-typing",
 "corrections": "(1) Promise conflation: the unfulfilled commitment is the docs/notes/ known-keys ledger note (promised in the frontend reply of backend-to-frontend-card-metadata-inline-edit-status.md; recorded \"Not done; flagged as the natural companion arc when Path 2 becomes the next move\" at frontend-to-backend-card-metadata-inline-edit-arc2-consumed.md:219-223; verified absent by grep over docs/ and frontend/). The wire-schemas.md gradingParameter section was never promised — it is the Path-2 formalization both sides deliberately deferred until \"the shape settles through use\" (backend's stated preference: Path 2 \"as a later tightening\"). Frame the proposal as activating the roadmapped Path 2 on new evidence, not as fulfilling a broken promise. (2) The asymmetry is narrower and the precedent stronger than stated: backend-service.ts::updateCardMetadata (lines 191-205) already performs an outbound domain-to-wire projection (camelCase CardMetadataPatch → snake_case body, gradingParameterData → grading_parameter:{data}). The create leg is the only outbound leg without a mapper — this is the in-file second use case that defuses any ADR-0003 wrong-abstraction objection and lowers the cost below the candidate's estimate. (3) Name collision: the codegen already declares components['schemas']['GradingParameterData'] ({gamma?: number|null} & {[key: string]: unknown}, types/backend.ts:1406-1414) and GradingParameterPatch for the PATCH leg, while CardCreate.grading_parameter is still opaque ({[key:string]: unknown} | null, types/backend.ts:1013-1015). A hand-authored frontend interface named GradingParameterData would duplicate a codegen declaration, which frontend/CLAUDE.md forbids (\"never duplicate its declarations\"). Native shape: dispatch the backend to reuse its existing GradingParameterData schema on CardCreate.grading_parameter, regenerate via npm run gen:api, and type the outbound mapper against codegen; any frontend domain-side type takes a distinct camelCase domain name (the CardMetadataPatch.gradingParameterData pattern). (4) Known-keys set incomplete: useMinting.ts also conditionally writes overrideSettings into grading_parameter.data (lines 107-113 and 130); the candidate's enumeration (gamma, default_visits, analysis_config) omits it — itself evidence the authoritative known-keys record is overdue, and a caution that the filed item's \"shape stabilized\" condition is not self-evidently met (the open-keyed escape in the proposal handles this honestly). (5) The brand-erasure cast is at useMinting.ts:51 (justification comment at :43-46) and is ADR-0002 Rule-2 compliant (justified by comment); it evidences wire-shape construction outside the ACL, not a cast-discipline violation — do not present it as one. (6) Ownership nuance: per the settled Ask 3 contract the backend reads exactly one key (data.gamma, open interval (0,1), default SR_DEFAULT_GAMMA); all other data keys are frontend-owned pass-through, so typing them is within frontend authority — the dispatch duty attaches specifically to the Path-2 backend mirroring and the CardCreate schema change, which the proposal already routes through the ledger. (7) Rule cite confirmed at frontend/CLAUDE.md:60; wire-schemas.md confirmed to contain zero grading_parameter content (whole-file grep).",
 "priorityHint": "keep",
 "reasoning": "Attempted refutation on all four grounds; the candidate survives with corrections. (1) Tenets: it does not fight ADR-0002 (it raises the outbound leg's loudness to compile time and is the natural enforcement point for ACL Rule 4 validate-not-coerce) and it does not fight ADR-0003's wrong-abstraction caution — that caution governs domain-portability Port extraction, and the second concrete use case for an outbound mapper already exists in-file (updateCardMetadata's domain-to-wire projection for PATCH). The rule contradiction it names is real and unsanctioned: frontend/CLAUDE.md:60 states the ACL is the only place wire shapes appear, CLAUDE.md documents exactly one exception (the ADR-0010 read-locality tension) and not this one, while types.ts:2115-2128 documents the composable-constructs/ACL-forwards status quo as a code comment. The candidate's either/or shape (mapper or explicitly sanctioned exception) composes with the codebase's documented-tension pattern rather than fighting it. The one by-design decision it brushes against — Path 1 chosen for the inline-edit ship with Path 2 deferred until the shape settles — is a roadmapped deferral, not a rejection, and the candidate names the filed item's conditional posture and argues activation on new evidence; with the promise-conflation correction applied, that is a legitimate priority argument, not an unnamed deviation. (2) Duplicate: gradingparameter-opacity-typing is the filed item and is named; its description covers only the Record<string,any> typing and read sites. The rule-text contradiction, the create-vs-PATCH asymmetry, and the dispatch-recorded \"Not done\" known-keys note are genuinely new evidence and a new auditability axis, so the duplicate test does not refute. (3) Cost/benefit: honest — no perf assertions (ADR-0009 not triggered), scope bounded to one payload, and the discovered PATCH-side precedent plus the backend's existing GradingParameterData schema make the mapper cheaper than the candidate's own estimate; the auditability gain (compile-time outbound shape enforcement, resolution of a rule contradiction that erodes sibling rules, fulfilment of a recorded open commitment) is the large-gain side of the review's stated tradeoff. (4) Vocabulary: ACL/composable/mapper-family/dispatch-ledger framing is native; the single misfit (hand-authoring an interface named GradingParameterData, colliding with the codegen declaration) is corrected, not refuting. Priority: keep — activation of the future-disposition item is justified by the new evidence, but the deferral was a deliberate mutually-recorded plan and the only outright broken commitment is a small ledger note, so this remains a small-to-medium discipline arc, not an urgent defect.",
 "refuted": false
}
````

### §3 · verify:exist:vue-lifecycle-footgun-guards

**Verdict (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Every load-bearing factual claim verified at HEAD. (1) Commits: 4756c30 (\"gate collapsed analysis charts off the packet path\") contains the verbatim safety claim \"Omitted/true => unaffected (other consumers safe)\"; the omitted path is exactly what broke, and 69810e2 is the withDefaults fix; 463a15e is the MiniBoardCanvas module-scope fix. (2) The five trap investigations exist in worklogs read end to end or located precisely: 2026-05-16-match-cursor-independence.md (structuredClone DataCloneError on Vue reactive Proxy; toRaw insufficient), 2026-05-22-responsive-arc.md (\"A container query cannot style its own container\"), 2026-05-30-perf-treewidget-nav-cost.md (\"layout is a churning shallowRef\" — the candidate's phrase is verbatim) + 2026-05-31-perf-treewidget-render-decouple.md, 2026-06-08-basechart-active-prop-default.md, 2026-06-09-mini-board-texture-scope-fix.md + investigation note. (3) todo DB item thumbnail-render-lifecycle-consolidation is open/future/frontend/medium and scoped to lifecycle consolidation (it mentions guarding shared resources against the script-setup footgun, but only as a property of the consolidated lifecycle — no syntactic/lint guard, no boolean-prop coverage), so this candidate is adjacent, not a duplicate. (4) Not already fixed as a guard: frontend/eslint.config.js (read in full) has no boolean-prop-default or script-setup-module-state rule; frontend/CLAUDE.md has no section on either class; only site-local comments and one BaseChart regression test exist. The custom-rule infrastructure (eslint-rules/clear-needs-ownership.js) confirms the RCA-minted-guard practice the proposal leans on. ADR-0010 (read in full) independently corroborates the meta-finding that prose records do not stop recurrence and names lint mechanization as its Revisit-when #1.",
 "corrections": "Four sharpenings, no refutations. (a) Proposal item (3)'s fold-in list overstates the v-memo gap: the render-vs-patch v-memo trap is already institutionalized as ADR-0010 Rule 2's verbatim corollary and frontend/CLAUDE.md's render-locality section; the un-consolidated residue is only the narrower key-stability lesson (\"v-memo only skips when its key is reference-stable\" — a churning shallowRef source busts it), which lives solely in docs/worklog/2026-05-30-perf-treewidget-nav-cost.md. The doc consolidation should target that delta plus container-query self-styling (docs/worklog/2026-05-22-responsive-arc.md) and structuredClone-vs-Proxy (docs/worklog/2026-05-16-match-cursor-independence.md). (b) Guard (1)'s omitted-prop rendering test already exists for the original defect: frontend/tests/integration/BaseChart-collapsed-gate.test.ts has a \"runs setOption when `active` is omitted\" case, verified red/green per the 2026-06-08 worklog — the new work is the convention/lint generalization to other gate-named boolean props, not the BaseChart test. (c) Cheaper first step than a custom name-heuristic rule: the installed eslint-plugin-vue ships vue/require-default-prop and vue/no-boolean-default (present in dist/rules at HEAD, unadopted because eslint.config.js deliberately avoids the broad recommended preset); assess these — including whether they handle type-based defineProps declarations — alongside the proposed vue-tsc strict check, under the config's measure-first adoption posture. (d) Minor framing: two of the five investigations are platform/CSS semantics traps intersecting Vue (structuredClone vs the reactive Proxy; container-query self-styling is pure CSS), so \"Vue semantics traps\" is slightly broad — the two lintable classes are the two genuinely Vue-specific ones, which is what the proposal targets anyway. Supporting context worth carrying: ADR-0010's Context section records exactly the candidate's meta-finding (\"a doc that *describes* a pattern does not stop it recurring one component over\"), and its Revisit-when #1 names lint mechanization on the minimal ESLint host as the sanctioned path — the proposal composes with an existing ADR trigger rather than introducing a new posture. Both shipped fixes carry site-local prose warnings (MiniBoardCanvas.vue line 29: \"These MUST live in a plain <script> (module scope), NOT <script setup>\"), consistent with the candidate's claim that current records are prose/site-local rather than checked.",
 "alreadyFiled": "",
 "priorityHint": "keep"
}
````

### §3 · verify:fit:vue-lifecycle-footgun-guards

**Verdict (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Survived all four challenges. (1) Tenet fit: ADR-0002 (read end to end) ranks build-time lint/test as loudness level 2 and directs authors to the strongest level that fits; the candidate moves two recurring trap classes from prose (level ~5-6) to lint/test. The proposal's posture — best-effort syntactic, named gaps, annotation escape, verified by reintroducing the original defect — is verbatim the established guard practice documented in frontend/eslint.config.js (error-message-reparse guard, RCA G1) and frontend/eslint-rules/clear-needs-ownership.js, and the config header explicitly anticipates ruleset growth. frontend/CLAUDE.md and ADR-0005 (both read end to end) raise no conflict; one Rule 1 caution carried in corrections. (2) Not a duplicate: thumbnail-render-lifecycle-consolidation (open/future/medium) is scoped by its DB description to consolidating the two thumbnail consumers' lifecycle — one surface; basechart-active-prop-default is closed as the point fix with a point regression test. No filed item covers a codebase-wide guard for either class; the candidate's scope-distinction claim checks out against the DB. (3) Cost/benefit honest: two shipped user-visible defects from this class within eight days (2026-06-01 gate commit -> 2026-06-08 discovery; 2026-06-09 texture flash), one behind an explicit safety claim verified verbatim in 4756c30 (\"Omitted/true => unaffected (other consumers safe)\"); guard cost is small, CI eslint wiring already exists, risks (heuristic false positives) are named rather than papered over. No perf property is asserted, so no ADR-0009 correction needed. (4) Vocabulary fit: modeled on the project's own guard genealogy (RCA-minted guards, scoped local rules, measured adoption), not a generic import; the script-setup convention codifies the exact fix shape shipped in 463a15e/PR #366 (plain <script> block with a MUST-comment in MiniBoardCanvas.vue).",
 "corrections": "Sharpenings to carry: (a) The 4756c30 safety claim is verbatim \"Omitted/true ⇒ unaffected (other consumers safe)\" — quotable directly; the falsity is precisely that omitted ≠ true for a bare boolean prop (Vue casts omission to false). (b) The vue-tsc question (adr-triggers finding 7) will close fast: strict template checking cannot police this class even in principle — omitting an optional boolean prop is type-legal; the defect is author intent (omitted ⇒ active) vs Vue runtime semantics. The substantive prior-art check is eslint-plugin-vue's existing prop rules (vue/require-default-prop; note vue/no-boolean-default encodes the opposite convention), assessed under the config's documented pattern of measured, scoped adoption rather than the broad preset it deliberately avoids. (c) Rule (2)'s trigger must not be all top-level let/Map/Set in <script setup> — per-instance non-reactive state is legitimate and common (the imperative-escape pattern's cached dims); the trigger is module-intent (e.g. a shared/loaded-once comment claim or configured shapes), as the candidate's risks section already implies. The shipped fix (463a15e) is the worked example the convention codifies: MiniBoardCanvas.vue now carries a plain <script> block with a \"MUST live in a plain <script> (module scope), NOT <script setup>\" comment. (d) Doc-consolidation leg: per ADR-0005 Rule 1, the v-memo render-coupling trap already has a canonical home (ADR-0010's corollary, restated verbatim in frontend/CLAUDE.md \"Render locality\"), and frontend/CLAUDE.md's resource-ownership checklist already names composable module-scope caches — the new footgun section should cross-link those homes, not restate them. (e) Overlap item for the record (not a duplicate): thumbnail-render-lifecycle-consolidation covers the per-instance class for the thumbnail surface only, via architectural consolidation; the candidate adds the codebase-wide syntactic guard, the boolean-gate-prop class, and the doc consolidation.",
 "alreadyFiled": "",
 "priorityHint": "keep"
}
````

### §3 · verify:exist:scoped-state-registry-by-default

**Verdict (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Every load-bearing existence claim verified at HEAD. (1) The docstring rot is real: /home/bork/w/omega/frontend/src/store/index.ts:331 reads \" * Four cleanups currently fire:\" and then enumerates TEN items — and the enumeration itself omits an eleventh live cleanup, stabilityTrajectoryStore.purgeBoard (index.ts:434, documented only by an inline comment). (2) closeBoard (index.ts:422-491) hand-sequences exactly 7 inline purges (stopBoardAnalysis, ledger.purgeBoard, stabilityTrajectoryStore.purgeBoard, analysisPersistenceService.discard, abortBoardReview, purgeBoardThumbnails, removeBoardCardTree) plus the BOARD_SCOPED_STORE_CELLS drain (4 Class A cells) — \"~7\" is exact. (3) board-scope.md (read end to end) records the Class-A-only bounding as a deliberate judgment, as the candidate's risks field says. (4) Both completeness-test tripwires exist in tests/integration/store-mutators.test.ts (exact-set label assertions for both registries). (5) All four cited bugs exist in the todo DB, closed/shipped 2026-06 (06-03 ×3, 06-09 ×1). (6) The convergence arc is real in git: incremental closeBoard teardown fixes 12cd38c/d79ceee/2b0b91f/c8ae1a2 (2026-05-04), 36b88ca (2026-05-07), then the P1b registry 3e14d79 (2026-06-08) — \"re-fixed four times\" slightly undercounts (4-5 passes) but is directionally right. (7) thumbnail-render-lifecycle-consolidation is open and indeed covers only the shared-render-resource leg. However: the registry-extension leg is substantially pre-filed — open item spa-board-scope-consistency-audit (active, large) records P1b verbatim as \"generalize IDENTITY_SCOPED_CACHES to a board-scope teardown registry so closeBoard's hand-wired O1-O14 list auto-registers\", and the shipped Class-A-only registry is that recommendation's deliberately-bounded execution per the 2026-06-05 scope-exhaustiveness consult (read end to end), whose blunt verdict explicitly recommends keeping Class B inline (\"legibility regression for no safety gain\"; the audit's §4 found no per-board teardown leaks; \"everything past that is more machinery than this codebase repays\"). The candidate's phase-as-data design does answer the consult's array-position objection (which the consult never evaluated), so it is not strictly the rejected option — but its supporting bug cluster supplies no in-scope counter-evidence: none of the four 2026-06 bugs is a closeBoard/per-board teardown leak. So: not refuted (state claims hold, docstring rot unfixed, evidence real), but priority lower — the genuinely new actionable content reduces to the small docstring fix plus the birth-registration authoring emphasis; the phase-migration leg re-litigates a four-day-old commissioned-consult decision against a scope with no recorded leak.",
 "corrections": "1) Docstring rot is worse than claimed: index.ts:331's header says \"Four cleanups\" against a ten-item enumeration, AND the enumeration omits an eleventh actual cleanup (stabilityTrajectoryStore.purgeBoard, index.ts:434) — eleven operations, ten enumerated, four counted. 2) The deliberate bounding is stronger than \"documented judgment in board-scope.md\": it is the recorded verdict of a commissioned firewalled consult (docs/notes/consult/opus-consult-2026-06-05-board-scope-exhaustiveness.md), grounded in the board-scope audit's §4 finding of NO per-board teardown leaks; the consult explicitly recommended keeping Class B inline. The candidate's phase-ordered-registry variant answers the consult's array-position objection (unevaluated there), but not its deeper ground: the guarantee comes from the completeness test, not the registry, and no leak walks through the closeBoard door. 3) \"Four shipped 2026-06 bugs reduce to this class\" is loose: tenancy-instance-cache-leak fits squarely (fixed by introducing IDENTITY_SCOPED_CACHES + identity :key); mini-board-texture-scope is scope-misdeclaration at birth (supports the birth-registration rule); but tag-mutation-ssot is a multi-writer-chokepoint miss and tags-fetch-hydration-race is scope misplacement + boot ordering — none of the four is a per-board closeBoard teardown leak, so the cluster supports the authoring-rule leg, not the phase-migration leg. 4) Already-filed pointer is wrong/incomplete: the closer overlap for the registry leg is the OPEN item spa-board-scope-consistency-audit, whose extra.audit_recommendations records P1b (\"generalize ... so closeBoard's hand-wired O1-O14 list auto-registers\"); the shipped BOARD_SCOPED_STORE_CELLS (commit 3e14d79, 2026-06-08, PR #357 lineage) is its bounded execution. The thumbnail item remains the right home only for the shared-render leg. 5) \"Re-fixed four times\" is four-to-five incremental passes (12cd38c, d79ceee, 2b0b91f, c8ae1a2 on 2026-05-04; 36b88ca 2026-05-07). 6) Note the candidate's birth-registration rule as stated (\"never joins a clear list\") would overturn board-scope.md's current authoring recipe, which explicitly directs Class B state to an ordered inline call — adopting it requires amending that note and the consult-recorded judgment, not just code.",
 "alreadyFiled": "spa-board-scope-consistency-audit",
 "priorityHint": "lower"
}
````

### §3 · verify:fit:scoped-state-registry-by-default

**Verdict (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Verified against: board-scope.md (end to end), the 2026-06-05 scope-exhaustiveness consult (end to end), ADR-0001 and ADR-0002 (end to end), store/index.ts closeBoard + both registries, store-mutators.test.ts tripwire tests, and the todo DB. Not refuted, on three grounds. (1) The factual core holds: store/index.ts:331 reads \"Four cleanups currently fire:\" above a ten-item enumeration, and the enumeration itself omits stabilityTrajectoryStore.purgeBoard (line 434) — eleven actual cleanups; an archived 2026-05-04 worklog shows the header was already directed past \"Four\" once, so the prose census has rotted through at least two cycles. That is verified new evidence against the \"well-documented inline code\" premise the bounded-registry judgment rests on, and ADR-0002 Rule 6 (design-time drift surfaces too) makes the corrective obligatory. (2) Not a duplicate: thumbnail-render-lifecycle-consolidation (open/future) covers only the MiniBoard shared-render lifecycle, exactly as the candidate says; the closer open item spa-board-scope-consistency-audit (open/active/large, the P1b arc's home) does not carry the docstring-rot evidence or the phase design point. (3) The candidate names the deviation from board-scope.md's documented judgment in its risks, and its phase-tagged registry genuinely answers the consult's stated objection (ordering knowledge moving into array position — phases encode the splice barrier as named data, and the consult's own supersession note concedes a (boardId)=>void closure covers Class B). However, the migration leg's evidence is materially weaker than claimed (see corrections): none of the four cited shipped bugs touched closeBoard's purges, and the consult's blunt verdict (\"keep Class B as explicitly-ordered inline calls... Stop there\") plus the in-code docstring (\"a legibility regression for no safety gain — the audit's §4 found no leaks\") record a deliberate decline of this exact refactor against a surface with no defect history. Cost/benefit per the review's bar: the docstring fix is small-cost/real-gain; the medium-cost migration does not deliver a LARGE auditability gain over the cheaper alternative (fix the docstring, extend the completeness test over the inline purges, file the evidence) and carries an admitted risk of converting documented-correct ordering into a subtle bug. Vocabulary fit is good (Class A/B, registries, completeness tests, mutators) — this is not a generic import, but the migration leg's instinct (\"registries always beat hand-enumeration\") overrides a codebase-specific judgment and must be framed as an explicit revisit requiring maintainer sign-off, per the umbrella CLAUDE.md's name-the-deviation-and-ask posture.",
 "corrections": "(1) The docstring rot is worse than stated: besides the stale \"Four cleanups\" header over ten enumerated items, the enumeration entirely omits stabilityTrajectoryStore.purgeBoard (store/index.ts:434) — eleven actual cleanups, ten documented. The header had already been directed to grow past \"Four\" by a 2026-05-04 worklog, so this is repeat rot — strengthening the candidate's lesson about prose censuses, and making the docstring fix the strongest leg (ADR-0002 Rule 6). (2) The four-bug cluster is misattributed as evidence for the closeBoard migration: per the todo DB, tags-fetch-hydration-race is a boot-ordering race (useAppBootstrap), tag-mutation-ssot is a multi-writer SSOT miss (knownTags), tenancy-instance-cache-leak is component-instance state explicitly unreachable from ANY module registry (per the IDENTITY_SCOPED_CACHES docstring's SCOPE note it was fixed by App.vue :key remount, not registration), and mini-board-texture-scope is per-instance-vs-shared render scope (the thumbnail item's leg). Zero of the four involve closeBoard's inline purges; the cluster supports the general scoped-state-needs-an-owner lesson, not the specific migration. (3) The migration leg re-litigates a documented decline: the 2026-06-05 scope-exhaustiveness consult's verdict (keep Class B inline; \"Stop there\") and the BOARD_SCOPED_STORE_CELLS docstring (\"legibility regression for no safety gain — the audit's §4 found no leaks\"). The phase design does answer the consult's array-position objection, and the consult's supersession note concedes a teardown closure covers Class B — so the proposal is admissible, but only reframed as an explicit revisit of the P1b boundary requiring maintainer sign-off, not as bug-driven correctness work. (4) The birth-registration authoring rule largely restates board-scope.md's existing authoring recipe; the only real delta is changing the Class B branch of step 2. (5) Closest-match filing: the candidate names only thumbnail-render-lifecycle-consolidation; the closer open item is spa-board-scope-consistency-audit (open/active/large) — the natural home for the docstring corrective and any P1b-boundary revisit. (6) Verified accurate: exact-set tripwire tests exist (frontend/tests/integration/store-mutators.test.ts:209 board cells, :334 identity caches); closeBoard hand-sequences 7 inline purges plus the registry drain.",
 "alreadyFiled": "",
 "priorityHint": "lower"
}
````

### §3 · verify:exist:split-types-ts-along-banner-seams

**Verdict (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Every load-bearing factual claim checks out at HEAD, several exactly: types.ts is 2362 lines; 90 commits touch it (the most-touched file under frontend/src, so \"co-change hub\" is fair); the types.ts+defaults.ts+migrations.ts triple is exactly 45 commits; growth is exactly +117 in the week 2026-06-03 (2245 lines at 5b57d25) to 2026-06-08 (2362). The section banners exist where claimed (knobs :472-:804, qEUBO :805, AppSettings :956, UISession :1419, GlobalStore :1906, wire types :2111/:2113; :1655 is a real sub-divider at HEAD). IDENTIFIERS.md drift is concrete and verifiable: it self-describes types.ts as \"2233-line\" vs 2362 actual, and its file:line citations are off (Brand :82→:84, StorePath :425→:502, NavNodeId :1502→:1591, dead types :1493/1494→:1582/1583); the two [dead] types still exist with zero references outside types.ts. ADR-0007 read end to end: the exception text is verbatim as quoted. The todo DB row refactoring-queue-adr0007 is open/active, records \"types.ts ~2245 keeps the type-catalogue exception\" and \"Handled on touch; no batch\" — exactly as the candidate says. The candidate makes no unmeasured perf assertion (its cost section already hedges build-time as hypothesis per ADR-0009). Not refuted; only small sharpenings.",
 "corrections": "1) The defaults.ts co-change count is 48 at HEAD, not 46 (four more types.ts commits landed 2026-06-08); the staleness strengthens rather than weakens the claim. 2) ADR-0007's recorded status is \"Proposed\", not Accepted — operative in practice (the queue item exists and frontend/CLAUDE.md applies it), but the report should say \"ADR-0007 (proposed)\" when leaning on its exception text. 3) The \":1655\" divider at HEAD is a doc-comment sub-banner (the ReviewCard gradingParameter block), not a top-level section banner; the top-level SR-domain banners at HEAD are :1640/:1744, and :2113 is a sub-banner under the :2111 wire-types banner — cosmetic, but cite :2111 for the wire seam. 4) Concrete drift instances worth carrying as evidence: IDENTIFIERS.md self-describes \"the 2233-line src/types.ts\" vs 2362 actual; Brand cited :82 actual :84; StorePath cited :425 actual :502; NavNodeId cited :1502 actual :1591; dead types cited :1493/1494 actual :1582/1583; the ProfileState field citations (:1760/:1770/:1771) also no longer match. 5) Scope the \"retire the decaying half\" phrasing: only IDENTIFIERS.md's construction-site file:line citations decay mechanically; its origin/lifetime/cardinality/erosion-tag/representation-cost content is exactly the residue the candidate's own proposal says a split cannot carry — the proposal is internally consistent on this, but the title's \"decaying half\" should be read as the line-citation layer. 6) likelyAlreadyFiled is correctly empty-as-duplicate: refactoring-queue-adr0007 records the opposite disposition (parked under the exception, handled-on-touch, no batch), so this candidate is a revisit-the-exception proposal against that item, not a duplicate of it.",
 "alreadyFiled": "",
 "priorityHint": "keep"
}
````

### §3 · verify:fit:split-types-ts-along-banner-seams

**Verdict (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Attempted refutation on all four grounds; the candidate survives. (1) No unnamed tenet fight: ADR-0007 (read end to end) makes type catalogues an exception whose own terms prescribe splitting \"along clean domain seams, not by line count alone\"; the banner seams exist at frontend/src/types.ts:83/:225/:255/:472/:805/:1585/:1640/:1769/:1925/:2111/:2239, so the proposal follows the exception's prescribed mode. The only deviation is from the queue item's parked status, which the candidate explicitly routes to the maintainer — matching the umbrella CLAUDE.md's name-and-ask requirement. ADR-0001/0003/0005/0009 (all read end to end) raise no conflict; ADR-0009 compliance is already correct (build-time effect marked unmeasured). (2) Closest filed item is refactoring-queue-adr0007, but the candidate adds verified new evidence beyond its parked snapshot: file 2362 lines (wc -l), net +117 across 7 commits since 2026-06-02 (git numstat), 90 total commits, 48 co-changes with store/defaults.ts, 45-commit triple with migrations.ts, and concrete IDENTIFIERS.md citation drift (Brand cited :82 actual :84; StorePath cited :425 actual :502; CardSetKey/ReviewSessionId cited :1493-4 actual :1582-3; NavNodeId cited :1502 actual :1591; analysisRange cited :212 actual :289; the doc's prose still says \"2233-line\"). The drift is the exact silent-failure mode IDENTIFIERS.md's own \"Updating this map\" section names. (3) Cost/benefit is honest: no perf claim, real auditability gain (the most-touched file in the repo is also the one most exposed to ADR-0004 partial-visibility risk; the 48/90 defaults.ts co-change ratio directly supports the store-schema colocation), risks named. (4) Vocabulary is native (bands, ACL, brands, barrel-for-import-stability, FILES.md/doc-graph cadence). Additional support the candidate can claim: ADR-0007's Neutral clause says queue files are \"addressed when next touched substantively,\" and types.ts is touched substantively ~weekly, so the handled-on-touch trigger is firing repeatedly without the split — the proposal partially composes with the policy rather than purely deviating. Priority: raise — the exception's own terms (clean seams exist, growth continuing, the compensating index demonstrably drifting) no longer protect the parked status, and the maintainer should re-decide with this evidence.",
 "corrections": "1) Co-change figure: 48 of 90 types.ts commits also touch store/defaults.ts (candidate said 46; recounted this session — direction unchanged, slightly stronger). Triple with migrations.ts confirmed at 45. 2) Soften the title/lesson framing \"retire the decaying half of IDENTIFIERS.md\" / \"the compiler-checked structure, not the index, is the fix\": read end to end, IDENTIFIERS.md's distinctive payload — construction sites in ~20 files other than types.ts, lifetime, cardinality, soundness/erosion tags ([leaky]/[under-determined]/[dead]), and the NodeId representation-cost analysis — is not compiler-checkable and survives any split. The split dissolves only the navigation half (\"without trawling the 2233-line src/types.ts\"). Moreover the split invalidates every src/types.ts:NNN citation in IDENTIFIERS.md (~12+ rows), so same-PR re-pointing of those rows is a mandatory coupled cost per the doc's own update discipline, not an optional ride-along. 3) The cited \":1655 divider\" is a sub-heading inside the ReviewCard doc comment (the gradingParameter Item-18 note), not a top-level section banner; top-level banners sit at :1640/:1744. Cosmetic. 4) types.ts is not types-only: it carries at least one runtime export (BUNDLE_COMPRESSION_SCHEMES const at :947) plus value re-exports, so the barrel is a runtime module — the circular-import check the candidate names (especially store-schema <-> defaults.ts) is load-bearing, not hypothetical. 5) Strengthening to add: ADR-0007's Neutral clause (\"addressed when next touched substantively\") is firing ~weekly on this file, so the proposal partially composes with handled-on-touch; the deviation is only from the parked type-catalogue-exception status. Also name ADR-0007's \"over-fragmentation is a real risk\" warning as the counter-pressure the split plan bounds (per-domain modules, not per-type files). 6) Dead types confirmed: CardSetKey/ReviewSessionId (types.ts:1582-1583) have zero references outside types.ts. 7) Extra auditability gain in-vocabulary: FILES.md tags the monolith \"[B2] ... Carries Move/StoneColor (B3 leakage)\" and ADR-0003 explicitly declined to band-classify it; a seam split lets each module carry an honest ADR-0003 band tag.",
 "alreadyFiled": "refactoring-queue-adr0007",
 "priorityHint": "raise"
}
````

### §3 · verify:exist:doc-graph-dangling-signal-cleanup

**Verdict (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Every load-bearing claim verified at HEAD. (1) Counts exact: docs/doc-graph-report.md lists 125 live danglers (counted), 246 frozen-archive; 55 of the 125 originate from docs/worklog/** (counted); 31 target docs/notes/deferred-items.md (counted). (2) CI posture confirmed twice: the report header and .github/workflows/doc-graph-ci.yml both state the gate checks freshness only and deliberately does not block on broken references \"because existing drift would make every PR red\". (3) Frozen-boundary mismatch confirmed: tools/doc-graph/generate.mjs:833-835 (isArchiveOrigin) freezes only docs/archive/**, while docs/notes/consolidation-xref-fallout.md (read end to end) classifies both docs/archive/** AND docs/worklog/** as frozen per the standing do-not-retro-edit rule — and even asserts the report \"already segregates them\", which is false for worklogs, strengthening the mismatch. (4) deferred-items.md is a deliberately retired hub: docs/notes/vestige/deferred-items/README.md records its dissolution into the SSOT on 2026-06-02. (5) Both stale code-side pointers real at HEAD: docs/adr/0010-...md:169 (read end to end) cites tests/integration/render-locality/ while the directory is frontend/tests/integration/render-count/; frontend/src/components/board/MoveSuggestions.vue:144 cites docs/notes/perf-audit-nav-and-pv-hover-2026-05-27.md, which now lives at docs/notes/audit/... (moved by the notes-hierarchy reorg, commit 59aef38). (6) Blind-spot and conflation claims confirmed in generate.mjs: SCAN_DIRS=[\"docs\"] plus a fixed .md EXTRA_NODE_FILES list (code files never scanned as sources); BACKTICK_PATH_RE (line 267) requires a .md suffix so directory refs and non-.md targets are never extracted; and targets that exist on disk but sit outside the node set (backend/qeubo/README.md, backend/samples/README.md, frontend/tests/CLAUDE.md, frontend/docs/i18n.md all exist) get the identical \"does not resolve to any node\" message as genuinely missing files (docs/INDEX.md, docs/release-scope.md). (7) Not already fixed: git log on generate.mjs, ADR-0010, and MoveSuggestions.vue shows no corrective landed; working tree clean for those paths. (8) Not already filed: todo DB has doc-graph-svg-spline-failure, doc-graph-svg-render-off-tree (different concerns), closed doc-graph-artifact, and the broader open adr-effectiveness-audits consolidation arc — adjacent parent context, not a duplicate of this specific tooling change.",
 "corrections": "Three sharpenings, none fatal. (1) The 55 worklog-origin and 31 deferred-items-target classes overlap by 3 (three worklog danglers target deferred-items.md), so \"31 more\" slightly double-counts: the distinct union is 83 of 125 (~66% — the \"two-thirds noise\" phrase still holds, just barely). Phrase as \"55 from worklogs; 31 target the retired deferred-items hub; 3 in both classes; 83 distinct\". (2) The consolidation note (docs/notes/consolidation-xref-fallout.md, \"Frozen bucket\" section) additionally asserts \"the doc-graph report already segregates them and the project does not fix them\" — currently false for worklog-origin refs, since generate.mjs:833-835 segregates only docs/archive/**. This strengthens the candidate: the working convention's own record believes the tooling already implements the boundary it doesn't. (3) The same note also names a third quasi-frozen class with in-repo precedent (\"docs/playbooks/monorepo/* executed playbooks, reference-only\" plus closed dispatches) — those playbooks contribute ~25 more of the 125 live danglers, so the proposal's \"(or a third bucket)\" option is already half-decided by existing convention; under the full working convention roughly 108 of 125 live entries are expected drift. Precision fixes for the final report: the conflation evidence is concrete — backend/qeubo/README.md, backend/samples/README.md, frontend/tests/CLAUDE.md, and frontend/docs/i18n.md all exist on disk yet are reported with the same message as truly-missing targets; the directory-skip is structural (BACKTICK_PATH_RE at generate.mjs:267 requires a .md suffix); the MoveSuggestions stale pointer is at frontend/src/components/board/MoveSuggestions.vue:144, target moved to docs/notes/audit/perf-audit-nav-and-pv-hover-2026-05-27.md in commit 59aef38 (notes-hierarchy reorg). Related open todo item for cross-linking, not duplication: adr-effectiveness-audits (\"doc-graph consolidation discipline\" — the broader doc-retirement/taxonomy arc this tooling change would serve).",
 "alreadyFiled": "",
 "priorityHint": "keep"
}
````

### §3 · verify:exist:retire-hand-maintained-censuses-in-code

**Verdict (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "All three named instances verified live at HEAD. (1) store/index.ts:331 says \"Four cleanups currently fire:\" above ten enumerated cleanups (lines 333-395) — stale exactly as claimed. (2) eslint.config.js claims four components violate the effectful-singleton import boundary \"today\" across five sites; grep of src/components/ finds zero such imports (AnalysisControls now imports only the exempt reactive-state analysis-ledger and a type-only analysis-bundle import) — census stale as claimed. (3) Read docs/archive/notes/resource-ownership-audit-plan.md end to end: its inventory ends at O15 and defines O12=useResizablePanel listeners, O13=BaseChart markerTimer, O14=MintCardModal setTimeout, O15=analysisService reconnect bookkeeping — all four collide with the closeBoard docstring's O12-O15 (removeBoardCardTree, persistence discard, cardTreeNav, forestNav.selection). Todo-DB keyword sweeps (collision/census/docstring/comment convention/eslint/O-number/audit-pair) return no tracking item, confirming the no-item claim. The \"Item 30c\" load-bearing-convention claim is corroborated: in-code tags exist in backend/repositories/lineage_repository.py, ports.py, pipeline.py, and the survives-squash observation is documented in docs/notes/audit/audit-work-status-liveness-2026-06-02.md section 5 (read end to end). Nothing was already fixed; the candidate survives with sharpenings only.",
 "corrections": "(1) The O-number collision spans all four numbers O12-O15, not just \"different O12/O14 definitions\" as the lesson states: the archived plan's O13 (BaseChart markerTimer) and O15 (analysisService reconnect bookkeeping) also differ from the docstring's O13 (analysisPersistenceService.discard) and O15 (forestNav.selection). The plan's inventory ends at O15, so the docstring tags are an ad-hoc continuation that re-used already-taken numbers. O1-O5 citations in the same docstring DO match the plan, so the collision is confined to O12-O15. (2) The eslint census sentence spans lines 81-86 (starts \"Four\" at end of line 81), not 82-86 — trivial. (3) Additional stale detail in the same closeBoard docstring worth folding into the fix: line 416 cites the plan at docs/notes/resource-ownership-audit-plan.md, but that path does not exist — the plan lives at docs/archive/notes/resource-ownership-audit-plan.md (frontend/CLAUDE.md cites the archive path correctly; only the docstring is stale). Code-comment O-tags also appear at store/index.ts lines 440, 444, 447, 468, not just the docstring block. (4) Sharpening on the \"Item 30c\" precedent: the tags live in backend Python (lineage_repository.py, ports.py, pipeline.py), and the audit that found them load-bearing (audit-work-status-liveness-2026-06-02.md section 5) ran against the then-current docs/work-status.json, which has since migrated to the todo Postgres DB — so the proposal's \"stable handle\" is now the DB item id, and the convention note should say so. (5) The eslint header carries two more present-tense-ish censuses sharing the same decay risk (line 137: no-explicit-any \"~152 sites\", max-lines \"~69 files over 250\") — candidates for the same convention treatment, though the \"~\" hedging and \"at adoption\" framing of other counts (e.g. no-floating-promises \"7 sites at adoption, all resolved\") are historical records, not live censuses, and need no change. (6) Evidence caveat: the four cited distiller findings (arch-ergonomics 9, adr-triggers 5(b), worklog-2026-05 1, audits 6) are session-internal artifacts not present in the repo; audits finding 6's substance is independently corroborated by the liveness-audit doc, the others could not be checked against repo files.",
 "alreadyFiled": "",
 "priorityHint": "keep"
}
````

### §3 · verify:fit:doc-graph-dangling-signal-cleanup

**Verdict (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Could not refute on any of the four grounds. (1) Tenet fit: read ADR-0002, ADR-0005, and ADR-0010 end to end. The candidate extends, not fights, the by-design discipline — ADR-0005 (Amendments 2026-06-01, Alternative C, Revisit-when #2) records the report as the partly-adopted mechanization of the cross-reference-resolution subset, maintainer-reviewed and explicitly non-gating; the candidate keeps that posture (advisory ratchet, not a red X, matching the §7.3 gate retraction CLAUDE.md records). ADR-0002 Revisit-when #2 (\"multiple anomalies collapse into one useless message... the fix isn't to silence the warnings; it's to make them more specific\") endorses the fix shape. The one judgment call — treating worklogs as frozen — is named and deferred to the maintainer, so no unnamed deviation. (2) Not a duplicate: queried the todo DB; closest items are adr-effectiveness-audits (doc retirement/taxonomy discipline, different mechanism), doc-graph-svg-render-off-tree, doc-graph-svg-spline-failure — none covers frozen-boundary alignment, dangling-class split, tombstones, or the code-comment pointer class; the candidate also carries new verified evidence regardless. (3) Cost/benefit honest: every factual claim reproduced (125 live danglers; 55 worklog-origin; 31 targeting retired deferred-items.md; isFrozenArchive = startsWith(\"docs/archive/\") at generate.mjs:832-834; both stale code pointers confirmed; class conflation confirmed against on-disk files). Extra corroboration found: docs/notes/consolidation-xref-fallout.md (read end to end) states the frozen convention as \"docs/archive/** and docs/worklog/**\" AND falsely asserts the report \"already segregates them\" — the convention mismatch has already produced a factual error in a committed live doc; and CLAUDE.md makes consulting this report part of the standard documentation audit, so the ~66% noise taxes every delivery. Small-to-medium tooling cost for restoring a load-bearing audit instrument is an honest trade per the review's stated calculus; no perf assertions, so no ADR-0009 issue. (4) Vocabulary fit: framed in the codebase's own terms (frozen-archive boundary, committed-artifact regeneration discipline, advisory-not-gate precedent, retire-on-ship hubs), a calibration of the existing bespoke tool rather than a generic link-checker import. Priority raised on the strength of the consolidation-note false assertion and the report's role in the standard pre-merge audit.",
 "corrections": "(a) Arithmetic overlap: 3 of the 31 live danglers targeting deferred-items.md are also worklog-origin, so the disjoint split is 55 worklog-origin + 28 additional deferred-items-target = 83/125 (66%); \"two-thirds noise\" stands but \"31 more\" double-counts 3. (b) Precise pointer facts: ADR-0010 line 169 cites tests/integration/render-locality/ while the harness lives at frontend/tests/integration/render-count/; the second stale pointer is frontend/src/components/board/MoveSuggestions.vue:144 citing docs/notes/perf-audit-nav-and-pv-hover-2026-05-27.md, now at docs/notes/audit/perf-audit-nav-and-pv-hover-2026-05-27.md. (c) Node-set facts for the class-split work: SCAN_DIRS=[\"docs\"] plus backend/docs plus 9 EXTRA_NODE_FILES (generate.mjs:157-211); verified outside-node-set-but-on-disk targets include frontend/tests/CLAUDE.md, backend/samples/README.md, backend/qeubo/README.md, currently reported with the same wording as genuinely deleted files. (d) New supporting evidence to carry: docs/notes/consolidation-xref-fallout.md both documents the worklogs-frozen convention (quoting the orientation note's \"capture moment / do not edit to fix\" standing rule) and incorrectly asserts the report \"already segregates\" worklog references — the boundary mismatch has already seeded a factual error in a committed live doc. (e) Scope: this is umbrella-scope work (tools/doc-graph, docs/, one ADR, one frontend comment), not frontend-scope — if filed, scope='umbrella', and the regeneration must ship in the same PR since the generator change alters report structure (committed-artifact discipline). (f) Minor: the ADR-0010 path fix should follow the ADR amendment convention (Amendments-line or explicit drift-repair sign-off) rather than a silent body edit, per ADR-0005 Rule 8 / ADR-0002 Rule 6 spirit, though a stale path is reference drift rather than load-bearing wrongness.",
 "alreadyFiled": "",
 "priorityHint": "raise"
}
````

### §3 · verify:exist:enrichment-merge-null-validation

**Verdict (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Every load-bearing claim verifies at HEAD. (1) mergeRecords at frontend/src/services/analysis-ledger.ts:185-195 (file is 373 lines) has exactly the flagged guard: per-key skip of null/undefined values, no detection of a non-null inner object carrying null fields — such an object replaces a populated entry wholesale via the nested-record call sites (extra.state per-turn objects, cwt) on the enrichment merge path (recordEnrichment → mergeEnrichment → mergeKataExtra → mergeRecords). (2) The cited postmortem (postmortem-adaptive-deeper-enrichment-2026-05.md, read end to end, dated 2026-05-12 — four weeks ago) names this exact edge in §5.5 first bullet, including the {Win: null} example and the NaN-under-asteval trigger. (3) Not fixed since: git log --follow shows the stratification (0792f9a) and brand-tightening (dd3b85e) commits moved/split the merge but added no inner-null validation; the proxy v1.0.21 fix addressed the missing-extra case from the same postmortem, not the null-bearing-inner-value shape. (4) DB scope check re-verified: silent-coercion-protocol-boundaries-audit's description is explicitly \"closed-set wire vocabularies parsed with an open-set default\" (enum coercion); broader ILIKE searches over the items table for merge/null-bearing/analysis-ledger/enrichment/adaptive/ledger/validation surface no item covering this merge-boundary shape — not already filed. (5) The \"accepted, recorded tradeoff\" claim about mergeEnrichment's no-visit-gate additive behaviour is confirmed by its docstring (lines 246-258). (6) ADR-0002 read end to end; Rule 4 (\"ACL boundaries must validate, not coerce\") supports the framing, with a sharpening noted. No perf claims asserted; no refutation grounds found.",
 "corrections": "Sharpenings, none fatal: (a) \"finding 5\" is loose — the anchor is §5.5 of the postmortem's Recommendations, explicitly framed there as \"Smaller SPA-side observations worth surfacing (not bugs today, but sharp edges)\" and \"not part of the postmortem's main thrust\"; cite it as a §5.5 observation, not a numbered finding. (b) ADR-0002 Rule 4 is stated for ACL boundaries; mergeRecords is a services-layer merge helper one step inside the engine ACL, so the candidate is applying Rule 4's validate-don't-coerce calibration by analogy, not citing a literal Rule 4 instance — say so. (c) The edge is specific to mergeRecords's nested-record uses (extra.state per-turn objects, and cwt); for flat numeric records (deltas) the existing top-level guard already suffices — a guard should target object-typed incoming values with null-bearing fields, which narrows the false-positive surface the risks section worries about. (d) Cost is slightly overstated: mergeEnrichment is already exported and directly unit-exercised in frontend/tests/integration/analysis-ledger-stratified.test.ts, so the null-bearing fixture has an existing home. (e) \"No observed incident in four weeks\" should read: one transient instance was observed at postmortem time under a misconfigured palette in a probe context (§5.5); none observed in normal operation since. (f) Post-stratification the postmortem's old line cite (:66-76, inside the retired mergeAnalysisPacket) is stale; HEAD's mergeRecords at :185-195 is the correct anchor, as the candidate already states.",
 "alreadyFiled": "",
 "priorityHint": "keep"
}
````

### §3 · verify:fit:retire-hand-maintained-censuses-in-code

**Verdict (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Could not refute on any of the four grounds. (1) Tenet fit: ADR-0002 and ADR-0005 were read end to end; the candidate composes with both rather than fighting them. The O12-O15 collision is a direct instance of ADR-0005 Rule 1 (single source of truth per nominal handle — one handle, two contending definitions), the census decay is the code-comment register of Rule 3 (content snapshots go stale; relations/stable handles don't), and the additive-slugs-never-renumber posture respects Rule 8 / ADR-0002 Rule 6 (no silent edits to frozen planning records). The proposal does not attack the documented enumerated-cleanups docstring discipline (frontend/CLAUDE.md §\"Resource ownership at mutation sites\"; archived plan §\"Comment convention\") — the decayed prose count (\"Four cleanups currently fire\") is not part of the prescribed shape, and the candidate keeps the numbered list and the audit-pair references, adding stable handles alongside. Its choice of frontend/CLAUDE.md as the convention home even matches the plan's own Pass-3 precedent (the PR-template-vs-CLAUDE.md question was resolved in favor of frontend/CLAUDE.md). (2) Duplication: read-only sweeps over the todo DB (items title+description regex sweeps for census/collision/convention/eslint/docstring/resource-ownership terms, plus review of all 49 open item titles) found no item tracking the collision, the censuses, or the convention; spa-board-scope-consistency-audit is adjacent in subject (board-scope coherence) but does not cover comment decay. (3) Cost/benefit honest: all three named instances re-verified concretely — store/index.ts:331 says \"Four cleanups currently fire:\" above a list of ten; eslint.config.js:81-86 asserts four components violate \"today\" while grep over src/components finds zero imports of the four restricted singletons and eslint runs clean; the O12-O15 definitions in closeBoard's docstring genuinely differ from the archived plan's inventory (plan: O12=useResizablePanel listeners, O13=BaseChart markerTimer, O14=MintCardModal setTimeout, O15=analysisService reconnect bookkeeping). The fix is comments-only — no perf loss at all, so the review's tradeoff is trivially satisfied; no perf claims are asserted, so ADR-0009 is not implicated. The candidate also honestly names its own decay risk and the mitigation (handles checkable against the todo DB). (4) Vocabulary fit: shaped in the codebase's own terms (work-status item ids per the SSOT consolidation/RCA G5, registries like BOARD_SCOPED_STORE_CELLS carrying what comments shouldn't, frozen-archive discipline, frontend/CLAUDE.md as home) — not a generic best-practice import. Verification additionally surfaced a fourth decay instance in the same family the candidate missed (stale plan path at store/index.ts:416).",
 "corrections": "Sharpenings to carry into the final report: (a) The collision is confined to O12-O15; O1-O5 references in the same closeBoard docstring DO match the archived plan's inventory. Exact collision: plan defines O12=useResizablePanel document listeners, O13=BaseChart markerTimer, O14=MintCardModal setTimeout, O15=analysisService reconnect bookkeeping; closeBoard's docstring uses O12=removeBoardCardTree card-tree slot, O13=analysisPersistenceService.discard, O14=cardTreeNav delete, O15=forestNav.selection delete. The numbering was extended past the frozen inventory without checking it. (b) Additional instance in the same decay family, not named by the candidate: store/index.ts:416 cites \"docs/notes/resource-ownership-audit-plan.md\" but the plan lives at docs/archive/notes/resource-ownership-audit-plan.md (frontend/CLAUDE.md cites the archive path correctly); fix in the same touch. (c) Composition requirement: the convention paragraph must amend, not sit beside, the existing prescription — frontend/CLAUDE.md §\"Resource ownership at mutation sites\" and the archived plan's §\"Function-docstring shape\" item 4 are what prescribe the audit-pair reference that produced the collision; adding a parallel convention without naming that it refines item 4 would create a second contending convention, the exact ADR-0005 Rule 1 failure the candidate decries. Since the plan is archived/frozen, the refinement lands in frontend/CLAUDE.md with a pointer — which also matches the plan's own Pass-3 precedent. (d) The eslint census is stale in the strong sense: as of today the four named components carry zero restricted-singleton imports (AnalysisControls imports only the exempt analysis-ledger reactive-state module plus a type) and eslint runs clean; the rewrite should use the \"at adoption … all resolved\" historical phrasing already used by the no-floating-promises entry in the same header. Any still-open layering-debt adjudication is work status and belongs in the todo DB per the SSOT consolidation (RCA G5), not in a config header. (e) ADR grounding for the report: collision = ADR-0005 Rule 1 instance; census decay = code-comment register of ADR-0005 Rule 3; additive-slug posture = ADR-0005 Rule 8 / ADR-0002 Rule 6 compliance.",
 "alreadyFiled": "",
 "priorityHint": "keep"
}
````

### §3 · verify:fit:enrichment-merge-null-validation

**Verdict (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Not refuted on any of the four grounds. (1) Tenet fit: read ADR-0002 end to end — the proposal is an application of the tenet, not a fight with it; the warn-first-then-promote shape matches the graded loudness hierarchy and the 'not crash on any anomaly' calibration, and the candidate explicitly carves out the one documented by-design tradeoff in the file (mergeEnrichment's no-visit-gate docstring, analysis-ledger.ts:246-255). That docstring's acceptability rests on 'enrichment is ~monotone'; a null-bearing inner object replacing a populated leaf is exactly the non-monotone case, and the project's own postmortem (§5.5) records it as a sharp edge, not design. (2) Duplicate check (read-only SELECTs over items/refs/labels): silent-coercion-protocol-boundaries-audit is scoped to closed-set wire vocabularies parsed with open-set defaults (enum coercion), confirming the candidate's scope check; no refs or descriptions touch analysis-ledger/mergeRecords/the postmortem; nearest open items (adaptive-query-cancellation-leak, semantic-clarity-refactors-effect-typing, refactoring-queue-adr0007) cover different surfaces. Not filed. (3) Cost/benefit honest: guard cost is one pass inside an existing helper that already does Object.entries+spread (ADR-0009 structural-by-inspection territory), benefit is removing a documented silent-overwrite on the analysis data path reachable via user-authored palettes (postmortem observed one real transient instance). Candidate honestly ranks urgency low given four incident-free weeks. (4) Vocabulary fit: Services-layer helper, ADR-0002 loudness calibration, names the preserved tradeoff — composes with the codebase, not a generic import. Factual claims re-verified at HEAD: mergeRecords at frontend/src/services/analysis-ledger.ts:185-195 has exactly the flagged guard shape; line citation correct. Priority: keep — the candidate's self-ranking is calibrated; the missed exposure-widening signal (see corrections) supports filing but not raising, absent any incident.",
 "corrections": "Four sharpenings to carry, none refuting: (a) The 'ADR-0002 Rule 4' citation is analogical, not literal — Rule 4's letter governs ACL (wire→domain) boundaries, and the ledger merge sits post-ACL inside the Services layer; ground the claim on ADR-0002's general decision text ('unexpected data shapes … surface through the loudest appropriate channel') with Rule 4 as the calibration analogy only. (b) Scope the guard: of mergeRecords' three call sites, the inner-null wholesale-replacement edge only bites the `state` site (values are nested Record<string, number>); at `deltas`/`cwt` (via mergePlayerExtra) values are numeric leaves already covered by the existing top-level null-skip. Targeting nested-record values (or the state call site) makes the cost even smaller than stated and avoids paying a scan at the flat sites. (c) Missed priority signal in the candidate's favor: the 2026-06-08 stratification removed the visit gate from the enrichment merge path (the documented mergeEnrichment tradeoff) — pre-stratification, a stale low-visit packet was discarded by mergeAnalysisPacket before reaching the extra merge; now every enrichment packet reaches mergeRecords regardless of visit ordering, widening the exposure window for a null-bearing packet relative to postmortem time. (d) Two form corrections: per ADR-0009, the 'small cost' claim on the packet-ingest flood path should be carried either as the structural-by-inspection argument (same-order as the existing Object.entries+spread in the helper) or with the explicit-unsubstantiated qualifier; and the promotion target should be left open between loudness levels 3-4 rather than pre-committing to throw — a throw inside recordEnrichment breaks the packet stream for a wire-origin anomaly the user cannot action, so pushSystemMessage (level 4) may be the correct terminal level, decided at implementation per the hierarchy.",
 "alreadyFiled": "",
 "priorityHint": "keep"
}
````


---


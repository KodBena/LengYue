# Appendix (part 3 of 3) — verbatim commissions and reports

Continuation of `audit-spa-history-lessons-2026-06-10-appendix-p1.md`:
the generalization run (2 fork-lens distillers, merge, 18 generalization
verdicts, 16 new-candidate verifier verdicts). The `[SHARED: ...]`
factoring conventions are defined in part 1 §0. License: Public Domain
(The Unlicense).

## §4 Generalization run — distillers, merge, verdicts

### §4 · gen-distill:second-consumer

**Commission** (= `[SHARED: GEN-DISCIPLINE]` + `[SHARED: FORK-CONSTRAINT]` + the following):

````
You are a distiller in the generalization pass of a retrospective review of the LengYue frontend. A prior 13-miner harvest produced the findings at /tmp/claude-1000/-home-bork-w-omega/spa-review/evidence.json (Read it — in chunks if needed; it is ~150KB of JSON: per-miner findings arrays with titles/evidence/implications). 18 candidates were already confirmed by the main review; their index is at /tmp/claude-1000/-home-bork-w-omega/spa-review/confirmed_index.json (Read it; do NOT duplicate them — your job is what they MISSED under your lens). Verify anything load-bearing in the tree yourself before proposing.

YOUR LENS: ADR-0003 SECOND-CONSUMER CALCULUS: read docs/adr/0003-frontend-portability-and-domain-boundaries.md end to end first. The generic flash-card fork is the second concrete consumer materializing. Which deferred seam-namings or extractions does that now justify — and which would still be premature? Consider: the "what would change for a Chess port" map vs a NON-board-game fork (the fork shares SR core, cards, browse, workspace, auth — not the board); where Go-bound gating logic sits inline instead of in named functions; whether the card/SR domain types are free of Go vocabulary. Propose candidates only where the history shows the seam is real (a worked pain point), not speculative tidiness.

Propose AT MOST 6 candidates for your lens (fewer is fine; zero is an acceptable answer if the evidence does not support more). Each: grounded in evidence (name the miner finding or your own file:line verification), ADR-consistent, honest cost/risk. axis may be 'generalization'.
````

**Result (verbatim):**

````json
{
 "candidates": [
  {
   "id": "adr0003-trigger1-fork-axis-remap",
   "title": "Record ADR-0003 Revisit #1 as fired and re-cut the band map for the consumer that actually materialized: a non-board-game fork, not a Chess port",
   "lesson": "ADR-0003's entire calculus is keyed on 'a second concrete consumer materializes' (Revisit #1), and the one that materialized does not match the axis the map was drawn on. The Chess-port question partitions B3 (replace) from B1+B2 (keep); a generic flash-card fork partitions B1 (keep) from B2+B3 (replace) — Band 2's 'game-tree-coupled' modules are exactly NOT shared. Worse, FILES.md's compressed legend states the weaker criterion for B1 ('Would survive a chess port without code change', FILES.md:25-26), while ADR-0003's actual Band 1 definition names 'language flashcards, music theory drills' (0003:105-111). The 215-tag map — the fork author's surgery list — currently cannot reliably answer the fork's keep/replace question.",
   "currentState": "ADR-0003 carries no record that trigger #1 fired (the maintainer stated the fork intent 2026-06-09/10). Its 'What a Chess port would actually require' sizing (0003:261-282) is the only worked port map. FILES.md:25-33 defines all three band tags against the chess axis. The todo DB has a chess-clone item (open/active) for the Chess axis and no flash-card-fork item (verified by description regex over items). The confirmed candidate adr-trigger-sweep-and-record-hygiene records only trigger #2 (inventory drift) for ADR-0003, per the harvest:adr-triggers evidence ('ADR-0001 #2 and ADR-0003 #2 are satisfied/partially satisfied').",
   "proposal": "One coherent ADR-0003 amendment (folded into, not parallel to, the confirmed trigger-recording candidate): (a) record Revisit #1 as fired, naming the generic knowledge-domain fork as the consumer; (b) add a short 'what would change for a knowledge-domain fork' sizing section beside the Chess sizing — the keep-list is SR core, cards, browse, workspace, auth, store/sync/api machinery; B2 and B3 are both replace; (c) correct FILES.md's [B1] legend line to the any-knowledge-domain criterion ADR-0003 already states; (d) execute the inventory delegation the harvest:adr-triggers miner proposed (ADR-0003 keeps band definitions + principle, FILES.md owns per-file tags); (e) file a work-status item for a one-pass audit of the [B1] tags against the stronger criterion — the known drift cases (see rehome-generic-utils candidate) show the tags were checked against chess-survival, not fork-survival.",
   "axis": "generalization",
   "evidence": "docs/adr/0003-frontend-portability-and-domain-boundaries.md read end to end: Revisit #1 at :317-321, Band 1 definition :105-111, Chess sizing :261-282. frontend/FILES.md:25-33 (band legend, chess-axis wording). Miner harvest:adr-triggers ('ADR-0003's band inventory is de-facto superseded by FILES.md but never delegated': 9/19 inventory paths moved; useQeubo.ts 979 lines, analysis-persistence-service.ts 502 absent from the ADR inventory). Todo DB queried read-only: chess-clone exists, no fork item matches 'flash|knowledge'.",
   "cost": "Doc-only plus doc-graph regeneration (structural doc change, so `node tools/doc-graph/generate.mjs` in the same PR per the umbrella CLAUDE.md gate). The B1-tag audit is a bounded single-question pass over ~215 rows; the legend fix is two lines.",
   "risks": "Overlap with the confirmed trigger-recording candidate — landing two separate ADR-0003 amendments in flight would conflict; this must extend that candidate's amendment, not duplicate it. The B1 audit could balloon if tags get re-litigated broadly instead of checked against the single 'survives a non-game port?' question. ADR-0003's inventory-vs-FILES.md delegation changes doc structure, so a second in-flight structural doc PR needs the rebase+regenerate discipline.",
   "likelyAlreadyFiled": "Partially: chess-clone (open) covers the Chess axis only; adr-effectiveness-audits (open) covers sweep cadence; the confirmed adr-trigger-sweep-and-record-hygiene covers trigger #2 recording. No item covers trigger #1 / the fork axis (DB probed)."
  },
  {
   "id": "reviewcard-canonical-content-vocabulary",
   "title": "Finish the rename the backend already executed: ReviewCard.sgf is the wire's generic canonical_content re-specialized to Go vocabulary by the frontend ACL",
   "lesson": "The backend paid for domain generalization (items 30a/30b/34): the wire shape CardWithRecall carries `canonical_content: string`, and the post-34b commit explicitly removed the `normalized_sgf` compat shims (doc comment in the generated types). The frontend ACL then maps that generic field back into instance vocabulary — `sgf: raw.canonical_content` — so the SR domain type, the core type the fork shares, is more Go-bound than the wire it consumes. ADR-0003's principle for agnostic-band code is 'name its abstractions for the problem class, not the instance'; here the instance name was reintroduced at the exact boundary whose job is translation.",
   "currentState": "frontend/src/types.ts:1683-1707 declares `readonly sgf: string` on ReviewCard; frontend/src/services/backend-service.ts (mapToReviewCard, ~:125) maps `sgf: raw.canonical_content`; frontend/src/types/backend.ts:1089-1141 shows the wire field and the 34b history. Domain-side consumers of the field are few: composables/review/useReviewSession.ts:251 (sgf.parse), composables/board/useDirtyBoardGuard.ts:122, components/charts/card-tree-echarts.ts:249.",
   "proposal": "Compiler-driven rename of the domain field to `canonicalContent` (or `content`), leaving the Go interpretation (sgf.parse / loadSgf) at the consumers that parse it — which are already engine-band call sites. The fork then inherits an SR card type whose content field is an opaque string envelope, matching the wire. Explicitly out of scope: `numMoves`/`num_moves` — that vocabulary is on the wire itself, so generalizing it is a backend dispatch question, not a frontend rename.",
   "axis": "generalization",
   "evidence": "Verified in tree: types/backend.ts:1106-1133 (canonical_content on CardWithRecall; comment documenting normalized_sgf shim removal), backend-service.ts mapToReviewCard (`sgf: raw.canonical_content`), types.ts:1683-1707, and the three consumer sites by grep. ADR-0003 principle block :54-69. No miner proposed this; it surfaced from the lens's 'are the card/SR domain types free of Go vocabulary' question.",
   "cost": "Small mechanical rename across ~4 code sites plus the types.ts doc comment; vue-tsc makes missed sites compile errors. Doc-content touches where prose cites ReviewCard.sgf (grep docs/ before landing).",
   "risks": "Pure-rename churn sits in tension with ADR-0003's 'existing code stays put' posture — the justification is Revisit #1 firing plus this being the frontend half of a rename the backend already executed and documented, i.e. closing a recorded asymmetry rather than speculative tidiness. If the maintainer judges rename-only PRs not worth the history noise, the fallback is a types.ts comment naming the asymmetry and a `future` work-status item.",
   "likelyAlreadyFiled": "No (DB probed for canonical_content/reviewcard; only refactoring-queue-adr0007 is adjacent, and it tracks file size, not vocabulary)."
  },
  {
   "id": "review-scoring-extraction-named-seam",
   "title": "Extract useReviewSession's per-move delta scoring into a named Go-bound function — the seam ADR-0003 §Band-mixed designed in prose, now with its consumer",
   "lesson": "ADR-0003 §Band-mixed flagged this exact module: 'The orchestration is portable; the scoring extraction is not' — and Revisit #3 names useReviewSession as the canary for band misclassification. The seam was designed in prose but never given a name in code: the SGF parse, the per-color move-index computation, and the `extra.{color}.deltas` enrichment walk sit inline in a 656-line composable that FILES.md consequently has to tag [B3] wholesale (FILES.md:229), burying the SR orchestration the fork shares inside a Go-bound label. The history shows the seam is a worked pain point, not tidiness: the extraction path carried the silent-0.5 substitution defect that corrupted Ebisu updates (the loud-failure branch's own comment records it) and the 2026-05-12 adaptive-deeper-enrichment diagnosis arc.",
   "currentState": "composables/review/useReviewSession.ts: sgf.parse(card.sgf) at :251; per-color index + s_1-then-scan deltas lookup at :470-509; ADR-0002 loud-failure branch at :509-527 documenting the prior silent corruption. The logic is reachable only through tier-3 integration tests today. ADR-0003's own prescription for the analogous analysis-recording case (:230-234): 'The Go-specific gating lives in a named, isolated function.'",
   "proposal": "Extract the score extraction into a named function in the engine band (e.g. engine/analysis/review-scoring.ts): (board, activePath, s1NodeId, ledgerKeys) -> delta | structured failure, with the per-color indexing and scan-order fallback inside it. useReviewSession keeps queue/state-machine/abort/submission — the fork-shared orchestration — and consults the named function. Tier-1 tests for per-color indexing and the s_0/s_1 scan become possible. Retag or annotate the composable's FILES.md row toward its band-mixed reality in the same PR.",
   "axis": "generalization",
   "evidence": "ADR-0003 §Band-mixed (:193-197) and Revisit #3 (:325-329), read end to end. Tree verification: useReviewSession.ts:251, :470-527 (read directly). Miners: harvest:mech-conformance (bare `as any` at useReviewSession.ts:266 on this path), harvest:postmortems (enrichment-path defect arc). FILES.md:229 ([B3] dominant tag).",
   "cost": "Moderate: a careful extraction in a defect-prone path, plus new tier-1 tests. No runtime behavior change intended; the existing useReviewSession integration tests are the regression net.",
   "risks": "Behavior drift in the scan-order fallback is the real hazard — the s_1-fast-path-then-path-scan order is load-bearing per the inline comment. Scope discipline: the adjacent 'review-session state split across three locations' finding (harvest:arch-ergonomics, not confirmed) is a different, lower-priority cohesion question; conflating the two would grow the PR past its seam. Per refactoring-queue-adr0007's recorded handled-on-touch policy, this should be framed as seam work that incidentally shrinks the file, and recorded as evidence on that item.",
   "likelyAlreadyFiled": "No item covers the scoring seam (DB probed). gradingparameter-opacity-typing (open/future) is adjacent — it types the blob the session reads at :324-325 — but distinct; refactoring-queue-adr0007 should get an evidence ref."
  },
  {
   "id": "rehome-generic-utils-sever-b1-to-b3-edges",
   "title": "Re-home generateUUID out of engine/util.ts and resolve the two [B1]-to-[B3] dependency edges — the concrete imports a fork would have to sever",
   "lesson": "Under the second-consumer calculus, every import from a fork-shared file into the Go-bound engine band is no longer an abstract band-hygiene blemish — it is a concrete line the fork author must cut. generateUUID is domain-agnostic by inspection (RFC4122 over crypto, nothing Go in it) but lives in engine/util.ts ('Board / SGF coord helpers', tagged [B3] at FILES.md:248), forcing false Band-3 coupling onto every consumer, including the fork-shared store factories and the [B1] qEUBO composable.",
   "currentState": "engine/util.ts:213 (generateUUID; its docstring even names secure-context fallback rationale, all generic). Consumers: composables/useQeubo.ts:87 ([B1], FILES.md:148), store/board-factory.ts:14, store/archived-migrations.ts. Second drifted edge: composables/auth-app/useAppBootstrap.ts:30 imports setIntensityHueShift from engine/suggestion-colors ([B3], FILES.md:246) while tagged [B1] (FILES.md:175); services/resource-service.ts:18 has the sibling initializeIntensityFactory import. Band tags are review-enforced only.",
   "proposal": "Move generateUUID to lib/ (compiler-driven import updates; FILES.md entries updated same PR). For useAppBootstrap, present the maintainer two honest options: retag the file (bootstrap is wiring and arguably legitimately calls domain init), or invert the dependency (the app shell registers domain-init hooks that bootstrap runs) — the latter is the fork-friendly shape but costs a small indirection. A band-aware ESLint rule is named but not proposed: it needs machine-readable band tags, and two known edges do not justify that build yet.",
   "axis": "generalization",
   "evidence": "Miner harvest:mech-conformance ('Band drift in two [B1] files'), independently verified by grep: engine/util.ts:213, useQeubo.ts:87, board-factory.ts:14, useAppBootstrap.ts:30, resource-service.ts:18; FILES.md band rows :148, :175, :246, :248. Not in the confirmed index (services-boundary-deny-by-default is a different lint with different polarity and territory).",
   "cost": "Small: one file move, a handful of import-path updates, FILES.md rows. The archived-migrations.ts touch is an import line at file head, not a frozen migration body — but the rolling-archive discipline ('bodies are frozen as they shipped') makes this worth flagging explicitly in the PR.",
   "risks": "If touching archived-migrations.ts is judged to violate the freeze's spirit, the alternative is a deprecated re-export shim left in engine/util.ts — which then rots like all shims; pick one and record it. The useAppBootstrap half needs a maintainer judgment call between honesty (retag) and structure (inversion); silently retagging would hide a seam the fork wants.",
   "likelyAlreadyFiled": "No (DB probed for generateUUID/engine/util; no hits)."
  },
  {
   "id": "review-input-gate-single-owner",
   "title": "Give the review-state input gate one owner: handleBoardMove and handlePastePv implement the same SR policy divergently, alignment is comment-enforced, and the divergence already produced one shipped bug",
   "lesson": "The review-state input gate (AWAITING_MOVE routes to the session; LOADING/ANALYZING blocks; IDLE/FINISHED is free study) is SR-core policy — fork-shared — instantiated today over Go inputs in two independent App.vue handlers. The intermission move-block bug (2026-05-27) happened precisely because the two copies drifted, and the fix's own worklog flags a remaining asymmetry (paste is still permitted during LOADING/ANALYZING, against the fix's own reasoning) with alignment maintained only by cross-referencing comments. Under the second-consumer lens this is the inline-gating shape ADR-0003 warns about: the generic policy and the Go input handling are fused, so the fork inherits neither cleanly.",
   "currentState": "frontend/src/App.vue:267-309 (three-branch move gate, comments cross-referencing the paste handler 'so the two stay aligned through future edits'); :330-332 (paste gate blocks only AWAITING_MOVE). docs/worklog/2026-05-27-review-intermission-move-block.md (read end to end): root cause was exactly this duplication (:20-40); §Asymmetry note :94-103 flags the residual and it is unfiled per the harvest:worklog-current dangling-follow-ups finding. App.vue's script is ~360 lines against ADR-0007's section budget.",
   "proposal": "Extract one owned decision function (a small composable or pure function over the review-state enum): gateForReviewState(state) -> 'route-to-session' | 'block' | 'free', consulted by both handlers (and any future input surface — keyboard move entry, engine-move injection). Decide the paste-during-ANALYZING question once, on the record — the worklog's own reasoning says it should block, but that is a user-visible behavior change needing maintainer sign-off. File the asymmetry as a work-status item regardless of which way it is decided.",
   "axis": "generalization",
   "evidence": "App.vue:267-355 read directly; docs/worklog/2026-05-27-review-intermission-move-block.md read end to end. Miners: harvest:arch-ergonomics ('App.vue script carries grading-integrity policy that belongs in a composable', proposing the superset useBoardMoveRouting extraction) and harvest:worklog-current (asymmetry among unfiled deferrals). Neither is in the confirmed index.",
   "cost": "Small-moderate: one pure function plus two call-site rewrites; App.vue shrinks toward its ADR-0007 budget. A tier-1 test over the gate's state table is nearly free after extraction.",
   "risks": "Tightening paste during LOADING/ANALYZING is a behavior change — must be the maintainer's call, not smuggled in as refactor fallout; the extraction is valuable even if the current asymmetry is ratified as deliberate (it then lives in one table instead of two comments). Keep scope to the gate; the arch-ergonomics superset (full move-routing + play-vs-engine extraction) is a bigger judgment with its own tradeoffs.",
   "likelyAlreadyFiled": "No (DB probed for paste/input gate/handleBoardMove; only refactoring-queue-adr0007 adjacent on file size — add an evidence ref there)."
  }
 ]
}
````

### §4 · gen-distill:erosion

**Commission** (= `[SHARED: GEN-DISCIPLINE]` + `[SHARED: FORK-CONSTRAINT]` + the following):

````
You are a distiller in the generalization pass of a retrospective review of the LengYue frontend. A prior 13-miner harvest produced the findings at /tmp/claude-1000/-home-bork-w-omega/spa-review/evidence.json (Read it — in chunks if needed; it is ~150KB of JSON: per-miner findings arrays with titles/evidence/implications). 18 candidates were already confirmed by the main review; their index is at /tmp/claude-1000/-home-bork-w-omega/spa-review/confirmed_index.json (Read it; do NOT duplicate them — your job is what they MISSED under your lens). Verify anything load-bearing in the tree yourself before proposing.

YOUR LENS: AGNOSTIC-BAND EROSION: places where the git history and current code show Go-specific convenience leaking into or eroding the domain-agnostic infrastructure (ADR-0003 band 1) — wrong-band placements, agnostic modules that grew Go imports, generic seams bypassed by Go-shaped shortcuts, storage/services that introspect domain payloads they should treat as opaque. These erosions are what would actually hurt the fork. Propose candidates that REPAIR the seams. The evidence file lists band-drift findings (mech-conformance miner) and architecture findings (arch-ergonomics miner) — start there, then verify in the tree yourself.

Propose AT MOST 6 candidates for your lens (fewer is fine; zero is an acceptable answer if the evidence does not support more). Each: grounded in evidence (name the miner finding or your own file:line verification), ADR-consistent, honest cost/risk. axis may be 'generalization'.
````

**Result (verbatim):**

````json
{
 "candidates": [
  {
   "id": "cold-start-calibration-to-band3",
   "title": "Repair the cold-start calibration erosion: resource-service's only public verb is Go-specific and the bootstrap carries the hue-shift wiring",
   "lesson": "When a Band-1 module's generic seam is module-private and its only public surface is a domain-specific convenience method, the generic seam is fully shadowed — every future caller is funneled through the Go-shaped shortcut, and the file can no longer lift into a fork unchanged.",
   "currentState": "services/resource-service.ts is ADR-0003's named Band-1 exemplar ('no domain in either', docs/adr/0003:119) and FILES.md:278 still tags it [B1], but resource-service.ts:18 imports initializeIntensityFactory from engine/suggestion-colors ([B3], FILES.md:246). The generic fetchResource<T> (resource-service.ts:40-43) is module-private; the sole public method loadVisitDistribution (:53-63) fetches KataGo visit-distribution calibration as fetchResource<any> and hands it to the B3 factory. The companion limb: composables/auth-app/useAppBootstrap.ts ([B1], FILES.md:175) imports setIntensityHueShift from engine/suggestion-colors at :30 and binds the hue-shift knob watch at :116-120, then fires loadVisitDistribution at :408-409. git shows loadVisitDistribution present since the initial frontend commit (e5c857b) — ADR-0003's Band-1 listing inherited the erosion — and the hue-shift watch added by feature commit 1545ea5.",
   "proposal": "Export the generic verb (getResource<T>(name)) from resource-service and move the calibration orchestration — fetch + initializeIntensityFactory + the hue-shift watch — into one Band-3 home (an init function in engine/suggestion-colors, or a small composables/board calibration module), invoked from the bootstrap through a single named domain-init call. Type the calibration payload at the B3 boundary instead of <any>. Update the FILES.md rows; resource-service's and useAppBootstrap's [B1] tags become true again.",
   "axis": "generalization",
   "evidence": "mech-conformance miner ('Band drift in two [B1] files') verified for useAppBootstrap:30; the resource-service.ts:18 edge and the seam-shadowing shape (private fetchResource<T>, public Go-only verb) are my own tree verification, not in the miner findings or the confirmed index.",
   "cost": "Small: one new ~40-line B3 module, two import-site moves, FILES.md row edits. No behavior change intended.",
   "risks": "The bootstrap ordering tolerance documented at useAppBootstrap.ts:108-115 (hue-shift value recorded before the distribution lands; rebuildIntensityColorFn early-returns) must be preserved when the watch moves — keep immediate:true semantics and the fire-and-forget catch posture (resource-service.ts:57-62). Verification is manual smoke (suggestion-color gradient after cold start); no component tests exist for this surface."
  },
  {
   "id": "registry-editor-vocabulary-injection",
   "title": "Un-bake the domain vocabularies from RegistryEditor: enum tables, dynamic-node path rules, and hints become injected configuration",
   "lesson": "A generic editor accretes domain vocabulary because an in-file table is cheaper at each feature touch than designing a prop seam once — the canonical Go-shaped shortcut through a generic component. Each accretion is invisible until a fork needs the editor without the domain.",
   "currentState": "components/editors/RegistryEditor.vue is ADR-0003's named Band-1 'registry editor' (docs/adr/0003:130-131) and FILES.md:111 tags it [B1] 'Generic managed-registry editor'. But :9 imports WINRATE_FRAMINGS from engine/katago/types ([B3]); the isDynamicNode rule (:26-39) hard-codes the KataGo 'overrideSettings' path; PATH_ENUMS (:63-92) bakes in 'engine.katago.overrideSettings.reportAnalysisWinratesAs' and friends; a warning-hints table below carries KataGo wire-framing semantics. The file's own comment concedes dynamic value sets 'need a per-root option resolver, not a static table' (:60-62).",
   "proposal": "Make the three tables injected configuration: a config object (enum vocabularies keyed by dot-path, dynamic-node predicates, hint strings) supplied at the mount roots (SettingsTab.vue mounts), with the domain tables moving to a B2/B3 config module that imports WINRATE_FRAMINGS et al. Thread via provide/inject at the editor root rather than per-level props, since the component recurses on `path`. The editor returns to true Band 1; the flash-card fork supplies its own vocabulary config through the same seam.",
   "axis": "generalization",
   "evidence": "My own tree verification (RegistryEditor.vue:9, :26-39, :63-92); not present in any miner finding or confirmed candidate. The component's in-file comment naming the missing resolver is the codebase itself pointing at the seam.",
   "cost": "Moderate-small: one config interface, one domain config module, provide/inject wiring at two-ish mount sites, three table moves. No behavior change intended.",
   "risks": "The editor is a deep recursive component; an injection seam done carelessly (per-level props) adds noise — provide/inject keeps the diff bounded. The settings surface has no component tests (per the testing posture in frontend/CLAUDE.md), so verification is a manual walk of the Settings tab dropdowns and the overrideSettings add/remove affordance."
  },
  {
   "id": "rehome-agnostic-utils-from-engine-util",
   "title": "Re-home the agnostic utilities stranded in engine/util.ts: generateUUID, updateRegistry, setDeep",
   "lesson": "Agnostic helpers get homed by authoring convenience next to their first caller's existing imports; each one then drags a false Band-3 edge onto every agnostic consumer, and the FILES.md one-line description quietly stops being true.",
   "currentState": "engine/util.ts ([B3], FILES.md:248, described as 'Board / SGF coord helpers; active-variation traversal') contains three domain-free exports the description doesn't mention: setDeep (:35), updateRegistry<T> (:191), generateUUID (:213). Consumers pulling false B3 edges: composables/useQeubo.ts:87 ([B1], FILES.md:148 — the mech-conformance miner's finding), components/SettingsTab.vue:35 ([B2]) and composables/board/useDirtyBoardGuard.ts:41 for updateRegistry — i.e. the generic settings/registry write path routes through the Go-bound module. git e57c90b ('fix crypto.randomUUID on insecure contexts') shows generateUUID was minted for the qEUBO arc and homed in engine/util; its own docstring names useQeubo as the call site.",
   "proposal": "Move the three functions to lib/ (FILES.md:316 already flags lib/utils.ts as a near-empty merger candidate — this is the natural occasion), update the ~8 import sites, and update the FILES.md rows. For store/archived-migrations.ts:29 (bodies are convention-frozen per frontend/CLAUDE.md's rolling-archive section), either treat the import line as header-level (not a body edit) and update it, or leave a re-export in engine/util.ts so the frozen file is untouched. Before moving setDeep, check it against lib/knobs.ts's path-walk accessors for near-duplication — consolidate only if they are actually the same function, per the 'co-location is not collapse' lesson.",
   "axis": "generalization",
   "evidence": "mech-conformance miner ('re-home generateUUID to lib/'); updateRegistry and setDeep extend it via my own verification (engine/util.ts:35,:191; SettingsTab.vue:35,:69-75; useDirtyBoardGuard.ts:41,:87). Not in the confirmed index.",
   "cost": "Trivial-to-small mechanical move; ~8 greppable import sites; FILES.md row edits.",
   "risks": "The archived-migrations touch needs an explicit one-line justification in the PR (frozen-file convention); the setDeep/knobs consolidation question should be answered, not assumed, before merging anything."
  },
  {
   "id": "split-keybindings-catalog-from-substrate",
   "title": "Split lib/keybindings.ts: the generic registry substrate from the Go-shaped action catalog",
   "lesson": "Substrate and catalog fused in one [B1]-tagged lib file means the domain handlers' imports (analysis service, navigation, store writes) are carried by the substrate's band tag — the fork wants the substrate wholesale and the catalog not at all, and today they are inseparable.",
   "currentState": "lib/keybindings.ts ([B1], FILES.md:311 'Keybindings registry substrate') imports useNavigation (:41), the store (:42), and analysisService (:43). Lines ~121-265 are the KEYBINDING_ACTIONS catalog whose handlers are Go verbs — ponder toggle (:200-206), showMoveSuggestions flip (:217), move-number toggle (:228) — while the substrate (KeybindingActionDecl/dispatch types :46-86, branded action ids :86-111, effectiveKey :274, normalizeKey :295, isActionEnabled :306, validateKeybindingsRegistry :323) is domain-free machinery. The sibling lib/keybindings-capture.ts is already substrate-clean (imports only store + ./keybindings). Consumers of the fused module: useUserIORegistry.ts, useAppBootstrap.ts, KeybindingsView.vue, KeybindingRow.vue.",
   "proposal": "Split the declarative catalog (the KeybindingActionDecl[] with handlers, plus the ACTIONS id constants if they prove domain-specific) into a separate catalog module tagged [B2]/[B3], leaving lib/keybindings.ts the pure substrate that takes the catalog as input. The seam needing actual design: the enabledWhen vocabulary ('activeBoardExists', evaluated by isActionEnabled against the store) — either catalog-supplied predicate functions (substrate stays predicate-agnostic) or a substrate-level capability interface the host implements. Retag both files in FILES.md.",
   "axis": "generalization",
   "evidence": "My own tree verification (lib/keybindings.ts:41-44, :97, :121-265, :217); the arch-ergonomics miner cited keybindings.ts:217 only as a multi-writer site (covered by the confirmed multi-writer candidate); the band fusion itself is not in any confirmed item. FILES.md:315's timing.ts entry shows the project already distinguishes 'structurally B1, catalogs domain values' — keybindings.ts fails even the structural half of that test.",
   "cost": "Small split plus one real design decision (enabledWhen). Four consumer modules to re-point; greppable.",
   "risks": "If ACTIONS ids are threaded through persisted keybinding state (store.profile), the split must not change the persisted id strings — pure code motion, no schema bump. The enabledWhen seam done lazily (substrate keeps importing the store) would re-create the fusion one level down; that half is the actual work."
  },
  {
   "id": "mechanize-band-conformance-check",
   "title": "Mechanize ADR-0003 band conformance: a CI check that a file's FILES.md band tag is consistent with the bands of its imports",
   "lesson": "ADR-0003 names its own weakness — 'the principle is policy, not mechanism' (docs/adr/0003:299-301) — and frontend/CLAUDE.md:103-107 names tag/dependency drift as the silent failure the FILES.md discipline exists to surface, with nothing mechanizing it. The codebase's proven recurrence-stoppers are exactly RCA-minted lint rules and CI gates (git-narrative miner); the band boundary now has a second consumer depending on it.",
   "currentState": "My sweep found 8 [B1]-tagged files importing from engine/*: main.ts:10 (window.Writer dev exposure at :17, no in-tree consumers), ColorDebugStrip.vue:9 (a mis-tag — the component's purpose is Go-gradient calibration), SidebarWidget.vue:13 (type-only BoardSnapshot), RegistryEditor.vue:9, useQeubo.ts:87, useAppBootstrap.ts:30, resource-service.ts:18, archived-migrations.ts:28-29 (frozen, band-mixed by nature). The mix of true erosions and stale tags is itself the finding: nothing today forces either repair or retag. B2-vs-B3 conformance is unmeasured — the checker would measure it. The FILES.md tag grammar is machine-readable (one regex-stable tag per row).",
   "proposal": "A doc-graph-style Node script in CI (the tools/doc-graph precedent) parsing FILES.md tags plus the import graph, enforcing band(file) >= band(import) with [B?] exempt and an explicit annotated-exception list (archived-migrations). Violations fail with 'repair the import or retag the file' — both resolutions are wins, loudly (ADR-0002). Adopt at warn against the measured baseline and ratchet to error after burn-down — the same adoption arc as the confirmed bare-as-any lint candidate and the existing import-boundary rules in eslint.config.js. Policy to settle at adoption: whether type-only imports count (I'd argue yes — the type dependency is real for a fork that deletes the module).",
   "axis": "generalization",
   "evidence": "mech-conformance miner ('the ESLint import-boundary pattern could grow a band-aware rule, mechanizing ADR-0003 conformance'); the 8-file baseline is my own sweep. The confirmed services-boundary-deny-by-default and cast-lint items are adjacent lint work on different boundaries; neither covers band conformance.",
   "cost": "Moderate: a parser script (must fail loudly on unparseable FILES.md rows), CI wiring, and a burn-down of ~8 findings — several of which are the sibling candidates in this set.",
   "risks": "FILES.md's format becomes load-bearing; a strict-grammar parse that errors on ambiguity is the ADR-0002-consistent mitigation. Band-mixing is sometimes intra-file (timing.ts's documented B1-structure/B3-values split, the migrations framework vs bodies) — the checker covers the structural (import) half only; the content half stays review judgment, and the check's docs should say so rather than imply full coverage."
  },
  {
   "id": "adr-0003-revisit-1-fired-flashcard-adopter",
   "title": "Record that ADR-0003 Revisit #1 has fired — and that the flash-card fork moves the load-bearing boundary from B2/B3 to B1/B2",
   "lesson": "ADR-0003's whole cost-benefit rests on 'extract when the second use case exists'; when the second consumer materializes, leaving the trigger unrecorded silently keeps the old calculus in force for every authoring decision that consults the ADR.",
   "currentState": "ADR-0003 Revisit #1 (docs/adr/0003:317-321: 'A second domain adopter materializes... Port extraction stops being premature') has fired via the maintainer's 2026-06-09/10 statement of the generic knowledge flash-card fork. Crucially, the fork is outside the game class, and ADR-0003 already encodes what that means: Band 2 is defined as not surviving 'a port outside that class (e.g., to language flashcards, where there is no game tree)' (docs/adr/0003:137-140), and Band 1 is defined against 'any other knowledge domain (... language flashcards, music theory drills)' (:107-111). So for this adopter the B1/B2 boundary, not B2/B3, is the one that must hold — every B1 erosion in this candidate set is fork-blocking, while B2 game-tree modules are replaceable surface. The work-status store has an open 'chess-clone' item (a B3-replacement port per its description) but nothing recording the flash-card adopter or the boundary shift; the confirmed adr-trigger candidate amends ADR-0003 only to delegate the per-file inventory to FILES.md.",
   "proposal": "A bounded ADR-0003 amendment per the project's amendment convention: record Revisit #1 as fired (dated, naming the flash-card fork and the maintainer's constraint), state the consequence — seam-naming and Port extraction on the B1/B2 boundary are no longer premature, while ADR-0003's authoring-time judgment against speculative abstraction still applies — and note that for this adopter the B1 band is the contract surface. File a work-status item for the fork-readiness arc (distinct from chess-clone) so the band-repair candidates have a parent item to ref.",
   "axis": "generalization",
   "evidence": "ADR-0003 read end to end (Revisit #1 at :317-321; Band definitions at :107-111 and :137-140); work-status query showing only the chess-clone item (id 'chess-clone', state open) touches domain-port territory; the maintainer's binding constraint is the trigger event itself.",
   "cost": "Small: one ADR amendment, one work-status item, doc-graph regeneration if the amendment adds cross-references (the structural-change rule in the umbrella CLAUDE.md).",
   "risks": "Recording the trigger must not be read as a refactoring mandate — ADR-0003's 'Not goals' section should keep its force; the amendment should say the calculus changed, not that a Port-extraction wave is sanctioned. Overlap to manage: the confirmed adr-trigger-sweep candidate also amends ADR-0003 (inventory delegation); the two amendments should land as one edit to avoid doc churn.",
   "likelyAlreadyFiled": "Partially adjacent: the confirmed adr-trigger-sweep-and-record-hygiene candidate amends ADR-0003 for inventory delegation, but does not record Revisit #1 or the boundary-shift consequence; the open chess-clone work-status item is a different adopter with a different (B3-replacement) shape."
  }
 ]
}
````

### §4 · gen-distill:merge

**Commission** (= `[SHARED: GEN-DISCIPLINE]` + `[SHARED: FORK-CONSTRAINT]` + the following + the two distillers' candidate arrays from above):

````
You are the merge/dedupe judge for the generalization pass. Below are raw candidates from two lens distillers. Also Read /tmp/claude-1000/-home-bork-w-omega/spa-review/confirmed_index.json — the 18 already-confirmed main-review candidates. Merge semantic duplicates among the raw candidates; DROP any that duplicates a confirmed candidate (unless it adds a genuinely different proposal — then sharpen the contrast); drop weakly-evidenced ones. Return at most 8, best first.
````

**Result (verbatim):**

````json
{
 "candidates": [
  {
   "id": "adr0003-revisit-1-fired-fork-axis-remap",
   "title": "Record ADR-0003 Revisit #1 as fired, and re-cut the band map for the adopter that actually materialized: a non-game knowledge-domain fork, not a Chess port",
   "lesson": "ADR-0003's entire cost-benefit calculus is keyed on 'extract when the second use case exists' (Revisit #1); leaving the trigger unrecorded silently keeps the old calculus in force for every authoring decision that consults the ADR. Worse, the consumer that materialized does not sit on the axis the map was drawn on: a Chess port partitions B3 (replace) from B1+B2 (keep), while a generic flash-card fork partitions B1 (keep) from B2+B3 (replace) — so for this adopter the B1/B2 boundary, not B2/B3, is the load-bearing contract, and every B1 erosion becomes fork-blocking while B2 game-tree modules become replaceable surface.",
   "currentState": "ADR-0003 carries no record that Revisit #1 fired (docs/adr/0003-frontend-portability-and-domain-boundaries.md:317-321; the maintainer stated the fork intent 2026-06-09/10). The ADR already encodes the axis shift: Band 1 is defined against any knowledge domain including 'language flashcards, music theory drills' (:105-111), and Band 2 explicitly does not survive 'a port outside that class (e.g., to language flashcards, where there is no game tree)' (:136-139) — yet the only worked sizing is the Chess port (:261-282), and frontend/FILES.md:25-33 defines all three band tags against the weaker chess axis ('[B1] — Would survive a chess port without code change'). The confirmed adr-trigger-sweep-and-record-hygiene candidate records only ADR-0003 trigger #2 (inventory drift / FILES.md delegation), not trigger #1. Per the distillers' todo-DB probes, a chess-clone item exists (open) but nothing records the flash-card adopter.",
   "proposal": "One coherent ADR-0003 amendment, folded into (not parallel to) the confirmed trigger-sweep candidate's amendment: (a) record Revisit #1 as fired, dated, naming the generic knowledge-domain fork and the maintainer's constraint; (b) state the consequence — seam-naming and Port extraction on the B1/B2 boundary stop being premature, while the 'Not goals' anti-mandate (:356-365) keeps its force; (c) add a short 'what a knowledge-domain fork would require' sizing beside the Chess sizing (keep: SR core, cards, browse, workspace, auth, store/sync/api machinery; replace: B2 and B3 both); (d) correct the FILES.md [B1] legend line to the any-knowledge-domain criterion ADR-0003 already states; (e) file a work-status item for the fork-readiness arc (distinct from chess-clone, parent for the band-repair candidates below) including a one-pass audit of the ~215 [B1] tags against the stronger criterion — the verified drift cases show tags were checked against chess-survival, not fork-survival.",
   "axis": "generalization",
   "evidence": "I read ADR-0003 end to end: Revisit #1 :317-321, Band 1 definition :105-111, Band 2 flash-card exclusion :136-139, Chess sizing :261-282, 'Not goals' :356-365. FILES.md:25-33 legend verified directly (chess-axis wording on all three tags). Confirmed-index check: adr-trigger-sweep-and-record-hygiene's title covers trigger recording but both distillers report it records ADR-0003's trigger #2 only. The DB-probe claims (chess-clone open; no item matching flash/knowledge) are the distillers'; not re-verified by me. Merged from two raw candidates that proposed the same amendment.",
   "cost": "Doc-only plus doc-graph regeneration in the same PR (structural doc change per the umbrella CLAUDE.md gate: node tools/doc-graph/generate.mjs). The B1-tag audit is a bounded single-question pass; the legend fix is a few lines.",
   "risks": "Two ADR-0003 amendments in flight would conflict — this must extend the confirmed candidate's amendment as one edit, and the second structural doc PR to merge needs the rebase+regenerate discipline. Recording the trigger must not read as a refactoring mandate; the amendment should say the calculus changed, not that a Port-extraction wave is sanctioned. The B1 audit can balloon if tags get re-litigated beyond the single 'survives a non-game port?' question.",
   "likelyAlreadyFiled": "Partially: the confirmed adr-trigger-sweep-and-record-hygiene candidate amends ADR-0003 for trigger #2 / inventory delegation but does not record Revisit #1 or the boundary-shift consequence; the open chess-clone work-status item is a different adopter on a different (B3-replacement) axis (distiller DB probe)."
  },
  {
   "id": "cold-start-calibration-to-band3",
   "title": "Repair the cold-start calibration erosion in two [B1] files: resource-service's only public verb is Go-specific, and the bootstrap carries the hue-shift wiring",
   "lesson": "When a Band-1 module's generic seam is module-private and its only public surface is a domain-specific convenience method, the generic seam is fully shadowed — every future caller is funneled through the Go-shaped shortcut, and the file can no longer lift into the fork unchanged. Under the second-consumer calculus these are no longer abstract band-hygiene blemishes; they are concrete import lines the fork author must cut.",
   "currentState": "services/resource-service.ts is ADR-0003's named Band-1 exemplar ('no domain in either', docs/adr/0003-frontend-portability-and-domain-boundaries.md:119-120) and FILES.md:278 tags it [B1], but resource-service.ts:18 imports initializeIntensityFactory from engine/suggestion-colors ([B3], FILES.md:246). The generic fetchResource<T> (resource-service.ts:40) is module-private; the sole public method loadVisitDistribution (:53) fetches KataGo visit-distribution calibration as fetchResource<any> (:55) and hands it to the B3 factory (:56). The companion limb: composables/auth-app/useAppBootstrap.ts ([B1], FILES.md:175) imports setIntensityHueShift from engine/suggestion-colors at :30, binds the hue-shift knob watch at :115-118, and fires loadVisitDistribution at :408-409. All line claims verified in tree; the git-history claims (loadVisitDistribution present since initial frontend commit e5c857b; hue-shift watch added by 1545ea5) are the distiller's.",
   "proposal": "Export the generic verb (getResource<T>(name)) from resource-service and move the calibration orchestration — fetch + initializeIntensityFactory + the hue-shift watch — into one Band-3 home (an init function in engine/suggestion-colors, or a small composables/board calibration module), invoked from the bootstrap through a single named domain-init call. Type the calibration payload at the B3 boundary instead of <any>. For useAppBootstrap there is a maintainer judgment call worth presenting honestly: either the single named domain-init call (the fork-friendly inversion, at the cost of a small indirection) or retagging the bootstrap as legitimately band-mixed wiring — silently retagging would hide a seam the fork wants, so the choice should be on the record. Update the FILES.md rows either way.",
   "axis": "generalization",
   "evidence": "Merged from two raw candidates (one per distiller lens) that found the same two edges independently; the seam-shadowing shape (private fetchResource<T>, public Go-only verb) verified by me directly: resource-service.ts:18/:40/:53-56; useAppBootstrap.ts:30/:115-118/:408-409; ADR-0003:119-120; FILES.md rows :175, :246, :278. Originated from the mech-conformance miner's 'Band drift in two [B1] files' finding; not in the confirmed index (services-boundary-deny-by-default is a different lint on a different boundary).",
   "cost": "Small: one new ~40-line B3 module, two import-site moves, FILES.md row edits. No behavior change intended.",
   "risks": "The bootstrap ordering tolerance documented around useAppBootstrap.ts:115 (hue-shift value recorded before the distribution lands) must be preserved when the watch moves — keep immediate-watch semantics and the fire-and-forget catch posture noted at :408. No component tests cover this surface; verification is a manual cold-start smoke of the suggestion-color gradient."
  },
  {
   "id": "rehome-agnostic-utils-from-engine-util",
   "title": "Re-home the agnostic utilities stranded in engine/util.ts — generateUUID, updateRegistry, setDeep — severing false [B1]-to-[B3] edges",
   "lesson": "Agnostic helpers get homed by authoring convenience next to their first caller's existing imports; each one then drags a false Band-3 edge onto every agnostic consumer — including fork-shared store factories and the generic settings/registry write path — and the FILES.md one-line description quietly stops being true.",
   "currentState": "engine/util.ts ([B3], FILES.md:248, 'Board / SGF coord helpers; active-variation traversal'; ADR-0003's own inventory lists it Band 3 at docs/adr/0003:180-181) contains three domain-free exports the description doesn't mention: setDeep (:35), updateRegistry<T> (:191), generateUUID (:213, RFC4122 over crypto with a secure-context fallback — nothing Go in it). Verified consumers pulling false B3 edges: composables/useQeubo.ts:87 ([B1], FILES.md:148), store/board-factory.ts:14, store/archived-migrations.ts:29 (generateUUID); components/SettingsTab.vue:35 and composables/board/useDirtyBoardGuard.ts:41 (updateRegistry) — i.e. the generic registry write path routes through the Go-bound module. The git claim that generateUUID was minted for the qEUBO arc (e57c90b) is the distiller's.",
   "proposal": "Move the three functions to lib/ — FILES.md:316 already flags lib/utils.ts as a near-empty merger candidate ('debounce helper (the only inhabitant)'), so this is the natural occasion. Update the ~8 greppable import sites and the FILES.md rows. For archived-migrations.ts:29 (bodies convention-frozen per the rolling-archive discipline), pick one of two options on the record: treat the import line as header-level (not a body edit) and update it, or leave a re-export shim in engine/util.ts so the frozen file is untouched — shims rot, so the choice should be explicit. Before moving setDeep, check it against lib/knobs.ts's path-walk accessors (FILES.md:313) for near-duplication; consolidate only if they are actually the same function.",
   "axis": "generalization",
   "evidence": "Merged from two raw candidates (one per lens; both extend the mech-conformance miner's 're-home generateUUID' finding). All function and consumer sites verified by me: engine/util.ts:35/:191/:213; useQeubo.ts:87; board-factory.ts:14; archived-migrations.ts:29; SettingsTab.vue:35; useDirtyBoardGuard.ts:41; FILES.md rows :148, :248, :316; ADR-0003:180-181. Not in the confirmed index.",
   "cost": "Trivial-to-small mechanical move; ~8 import sites, compiler-driven; FILES.md row edits.",
   "risks": "The archived-migrations touch needs an explicit one-line justification in the PR either way (frozen-file convention); the setDeep/knobs consolidation question should be answered, not assumed, before merging anything — co-location is not collapse."
  },
  {
   "id": "registry-editor-vocabulary-injection",
   "title": "Un-bake the domain vocabularies from RegistryEditor: enum tables, dynamic-node path rules, and hints become injected configuration",
   "lesson": "A generic editor accretes domain vocabulary because an in-file table is cheaper at each feature touch than designing a prop seam once — the canonical Go-shaped shortcut through a generic component. Each accretion is invisible until a fork needs the editor without the domain.",
   "currentState": "components/editors/RegistryEditor.vue is ADR-0003's named Band-1 'registry editor' (docs/adr/0003:130-131) and FILES.md:111 tags it [B1] 'Generic managed-registry editor'. But :9 imports WINRATE_FRAMINGS from engine/katago/types ([B3]); the isDynamicNode rule (:25-39) hard-codes the KataGo 'overrideSettings' path (endsWith check at :39, gating the add/delete affordances at :224/:253/:286); PATH_ENUMS (:63 onward) bakes in 'engine.katago.overrideSettings.reportAnalysisWinratesAs' (:78) and friends; a warning-hints table (:98-114) carries KataGo wire-framing semantics. The file's own comment concedes dynamic value sets 'need a per-root option resolver, not a static table' (:60-62). All verified in tree.",
   "proposal": "Make the three tables injected configuration: a config object (enum vocabularies keyed by dot-path, dynamic-node predicates, hint strings) supplied at the mount roots, with the domain tables moving to a B2/B3 config module that imports WINRATE_FRAMINGS et al. Thread via provide/inject at the editor root rather than per-level props, since the component recurses on `path`. The editor returns to true Band 1; the fork supplies its own vocabulary config through the same seam.",
   "axis": "generalization",
   "evidence": "Single-lens finding, fully verified by me in tree (RegistryEditor.vue:9, :25-39, :60-62, :63-114, :224-286); not in any miner finding or confirmed candidate. The component's in-file resolver comment is the codebase itself pointing at the seam; ADR-0003:130-131 and FILES.md:111 establish the [B1] claim being eroded.",
   "cost": "Moderate-small: one config interface, one domain config module, provide/inject wiring at the mount sites, three table moves. No behavior change intended.",
   "risks": "The editor is a deep recursive component; an injection seam done carelessly (per-level props) adds noise — provide/inject keeps the diff bounded. The settings surface has no component tests, so verification is a manual walk of the Settings tab dropdowns and the overrideSettings add/remove affordance."
  },
  {
   "id": "split-keybindings-catalog-from-substrate",
   "title": "Split lib/keybindings.ts: the generic registry substrate from the Go-shaped action catalog",
   "lesson": "Substrate and catalog fused in one [B1]-tagged lib file means the domain handlers' imports (analysis service, navigation, store writes) are carried by the substrate's band tag — the fork wants the substrate wholesale and the catalog not at all, and today they are inseparable.",
   "currentState": "lib/keybindings.ts ([B1], FILES.md:311, self-described 'Keybindings registry substrate') imports useNavigation (:41), the store (:42), and analysisService (:43); the catalog's handlers are Go verbs (e.g. the ponder toggle dispatching analysisService.isPondering / stopPonderOnBoard / analyzeActiveNode at :201-204) while the substrate helpers the FILES.md row names (effectiveKey, isActionEnabled, normalizeKey, validateKeybindingsRegistry) are domain-free machinery. The sibling lib/keybindings-capture.ts (FILES.md:312) imports only store + ./keybindings. FILES.md:315's timing.ts entry shows the project already distinguishes 'structurally B1, catalogs domain values' — keybindings.ts fails the structural half of that test. Imports and FILES.md rows verified by me; the catalog line ranges and the four-consumer list are the distiller's, consistent with what I read.",
   "proposal": "Split the declarative catalog (the KeybindingActionDecl[] with handlers, plus the ACTIONS id constants if they prove domain-specific) into a separate catalog module tagged [B2]/[B3], leaving lib/keybindings.ts the pure substrate that takes the catalog as input. The seam needing actual design: the enabledWhen vocabulary ('activeBoardExists', evaluated against the store) — either catalog-supplied predicate functions (substrate stays predicate-agnostic) or a substrate-level capability interface the host implements. Retag both files in FILES.md.",
   "axis": "generalization",
   "evidence": "Single-lens finding. Verified by me: keybindings.ts:41-43 (the three domain imports), :119 (module-scope useNavigation), :201-204 (ponder handler); FILES.md:311-312, :315. The arch-ergonomics miner cited keybindings.ts:217 only as a multi-writer site (covered by the confirmed multi-writer candidate); the band fusion itself is in no confirmed item.",
   "cost": "Small split plus one real design decision (enabledWhen). Four consumer modules to re-point; greppable.",
   "risks": "If ACTIONS ids are threaded through persisted keybinding state (store.profile), the split must not change the persisted id strings — pure code motion, no schema bump. The enabledWhen seam done lazily (substrate keeps importing the store) would re-create the fusion one level down; that half is the actual work."
  },
  {
   "id": "review-scoring-extraction-named-seam",
   "title": "Extract useReviewSession's per-move delta scoring into a named Go-bound function — the seam ADR-0003 designed in prose, now with its consumer",
   "lesson": "ADR-0003's band-mixed section flagged this exact module ('The orchestration is portable; the scoring extraction is not') and Revisit #3 names useReviewSession as the canary for band misclassification — the canary has in fact tripped: the ADR inventories it Band 2 while FILES.md tags it [B3] wholesale, burying the SR orchestration the fork shares inside a Go-bound label, because the seam was designed in prose but never given a name in code.",
   "currentState": "composables/review/useReviewSession.ts (656 lines): sgf.parse(card.sgf) at :251; the per-color `extra.{color}.deltas` indexing with s_1-fast-path-then-path-scan lookup at :477-511; the ADR-0002 loud-failure branch at :511 onward documenting the prior silent-0.5 corruption of Ebisu updates. FILES.md:229 tags the file [B3] while ADR-0003 inventories it Band 2 (docs/adr/0003:149-152) and band-mixed (:193-196); Revisit #3 (:325-329) names it explicitly. ADR-0003's prescription for the analogous analysis-recording case: 'The Go-specific gating lives in a named, isolated function' (:230-234). All of the above verified by me. The claims that the logic is reachable only through tier-3 integration tests and the 2026-05-12 enrichment-defect arc are the distiller's (drawing on the mech-conformance and postmortems miners).",
   "proposal": "Extract the score extraction into a named function in the engine band (e.g. engine/analysis/review-scoring.ts): (board, activePath, s1NodeId, ledgerKeys) -> delta | structured failure, with the per-color indexing and scan-order fallback inside it. useReviewSession keeps queue/state-machine/abort/submission — the fork-shared orchestration — and consults the named function. Tier-1 tests for per-color indexing and the s_0/s_1 scan become possible. Retag or annotate the composable's FILES.md row toward its band-mixed reality in the same PR (which also resolves the ADR-vs-FILES.md band disagreement on the record).",
   "axis": "generalization",
   "evidence": "Single-lens finding. ADR-0003 read end to end by me (:149-152, :193-196, :230-234, :325-329); tree verification of useReviewSession.ts:251, :477-511, the :511 ADR-0002 comment, and the 656-line count; FILES.md:229. Miner support (bare `as any` at :266 on this path; enrichment-defect arc) is the distiller's attribution.",
   "cost": "Moderate: a careful extraction in a defect-prone path, plus new tier-1 tests. No runtime behavior change intended; existing integration tests are the regression net.",
   "risks": "Behavior drift in the scan-order fallback is the real hazard — the s_1-fast-path-then-path-scan order is load-bearing per the inline comments around :490-510. Scope discipline: the adjacent 'review-session state split across three locations' arch-ergonomics finding is a different, lower-priority cohesion question; conflating them would grow the PR past its seam. Frame as seam work that incidentally shrinks the file and record as evidence on the refactoring-queue item.",
   "likelyAlreadyFiled": "No item covers the scoring seam (distiller DB probe). gradingparameter-opacity-typing (open/future) is adjacent but distinct; refactoring-queue-adr0007 should get an evidence ref."
  },
  {
   "id": "mechanize-band-conformance-check",
   "title": "Mechanize ADR-0003 band conformance: a CI check that a file's FILES.md band tag is consistent with the bands of its imports",
   "lesson": "ADR-0003 names its own weakness — 'The principle is policy, not mechanism' (docs/adr/0003:299-301) — and the verified baseline shows what policy-only enforcement produced: a mix of true B1 erosions and stale tags that nothing today forces toward either repair or retag. With the fork making the B1 boundary contractual, the boundary needs the same treatment the codebase already gives its other recurrence-stoppers (RCA-minted lint rules and CI gates).",
   "currentState": "The sweep of [B1]-tagged files importing from engine/*, now fully verified by me: main.ts:10 (engine/sgf-writer), components/charts/ColorDebugStrip.vue:9 (engine/suggestion-colors — likely a mis-tag, the component is a gradient-calibration debug strip per FILES.md:81), components/chrome/SidebarWidget.vue:13 (type-only BoardSnapshot from engine/board-geometry; [B1] at FILES.md:97), RegistryEditor.vue:9, useQeubo.ts:87, useAppBootstrap.ts:30, resource-service.ts:18, archived-migrations.ts:29 (frozen, band-mixed by nature). B2-vs-B3 conformance is unmeasured — the checker would measure it. The FILES.md tag grammar is regex-stable (one tag per row, verified across the rows read).",
   "proposal": "A doc-graph-style Node script in CI (the tools/doc-graph precedent) parsing FILES.md tags plus the import graph, enforcing band(file) >= band(import) with [B?] exempt and an explicit annotated-exception list (archived-migrations). Violations fail with 'repair the import or retag the file' — both resolutions are wins, loudly (ADR-0002). Adopt at warn against the measured baseline and ratchet to error after burn-down — the adoption arc of the confirmed bare-as-any lint candidate. Note the burn-down list is largely the sibling candidates above (calibration, util re-home, RegistryEditor, keybindings), so this lands naturally after or alongside them. Policy to settle at adoption: whether type-only imports count (arguably yes — the type dependency is real for a fork that deletes the module; SidebarWidget.ts:13 is the test case).",
   "axis": "generalization",
   "evidence": "Extends the mech-conformance miner's suggestion ('the ESLint import-boundary pattern could grow a band-aware rule'); all 8 baseline files independently verified by me at the cited lines, including locating the two the raw candidate cited without directories (charts/ColorDebugStrip.vue, chrome/SidebarWidget.vue). ADR-0003:299-301 read in context. The confirmed services-boundary-deny-by-default and cast-lint items are adjacent lint work on different boundaries; neither covers band conformance.",
   "cost": "Moderate: a parser script (must fail loudly on unparseable FILES.md rows), CI wiring, and a burn-down of ~8 findings, several of which are the sibling candidates in this set.",
   "risks": "FILES.md's format becomes load-bearing; a strict-grammar parse that errors on ambiguity is the ADR-0002-consistent mitigation. Band-mixing is sometimes intra-file (timing.ts's documented B1-structure/B3-values split at FILES.md:315; the migrations framework vs bodies) — the checker covers the structural (import) half only; the content half stays review judgment, and the check's docs should say so rather than imply full coverage."
  },
  {
   "id": "reviewcard-canonical-content-vocabulary",
   "title": "Finish the rename the backend already executed: ReviewCard.sgf is the wire's generic canonical_content re-specialized to Go vocabulary by the frontend ACL",
   "lesson": "The backend paid for domain generalization (items 30a/30b/34): the wire shape carries `canonical_content: string`, and the frontend ACL maps that generic field back into instance vocabulary — `sgf: raw.canonical_content` — so the SR domain type the fork shares is more Go-bound than the wire it consumes. ADR-0003's principle for agnostic-band code is 'name its abstractions for the problem class, not the instance' (docs/adr/0003:62-63); here the instance name was reintroduced at the exact boundary whose job is translation.",
   "currentState": "Verified by me: frontend/src/types.ts:1685 declares `readonly sgf: string` on ReviewCard; frontend/src/services/backend-service.ts:125 maps `sgf: raw.canonical_content`; frontend/src/types/backend.ts:1133 shows the wire field `canonical_content: string`. The distiller's additional claims — the post-34b comment documenting removal of the `normalized_sgf` compat shims, and the three domain-side consumer sites (useReviewSession.ts:251, useDirtyBoardGuard.ts:122, card-tree-echarts.ts:249) — are consistent with what I read at useReviewSession.ts:251 but otherwise not independently re-verified.",
   "proposal": "Compiler-driven rename of the domain field to `canonicalContent` (or `content`), leaving the Go interpretation (sgf.parse / loadSgf) at the consumers that parse it — already engine-band call sites. The fork then inherits an SR card type whose content field is an opaque string envelope, matching the wire. Explicitly out of scope: `numMoves`/`num_moves` — that vocabulary is on the wire itself, so generalizing it is a backend dispatch question (docs/dispatch/ convention), not a frontend rename.",
   "axis": "generalization",
   "evidence": "Single-lens finding, no miner origin; core three sites verified by me (types.ts:1685, backend-service.ts:125, types/backend.ts:1133). ADR-0003 principle block read in full. Not in the confirmed index (outbound-wire-mappers-acl-symmetry is about a missing outbound mapper, different direction and different field).",
   "cost": "Small mechanical rename across ~4 code sites plus the types.ts doc comment; vue-tsc makes missed sites compile errors. Grep docs/ for prose citing ReviewCard.sgf before landing.",
   "risks": "Pure-rename churn sits in tension with ADR-0003's 'existing code stays put' posture — the justification is Revisit #1 firing plus this being the frontend half of a rename the backend already executed, i.e. closing a recorded asymmetry rather than speculative tidiness. If the maintainer judges rename-only PRs not worth the history noise, the fallback is a types.ts comment naming the asymmetry and a `future` work-status item.",
   "likelyAlreadyFiled": "No (distiller DB probe for canonical_content/reviewcard; only refactoring-queue-adr0007 is adjacent, and it tracks file size, not vocabulary)."
  }
 ]
}
````

### §4 · gen-verify:multi-writer-slots-get-owners

**Commission** (= `[SHARED: GEN-DISCIPLINE]` + `[SHARED: FORK-CONSTRAINT]` + the following + `[SHARED: CANDIDATE-JSON <id>]`):

````
You are the GENERALIZATION verifier for one ALREADY-CONFIRMED candidate of the main review. Apply the constraint two-sidedly:
1. Set refuted=true ONLY if implementing the proposal would collapse/inline/specialize domain-agnostic infrastructure in a way that hinders the generic fork — name the seam and the mechanism. Simplification of Go-BOUND code, band-internal cleanup, and additions of validation/typing are NOT grounds for refutation.
2. If compatible but cheaply reshapeable to actively SERVE the fork (e.g. perform the proposed split along ADR-0003 band lines; extract a seam while already touching the file), describe that in reshape.
3. If fully orthogonal, say so (forkRelevance=orthogonal, empty reshape).
Read docs/adr/0003-frontend-portability-and-domain-boundaries.md end to end before judging any band question; check the actual files the candidate touches.
````

**Result (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "The proposal adds ownership structure; it collapses nothing. Tested against the constraint leg by leg: (1) Routing analysis-service.ts:463 through mutateBoard (store/index.ts:153) routes a Band-3 caller through the Band-1 named-mutator machinery ADR-0003 lines 117-120 names as generic — it strengthens the exact infrastructure the fork shares. (2) The blind-mode owner is a correctness fix inside useReviewSession, whose SR orchestration skeleton (ADR-0003 lines 149-152) is what the generic flash-card fork keeps; snapshot/restore of session-UI prefs is mechanism the fork inherits. (3) Extracting an engine-connection owner module is seam *creation*, not dissolution: today ~40 raw store.engine.* writes are smeared through a 1000+-line [B3] KataGo-bound service, which is precisely what makes the engine subtree expensive to excise or replace in a non-game fork; concentrating those writes behind one owner is ADR-0003's \"isolate it behind a clear interface so a [second-domain] version could replace it wholesale\" (lines 60-62). Named mutators for persisted-profile collections likewise extend the Band-1 mutator vocabulary. (4) The ESLint writer-enumeration rule mechanizes ownership discipline over store subtrees — domain-agnostic tooling the fork inherits wholesale, and per ADR-0003's stated negative consequence (\"the principle is policy, not mechanism\", lines 299-302) it converts review-time policy into mechanism. No leg inlines, specializes, or dissolves a domain-agnostic abstraction; the only specialization risk is the weaker variant of leg 3 (\"document analysis-service as owner\"), which still doesn't collapse anything — it just forgoes the seam the stronger variant builds. Refutation grounds absent.",
 "reshape": "Three cheap nudges, all within the proposal's existing scope: (1) In leg 3, prefer the extract-an-engine-connection-owner-module variant over \"document analysis-service as owner\", and name its mutators for the problem class (analysis-provider connection lifecycle: connect/disconnect-reset/info/selection/metrics) rather than KataGo specifics — the collapsed single reset lands there. That makes store.engine plus its owner a wholesale-replaceable unit per ADR-0003's isolate-behind-a-clear-interface clause; the fork (which may have no engine, or a different analysis provider) swaps or deletes one module instead of unpicking ~40 scattered writes from a [B3] service. (2) In leg 2, shape the blind-mode owner as \"snapshot/restore this list of session-UI pref keys on loadCard/finish/abort\" with showMoveSuggestions and treeExpanded as the supplied list — mechanism generic, key list domain-supplied; no Port, just code shape, exactly ADR-0003's authoring-time seam discipline. (3) In leg 4, make the ESLint rule data-driven: a config map { storeSubtreePath -> ownerFiles } plus a config-carved ADR-0001 template-toggle exception, with store.boards/store.engine/store.profile as today's map entries rather than literals in rule logic — ADR-0003 lines 117-120 already flag those field names as Go-flavored while the machinery is generic, so the fork edits a map, not a rule.",
 "forkRelevance": "advances"
}
````

### §4 · gen-verify:engine-subscription-union-narrowing

**Commission** (= `[SHARED: GEN-DISCIPLINE]` + `[SHARED: FORK-CONSTRAINT]` + the following + `[SHARED: CANDIDATE-JSON <id>]`):

````
You are the GENERALIZATION verifier for one ALREADY-CONFIRMED candidate of the main review. Apply the constraint two-sidedly:
1. Set refuted=true ONLY if implementing the proposal would collapse/inline/specialize domain-agnostic infrastructure in a way that hinders the generic fork — name the seam and the mechanism. Simplification of Go-BOUND code, band-internal cleanup, and additions of validation/typing are NOT grounds for refutation.
2. If compatible but cheaply reshapeable to actively SERVE the fork (e.g. perform the proposed split along ADR-0003 band lines; extract a seam while already touching the file), describe that in reshape.
3. If fully orthogonal, say so (forkRelevance=orthogonal, empty reshape).
Read docs/adr/0003-frontend-portability-and-domain-boundaries.md end to end before judging any band question; check the actual files the candidate touches.
````

**Result (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Both files the candidate touches are Band 3 under ADR-0003: engine/katago/* is named wholesale-replacement territory (docs/adr/0003-frontend-portability-and-domain-boundaries.md:160-167), and frontend/FILES.md tags katago-client.ts [B3] (line 258) and analysis-service.ts [B3] (line 272). Re-verified at HEAD: the union-erasing casts sit at analysis-service.ts:646 and :836, katago-client.ts:94-104 forwards KataErrorResponse packets to per-id subscribers unconditionally, and the union is types.ts:498-501. The proposal adds a discriminant-narrowing helper, a loud typed-error route, deletes the two casts, adds a unit test, and widens an existing audit item's description — it collapses, inlines, or specializes nothing domain-agnostic. The KataGoClient transport skeleton (subscriber map, per-id routing), the ResponseCallback union type, and the sendCommand action path are untouched, and the structured-error surface it would route into is Band 1 machinery the fork inherits. This is squarely the validation/typing-addition class the maintainer constraint excludes from refutation, and it strengthens ADR-0002 at a protocol boundary. Fork-side, the concrete code lives entirely in the directory a generic flash-card fork replaces, so as written it is near-orthogonal — but a cheap reshape inside the proposal's own stated latitude would make it an exemplar of the seam the fork needs, so needs-reshape rather than plain orthogonal.",
 "reshape": "Two shaping choices, both already within the proposal's stated options, make it actively serve the fork: (1) implement the loud error route via the ApiError-style typed channel / generic structured-error surface (Band 1 infrastructure per ADR-0003) rather than an engine-local throw, so the fail-loud path exercised and unit-tested here is exactly the machinery the fork keeps when it swaps the engine; (2) colocate the discriminant-narrowing guard with the union inside engine/katago/ (e.g. exported next to KataGoResponse in engine/katago/types.ts or in katago-client.ts) instead of an inline closure in analysis-service.ts, so the wire union never crosses the Band 3 directory boundary un-narrowed and the fork's replacement engine adapter has a one-file pattern to copy: wire union + narrowing guard live together, everything outside receives the narrowed type or a generic typed error. This is ADR-0003's design-the-seam-without-extracting-a-Port discipline (Decision section) applied now that the second consumer is concrete (Revisit-when #1); no new Port or generic type parameter is needed. Optionally, when amending the silent-coercion audit item, note that union-erasing casts at engine boundaries are precisely the shape that leaks Band 3 vocabulary into agnostic code, so the sweep doubles as fork-readiness hygiene.",
 "forkRelevance": "needs-reshape"
}
````

### §4 · gen-verify:migration-silent-noop-guard

**Commission** (= `[SHARED: GEN-DISCIPLINE]` + `[SHARED: FORK-CONSTRAINT]` + the following + `[SHARED: CANDIDATE-JSON <id>]`):

````
You are the GENERALIZATION verifier for one ALREADY-CONFIRMED candidate of the main review. Apply the constraint two-sidedly:
1. Set refuted=true ONLY if implementing the proposal would collapse/inline/specialize domain-agnostic infrastructure in a way that hinders the generic fork — name the seam and the mechanism. Simplification of Go-BOUND code, band-internal cleanup, and additions of validation/typing are NOT grounds for refutation.
2. If compatible but cheaply reshapeable to actively SERVE the fork (e.g. perform the proposed split along ADR-0003 band lines; extract a seam while already touching the file), describe that in reshape.
3. If fully orthogonal, say so (forkRelevance=orthogonal, empty reshape).
Read docs/adr/0003-frontend-portability-and-domain-boundaries.md end to end before judging any band question; check the actual files the candidate touches.
````

**Result (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "The candidate adds validation to domain-agnostic infrastructure; it collapses nothing. The migration framework — the migrate() walker (frontend/src/store/migrations.ts:190-214), the append-only ledger, the ADR-0002 failure contract (migrations.ts header lines 51-67), the rolling archive — is Band 1 per frontend/FILES.md:287 ('Schema-versioning framework (B1); the migrations themselves touch every band') and per ADR-0003's port sizing ('No change (Band 1): everything in store/'). ADR-0003's Related section states the fork-relevance directly: fail-loud discipline that lives in Band 1 'a Chess port inherits ... for free' — the same holds for a flash-card fork. The bug class guarded (a hand-walked blob path drifting from the real schema path, producing a silent no-op stamped as migrated — the 47→48 knobs walk) is domain-independent: the fork's migration authors will hand-walk their own blob paths against exactly this failure mode, so a post-migration leaf-assertion helper hardens substrate the fork ships with. The proposal also explicitly preserves the two framework properties the fork depends on — shipped bodies stay frozen (append-only invariant intact) and the cadence-relaxation idea is excluded (honest-version-marker property intact). The optional composition-level invariant test guards the generic walker+hydrate+save pipeline; I verified the suite does not currently have it (only the narrow tests/integration/hydration-knowntags.test.ts covers one hydration aspect), and the existing test file already separates generic harness helpers from Go-shaped fixtures (tests/unit/store/migrations.test.ts:38-93), so the test lands on the right side of the seam naturally. No domain-agnostic seam is inlined or specialized; refutation grounds are absent.",
 "reshape": "Author the assertion helper instance-free, per ADR-0003's 'name its abstractions for the problem class, not the instance': a generic pre-image/post-image leaf assertion (e.g. assertLeafMigrated(pre, post, path, expectation) throwing with structured detail) that knows nothing of Go field names — concrete paths like 'profile.settings.knobs' stay at call sites inside the instance-bound migration bodies. House it in the B1 framework portion of migrations.ts (or a tiny sibling module with a [B1] FILES.md entry), not inline per-body, so the fork inherits the guard mechanism rather than just the habit. For the composition-level invariant test, keep the harness (migrate + updateFromRemote + save + key-set compare) separable from the Go-shaped fixtures (ancientMinimalBlob et al.), mirroring the existing helper/fixture split in migrations.test.ts — the fork then swaps fixtures and keeps the harness verbatim.",
 "forkRelevance": "advances"
}
````

### §4 · gen-verify:branded-path-types

**Commission** (= `[SHARED: GEN-DISCIPLINE]` + `[SHARED: FORK-CONSTRAINT]` + the following + `[SHARED: CANDIDATE-JSON <id>]`):

````
You are the GENERALIZATION verifier for one ALREADY-CONFIRMED candidate of the main review. Apply the constraint two-sidedly:
1. Set refuted=true ONLY if implementing the proposal would collapse/inline/specialize domain-agnostic infrastructure in a way that hinders the generic fork — name the seam and the mechanism. Simplification of Go-BOUND code, band-internal cleanup, and additions of validation/typing are NOT grounds for refutation.
2. If compatible but cheaply reshapeable to actively SERVE the fork (e.g. perform the proposed split along ADR-0003 band lines; extract a seam while already touching the file), describe that in reshape.
3. If fully orthogonal, say so (forkRelevance=orthogonal, empty reshape).
Read docs/adr/0003-frontend-portability-and-domain-boundaries.md end to end before judging any band question; check the actual files the candidate touches.
````

**Result (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Not refuted. The proposal is additive typing plus a parameter-passing discipline inside game-tree-coupled code; it collapses no domain-agnostic seam. Verified against the actual surfaces: the two path producers are getActiveVariationPath (frontend/src/engine/util.ts:64, root→leaf via activeChildIndex) and getPath (frontend/src/engine/navigator.ts:11, root→target); the confusion class is real and documented at usePlayFromPosition.ts:223-236 (fix commit ec4cb3d, 'first move wrong, succeeding moves alright'). All 16 worklist files carry [B2]/[B3] tags in frontend/FILES.md (e.g. useVariationPath [B2] line 187, navigator.ts [B3] line 242, analysis-service.ts [B3] line 272); none of ADR-0003's Band 1 inventory (store machinery, sync-service, api-client, analysis-ledger, wait-for-analysis — ADR-0003 lines 105-131) is touched. Per ADR-0003's Band 2 definition (lines 133-139), variation-path machinery does not survive a port outside the game-tree class, so the generic flash-card fork drops this code rather than shares it — the brands neither burden nor build the shared substrate. Two elements actively help the fork rather than hinder it: (a) the 'functions acting at a position take it explicitly rather than reading global cursor state' rule reduces ambient GlobalStore coupling, which is precisely what makes later Band 1 extraction cheap; (b) brand-tightening here has precedent as cast-retiring band-internal cleanup (useVariationPath.ts:13-21; the surviving 'as NodeId[]' casts at analysis-service.ts:447,683 and useReviewSession.ts:350 are the kind the brands retire, and IDENTIFIERS.md's NodeId row already flags the 32-cast [leaky] erosion). One watch-point keeps this from being plainly 'advances': useReviewSession.ts is in the worklist, and per ADR-0003 (lines 149-152, 193-196) its SR orchestration skeleton is the part the flash-card fork literally reuses — threading branded tree-path types indiscriminately through its session-state-machine signatures would entangle the reusable skeleton with tree vocabulary. Cheaply avoidable; see reshape.",
 "reshape": "Three zero-extra-file adjustments while executing the same 16-file worklist. (1) Mint RootToLeafPath / RootToCurrentPath in the identifier vocabulary next to NodeId (src/types.ts, plus the IDENTIFIERS.md row frontend/CLAUDE.md requires for any new brand) rather than inside engine/util.ts or engine/navigator.ts — both sit in ADR-0003's Band 3 inventory (lines 164-181), and per the ADR's naming principle (lines 54-65) these are problem-class (tree) concepts that should stay separable from Go specifics. (2) Treat brand acquisition in a signature as a de-facto seam marker: in useReviewSession.ts / ReviewSessionPanel.vue specifically, confine the branded path types to the card-load and per-move-scoring leaves (e.g. around useReviewSession.ts:269, 298, 350) and keep the session-state-machine surface (AWAITING_MOVE/INTERMISSION/FINISHED flow) path-type-free — this performs ADR-0003's 'design the seam deliberately' (lines 193-196, 251-257) on the exact module the flash-card fork reuses, at the cost of placement discipline only. (3) Keep the explicit-position-parameter rule as written — it is already fork-serving (ambient-cursor decoupling is what makes extraction cheap) — and note in the todo-DB item that the audit doubles as a Band 2/3 coupling census for the files whose signatures acquire a brand, feeding the FILES.md band-retag discipline if any tag proves stale (useReviewSession is tagged [B3] in FILES.md line 229 but Band 2 in ADR-0003 lines 149-152 — the audit will surface which is right).",
 "forkRelevance": "needs-reshape"
}
````

### §4 · gen-verify:hydration-rebind-audit-and-barrier

**Commission** (= `[SHARED: GEN-DISCIPLINE]` + `[SHARED: FORK-CONSTRAINT]` + the following + `[SHARED: CANDIDATE-JSON <id>]`):

````
You are the GENERALIZATION verifier for one ALREADY-CONFIRMED candidate of the main review. Apply the constraint two-sidedly:
1. Set refuted=true ONLY if implementing the proposal would collapse/inline/specialize domain-agnostic infrastructure in a way that hinders the generic fork — name the seam and the mechanism. Simplification of Go-BOUND code, band-internal cleanup, and additions of validation/typing are NOT grounds for refutation.
2. If compatible but cheaply reshapeable to actively SERVE the fork (e.g. perform the proposed split along ADR-0003 band lines; extract a seam while already touching the file), describe that in reshape.
3. If fully orthogonal, say so (forkRelevance=orthogonal, empty reshape).
Read docs/adr/0003-frontend-portability-and-domain-boundaries.md end to end before judging any band question; check the actual files the candidate touches.
````

**Result (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "The proposal is additive discipline over Band 1 infrastructure, not a collapse of it. The audit's verification targets (reactive rebind per the postmortem's §7.1, registration in IDENTITY_SCOPED_CACHES at frontend/src/store/index.ts:530-538, or a new whenHydrated() barrier) all strengthen the registry-plus-callback shape ADR-0003 prescribes: generic machinery owns the lifecycle, domain-bound clears/rebinds register into it. Nothing is inlined, dissolved, or specialized — the proposal's three dispositions are each a generic mechanism that domain-bound state plugs into. The race class it closes (persistent-truth → ephemeral-substrate binding firing before hydrate completes; postmortem §4's substitution test) is fully domain-independent: a flash-card fork keeps SyncService hydration, auth bootstrap, profile persistence, and the tenancy reset verbatim (all [B1] per frontend/FILES.md — useAppBootstrap:175, sync-service:280, knobs.ts:313, useQeubo:148), and will carry its own module-scope caches derived from persisted truth subject to the identical latent race. Hardening that layer now, and naming the barrier idiom, is work the fork inherits for free — this is ADR-0002's fail-loud discipline landing in exactly the 60-70% no-change band ADR-0003's Chess-port sizing identifies (ADR-0003 lines 274-276). The profile-field classification (user-authored vs server-derived, per the knownTags precedent at store/index.ts:647-652) likewise clarifies the persistence contract the fork's profile schema reuses. whenHydrated() does not exist yet (grep-confirmed), so the proposal creates a seam rather than consuming one. No grounds for refutation; the candidate actively advances the fork.",
 "reshape": "Three cheap sharpenings while executing the audit: (1) House whenHydrated() in a Band 1 home (alongside sync-service/useAppBootstrap, not inside any consumer) and name it for the problem class — a hydration-lifecycle barrier over the persisted store — with domain-bound rebind callbacks passed in or registered, never imported by the barrier (the same parameterized shape ADR-0003 prescribes for the analysis-recording predicate and that IDENTITY_SCOPED_CACHES already exhibits). (2) Record each audited state-site's ADR-0003 band tag next to its rebind verdict, so the audit doubles as the fork's inventory of hydration-coupled state — which rebind contracts the fork inherits unchanged ([B1]: knobs claims, qeubo, query telemetry) versus replaces wholesale ([B3]: board/card thumbnails, analysis-ledger); this also surfaces known inventory drift (ADR-0003's 2026-04 text places analysis-ledger in Band 1, frontend/FILES.md:270 now tags it [B3]). (3) In the profile-field classification, add a third axis per field — domain-generic vs Go-instance — so the fork knows which defaultProfile slices and migrations survive the port. All three are recording/placement decisions made while already touching the files, not extra arcs.",
 "forkRelevance": "advances"
}
````

### §4 · gen-verify:cast-hygiene-lint-bare-as-any

**Commission** (= `[SHARED: GEN-DISCIPLINE]` + `[SHARED: FORK-CONSTRAINT]` + the following + `[SHARED: CANDIDATE-JSON <id>]`):

````
You are the GENERALIZATION verifier for one ALREADY-CONFIRMED candidate of the main review. Apply the constraint two-sidedly:
1. Set refuted=true ONLY if implementing the proposal would collapse/inline/specialize domain-agnostic infrastructure in a way that hinders the generic fork — name the seam and the mechanism. Simplification of Go-BOUND code, band-internal cleanup, and additions of validation/typing are NOT grounds for refutation.
2. If compatible but cheaply reshapeable to actively SERVE the fork (e.g. perform the proposed split along ADR-0003 band lines; extract a seam while already touching the file), describe that in reshape.
3. If fully orthogonal, say so (forkRelevance=orthogonal, empty reshape).
Read docs/adr/0003-frontend-portability-and-domain-boundaries.md end to end before judging any band question; check the actual files the candidate touches.
````

**Result (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "The proposal is an addition of lint tooling plus a justification discipline — it collapses, inlines, or specializes nothing. Per the constraint, additions of validation/typing are not grounds for refutation, so refuted=false. Beyond mere compatibility, it actively serves the fork on three grounds. (1) The lint host (frontend/eslint.config.js) is domain-agnostic contributor infrastructure the fork inherits wholesale; every existing rule there guards a Band 1 discipline (ACL import boundary, exhaustiveness, no-floating-promises), and this rule joins that set. (2) The bare-`as any` population (14 sites measured by grep on src/) concentrates at ADR-0003 Band 3 brand-bypass sites — sgf-loader.ts:76/77/89 constructing branded NodeId/parent/children ids without the asNodeId factory, katago-client.ts:133 erasing the query union — i.e., exactly the boundary the generic fork replaces wholesale (ADR-0003 \"What a Chess port would actually require\"). Making those casts loud makes the replacement boundary machine-visible. (3) The brand()/justifiedCast helper the escape hatch introduces is the fix frontend/IDENTIFIERS.md's \"Known erosions\" already prescribes (line 273: \"a re-brand helper at the call boundary, not scattered double-casts\" for the [leaky] NodeId proliferation and ACL brand-stripping) — brand integrity at the ACL is the fork's load-bearing type seam. One execution watch-out, named but not refuting: a burn-down must not dodge the lint by narrowing a generic signature to a Go concrete type (e.g. wait-for-analysis.ts's KataAnalysisResponse return on a fully generic abstraction, ADR-0003 Band 1 inventory) — the justify-and-keep escape hatch exists precisely so structurally-true casts stay annotated rather than getting \"fixed\" by specialization.",
 "reshape": "Two cheap reshapings while already touching these files: (1) Home the brand()/justifiedCast helper in a Band 1 location (src/lib/, tagged [B1] in FILES.md), generic over the existing Brand<K,T> utility at types.ts:84 — not in engine/ and not co-located with Go-bound types — so the fork inherits the escape hatch unchanged; this simultaneously discharges IDENTIFIERS.md erosion (b)'s standing call for a re-brand helper at the call boundary. (2) During the warn-baseline burn-down, have each justification comment name the cast's ADR-0003 band character where it sits on a boundary (e.g. \"Band 3 loader minting Band 1/2 branded ids — replaced wholesale in a non-Go fork\" at sgf-loader.ts:76); the justified-cast inventory then doubles as the seam map ADR-0003's \"design the seam, don't extract the Port\" discipline asks for, handed to the fork for free. Optionally, when measuring the baseline, also count the `as unknown as` double-cast brand-strip shape (useMinting.ts:50, useCardTreeData.ts:282 et al. per IDENTIFIERS.md) as a follow-on ratchet target, since ACL brand integrity is the fork's primary type seam — measured-first per the established a75814c adoption pattern.",
 "forkRelevance": "advances"
}
````

### §4 · gen-verify:typed-capability-metadata-mirror

**Commission** (= `[SHARED: GEN-DISCIPLINE]` + `[SHARED: FORK-CONSTRAINT]` + the following + `[SHARED: CANDIDATE-JSON <id>]`):

````
You are the GENERALIZATION verifier for one ALREADY-CONFIRMED candidate of the main review. Apply the constraint two-sidedly:
1. Set refuted=true ONLY if implementing the proposal would collapse/inline/specialize domain-agnostic infrastructure in a way that hinders the generic fork — name the seam and the mechanism. Simplification of Go-BOUND code, band-internal cleanup, and additions of validation/typing are NOT grounds for refutation.
2. If compatible but cheaply reshapeable to actively SERVE the fork (e.g. perform the proposed split along ADR-0003 band lines; extract a seam while already touching the file), describe that in reshape.
3. If fully orthogonal, say so (forkRelevance=orthogonal, empty reshape).
Read docs/adr/0003-frontend-portability-and-domain-boundaries.md end to end before judging any band question; check the actual files the candidate touches.
````

**Result (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Every surface the proposal touches sits inside the Go-bound band or its immediate display surface. ADR-0003's inventory places `engine/katago/*` squarely in Band 3 (\"KataGo's wire protocol — query shape, response shape, action verbs... the entire engine/katago/ directory gets replaced\", docs/adr/0003-frontend-portability-and-domain-boundaries.md, Band 3 section), and the three concrete sites confirm it: the mirror declaration at frontend/src/engine/katago/types.ts:276, the cast at frontend/src/engine/katago/capability-injection.ts:203, and the cast at frontend/src/components/editors/AnalysisControls.vue:43-51 (an engine-analysis control surface that exists only because KataGo does). The proposal collapses, inlines, or specializes no Band 1 or Band 2 infrastructure — it is an addition of typing and boundary validation within Band 3, which the constraint explicitly exempts from refutation. The generic flash-card fork is outside the game-tree class entirely (ADR-0003 names \"language flashcards\" as the example domain where there is no game tree), so the entire capability-metadata mirror is code the fork drops rather than reuses; the typed mirror neither advances nor hinders that. Two compatibility details worth noting, both already handled by the proposal's own wording: (a) the \"unknown extra keys passing through\" clause should also hold at the capability-name level — the union should narrow known capability names without closing the dict's domain, preserving the proxy's open dict-of-dicts contract for future capabilities; (b) deleting the AnalysisControls.vue:48 cast is best done by exposing a typed accessor from the validated boundary object rather than re-typing the component's raw-dict read, since the frontend layering rule says only the ACL sees wire shapes (snake_case `available_value_bindings` currently leaks into a component). Both are intra-proposal hygiene, not fork concerns — the fork discards this surface either way.",
 "reshape": "",
 "forkRelevance": "orthogonal"
}
````

### §4 · gen-verify:services-boundary-deny-by-default

**Commission** (= `[SHARED: GEN-DISCIPLINE]` + `[SHARED: FORK-CONSTRAINT]` + the following + `[SHARED: CANDIDATE-JSON <id>]`):

````
You are the GENERALIZATION verifier for one ALREADY-CONFIRMED candidate of the main review. Apply the constraint two-sidedly:
1. Set refuted=true ONLY if implementing the proposal would collapse/inline/specialize domain-agnostic infrastructure in a way that hinders the generic fork — name the seam and the mechanism. Simplification of Go-BOUND code, band-internal cleanup, and additions of validation/typing are NOT grounds for refutation.
2. If compatible but cheaply reshapeable to actively SERVE the fork (e.g. perform the proposed split along ADR-0003 band lines; extract a seam while already touching the file), describe that in reshape.
3. If fully orthogonal, say so (forkRelevance=orthogonal, empty reshape).
Read docs/adr/0003-frontend-portability-and-domain-boundaries.md end to end before judging any band question; check the actual files the candidate touches.
````

**Result (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Read end to end: docs/adr/0003-frontend-portability-and-domain-boundaries.md, docs/adr/0010-render-locality-and-canvas-for-data-dense-visuals.md, frontend/eslint.config.js, frontend/CLAUDE.md; spot-checked frontend/FILES.md rows (sanctioned lookup) and src/services/analysis-bundle.ts header.\n\nNot refuted — the proposal moves in the opposite direction from the failure mode the constraint forbids. Nothing is collapsed, inlined, or specialized; the change strengthens domain-agnostic enforcement infrastructure (the lint host eslint.config.js, itself Band-1-shaped machinery) and adds a work-status item.\n\nThe fork-relevant observation is that the CURRENT shape is the instance-coupled one: EFFECTFUL_SERVICE_PATTERN (eslint.config.js:166-179) enumerates four concrete Go-app service filenames (backend-service, library-service, analysis-service, analysis-persistence-service). A generic flash-card fork replaces that service set wholesale — and every replacement service silently falls outside the enumerated blocklist, so the layering boundary loses its teeth at exactly the moment the fork authors new effectful code. This is not hypothetical: it has already happened within the current domain — AnalysisControls.vue imports services/analysis-bundle (verified by grep this session), a module neither blocked nor exempted, invisible to the blocklist. Deny-by-default names the problem class (the services band) instead of the instance, which is ADR-0003's own principle (lines 62-65: \"name its abstractions for the problem class, not the instance\") applied to the boundary mechanism. The rule then survives the fork's service-set replacement with zero edits.\n\nStep (b) — relocating analysis-ledger / analysis-config / stability-trajectory-store to a state/ band — is the resolution path ADR-0010 Revisit #4 (lines 209-227) already names (\"relocating reactive-state modules out of services/\"), and it dissolves the instance-named exemption list into a directory-structural rule, completing the same instance→class move. Per the binding constraint clause (c), the fork is the second concrete consumer that makes this band-naming non-premature; the proposal correctly gates it behind sign-off and the ADR-0003 amendment path (FILES.md re-tags, doc-graph regeneration), so ADR-0003's authoring-time judgment is respected rather than bypassed.\n\nResidual risk to the fork: only the ordinary friction of new exemption adjudications under deny-by-default, which the measure-first step (the a75814c pattern, verified to exist) is designed to absorb before error severity. No seam the fork needs is dissolved; one the fork needs (the services-band boundary) is hardened, and another (the reactive-state band) is given a structural home.",
 "reshape": "Three cheap sharpenings while already touching these files: (1) In step (a), encode the exemption as one named constant (e.g. REACTIVE_STATE_EXEMPTIONS) whose comment names it as the provisional form of the future state/ band — step (b) then reduces to deleting the constant and switching the rule to a pure directory glob, and the seam is visible in the config before the relocation lands. (2) When the measure-first sweep adjudicates AnalysisControls' analysis-bundle import, classify the module by band rather than exempting ad hoc — its header (src/services/analysis-bundle.ts:1-19) declares it a pure, effect-free projection between the ledger and the persistence wire shape, so where it falls (state/-adjacent pure projection vs. effectful service) is itself a data point for where the state/ band boundary sits in the fork. (3) In step (b)'s relocation, record the machinery-vs-payload seam in the moved files' headers and FILES.md entries: analysis-ledger is tagged [B3] in FILES.md for its KataAnalysisResponse payload, but ADR-0003's inventory (lines 121-125) describes its machinery — \"a versioned reactive cache of expensive computed values keyed by config and node identity\" — as Band 1 and payload-independent. Naming that split at relocation time is the ADR-0003 \"design the seam deliberately\" move the flash-card fork consumes directly (swap payload, keep machinery). Also file the Revisit #4 work-status item with a cross-reference to ADR-0003 Revisit #1 (second-domain-adopter trigger), since the fork is what upgrades this from documented tension to actionable work.",
 "forkRelevance": "advances"
}
````

### §4 · gen-verify:keyed-cache-brand-at-construction

**Commission** (= `[SHARED: GEN-DISCIPLINE]` + `[SHARED: FORK-CONSTRAINT]` + the following + `[SHARED: CANDIDATE-JSON <id>]`):

````
You are the GENERALIZATION verifier for one ALREADY-CONFIRMED candidate of the main review. Apply the constraint two-sidedly:
1. Set refuted=true ONLY if implementing the proposal would collapse/inline/specialize domain-agnostic infrastructure in a way that hinders the generic fork — name the seam and the mechanism. Simplification of Go-BOUND code, band-internal cleanup, and additions of validation/typing are NOT grounds for refutation.
2. If compatible but cheaply reshapeable to actively SERVE the fork (e.g. perform the proposed split along ADR-0003 band lines; extract a seam while already touching the file), describe that in reshape.
3. If fully orthogonal, say so (forkRelevance=orthogonal, empty reshape).
Read docs/adr/0003-frontend-portability-and-domain-boundaries.md end to end before judging any band question; check the actual files the candidate touches.
````

**Result (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "The proposal is purely additive discipline — a CLAUDE.md/pre-merge-checklist rule, branded-key typing at construction, a bounded audit, and an optional lint. Nothing in it collapses, inlines, or specializes domain-agnostic infrastructure; typing/validation additions are explicitly outside refutation grounds. Checked against the actual artifacts: (1) The Brand machinery (`frontend/src/types.ts:84`, `RawKey`/`EnrichedKey` at :132-133) is domain-neutral and runtime-erased; the 2026-06-08 arc (`docs/worklog/2026-06-08-key-brand-tightening.md`) added brands across both agnostic ids (QueryId, CardTreeExpandKey) and Go-bound ones (ExtractorId) without specializing any seam. (2) The r:/e: audit target (`frontend/src/services/analysis-bundle.ts:176-285`) confirms single mint+parse boundary (mint :226/:231, parse :259-274; grep finds no other site in src/ or tests/). The promotion fallback (\"typed/versioned record, the v1/v2 bundle-union precedent\") would strengthen, not collapse, the generic envelope ADR-0003 prescribes for exactly this surface (ADR-0003:235-239: opaque `{configHash, nodeId, payload}` a Chess/flash-card fork reuses with no schema change) — moving the discriminator from string-prefix sniffing into a typed record shape is the discriminated-union idiom frontend/CLAUDE.md's \"Type-driven design\" section already mandates. (3) The rule itself mechanizes ADR-0003's authoring-time question (\"design discipline at authoring time\", ADR-0003:47-69) for one recurring shape: a key declaration that names the value's dependency set is the \"honest separation between the abstraction and the instance\" test applied to cache keys. For the fork specifically, this advances the work: the keyed-cache pattern is Band 1 machinery (ADR-0003:121-125) that the flash-card fork inherits, while today's concrete keys carry domain-bound legs (KataGo config hashes in RawKey/EnrichedKey, ExtractorId — files tagged [B3] in frontend/FILES.md:270,279). Dependency-set-naming at declaration makes the fork's cache-key swap surface enumerable from `src/types.ts` + IDENTIFIERS.md rather than reverse-engineered from key-construction call sites. The rule names the problem class (keyed caches), not the instance, per ADR-0003:62-63. One caveat to carry forward: the prefix-to-typed-record promotion, if triggered, touches the backend-gated `schema_version` literal (analysis-bundle.ts:184-186), so it is a dispatch-mediated cross-boundary change, not a frontend-only edit.",
 "reshape": "When codifying the rule in frontend/CLAUDE.md (natural home: the \"Type-driven design\" or \"Identifier map\" sections) and the checklist, state the dependency-set declaration in ADR-0003 band terms: each leg of a new cache key gets a one-word coupling call — domain-bound (Go config hash, extractor id) vs agnostic (NodeId, BoardId) — recorded in the key's required IDENTIFIERS.md row (the doc already carries origin-class and soundness columns; this is one notes-line convention, not a new doc). That makes the fork's cache-key swap inventory grep-able at near-zero cost. Second, if the bounded audit does trigger the typed/versioned-record promotion for r:/e:, perform the split along the seam analysis-bundle.ts's own header already names (lines 6-12): keep the envelope `{kind, nodeId, payload}` opaque per ADR-0003:235-239 and confine the KataGo-shaped `ENRICHMENT_PLACEHOLDER_RAW` (analysis-bundle.ts:197-203) behind the record-kind discriminator, so the fork swaps payload types without touching record routing — and route the schema_version implication through a backend dispatch per the existing cross-boundary protocol.",
 "forkRelevance": "advances"
}
````

### §4 · gen-verify:work-status-authoring-hygiene

**Commission** (= `[SHARED: GEN-DISCIPLINE]` + `[SHARED: FORK-CONSTRAINT]` + the following + `[SHARED: CANDIDATE-JSON <id>]`):

````
You are the GENERALIZATION verifier for one ALREADY-CONFIRMED candidate of the main review. Apply the constraint two-sidedly:
1. Set refuted=true ONLY if implementing the proposal would collapse/inline/specialize domain-agnostic infrastructure in a way that hinders the generic fork — name the seam and the mechanism. Simplification of Go-BOUND code, band-internal cleanup, and additions of validation/typing are NOT grounds for refutation.
2. If compatible but cheaply reshapeable to actively SERVE the fork (e.g. perform the proposed split along ADR-0003 band lines; extract a seam while already touching the file), describe that in reshape.
3. If fully orthogonal, say so (forkRelevance=orthogonal, empty reshape).
Read docs/adr/0003-frontend-portability-and-domain-boundaries.md end to end before judging any band question; check the actual files the candidate touches.
````

**Result (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "The proposal is process/auditability hygiene, not code restructuring. Verified surfaces: all five named items exist open in the todo DB (pv-hover-jank-range-query, many-boards-open-slowness, spa-board-scope-consistency-audit, nav-during-range-query-perf, i18n-string-sweep — all scope=frontend, state=open); the two perf items indeed carry zero refs rows; the audit doc exists at docs/notes/audit/perf-audit-nav-and-pv-hover-2026-05-27.md. The proposal's writes are purely additive against existing schema seams: refs rows use the existing refs table whose kind vocabulary already covers 'design-note'/'worklog' (tools/work-status/schema.sql:83), and design-note refs into docs/notes/audit/ are an established pattern (spa-board-scope-consistency-audit already has one). No frontend module is touched, so no Band 1/2 infrastructure (per docs/adr/0003-frontend-portability-and-domain-boundaries.md, read end to end) can be collapsed, inlined, or specialized by it. The work-status store itself is umbrella-level domain-agnostic tooling and its schema is left intact. The ADR-0009 re-profile-before-lever step and the maintainer-question escalation for spa-board-scope-consistency-audit are both measurement/process discipline, not abstraction dissolution. One latent risk, addressable in the reshape rather than grounds for refutation: the drop-with-dated-record triage bucket could, if applied with a Go-only mindset, drop deferrals whose rationale was generalization/seam-design — exactly the items the fork (ADR-0003's 'second domain adopter materializes' trigger, Revisit-when item 1) revalues. The proposal as written doesn't mandate such drops and already has an escalation channel, so this is a rubric addition, not a refutation.",
 "reshape": "Two same-pass additions, both cheap because the harvest/retitle pass already touches every retained frontend item: (1) Band-annotate each open frontend item with its ADR-0003 band (1 agnostic / 2 game-tree / 3 Go-bound) while triaging — the items.extra jsonb column (schema.sql:59) takes this with zero DDL since the labels enum is closed (schema.sql:90-93). This makes fork planning a SQL query: open debt the fork inherits (Band 1/2) vs debt moot for a non-board domain (Band 3, e.g. spa-board-scope-consistency-audit residuals, whose existing refs all point at board-scope composables). The mandated ADR-0009 re-profiling should record band attribution as part of its output — whether many-boards-open-slowness localizes to Band 1 store machinery or Band 3 canvas code determines whether the fork inherits the defect. (2) Add one fork-aware guard to the drop-with-dated-record triage rubric: a deferral whose stated rationale is generalization or seam-design is never dropped on 'single-domain future' grounds — the fork is the second consumer that flips ADR-0003's extraction calculus — and instead routes through the proposal's existing maintainer-question channel.",
 "forkRelevance": "needs-reshape"
}
````

### §4 · gen-verify:adr-trigger-sweep-and-record-hygiene

**Commission** (= `[SHARED: GEN-DISCIPLINE]` + `[SHARED: FORK-CONSTRAINT]` + the following + `[SHARED: CANDIDATE-JSON <id>]`):

````
You are the GENERALIZATION verifier for one ALREADY-CONFIRMED candidate of the main review. Apply the constraint two-sidedly:
1. Set refuted=true ONLY if implementing the proposal would collapse/inline/specialize domain-agnostic infrastructure in a way that hinders the generic fork — name the seam and the mechanism. Simplification of Go-BOUND code, band-internal cleanup, and additions of validation/typing are NOT grounds for refutation.
2. If compatible but cheaply reshapeable to actively SERVE the fork (e.g. perform the proposed split along ADR-0003 band lines; extract a seam while already touching the file), describe that in reshape.
3. If fully orthogonal, say so (forkRelevance=orthogonal, empty reshape).
Read docs/adr/0003-frontend-portability-and-domain-boundaries.md end to end before judging any band question; check the actual files the candidate touches.
````

**Result (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "All four proposed edits are documentation/process changes; none touches code, so no domain-agnostic infrastructure is collapsed, inlined, or specialized — refutation does not apply. The candidate's factual basis checks out: ADR-0010 really ends with two committed harness-envelope lines (docs/adr/0010-render-locality-and-canvas-for-data-dense-visuals.md:266-267, `</content>`/`</invoke>`); ADR-0001:99-107 really misdescribes mutateBoard (it lists the `store.boards[index] = { ...board }` re-wrap as a benefit, but frontend/src/store/index.ts:157-172 documents that identity swap was deliberately removed in the 2026-05-28 perf arc — the same evidence that ADR-0001 Revisit trigger #2, lines 241-246, fired); and ADR-0003's inventory has drifted (7 of 20 spot-checked paths no longer exist at their stated locations, e.g. composables/wait-for-analysis.ts, useReviewSession.ts, components/BoardDisplay.vue), while frontend/FILES.md carries 215 maintained band tags with a same-PR update discipline (frontend/CLAUDE.md 'File map' section). Delegating the per-file inventory to FILES.md is consolidation toward the surface that already has a freshness discipline — it makes the band map a fork planner reads MORE accurate, not less. Two fork-relevant gaps keep this from 'advances' as written: (1) the stated scope ('keeping band definitions and the principle') would, read literally, cut ADR-0003's 'Band-mixed — the seams' section (lines 184-204) and the port-sizing sketch (lines 261-282) along with the inventory, since those are neither definitions nor principle — and FILES.md's one-line dominant-concern tags (FILES.md:35-36) structurally cannot carry within-file seam analysis ('the navigator's skeleton is portable; its delta-application is not'; useReviewSession's orchestration vs scoring extraction). That prose is precisely the extraction map the generic fork needs. (2) The proposal records two silently-fired triggers while a third has also fired: ADR-0003 Revisit #1 (lines 317-321, 'A second domain adopter materializes') is now true per the binding maintainer constraint — and the fork being a NON-game domain means the load-bearing boundary shifts to Band 1 vs Bands 2+3 (ADR-0003:136-140 states Band 2 does not survive a port to language flashcards; Band 1's definition at lines 105-111 already names 'language flashcards, music theory drills'). Amending ADR-0003 for trigger #2 while leaving trigger #1 unrecorded would perpetuate exactly the silent-fired-trigger condition the candidate's own auditability axis targets.",
 "reshape": "While already amending ADR-0003 (edit 2): (a) record Revisit trigger #1 as fired by the generic knowledge flash-card fork, with the consequence the ADR itself states (lines 317-321: the second consumer flips the extraction cost-benefit; the documented seams become the natural extraction points), and note that because this consumer is outside the game-tree class, the operative boundary is Band 1 vs Bands 2+3 (per lines 136-140) and the authoring question gains a non-game variant ('what would change for a non-game knowledge domain?') alongside the Chess one; (b) scope the inventory delegation to the per-file band LISTING only — retain, with refreshed paths, the 'Band-mixed — the seams' analysis (lines 184-204) and the port-sizing sketch (lines 261-282), because FILES.md's one-line dominant-concern tags cannot carry within-file seam detail and that prose is the fork's extraction map; (c) in edit 4, have the folded trigger-sweep checklist name FILES.md band-tag freshness as the standing replacement for ADR-0003's prose-inventory refresh (trigger #2), so the band map the fork planner reads stays current by cadence rather than by accident.",
 "forkRelevance": "needs-reshape"
}
````

### §4 · gen-verify:outbound-wire-mappers-acl-symmetry

**Commission** (= `[SHARED: GEN-DISCIPLINE]` + `[SHARED: FORK-CONSTRAINT]` + the following + `[SHARED: CANDIDATE-JSON <id>]`):

````
You are the GENERALIZATION verifier for one ALREADY-CONFIRMED candidate of the main review. Apply the constraint two-sidedly:
1. Set refuted=true ONLY if implementing the proposal would collapse/inline/specialize domain-agnostic infrastructure in a way that hinders the generic fork — name the seam and the mechanism. Simplification of Go-BOUND code, band-internal cleanup, and additions of validation/typing are NOT grounds for refutation.
2. If compatible but cheaply reshapeable to actively SERVE the fork (e.g. perform the proposed split along ADR-0003 band lines; extract a seam while already touching the file), describe that in reshape.
3. If fully orthogonal, say so (forkRelevance=orthogonal, empty reshape).
Read docs/adr/0003-frontend-portability-and-domain-boundaries.md end to end before judging any band question; check the actual files the candidate touches.
````

**Result (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "The proposal is purely additive at the ACL: an outbound mapper family symmetric to mapToReviewCard, a typed GradingParameterData with an explicit open-keyed escape field, and a wire-schemas.md section. Nothing domain-agnostic is collapsed, inlined, or specialized, so there is no refutation ground. It in fact advances the fork on three counts. (1) It moves snake_case wire construction out of a Band-3 composable (useMinting.ts is tagged [B3] in frontend/FILES.md:228 — \"Go-board → backend mint payload\"; it currently hand-builds the wire shape at useMinting.ts:108-154) into the [B2] ACL (backend-service.ts, FILES.md:274), restoring the layering rule frontend/CLAUDE.md states (\"The ACL is the only place wire shapes appear; no other module sees snake_case\") that types.ts:2115-2128 documents as currently broken for the create flow. For a generic flash-card fork, the SR card-CRUD ACL is shared infrastructure (the backend was already domain-generalized per ADR-0003's Context, items 30a/30b/34) while useMinting is a swap-surface; concentrating wire knowledge in the surviving layer lowers fork cost. (2) The typing is construction-side only — the wire stays the opaque `{[key:string]: unknown} | null` blob the backend doesn't introspect (backend-service.ts:49-65), and the contract \"backend reads exactly gamma; every other key is frontend-defined pass-through\" (types.ts:1725-1729) is preserved by the proposed escape field. That is exactly ADR-0003's generic-envelope prescription (ADR-0003:235-239), kept intact, not specialized. (3) The interface is not speculative: a second outbound site for the same blob already exists at backend-service.ts:199-201 (updateCardMetadata projects CardMetadataPatch.gradingParameterData, currently Readonly<Record<string, unknown>> at types.ts:1740), so GradingParameterData has two concrete consumers today. One caveat: the proposal's alternative branch (sanction the outbound exception via a frontend/CLAUDE.md amendment, no mapper) is fork-neutral at best — it leaves wire construction inside the B3 composable; the mapper branch is the fork-serving choice and the reshape below assumes it.",
 "reshape": "When defining GradingParameterData, cut it along the ADR-0003 seam instead of declaring the four known keys flat: `gamma` (SR-generic, the one backend-read key) sits directly on the envelope; the engine-instance keys (`analysis_config`, `overrideSettings`, `default_visits` — all KataGo-bound content compiled by compileAnalysisConfig/compileEngineOverrides) group under a named domain-payload sub-type whose declaration carries the band attribution, with the open-keyed escape on the envelope. The ACL's outbound mapper stays generic over the sub-shape — it forwards, never introspects — mirroring ADR-0003:225-239's analysis-recording prescription (generic envelope, opaque payload). Two cheap unifications while the files are open: (a) have CardMetadataPatch.gradingParameterData (types.ts:1740) reference the same type so both outbound sites (createCard, updateCardMetadata at backend-service.ts:199-201) share one declaration; (b) in the new wire-schemas.md gradingParameter.data section, record the split as the section's lifecycle line — backend reads `data.gamma` only, all other keys frontend-domain-owned — which becomes the fork's documented swap-contract. This is the now-justified seam-naming the second-consumer trigger (ADR-0003 \"Revisit when…\" #1) anticipates, at near-zero marginal cost since the proposal already touches every one of these files.",
 "forkRelevance": "advances"
}
````

### §4 · gen-verify:vue-lifecycle-footgun-guards

**Commission** (= `[SHARED: GEN-DISCIPLINE]` + `[SHARED: FORK-CONSTRAINT]` + the following + `[SHARED: CANDIDATE-JSON <id>]`):

````
You are the GENERALIZATION verifier for one ALREADY-CONFIRMED candidate of the main review. Apply the constraint two-sidedly:
1. Set refuted=true ONLY if implementing the proposal would collapse/inline/specialize domain-agnostic infrastructure in a way that hinders the generic fork — name the seam and the mechanism. Simplification of Go-BOUND code, band-internal cleanup, and additions of validation/typing are NOT grounds for refutation.
2. If compatible but cheaply reshapeable to actively SERVE the fork (e.g. perform the proposed split along ADR-0003 band lines; extract a seam while already touching the file), describe that in reshape.
3. If fully orthogonal, say so (forkRelevance=orthogonal, empty reshape).
Read docs/adr/0003-frontend-portability-and-domain-boundaries.md end to end before judging any band question; check the actual files the candidate touches.
````

**Result (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "The proposal is purely additive guard infrastructure: (1) a lint/test convention requiring explicit defaults on boolean gate-named props, (2) a lint forbidding module-intent mutable state inside <script setup>, (3) a doc consolidation into frontend/CLAUDE.md. Nothing is collapsed, inlined, or specialized — the constraint's refutation clause does not engage, and additions of validation are explicitly exempted. Both footguns are framework-level (Vue's omitted-boolean-prop → false cast, documented in docs/worklog/2026-06-08-basechart-active-prop-default.md:28-34/97-103; the SFC compiler's per-instance scoping of <script setup> top-level state, documented in docs/worklog/2026-06-09-mini-board-texture-scope-fix.md:12-21), not domain-level, so the guards apply uniformly across ADR-0003 bands — BaseChart.vue is Band 2 'Generic charting machinery; no domain' (docs/adr/0003-frontend-portability-and-domain-boundaries.md:153-154), MiniBoardCanvas is Band 3. The fork, being another Vue SPA sharing the agnostic substrate, is exposed to both footguns identically; mechanizing the lessons out of domain-entangled worklogs into frontend/eslint-rules/ (precedent: clear-needs-ownership.js, CI-gated per frontend/CLAUDE.md:462-466) and a checked CLAUDE.md section produces portable, mechanically-enforced infrastructure the fork inherits wholesale. Net: the candidate advances the generic fork.",
 "reshape": "Three authoring-time framing choices, all within the proposed scope: (a) key the gate-prop lint on name patterns (active/enabled/visible) only — never an allowlist enumerating today's Go-bound component names — so the rule transfers verbatim to the fork; (b) implement the omitted-prop rendering test as a reusable test helper under tests/ rather than ad hoc per-component assertions, so the fork's new shared components apply the same guard cheaply; (c) when the <script setup> lint forces extraction of module-intent state to a separate module file, have the convention cite ADR-0003's 'name abstractions for the problem class, not the instance' rule — e.g. the MiniBoardCanvas wood-texture/stone-sprite cache extracts as a generic render-resource cache module (a Band 1 seam), aligning with the already-filed thumbnail-render-lifecycle-consolidation follow-up rather than re-baking Go-shaped inline state.",
 "forkRelevance": "advances"
}
````

### §4 · gen-verify:scoped-state-registry-by-default

**Commission** (= `[SHARED: GEN-DISCIPLINE]` + `[SHARED: FORK-CONSTRAINT]` + the following + `[SHARED: CANDIDATE-JSON <id>]`):

````
You are the GENERALIZATION verifier for one ALREADY-CONFIRMED candidate of the main review. Apply the constraint two-sidedly:
1. Set refuted=true ONLY if implementing the proposal would collapse/inline/specialize domain-agnostic infrastructure in a way that hinders the generic fork — name the seam and the mechanism. Simplification of Go-BOUND code, band-internal cleanup, and additions of validation/typing are NOT grounds for refutation.
2. If compatible but cheaply reshapeable to actively SERVE the fork (e.g. perform the proposed split along ADR-0003 band lines; extract a seam while already touching the file), describe that in reshape.
3. If fully orthogonal, say so (forkRelevance=orthogonal, empty reshape).
Read docs/adr/0003-frontend-portability-and-domain-boundaries.md end to end before judging any band question; check the actual files the candidate touches.
````

**Result (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "The proposal generalizes infrastructure rather than collapsing it, so the refutation test fails on every limb. The machinery it extends — BOARD_SCOPED_STORE_CELLS (frontend/src/store/index.ts:309-314) and IDENTITY_SCOPED_CACHES (:530-538) — is Band 1 by ADR-0003's explicit inventory (\"store/index.ts and the GlobalStore machinery... the machinery is generic\", ADR-0003 'Band 1' section), and a phase-ordered drain registry stays domain-agnostic: a flash-card fork has the same problem class (per-entity scoped state needing quiesce-before-purge, child-walking purges before removal, pending-wait aborts).\n\nBeyond mere compatibility, the proposal addresses a real fork obstacle. Today the Band 1 store imports and hand-calls Band 2/3 modules to do teardown (store/index.ts:34-41: analysis-service, useCardThumbnail, useThumbnailCache, board-card-trees, useReviewSession; the file header at :7-12 acknowledges this coupling). A fork that wholesale-replaces those domain modules must edit closeBoard's inline sequence (:427-469). Register-at-birth inverts the dependency: replaced domain modules carry their own {phase, clear} registration and the generic drain never changes. This is exactly the seam-design ADR-0003's 'Revisit when' #1 says becomes justified once 'a second domain adopter materializes' — which the binding constraint states is now the case.\n\nTwo cautions, neither refuting: (1) the proposal overrides a documented prior judgment (board-scope.md 'Two teardown classes'; store/index.ts:290-303, audit P1b) that the registry is bounded to Class A on purpose — but the original objection was that ordering would move into array position, and the explicit phase field answers that objection directly; the implementation must update board-scope.md and the registry docstring in the same change or the doc graph rots (ADR-0005). (2) The proposed phase names mix instance vocabulary ('stop-engine') into Band 1 machinery — fixable, see reshape. The docstring fix at :331 and the work-status filing are orthogonal hygiene with no fork bearing.",
 "reshape": "Three cheap adjustments make the proposal actively serve the fork rather than merely permit it. (1) Name the phases for the problem class, not the instance (ADR-0003's naming principle for agnostic modules): 'quiesce-producers' / 'purge-derived' / 'abort-pending' / 'pre-remove' instead of 'stop-engine' / 'pre-splice' — a flash-card fork has no engine and no board splice, but it has producers to quiesce and entity-derived caches to walk before removal. (2) Put the registration calls in the owning modules (analysis-service registers its own quiesce entry; the thumbnail cache its own purge), so frontend/src/store/index.ts sheds its Band 2/3 imports (:34-41) — that import-edge deletion is the concrete decoupling the fork needs, and 'registers at birth' naturally means registration at the owner, not a grown array literal in the store. The known cost — import-time registration order and side effects — is bounded by keeping the existing label-tripwire completeness-test pattern (boardScopedStoreCellLabels, :321-323), which the fork then inherits as its teardown guarantee. (3) When overriding the P1b bounded-registry judgment, revise board-scope.md's 'Two teardown classes' section and the registry docstring (:290-303) in the same PR, recording that the second consumer (the fork) plus the named-phase field is what changed the calculus — so the doc graph stays truthful per ADR-0005.",
 "forkRelevance": "advances"
}
````

### §4 · gen-verify:split-types-ts-along-banner-seams

**Commission** (= `[SHARED: GEN-DISCIPLINE]` + `[SHARED: FORK-CONSTRAINT]` + the following + `[SHARED: CANDIDATE-JSON <id>]`):

````
You are the GENERALIZATION verifier for one ALREADY-CONFIRMED candidate of the main review. Apply the constraint two-sidedly:
1. Set refuted=true ONLY if implementing the proposal would collapse/inline/specialize domain-agnostic infrastructure in a way that hinders the generic fork — name the seam and the mechanism. Simplification of Go-BOUND code, band-internal cleanup, and additions of validation/typing are NOT grounds for refutation.
2. If compatible but cheaply reshapeable to actively SERVE the fork (e.g. perform the proposed split along ADR-0003 band lines; extract a seam while already touching the file), describe that in reshape.
3. If fully orthogonal, say so (forkRelevance=orthogonal, empty reshape).
Read docs/adr/0003-frontend-portability-and-domain-boundaries.md end to end before judging any band question; check the actual files the candidate touches.
````

**Result (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Read docs/adr/0003-frontend-portability-and-domain-boundaries.md end to end, plus frontend/CLAUDE.md (provided in full), the banner structure and key sections of frontend/src/types.ts, and the types.ts row of FILES.md (sanctioned lookup). The proposal is additive modularization — a split along existing banners with a barrel re-export from types.ts — so no domain-agnostic abstraction is collapsed, inlined, or specialized; import sites stay stable and every seam survives. Refutation grounds are absent.\n\nMore than compatible, it advances the fork. types.ts currently smears Band 1 substrate together with Band 3 Go types in one 2362-line file: the knob registry is explicitly \"substrate-level\"/\"widget-agnostic\" (types.ts:472-540), the qEUBO calibration types (types.ts:805-853) and the Brand<> utility (types.ts:84) are domain-agnostic, while Point/Move/NodeDelta/GameMetadata (types.ts:225-253), StoneColor (types.ts:223), BoardState/GameNode (types.ts:255-277), and the KataGo wire re-exports (types.ts:48-55) are Go-bound. FILES.md:46 already tags the file \"[B2] ... Carries Move/StoneColor (B3 leakage)\". A per-banner split converts the fork's hardest types.ts question — which declarations the generic fork keeps versus replaces — into module boundaries, i.e., a take-vs-replace manifest. ADR-0003 excluded types.ts from its band inventory on the grounds that classifying its declarations \"misses the point\" (ADR-0003:206-215), but that was premised on no second adopter; Revisit trigger 1 (ADR-0003:317-321) is now live under the binding fork constraint, so band-classifying the resulting type modules is exactly the inventory refresh the ADR anticipates. The proposal's process posture (named deviation from the handled-on-touch policy, doc-graph regeneration, FILES.md/IDENTIFIERS.md updates in the same PR) is a maintainer-approval question, not a generalization hazard; shrinking IDENTIFIERS.md retires a drifting lookup doc, not runtime infrastructure.\n\nOne caution, handled in the reshape: the banner seams are not all band seams. The \"Type Branding Utilities\" banner (types.ts:83-223) mixes the generic Brand<> helper and agnostic identity brands with game-coupled brands (ColorMoveIndex/PlyIndex, types.ts:220-221; StoneColor, types.ts:223), and the value-object banners are organized by ADR-0001 readonly policy, not domain. A purely banner-faithful carve would leave Band 3 residue inside otherwise Band 1 modules — not a regression versus the status quo, but a missed opportunity.",
 "reshape": "Perform the carve band-aware, not merely banner-aware, since the files are being touched anyway: (1) keep types/ids.ts to the Brand<> utility, PerBoard, and domain-agnostic identity brands; route the game-coupled brands (StoneColor, ColorMoveIndex, PlyIndex) into the same Go-bound value-object module as Point/Move/NodeDelta/GameMetadata, so the fork replaces exactly one module wholesale instead of editing ids.ts; (2) give each new types/ module its own ADR-0003 band tag in FILES.md, dissolving the current \"[B2] ... B3 leakage\" compromise tag at FILES.md:46 into honest per-module tags ([B?] is available where coupling hasn't crystallised); (3) note in the PR that this retires ADR-0003's types.ts inventory exclusion (ADR-0003:206-215) under Revisit trigger 1, making the split itself the band inventory for the type layer — the fork's take-vs-replace manifest. All three are within the work the proposal already schedules (FILES.md updates, doc-graph regeneration, maintainer sign-off).",
 "forkRelevance": "advances"
}
````

### §4 · gen-verify:doc-graph-dangling-signal-cleanup

**Commission** (= `[SHARED: GEN-DISCIPLINE]` + `[SHARED: FORK-CONSTRAINT]` + the following + `[SHARED: CANDIDATE-JSON <id>]`):

````
You are the GENERALIZATION verifier for one ALREADY-CONFIRMED candidate of the main review. Apply the constraint two-sidedly:
1. Set refuted=true ONLY if implementing the proposal would collapse/inline/specialize domain-agnostic infrastructure in a way that hinders the generic fork — name the seam and the mechanism. Simplification of Go-BOUND code, band-internal cleanup, and additions of validation/typing are NOT grounds for refutation.
2. If compatible but cheaply reshapeable to actively SERVE the fork (e.g. perform the proposed split along ADR-0003 band lines; extract a seam while already touching the file), describe that in reshape.
3. If fully orthogonal, say so (forkRelevance=orthogonal, empty reshape).
Read docs/adr/0003-frontend-portability-and-domain-boundaries.md end to end before judging any band question; check the actual files the candidate touches.
````

**Result (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "The proposal touches three surfaces, none of which is domain-agnostic SPA infrastructure that the generic flash-card fork depends on, and nothing in it collapses, inlines, or specializes a seam.\n\n(1) tools/doc-graph/generate.mjs is umbrella documentation tooling, entirely outside ADR-0003's inventory (the ADR's Scope line restricts it to the frontend; the doc-graph tool scans markdown + git history and carries no Go concept anywhere in its 1015 lines). The proposal is additive classification, not dissolution: the frozen-origin boundary is today a single hardcoded prefix (isArchiveOrigin, generate.mjs:833-835); the dangling-class conflation the proposal would split is real and visible in the code — extractEdges (generate.mjs:375-385) marks any .md target outside the node set as \"dangling\" without ever consulting disk, so missing-on-disk drift (e.g. docs/notes/deferred-items.md, retired to the vestige directory) and exists-but-outside-scan-scope targets (e.g. backend/samples/README.md, real on disk but absent from EXTRA_NODE_FILES, generate.mjs:160-170) land in the same 125-entry live list (docs/doc-graph-report.md:24). Tombstone handling and an advisory (non-gating) ratchet section are likewise additions of structure. No abstraction used by the fork is removed.\n\n(2) The ADR-0010:169 fix is a content-only doc path correction (render-locality/ → render-count/; verified against frontend/tests/integration/, which contains render-count). Non-.md backtick paths are outside the doc-graph scan (BACKTICK_PATH_RE, generate.mjs:267 requires a .md suffix), so this has no structural doc-graph effect either.\n\n(3) The MoveSuggestions.vue:144 fix is a comment-only path correction inside a Band 3 Go-bound board component (ADR-0003 places board components and use-move-suggestions.ts in Band 3, lines 174-179) — band-internal cleanup, explicitly permitted.\n\nTwo-sided check: the proposal also does not need refutation protection from the other direction — its \"report section, not a red X\" framing preserves the existing non-gating posture (docs/doc-graph-report.md:5-9), and the open design choices it defers to the maintainer (frozen-boundary classification, extend-checking vs retrofit-on-touch for code-comment paths) are domain-neutral either way. The fork inherits the doc-graph discipline wholesale, and a reviewable genuine-drift list is mildly useful to it (forking will retire/replace Go-bound docs, producing exactly the dangling-reference wave this report exists to surface), but that benefit is incidental to the proposal's purpose, so the honest classification is orthogonal rather than advances. No reshape is needed: the generator already implements every classification knob as a named single-source data table (AGE_BUCKETS, GENRE_BY_PREFIX, HUB_NODES, PLACEHOLDER_RE — each annotated \"named here as the single source\"), so any competent implementation of the frozen-prefix set and tombstone map will follow the same convention and remain a one-table tuning surface for a fork with a different doc-tree shape.",
 "reshape": "",
 "forkRelevance": "orthogonal"
}
````

### §4 · gen-verify:retire-hand-maintained-censuses-in-code

**Commission** (= `[SHARED: GEN-DISCIPLINE]` + `[SHARED: FORK-CONSTRAINT]` + the following + `[SHARED: CANDIDATE-JSON <id>]`):

````
You are the GENERALIZATION verifier for one ALREADY-CONFIRMED candidate of the main review. Apply the constraint two-sidedly:
1. Set refuted=true ONLY if implementing the proposal would collapse/inline/specialize domain-agnostic infrastructure in a way that hinders the generic fork — name the seam and the mechanism. Simplification of Go-BOUND code, band-internal cleanup, and additions of validation/typing are NOT grounds for refutation.
2. If compatible but cheaply reshapeable to actively SERVE the fork (e.g. perform the proposed split along ADR-0003 band lines; extract a seam while already touching the file), describe that in reshape.
3. If fully orthogonal, say so (forkRelevance=orthogonal, empty reshape).
Read docs/adr/0003-frontend-portability-and-domain-boundaries.md end to end before judging any band question; check the actual files the candidate touches.
````

**Result (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "The proposal is a comment-and-convention change only: add descriptive slugs alongside the colliding O-numbers (additive, frozen archive untouched), let the two census fixes ride with siblings, and adopt a stable-handles / no-hand-maintained-censuses convention in frontend/CLAUDE.md. No abstraction is collapsed, inlined, or specialized — it touches docstrings and a contributor doc, not code shape — so it cannot hinder the generic fork's seams. Verified all three instances: store/index.ts:331 says \"Four cleanups currently fire:\" above an enumerated list of ten (lines 333-395); eslint.config.js:82-86 carries the \"Four components ... five import sites\" violation census; and the O-number collision is real (archived plan lines 159-167 define O12-O15 as useResizablePanel/BaseChart/MintCardModal/analysisService-reconnect, while code's O12-O15 at store/index.ts:373,384,389,395, types.ts:1571, board-card-trees.ts:22, useAutoSaveAnalyses.ts:56 mean card-tree slot / persistence discard / cardTreeNav / forestNav.selection — later minting outgrew the frozen table that the plan's §\"Function-docstring shape\" item 4 told docstrings to cite). Fork-relevance is mildly positive rather than neutral: per ADR-0003 (read end to end), store/index.ts and the GlobalStore machinery are Band 1 (ADR-0003 \"Domain Coupling Inventory\", Band 1 first entry), and the Band 1 store/services tree is exactly the \"No change\" portion a port inherits (ADR-0003 \"What a Chess port would actually require\"). closeBoard/resetWorkspace are the worked examples of the resource-ownership discipline the fork keeps, so stale censuses and ambiguous handles there are rot the second consumer inherits directly. One wrinkle motivates the reshape: the convention's proposed stable handles include work-status item ids, which resolve only against the maintainer's private todo Postgres DB (connection facts in the gitignored services_local.gitignore per umbrella CLAUDE.md) — a fork gets the tree but not the DB, so bare work-status ids in Band 1 docstrings would be dangling for the fork maintainer, reintroducing the unresolvable-handle failure this candidate exists to fix.",
 "reshape": "When writing the frontend/CLAUDE.md convention, rank handles by in-tree resolvability: prefer repo-resident handles first — descriptive slugs, anchors into committed docs (the archived plan's pair-ids qualify when uncolliding), registry labels (the BOARD_SCOPED_STORE_CELLS registry + boardScopedStoreCellLabels() tripwire at store/index.ts:309-323 is the worked example of a machine-checked enumeration replacing a comment census), and test names — and require that a work-status DB id, when cited, always travels with a descriptive slug, so every code-comment handle resolves in any clone or fork of the tree without access to the maintainer's todo database. This is a one-sentence addition to the convention text and makes the candidate actively serve the fork: the Band 1 docstrings the fork inherits stay self-resolving.",
 "forkRelevance": "needs-reshape"
}
````

### §4 · gen-verify:enrichment-merge-null-validation

**Commission** (= `[SHARED: GEN-DISCIPLINE]` + `[SHARED: FORK-CONSTRAINT]` + the following + `[SHARED: CANDIDATE-JSON <id>]`):

````
You are the GENERALIZATION verifier for one ALREADY-CONFIRMED candidate of the main review. Apply the constraint two-sidedly:
1. Set refuted=true ONLY if implementing the proposal would collapse/inline/specialize domain-agnostic infrastructure in a way that hinders the generic fork — name the seam and the mechanism. Simplification of Go-BOUND code, band-internal cleanup, and additions of validation/typing are NOT grounds for refutation.
2. If compatible but cheaply reshapeable to actively SERVE the fork (e.g. perform the proposed split along ADR-0003 band lines; extract a seam while already touching the file), describe that in reshape.
3. If fully orthogonal, say so (forkRelevance=orthogonal, empty reshape).
Read docs/adr/0003-frontend-portability-and-domain-boundaries.md end to end before judging any band question; check the actual files the candidate touches.
````

**Result (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "The proposal adds a fail-loud validation guard at the mergeRecords boundary (frontend/src/services/analysis-ledger.ts:185-195, re-verified at HEAD) — an addition of validation, not a collapse/inline/specialization of domain-agnostic infrastructure, so no refutation grounds exist. Band geography: FILES.md:270 tags analysis-ledger.ts [B3], but ADR-0003 (read end to end) lists the ledger abstraction in Band 1 (0003:121-126, \"a versioned reactive cache of expensive computed values... independent of what the cached value is\"); at HEAD the file is band-mixed — the store machinery (version refs, rAF-coalesced flush, onLedgerFlush, purges; ~lines 51-181, 260-373) is the Band 1 substrate a generic flash-card fork keeps, while the merge helpers (lines 183-258) are the KataGo-shaped leaf layer the fork replaces wholesale (no engine enrichment to merge). mergeRecords itself is the one generic helper in that leaf section (<T extends Record<string, unknown>>), consumed only by Go-typed callers (mergePlayerExtra :207-218, mergeKataExtra :220-231). The guard's core mechanism — detect an incoming object value that would replace a populated leaf with a null-bearing object, warn loudly, promote to throw — is purely structural and domain-blind, and ADR-0003:352-354 explicitly names fail-loud discipline in the storage layer as something a port \"inherits for free\"; the postmortem source (postmortem-adaptive-deeper-enrichment-2026-05.md §5.5, read end to end) frames the edge structurally too (\"state[turn] = {Win: null} replacing a populated entry\"). The one fork-relevant wrinkle is the phrase \"calibrated against real adaptive packet shapes\": if the calibration is implemented as KataGo vocabulary inside mergeRecords (recognizing adaptive packet field names to suppress false positives), the generic helper acquires instance knowledge — a mild, avoidable specialization of generic machinery, not enough to hinder (worst case the fork discards the merge-helper section anyway) but exactly what a cheap reshape avoids. Hence: compatible, cheaply reshapeable to actively serve the fork.",
 "reshape": "Keep the guard inside mergeRecords purely structural and instance-blind: \"incoming value is an object whose own values include null/undefined AND it would replace an existing populated leaf → emit a structured loud-warn (later throw)\". Put all KataGo-specific calibration knowledge — which adaptive packet shapes legitimately carry inner nulls, the false-positive ledger, the warn→throw promotion decision — at the Go-typed call sites (mergePlayerExtra/mergeKataExtra, analysis-ledger.ts:207-231) or in the structured warn context they pass down (e.g. a caller-supplied label/escalation parameter), never as field-name vocabulary inside mergeRecords' logic. That way the guard travels with the Band 1 merge machinery the flash-card fork inherits for free (per ADR-0003:352-354's fail-loud-in-storage-layer framing), and the calibration dies with the Band 3 enrichment leaves at fork time. This is seam-conscious authoring per ADR-0003's principle, not a Port extraction — no interface, no registry, just keeping instance knowledge out of the one generic helper while the file is already being touched.",
 "forkRelevance": "needs-reshape"
}
````

### §4 · new-exist:adr0003-revisit-1-fired-fork-axis-remap

**Commission** (= `[SHARED: GEN-DISCIPLINE]` (+ `[SHARED: FORK-CONSTRAINT]` for fit) + the following + the candidate's JSON from §4's merge output):

````
You are an adversarial EXISTENCE/EVIDENCE verifier. Try to REFUTE this candidate on factual grounds: does the claimed current state hold at HEAD (read the named files/sections; run git log where commits are cited)? Is the load-bearing evidence real and correctly characterized (read cited docs end to end)? Already fixed? If it survives with details off, refuted=false + sharpenings in corrections. Default to refuted=true only on concrete grounds.
````

**Result (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Read end to end: docs/adr/0003-frontend-portability-and-domain-boundaries.md (369 lines), docs/adr-synopsis.md (318 lines), services_local.gitignore. frontend/FILES.md consulted partially (sanctioned lookup reference). Queried the todo Postgres DB directly. Findings: (1) ADR-0003 carries no record of Revisit #1 firing — status \"Accepted\", no amendment section; git log shows only e5c857b (initial) and ca871d1 (doc consolidation) ever touched the file. (2) All load-bearing citations hold: Revisit #1 at :317-321 (\"A second domain adopter materializes… Port extraction stops being premature\"); Band 1 defined against \"any other knowledge domain (Chess, Shogi, language flashcards, music theory drills)\" at :105-111; Band 2's flash-card exclusion at :136-139 (candidate quotes it near-verbatim); the only worked sizing is the Chess port at :261-282. (3) frontend/FILES.md:25-33 does key all three band tags to the chess axis (\"Would survive a chess port without code change\" / \"A chess port reuses these unchanged\" / \"the ~30–40% surface a chess port would rewrite\"). (4) DB probes re-verified first-hand (candidate had only distiller attribution): items table contains chess-clone (open/active, scope both, tier medium) whose description is explicitly the B3-replacement Chess arc (\"replace the ~30-40% Band-3 frontend surface… Stockfish/Leela engine bridge\"); no item matches flash/fork/knowledge-domain/adopter for the flash-card fork (the two 'flash' hits are unrelated texture-flash items). (5) Nothing anywhere in the repo records the fork intent (grep over docs/ for flash-card/knowledge-domain fork: no hits; no commits since 2026-06-08 touch it) — consistent with, and supporting, the candidate's \"unrecorded\" thesis. (6) The axis-remap logic is sound per the ADR's own text: B2 explicitly does not survive a non-game port (:136-139), so a flash-card fork's load-bearing boundary is B1/B2, not the B2/B3 boundary the Chess sizing exercises. Unverifiable from the repo (flagged, not refuted, and the candidate already discloses both): the maintainer's 2026-06-09/10 fork-intent statement (conversation-context premise) and the claim that the confirmed adr-trigger-sweep-and-record-hygiene candidate records only trigger #2 (that candidate is workflow-side; no repo artifact mentions it). One material numeric error and several sharpenings found — see corrections — none of which touch the candidate's core existence claim or its proposal's spine.",
 "corrections": "(1) \"~215 [B1] tags\" is wrong: FILES.md carries 78 [B1], 39 [B2], 97 [B3], 1 [B?] — 215 is the TOTAL band-tag count; the candidate conflated total with [B1]. Band tags appear only in FILES.md (zero in frontend/src). The proposed audit scope is therefore the 78 [B1] entries (+1 [B?]) — smaller and cheaper than claimed. (2) \"Not goals\" section is :356-369, not :356-365; the cited range cuts the fourth bullet (\"Not a substitute for backend item 34\"). (3) The [B1] legend line actually reads \"[B1] — truly domain-agnostic. Would survive a chess port without code change.\" — the strong any-domain label is already present; only the operational test sentence is chess-keyed. Proposal item (d) should re-key the test sentence, not imply the agnostic label is absent. (4) The proposed fork sizing's keep-list (\"SR core, cards, browse, workspace…\") is internally tense with \"replace: B2 and B3 both\" given current classifications: cards/browse composables are mostly [B2] (FILES.md:189-206, ForestDirectory :137), the review composables are [B3] (FILES.md:227-229 — useMinting, useReviewSession), and ADR-0003 itself places useReviewSession in Band 2 (:149-152) with a band-mixed note (:193-196). The sizing should either give SR/cards/browse orchestration a \"refactor with reuse\" tier (the fork analog of the Chess sizing's Band-2-mixed tier) or name their detangling as the fork-readiness work. (5) Bonus drift evidence for the amendment: useReviewSession is Band 2 in the ADR (:149-152) but [B3] in FILES.md:229 — an undocumented ADR-vs-FILES.md disagreement that is itself an instance of Revisit trigger #3, worth folding into the same amendment. (6) Cost addition: docs/adr-synopsis.md's ADR-0003 entry (:68-85, \"Ports are extracted only when a second concrete consumer exists\") must co-change with the amendment — a CI co-change advisory (tools/doc-graph/cochange-advisory.mjs, per synopsis :12-14) flags the synopsis when an ADR changes without it. So the PR shape is: ADR amendment + synopsis touch + doc-graph regen + DB item insert. (7) DB schema detail for proposal item (e): items has no 'slug' column; the id itself is the slug (items_id_check regex), state/disposition are constrained enums (open requires disposition, closed requires resolution+closed_on), and parent is a self-FK — so the proposed parent-item arc shape is directly expressible.",
 "alreadyFiled": "Only partially, exactly as the candidate states: the todo DB's chess-clone item (open/active, scope both) covers a Chess-port adopter on the B3-replacement axis — its description names PGN normalization, Band-3 surface replacement, and a Stockfish/Leela bridge — but no work-status item, doc, or commit records the flash-card/knowledge-domain fork or Revisit #1 firing. The confirmed adr-trigger-sweep-and-record-hygiene candidate is not in the repo, so its trigger-#2-only scope rests on the distillers' reports (consistent with everything I could check first-hand).",
 "priorityHint": "keep"
}
````

### §4 · new-fit:adr0003-revisit-1-fired-fork-axis-remap

**Commission** (= `[SHARED: GEN-DISCIPLINE]` (+ `[SHARED: FORK-CONSTRAINT]` for fit) + the following + the candidate's JSON from §4's merge output):

````
You are an adversarial FIT/VALUE verifier. Try to REFUTE this candidate on judgment grounds: (1) does it fight a binding tenet or by-design discipline (read the ADRs it touches end to end) without naming the deviation — including ADR-0003's own caution against premature Port extraction (a planned fork justifies seam-NAMING; does it justify this candidate's full proposal)? (2) duplicate of a filed work-status item (psql -h 192.168.122.1 -d todo, read-only) without new signal? (3) honest cost/benefit for a correctness/auditability/generalization gain? (4) shaped to this codebase's vocabulary? Default to refuted=true only on concrete grounds.
````

**Result (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Attempted refutation on all four grounds; the candidate survives, with corrections. (1) Tenet fight: I read ADR-0003 end to end (docs/adr/0003-frontend-portability-and-domain-boundaries.md, 369 lines). The candidate is the move the ADR itself prescribes, not a fight against it. Revisit #1 (:317-321) names \"someone wants to fork for Chess, Shogi, or another problem class\" — the flash-card fork matches \"another problem class\" verbatim. The ADR's premise at :94-97 (\"no concrete second-domain consumer exists\") is now false under the binding maintainer constraint, so every session consulting the ADR (mandatory end-to-end read) operates on a stale calculus until the trigger is recorded. On the prompt's specific question — does a planned fork justify the full proposal, not just seam-naming? — yes, because the full proposal IS seam-naming at the documentation level: record the trigger, re-cut the band legend, audit tags, file the arc. No Port is extracted; proposal (b) explicitly preserves the \"Not goals\" anti-mandate (:356-369), and the risks section pre-empts the refactoring-mandate misreading. The axis-remap claim is grounded in the ADR's own text: Band 1 is defined against any knowledge domain including language flashcards (:105-111); Band 2 explicitly does not survive a non-game port (:136-139); the only worked sizing is Chess (:261-282). All cited line numbers check out. (2) Duplicates: probed the todo DB read-only. chess-clone (open/active/medium/both) is confirmed as a different adopter on the B3-replacement axis (\"replace the ~30-40% Band-3 frontend surface... Stockfish/Leela engine bridge... PGN\"). adr-effectiveness-audits (open/umbrella/large) is the per-tenet audit cadence — adjacent, distinct duty. No item matches flash/fork/knowledge-domain (direct probe across id/title/description). I also read docs/handoff-current.md end to end (621 lines): the fork is recorded nowhere — the \"Domain extension\" bullet (:459-465) and the frontend portability paragraph (:159-162) speak only of chess/shogi. git log on ADR-0003 shows no amendment exists or is in flight. New signal is real. (3) Cost/benefit honest: doc-only + doc-graph regeneration + a bounded audit — and the audit is actually smaller than claimed (78 [B1] tags, not ~215; see corrections). The drift claim is substantiated by my own spot-check: frontend/FILES.md:93 (EngineQueueTooltip.vue [B1] — \"in-flight KataGo queries\") and :149 (useQueryTelemetry.ts [B1] — \"Singleton in-flight KataGo query queue\") are B1 tags whose purpose lines name KataGo, untenable under the fork criterion. FILES.md legend (:25-33) verified — all three bands defined against the chess axis, weaker than ADR-0003's own Band-1 criterion. (4) Vocabulary fit: bands, seams, Revisit-when, the rebase+regenerate discipline for stacked structural doc PRs, work-status parent items — all correct codebase idiom. Residual unverified: the confirmed sibling candidate (adr-trigger-sweep-and-record-hygiene) is not locatable in repo or DB; its \"records trigger #2 only\" scope rests on the distillers' report and must be re-verified at execution time before folding in.",
 "corrections": "1. Tag-count error: \"~215 [B1] tags\" conflates the total with the B1 count. frontend/FILES.md carries 78 [B1], 39 [B2], 97 [B3], 1 [B?] — 215 total. The B1 audit is a ~78-tag pass (cost overstated, benign direction, but the item text must say 78). 2. The audit should cover [B2] (39 tags) as well, in the opposite direction: under the fork axis the B1/B2 boundary is load-bearing both ways, and ADR-0003's own Band-2 list contains entries it describes as \"Generic charting machinery; no domain\" (:153-155, useChartNavigation/BaseChart) — keepable surface parked in the replace band. 3. Internal inconsistency in proposal (c): \"keep: SR core ... replace: B2 and B3 both\" contradicts itself — the SR core (useReviewSession.ts) is Band 2 per ADR-0003:149-152, and the ADR itself says its flow is portable with only per-move scoring Go-shaped (:193-196). The honest sizing is: B1 kept, B3 replaced, B2 SPLIT (game-tree skeleton goes; SR-orchestration flow and generic charting are the seam). This strengthens the candidate's own thesis, but folded in verbatim the sizing line would propagate a wrong cut into the ADR. 4. Proposal (d) is too narrow: not just the [B1] legend line — FILES.md's [B2] line says \"A chess port reuses these unchanged\" (:28-30) and [B3]'s \"~30-40% surface a chess port would rewrite\" (:31-33) are both fork-misleading; re-cut all three legend lines on the two-axis criterion. 5. Scope additions the amendment arc should carry: ADR-0003:94-97 (\"no concrete second-domain consumer exists\") must be annotated/corrected as part of the trigger record; docs/handoff-current.md's \"Domain extension\" bullet (:459-465) and frontend portability paragraph (:159-162) are chess/shogi-keyed orientation surfaces the same change affects (umbrella CLAUDE.md handoff-audit duty); docs/adr-synopsis.md's ADR-0003 entry should be checked for the same staleness (flagged, not asserted — I did not read it). 6. Minor: \"Not goals\" spans :356-369, not :356-365. 7. Execution-time precondition: verify the actual scope of the confirmed adr-trigger-sweep-and-record-hygiene candidate before folding in — it is not in the repo or DB, and the \"trigger #2 only\" claim is distiller-reported, unverified here.",
 "alreadyFiled": "Partially, and the candidate characterizes it accurately. Verified by direct read-only DB probe: chess-clone (open, active, scope=both, tier=medium) is a different adopter on a different boundary — its description is explicitly Band-3 replacement (PGN PositionNormalizerPort, Stockfish/Leela bridge, \"replace the ~30-40% Band-3 frontend surface\") with a trigger condition (\"a chess-playing contributor with a PoC\") that has not fired; the flash-card fork fires Revisit #1 on the B1/B2 boundary instead. adr-effectiveness-audits (open, umbrella, large) covers per-tenet audit cadence, not trigger recording or band re-cutting. No item in the DB matches flash/flashcard/fork/knowledge-domain. ADR-0003 itself has no amendment (git log: initial + doc-consolidation commits only) and no branch suggests one in flight. The one overlap is the confirmed sibling candidate adr-trigger-sweep-and-record-hygiene, which this candidate already proposes folding into rather than paralleling — correct per the stacked-structural-doc-PR discipline — but that candidate's text is not locatable in repo or DB, so its claimed trigger-#2-only scope needs re-verification at execution time.",
 "priorityHint": "raise"
}
````

### §4 · new-exist:cold-start-calibration-to-band3

**Commission** (= `[SHARED: GEN-DISCIPLINE]` (+ `[SHARED: FORK-CONSTRAINT]` for fit) + the following + the candidate's JSON from §4's merge output):

````
You are an adversarial EXISTENCE/EVIDENCE verifier. Try to REFUTE this candidate on factual grounds: does the claimed current state hold at HEAD (read the named files/sections; run git log where commits are cited)? Is the load-bearing evidence real and correctly characterized (read cited docs end to end)? Already fixed? If it survives with details off, refuted=false + sharpenings in corrections. Default to refuted=true only on concrete grounds.
````

**Result (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Verified at HEAD 2dc056e by reading both source files end to end, ADR-0003 end to end, the cited FILES.md rows (sanctioned partial lookup), and running the cited git archaeology. Every load-bearing claim holds: (1) frontend/src/services/resource-service.ts:18 imports initializeIntensityFactory from '../engine/suggestion-colors'; fetchResource<T> at :40 is module-private (no export); the sole public method loadVisitDistribution at :53 calls fetchResource<any>('visit-distribution') at :55 and hands the payload to the B3 factory at :56. (2) frontend/src/composables/auth-app/useAppBootstrap.ts:30 imports setIntensityHueShift; the hue-shift watch exists (actual span :116-120, see corrections); the fire-and-forget loadVisitDistribution call is at :408-409. (3) ADR-0003:119-120 reads verbatim \"services/api-client.ts + services/resource-service.ts. HTTP client and static-resource fetcher; no domain in either\" (Band 1 list). (4) FILES.md:175 tags useAppBootstrap [B1], :246 tags suggestion-colors [B3], :278 tags resource-service [B1] — exact line numbers confirmed via grep -n. (5) git log -S confirms loadVisitDistribution present since e5c857b (\"initial\", the monorepo's initial commit) and the hue-shift watch added by 1545ea5 (\"frontend: intensity gradient hue-shift slider\", 2026-04-28, +15 lines to the then-unmoved composables/useAppBootstrap.ts). Not already fixed: the imports and the <any> are live at HEAD. Not already filed: queried the todo Postgres store (items table) across title/id/description for resource/band/calibration/suggestion-color/bootstrap/hue/initializeIntensityFactory — only unrelated items (tags-fetch-hydration-race closed, two pv-*-calibration future items, qeubo-e2e-validation closed). No mechanical guard exists: frontend/eslint.config.js's import boundary restricts wire-types outside services/** and effectful singletons in components; it does not police services→engine imports. The risks section's ordering-tolerance and no-test-coverage claims also check out: the comment block at useAppBootstrap.ts:108-115 documents the record-then-rebuild tolerance, independently corroborated by docs/notes/consult/opus-consult-2026-06-03-knowntags-fence-and-boot-ordering.md (read end to end; Part 2 row 2 classifies loadVisitDistribution as module-scope-only and ordering-safe via the :116-120 watcher); grep over frontend/tests/ finds no test touching resource-service, loadVisitDistribution, setIntensityHueShift, or initializeIntensityFactory (the single useAppBootstrap hit is a header comment in analysis-persistence-watch-contract.test.ts:16 about a different watcher). The candidate survives with minor sharpenings only.",
 "corrections": "Sharpenings, none fatal: (1) Paths omit src/ — actual locations are frontend/src/services/resource-service.ts and frontend/src/composables/auth-app/useAppBootstrap.ts. (2) The hue-shift watch spans useAppBootstrap.ts:116-120, not :115-118; :115 is the last line of the preceding ordering-tolerance comment (:108-115) the risks section correctly leans on. (3) The proposal's \"type the calibration payload at the B3 boundary\" is already half-built in tree: engine/suggestion-colors.ts:40 exports `interface VisitDistributionData`, the typed entry `setVisitDistribution(distributionData: VisitDistributionData)` is at :211, and `initializeIntensityFactory` is merely a back-compat alias (`export const initializeIntensityFactory = setVisitDistribution;` at :231). So resource-service.ts:55's `fetchResource<any>` discards a type that already exists one import away, and the proposed B3 init function partially exists — the remaining work is relocating the fetch orchestration, likely smaller than the \"~40-line new module\" estimate. (4) \"ADR-0003's named Band-1 exemplar\" mildly overstates: resource-service is one of six Band-1 bullets, named jointly with api-client in a shared bullet (ADR-0003:119-120); the quote itself is verbatim-accurate. (5) A tension the proposal should name explicitly: ADR-0003's \"Not goals\" section states \"Not a refactoring mandate. Existing code stays put,\" with Port-extraction gated on a second adopter (Revisit-when #1, none exists). What makes this actionable today under existing discipline is narrower: frontend/CLAUDE.md's FILES.md retag-on-drift rule (\"Drift between the file's actual dependencies and its FILES.md band tag is the silent-failure mode this discipline exists to surface\") plus ADR-0003's own Revisit-when #3 (band classification wrong in practice). The minimal discipline-compliant alternative is retagging both FILES.md rows without code changes; the candidate's \"present the choice on the record\" framing for useAppBootstrap should extend to resource-service as well. (6) The retag-as-wiring branch is stronger for the bootstrap than the candidate implies: useAppBootstrap also imports backend-service ([B2], FILES.md:274) and analysis-service at :26-:27, so its [B1] tag is already inaccurate independent of the hue-shift edge; resource-service's [B1] claim, by contrast, is broken by exactly the one import at :18, making the repair branch cleanest there. (7) Git nuance: e5c857b is the whole monorepo's initial commit, not a frontend-only commit; at 1545ea5 the bootstrap lived at src/composables/useAppBootstrap.ts (the auth-app/ relocation came later) — immaterial to the argument. (8) Corroborating prior art worth citing in the filed item: the 2026-06-03 Opus consult (docs/notes/consult/opus-consult-2026-06-03-knowntags-fence-and-boot-ordering.md, Part 2 row 2) already characterizes this exact surface as module-scope-only and ordering-safe, which both de-risks the proposed move and documents the immediate-watch + fire-and-forget semantics that must be preserved.",
 "alreadyFiled": "No. Queried the work-status store (psql -h 192.168.122.1 -d todo, items table) across title/id/description for resource, band, calibration, suggestion-color, bootstrap, hue, and initializeIntensityFactory: only unrelated hits (tags-fetch-hydration-race — closed, a different useAppBootstrap bug since fixed at :412-419; pv-overlay-typography-calibration and pv-animation-defaults-calibration — open/future, unrelated; qeubo-e2e-validation — closed). No ESLint rule covers the services-to-engine direction, and the FILES.md rows at :175/:278 carry no erosion annotation, so the drift is currently recorded nowhere.",
 "priorityHint": "keep"
}
````

### §4 · new-fit:cold-start-calibration-to-band3

**Commission** (= `[SHARED: GEN-DISCIPLINE]` (+ `[SHARED: FORK-CONSTRAINT]` for fit) + the following + the candidate's JSON from §4's merge output):

````
You are an adversarial FIT/VALUE verifier. Try to REFUTE this candidate on judgment grounds: (1) does it fight a binding tenet or by-design discipline (read the ADRs it touches end to end) without naming the deviation — including ADR-0003's own caution against premature Port extraction (a planned fork justifies seam-NAMING; does it justify this candidate's full proposal)? (2) duplicate of a filed work-status item (psql -h 192.168.122.1 -d todo, read-only) without new signal? (3) honest cost/benefit for a correctness/auditability/generalization gain? (4) shaped to this codebase's vocabulary? Default to refuted=true only on concrete grounds.
````

**Result (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Attempted refutation on all four grounds; the candidate survives, with corrections.\n\n(1) Tenet fight — no. ADR-0003 read end to end. Its \"Not a refactoring mandate / existing code stays put\" posture (lines 309-311, 358-359) is the obvious attack, but the ADR's own Revisit-when #1 (lines 317-321) flips the calculus when \"a second domain adopter materializes,\" and the binding maintainer constraint declares the generic flash-card fork as exactly that consumer. The candidate names this calculus shift explicitly rather than deviating silently. More decisively: the proposal is NOT Port extraction — no interface, no registry, no strategy. It is the lighter idiom ADR-0003 itself prescribes for analysis-recording (lines 226-234: the generic service \"takes the predicate as a parameter; it doesn't import it\"; lines 251-257: \"the seam is a property of the code's shape\"). resource-service.ts:18 importing initializeIntensityFactory is precisely the anti-pattern that section names; the fix un-shadows an already-written generic function (fetchResource<T>, :40) rather than speculating a new abstraction. Band 1's survival domains explicitly include \"language flashcards\" (ADR-0003:108), so this module is fork-load-bearing under the stated constraint, not hypothetically general.\n\n(2) Duplicate — no. Swept the todo DB (items table) by id/title/description across resource/band/calibration/bootstrap/fork/domain/seam/FILES.md terms plus a full open-frontend-items scan. Closest items are different shapes: chess-clone (full domain port, trigger-gated on a chess contributor), refactoring-queue-adr0007 (file-size budget, handled-on-touch), semantic-clarity-refactors-effect-typing (multiplexer naming + error encodings), silent-coercion-protocol-boundaries-audit (wire-vocabulary coercion). \"services-boundary-deny-by-default\" is not in the DB; the existing ESLint boundary (frontend/eslint.config.js) restricts wire-type and component→service imports, not services→engine — so no mechanism currently catches this drift, supporting the candidate's new-signal claim.\n\n(3) Cost/benefit — honest, and cost is actually overstated (see corrections): the typed payload contract already exists on the B3 side, and the B3 home can be an exported init in suggestion-colors.ts, so possibly zero new modules. Benefit is concrete: ADR-0003:119-121 names resource-service as a Band-1 exemplar (\"no domain in either\") and FILES.md:278 tags it [B1], but the tree contradicts both — that is exactly the band-drift silent-failure the FILES.md discipline (frontend/CLAUDE.md, File map section) exists to surface. The no-test-coverage claim verified (no test under frontend/tests/ references resource-service, loadVisitDistribution, or suggestion-colors); the ordering-tolerance risk is real and correctly located (useAppBootstrap.ts:108-115 comment; suggestion-colors.ts rebuild early-return at :181 and the linear-variant note at :81-83).\n\n(4) Vocabulary — native. Bands, seams, FILES.md rows, fire-and-forget posture, the named-domain-init inversion mirroring ADR-0003's worked example. The judgment call on the bootstrap (invert vs retag, on the record) is the right collaborative shape.\n\nAll line/history claims verified: resource-service.ts:18/:40/:53-56 exact; useAppBootstrap.ts:30/:408-409 exact (watch is :116-120, comment :108-115 — minor drift from the claimed :115-118); e5c857b is the root commit (\"initial\", 2026-04-26) and carries loadVisitDistribution at :53; 1545ea5 (2026-04-28) added the hue-shift watch and the setVisitDistribution/setIntensityHueShift split.",
 "corrections": "Three substantive corrections, none fatal:\n\n1. The bootstrap limb is overstated as framed. useAppBootstrap.ts ([B1] per FILES.md:175) also imports backend-service [B2] (:26), analysis-service [B3] (:27; FILES.md:272), and analysis-persistence-service [B3] (:28; FILES.md:271), and carries substantial B3 wiring beyond the hue-shift watch — restartActiveAnalyses on qEUBO toolbar change (:269-272) and the per-board analysis-restore machinery (:274-391). Moving only the calibration wiring does NOT make the bootstrap fork-liftable; the [B1] tag is inaccurate regardless of this candidate. Of the two options the proposal presents, the retag (band-mixed wiring, per ADR-0003:208-210's App.vue precedent — \"It's a wiring file\") is effectively forced; the named domain-init call is then tidiness, not the fork-critical fix. The resource-service limb is the load-bearing half and stands alone.\n\n2. The \"type the calibration payload at the B3 boundary\" sub-task is already half-done. suggestion-colors.ts:40-43 declares VisitDistributionData; setVisitDistribution(distributionData: VisitDistributionData) at :211 is the typed setter; initializeIntensityFactory (:231) is documented as a backwards-compat alias for the resource-service call site. The erosion is only that resource-service.ts:55 fetches as <any> instead of consuming the published type. Cost is therefore lower than stated — likely no new ~40-line module at all if the init lands in suggestion-colors.ts.\n\n3. Minor precision: actual paths are frontend/src/services/resource-service.ts and frontend/src/composables/auth-app/useAppBootstrap.ts (candidate omits src/); the hue-shift watch body is at :116-120 (the cited :115-118 spans the comment/watch boundary); ADR-0003's exemplar line is :119-121.\n\nSuggested reshaping: lead with the resource-service inversion (export the generic verb; move the fetch+initialize orchestration into a suggestion-colors init or a board-side calibration composable that calls getResource<VisitDistributionData>), and demote the bootstrap limb to \"retag useAppBootstrap honestly (it is band-mixed through three other imports) + relocate the calibration wiring as part of the same move.\" Preserve { immediate: true } on the moved watch and the fire-and-forget catch posture (:408-409); verification is a manual cold-start smoke of the gradient (no automated coverage exists — verified).",
 "alreadyFiled": "No duplicate in the todo store. Nearest items, all materially different: chess-clone (open; full Band-3 domain port gated on a chess contributor materializing — not band-hygiene repair of B1 files), refactoring-queue-adr0007 (open; ADR-0007 file-size backlog, handled-on-touch), semantic-clarity-refactors-effect-typing (open; packet-multiplexer naming + error-encoding unification), silent-coercion-protocol-boundaries-audit (open; wire-vocabulary coercion sweep). The candidate's own disambiguation from \"services-boundary-deny-by-default\" holds — no such item exists in the DB, and the existing ESLint import-boundary governs a different edge (components→services and wire-types), not services→engine.",
 "priorityHint": "keep"
}
````

### §4 · new-exist:rehome-agnostic-utils-from-engine-util

**Commission** (= `[SHARED: GEN-DISCIPLINE]` (+ `[SHARED: FORK-CONSTRAINT]` for fit) + the following + the candidate's JSON from §4's merge output):

````
You are an adversarial EXISTENCE/EVIDENCE verifier. Try to REFUTE this candidate on factual grounds: does the claimed current state hold at HEAD (read the named files/sections; run git log where commits are cited)? Is the load-bearing evidence real and correctly characterized (read cited docs end to end)? Already fixed? If it survives with details off, refuted=false + sharpenings in corrections. Default to refuted=true only on concrete grounds.
````

**Result (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Every load-bearing claim checks out at HEAD (2dc056e). frontend/src/engine/util.ts (read end to end) exports setDeep at :35, updateRegistry<T> at :191, generateUUID at :213 (RFC4122 over crypto.getRandomValues with the secure-context fallback, per its own docstring) — none Go-coupled. FILES.md:248 tags util.ts [B3] with exactly 'Board / SGF coord helpers; active-variation traversal', which omits all three. ADR-0003 (read end to end) lists engine/util.ts in the Band 3 inventory at docs/adr/0003-frontend-portability-and-domain-boundaries.md:180-181. All five cited consumer sites verified exactly: useQeubo.ts:87 ([B1] per FILES.md:148), board-factory.ts:14, archived-migrations.ts:29 (generateUUID); SettingsTab.vue:35, useDirtyBoardGuard.ts:41 (updateRegistry). The git claim holds: e57c90b ('frontend/qeubo: fix crypto.randomUUID on insecure contexts', 2026-04-28) is where generateUUID was added to engine/util.ts for useQeubo's pinCurrent. FILES.md:316 does carry the 'debounce helper (the only inhabitant; lib/utils merger flagged separately)' row. Not already fixed; no work-status item covers it (queried the todo store over items.title/description for util/uuid/merge/home/setDeep/debounce — only unrelated hits). The inaccuracies found are characterization-level (two of five consumers are themselves [B3], one missed import site, a stale quote propagated from FILES.md itself) — sharpenings, not refutation.",
 "corrections": "1) Band-edge framing overstated: of the five consumers, only three create cross-band edges — useQeubo.ts [B1] (FILES.md:148), archived-migrations.ts [B1] (FILES.md:283), SettingsTab.vue [B2] (FILES.md:57). board-factory.ts is itself [B3] (FILES.md:284, 'Pure factory functions for board state construction' — so 'fork-shared store factories' pulling a false edge is wrong at the tag level) and useDirtyBoardGuard.ts is [B3] (FILES.md:182). The title's '[B1]-to-[B3]' applies to two consumers; SettingsTab is B2→B3. The semantic point (agnostic helpers mis-homed in a Go-bound module) stands for all five. 2) Missed import site: src/engine/sgf-loader.ts:5 also imports generateUUID (B3→B3, but it must be updated in the move). Actual count: 6 src import lines + tests/unit/engine/util.test.ts (imports/tests generateUUID at :25/:164-174; the test would move or re-point too) — '~8' was a slight overcount of import lines, undercount of usage sites. 3) setDeep has ZERO external consumers; its only caller is updateRegistry in the same file (:196, a one-line delegation). The move is really 'two consumed exports + one internal helper', and the mover may fold setDeep into updateRegistry. 4) The setDeep-vs-knobs.ts question is answerable now, against consolidation: lib/knobs.ts writeKnob (:94-130) deliberately throws on missing intermediates and on non-existent leaves ('knobs write to paths that already exist... not through first-write side-effects') and accepts only finite numbers; setDeep (:35-49) silently creates intermediate objects and accepts any value. Different contracts — co-location fine, collapse wrong. 5) The quoted FILES.md:316 row is itself stale at HEAD: lib/utils.ts has three exports — debounce, isObject (:21), deepMerge (:28, load-bearing in hydration per docs/notes/consult/opus-consult-2026-06-03-knowntags-fence-and-boot-ordering.md:24). The same PR should fix that row; this also strengthens lib/utils.ts as the natural landing spot (already a generic grab-bag, not single-inhabitant). 6) 'Merger flagged separately' resolves to the archived note docs/archive/notes/frontend-source-tree-reorganization.md:374-378 (merge lib/ with utils/, pick one name) — a directory-naming follow-up, not a stray-helper re-home; compatible but not identical occasions, and neither is tracked in the work-status store. 7) On the frozen-file risk: frontend/CLAUDE.md's rolling-archive section freezes migration *bodies* only ('never edit the body during the move'); archived-migrations.ts:29 is in the import block, so option (a) (header-level edit) is consistent with the convention as written — the on-the-record justification ask is still reasonable. 8) Bonus drift: ADR-0003:180-181 describes engine/util.ts as 'board-state walks, captures math' — there is no captures math in the file at HEAD, so the ADR inventory line has drifted too (ADR-0003 'Revisit when… #2/#3 — inventory drift' names the trigger class). 9) Priority context: the chess-clone item is open/active in the work-status store, which keeps ADR-0003 band hygiene non-academic; but ADR-0003 is explicitly 'Not a refactoring mandate' (:358), so this is discretionary hygiene, correctly costed as trivial-to-small.",
 "alreadyFiled": "No. Queried the todo work-status store (items.title/description ILIKE over util/uuid/home/merge/merger/setdeep/debounce/lib variants, plus a review of all 49 open items): no item covers re-homing engine/util.ts's agnostic helpers or the lib/-vs-utils/ merger. The merger flag exists only in the archived note docs/archive/notes/frontend-source-tree-reorganization.md:374-378 and the FILES.md:316 parenthetical. Nearest open items (refactoring-queue-adr0007, semantic-clarity-refactors-effect-typing, chess-clone) do not subsume it.",
 "priorityHint": "keep"
}
````

### §4 · new-fit:rehome-agnostic-utils-from-engine-util

**Commission** (= `[SHARED: GEN-DISCIPLINE]` (+ `[SHARED: FORK-CONSTRAINT]` for fit) + the following + the candidate's JSON from §4's merge output):

````
You are an adversarial FIT/VALUE verifier. Try to REFUTE this candidate on judgment grounds: (1) does it fight a binding tenet or by-design discipline (read the ADRs it touches end to end) without naming the deviation — including ADR-0003's own caution against premature Port extraction (a planned fork justifies seam-NAMING; does it justify this candidate's full proposal)? (2) duplicate of a filed work-status item (psql -h 192.168.122.1 -d todo, read-only) without new signal? (3) honest cost/benefit for a correctness/auditability/generalization gain? (4) shaped to this codebase's vocabulary? Default to refuted=true only on concrete grounds.
````

**Result (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Attempted refutation on all four grounds; none holds. (1) ADR-0003 fight: read end to end. Its caution (lines 47-97, \"Why not extract Ports preemptively\"; Not-goals at 356-365) targets premature *abstraction* — Port interfaces, registries, strategy patterns. This candidate extracts no abstraction: it relocates three already-concrete functions to the band-correct directory. The operative discipline is the FILES.md band-coherence rule (frontend/CLAUDE.md \"File map\": drift between a file's actual dependencies and its band tag \"is the silent-failure mode this discipline exists to surface\"), which the candidate implements rather than fights. ADR-0003's \"existing code stays put\" is a statement of what that ADR mandates, not a prohibition; its Revisit-when #1 (second adopter — now the maintainer's stated fork) and #3 (band classification wrong in practice — three of engine/util.ts's exports are B1) both lean the candidate's way. Notably, ADR-0003:265-269 puts engine/util.ts on the Chess-port wholesale-replacement list, so a fork replacing it wholesale would silently lose the secure-context UUID fallback that commit e57c90b exists to provide — the false-edge claim has concrete fork consequences. The proposal also stays inside the maintainer constraint: it un-strands general infrastructure, it doesn't collapse any. (2) Not a duplicate: multiple sweeps of the todo store (items table; title/description ILIKE over util/uuid/lib/band/merger/consolidate/agnostic/fork/reorg/engine/setDeep/updateRegistry/generateUUID/engine-util/lib-utils) found nothing overlapping. The adjacent \"lib/ vs utils/ merger\" flag (FILES.md:316; archived note docs/archive/notes/frontend-source-tree-reorganization.md:375-378, 560-561 — read end to end) is an unfiled open arc, and the candidate composes with it rather than duplicating it; the candidate also carries new signal (verified consumer list, the e57c90b provenance now confirmed, the knobs non-duplication answer below). (3) Cost/benefit honest: 6 import lines (not ~8 — see corrections), a few comment references, FILES.md row edits; benefit is real band coherence on the generic settings write path (SettingsTab.vue:69-75 and useDirtyBoardGuard.ts:87 mutate store.profile via a function homed in a Go-bound module) plus fork-relevant severing of B1→B3 edges. (4) Vocabulary fit: bands, FILES.md rows, the rolling-archive freeze, \"co-location is not collapse\" — all native. Every factual citation checked out exactly: engine/util.ts:35/:191/:213; useQeubo.ts:87; board-factory.ts:14; archived-migrations.ts:29; SettingsTab.vue:35; useDirtyBoardGuard.ts:41; FILES.md:148/:248/:313/:316; ADR-0003:180-181. The git claim flagged as the distiller's is now verified: e57c90b (2026-04-28, \"frontend/qeubo: fix crypto.randomUUID on insecure contexts\") added generateUUID to engine/util.ts for useQeubo's pinCurrent — the commit message itself documents the authoring-convenience homing the lesson names.",
 "corrections": "Five corrections/refinements, none load-bearing against the candidate. (a) The setDeep-vs-knobs question the candidate left open is answerable now: they are NOT the same function. lib/knobs.ts's walkTo/writeKnob (knobs.ts:85-130) are fail-loud by design — throw on missing parents, refuse non-existent final segments (comment at :119: \"knobs write to paths that already exist\"), numeric-finite leaves only — while setDeep (engine/util.ts:35-49) silently creates intermediate objects and accepts any value. Consolidating them would erase a deliberate ADR-0002 calibration difference; co-locating both in lib/ is fine. (b) setDeep has zero external importers — its only caller is updateRegistry in the same file — so the false-edge claim is strictly two functions wide (generateUUID, updateRegistry); setDeep could move as a private helper rather than an export. (c) \"~8 greppable import sites\" overstates imports: 6 actual import lines (useQeubo.ts:87, board-factory.ts:14, archived-migrations.ts:29, SettingsTab.vue:35, useDirtyBoardGuard.ts:41, plus sgf-loader.ts:5 which the candidate omits — B3-internal, no false edge, but its import must still be rewritten). Comment-only references also need updating: board-factory.ts:26-33 cites \"engine/util.ts::generateUUID\" in prose, AnalysisTabsEditor.vue:14 names updateRegistry, and generateUUID's own docstring (engine/util.ts:209) still says \"currently useQeubo's pinCurrent\" — stale at four importers and worth refreshing in the move. (d) FILES.md:316's \"the only inhabitant\" is itself stale: lib/utils.ts actually exports debounce, isObject, and deepMerge (read directly). This doesn't weaken the \"natural merger occasion\" point — the file is still small — but the PR should fix that row too. (e) The archived-migrations choice is less open than the candidate frames it: the freeze convention's own wording (archived-migrations.ts header :18-20, \"The bodies here are frozen exactly as they shipped\"; frontend/CLAUDE.md rolling-archive section, \"never edit the body during the move\") freezes migration BODIES; the import block at :29 is outside any body, and the three generateUUID call sites inside bodies (:1001, :1086, :1168) are untouched by an import-path rewrite. Option (a) (header-level edit, no shim) is the convention-consistent choice; the shim option can be dropped. Minor additional duty the candidate omits: FILES.md row edits here are content-level (no node/edge changes), so doc-graph regeneration should not be triggered, but the PR should confirm against docs/doc-graph-report.md per the umbrella checklist.",
 "alreadyFiled": "No matching item in the work-status store (todo DB items table; searched title and description for util/uuid/lib/band/re-home/merger/consolidate/agnostic/fork/reorg/engine/setDeep/updateRegistry/generateUUID/\"engine/util\"/\"lib/utils\"/\"band 3\"). Closest adjacent: the unfiled \"lib/ vs utils/ merger\" follow-up flagged in docs/archive/notes/frontend-source-tree-reorganization.md:375-378 and :560-561 (and echoed at FILES.md:316), which this candidate composes with — same occasion, not the same work.",
 "priorityHint": "keep"
}
````

### §4 · new-exist:registry-editor-vocabulary-injection

**Commission** (= `[SHARED: GEN-DISCIPLINE]` (+ `[SHARED: FORK-CONSTRAINT]` for fit) + the following + the candidate's JSON from §4's merge output):

````
You are an adversarial EXISTENCE/EVIDENCE verifier. Try to REFUTE this candidate on factual grounds: does the claimed current state hold at HEAD (read the named files/sections; run git log where commits are cited)? Is the load-bearing evidence real and correctly characterized (read cited docs end to end)? Already fixed? If it survives with details off, refuted=false + sharpenings in corrections. Default to refuted=true only on concrete grounds.
````

**Result (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Every existence claim checks out at HEAD. /home/bork/w/omega/frontend/src/components/editors/RegistryEditor.vue:9 imports WINRATE_FRAMINGS from ../../engine/katago/types (tagged [B3] at frontend/FILES.md:259, 'SSOT for KataGo wire types + enrichment envelope'). isDynamicNode (:25-41) hard-codes pathStr.endsWith('overrideSettings') at :39 with the KataGo rationale comment at :33-38, gating delete buttons at :224 and :253 and the add-key row at :286 — all three line cites exact. PATH_ENUMS is declared at :63; 'engine.katago.overrideSettings.reportAnalysisWinratesAs': [...WINRATE_FRAMINGS] is at :78. The per-root-option-resolver concession is a verbatim quote at :60-62. ADR-0003 (read end to end) lists 'the registry editor' under Band 1 at docs/adr/0003-frontend-portability-and-domain-boundaries.md:130-131 ('All generic infrastructure'), and FILES.md:111 tags it [B1] 'Generic managed-registry editor with defaults and structural protection.' Not already fixed: git log on the file shows the accretion is recent and ongoing (9b40b1a added the analysisAutoSave hint, 0e06705/6616fba/189424e/355b1e2 added bundle-compression vocabulary), which substantiates the lesson's 'cheaper at each feature touch' mechanism. Not in the work-status store (see alreadyFiled). The one soft spot is interpretive, not factual: ADR-0003 itself is explicitly 'not a refactoring mandate' (:356-365) and argues extract-when-the-second-use-case-exists (:71-97), so the proposal as written fights the cited ADR's posture — the band-tag erosion is real, but the ADR-consistent minimal remedy is a FILES.md retag plus documented erosion (the frontend CLAUDE.md 'File map' section names band drift as exactly the silent failure the map surfaces; ADR-0003 'Revisit when…' #3 at :325-329 covers a wrong-in-practice band). That goes to priority, not refutation.",
 "corrections": "Sharpenings, none load-bearing against existence: (1) The warning-hints table cite ':98-114' is off — the comment block is :97-112 and the PATH_TOOLTIPS table itself spans :113-156; it carries more than 'KataGo wire-framing semantics': reportAnalysisWinratesAs framing (:114-120), analysisAutoSave persistence/quota semantics (:121-129), and a long bundle-compression wire-format exposition (:130-155). (2) The candidate omits a second domain-vocabulary import: :10 imports BUNDLE_COMPRESSION_SCHEMES from src/types.ts ([B2] per FILES.md:46, itself noted as carrying B3 leakage), feeding PATH_ENUMS:84 and the :130-155 tooltip — the injection seam must carry it too. Also :8 imports SUPPORTED_LOCALES (i18n) feeding PATH_ENUMS:67; not Go-bound but still app vocabulary a fork would swap. (3) PATH_ENUMS also bakes in session-ui app vocabulary (analysisLayout :86, pvAnimation :87-88, qeuboToolbarView :89, boardVariations :90) — the whole table moves, consistent with the proposal's 'three tables' framing. (4) The mount roots are SettingsTab.vue:122 and :129 (store.profile.settings and store.session.ui), not App.vue — the editor's own comment at :52-54 claiming 'App.vue mounts the editor twice' is stale (predates the 39e200d feature-surface reorg) and should be corrected in the same change; engine/katago/types.ts:28 likewise still says 'components/RegistryEditor.vue' (now components/editors/). (5) isDynamicNode spans :25-41, not :25-39 (the endsWith check at :39 is correctly cited). (6) Priority counterweight: ADR-0003 explicitly declines preemptive extraction ('Not goals', :356-365; Sandi Metz rationale :71-97 — no second-domain consumer exists), so the cheaper closest-match correction for the verified [B1] erosion is a FILES.md:111 retag (e.g. [B1] with a named leak, or [B2]) plus a note against ADR-0003 'Revisit when…' #3 (:325-329); the provide/inject refactor only pays for itself if a fork materializes (ADR-0003 'Revisit when…' #1). The candidate should name the retag as the fallback delivery. (7) Risks section is right that no component tests exist for this surface — frontend/tests has no RegistryEditor coverage, and component/template tests are explicitly out of scope per frontend/CLAUDE.md's testing posture, so 'manual walk of the Settings tab' is the accurate verification plan, not a gap to fix in-PR.",
 "alreadyFiled": "No. The todo work-status DB (psql -h 192.168.122.1 -d todo) has no item matching registry/vocabulary/RegistryEditor; the only adjacent row is 'unified-user-controllable-scalar' (knob registry surface, state=closed), which concerns KnobRegistryEditor.vue ([B1], FILES.md:55), a different component.",
 "priorityHint": "lower"
}
````

### §4 · new-fit:registry-editor-vocabulary-injection

**Commission** (= `[SHARED: GEN-DISCIPLINE]` (+ `[SHARED: FORK-CONSTRAINT]` for fit) + the following + the candidate's JSON from §4's merge output):

````
You are an adversarial FIT/VALUE verifier. Try to REFUTE this candidate on judgment grounds: (1) does it fight a binding tenet or by-design discipline (read the ADRs it touches end to end) without naming the deviation — including ADR-0003's own caution against premature Port extraction (a planned fork justifies seam-NAMING; does it justify this candidate's full proposal)? (2) duplicate of a filed work-status item (psql -h 192.168.122.1 -d todo, read-only) without new signal? (3) honest cost/benefit for a correctness/auditability/generalization gain? (4) shaped to this codebase's vocabulary? Default to refuted=true only on concrete grounds.
````

**Result (verbatim):**

````json
{
 "refuted": true,
 "reasoning": "All in-tree factual claims verified (RegistryEditor.vue:9 imports WINRATE_FRAMINGS from engine/katago/types.ts:53 [B3 per FILES.md:259]; isDynamicNode endsWith 'overrideSettings' at :39 gating :224/:253/:286; PATH_ENUMS :63-91 with the reportAnalysisWinratesAs entry at :78; resolver comment :60-62; ADR-0003:130-131 names the registry editor Band 1; FILES.md:111 tags [B1]). The candidate is refuted on duplicate grounds (verifier question 2): the open work-status item `config-schema-projections` (state=open, disposition=future, tier=large, scope=frontend) carries a filed, adversarially-consulted design note — docs/notes/design/config-schema-projection-plan.md (2026-05-30, 903 lines, read end to end; consult appendix at docs/notes/consult/opus-consult-2026-05-30-config-schema-refactor.md) — that subsumes this candidate as a strict subset of its Phases 0-1. The note's §1.3(a) already enumerates the hard-coded tables (and finds FOUR, including getFieldType at :162-169, which the candidate missed); §9 Phase 1 is verbatim 'Rewrite RegistryEditor.vue to render schema nodes. Delete PATH_ENUMS / PATH_TOOLTIPS / isDynamicNode / getFieldType'; §8 already states the candidate's ADR-0003 thesis ('schema framework is band-1, domain-agnostic; the instance is band-3; a port authors a new schema instance') and explicitly anticipates the general-education expansion that is the maintainer's flash-card fork; §3.2 already cites the candidate's key evidence (the :60-62 comment) and resolves it with a dynamic EnumSource resolver — which the candidate's static injected table does not, making that evidence citation an overreach (the comment points at a resolver seam, not a relocated static table). The candidate's evidence claim 'not in any miner finding or confirmed candidate' is false with respect to the store. On question 3 (honest cost/benefit): the 'moderate-small' estimate omits costs the filed note documents — author-facing comments at engine/katago/types.ts:27, winrate-framing.ts:64, and types.ts:852/989/1180/1183/1205 directing contributors to extend PATH_ENUMS (§10.9, 'documentation is part of the work'); the latent 'bindings' suffix over-match catching the keybindings map (§10.4); the non-editor updateRegistry caller useDirtyBoardGuard.ts:92; and the label-convention question (raw camelCase keys vs i18n) the injected-tables shape leaves untouched. On question 1 (ADR-0003, read end to end): the fork constraint does flip Revisit-when #1, so extraction is no longer premature in principle — but this candidate's particular extraction (an ad-hoc provide/inject config object) is differently shaped from the filed schema design for the same seam, so shipping it first creates exactly the wrong-abstraction churn ADR-0003 cautions about: Phase 0/1 would delete and re-do it. The fork justifies raising the filed item's priority, not filing a parallel, weaker-shaped abstraction.",
 "corrections": "Salvageable new signal, best routed as amendments rather than a new item: (1) Band-drift fact not recorded anywhere — FILES.md:111 tags RegistryEditor.vue [B1] while :9 imports WINRATE_FRAMINGS from [B3] engine/katago/types.ts; per frontend/CLAUDE.md's FILES.md discipline this drift should be surfaced (cheap corrective: annotate/retag the FILES.md entry now, noting config-schema-projections Phase 1 as the structural fix). (2) Priority signal: the binding generic-fork constraint is the second consumer ADR-0003 Revisit-when #1 names, and the filed design note's §8 explicitly frames the schema-instance split as what 'a general-education expansion would lean on' — grounds to raise config-schema-projections from disposition 'future'. (3) Stale comment: RegistryEditor.vue:52-54 says 'App.vue mounts the editor twice'; the actual mounts are SettingsTab.vue:122 and :129. (4) Candidate factual fixes if retained in any form: the hints table spans :113-156 (not :98-114, which is mostly the comment block); the count is four baked vocabularies, not three — getFieldType (:162-169) hard-codes parent-key semantics for 'symbols'/'bindings'/'state_fns' plus a length>40 widget heuristic; and the :60-62 comment is evidence for a per-root dynamic resolver (which the filed design's EnumSource provides), not for a relocated static table.",
 "alreadyFiled": "config-schema-projections (todo DB: open/future/large/frontend; labels: refactor, architectural-cruft) — design note docs/notes/design/config-schema-projection-plan.md (Phases 0-1 subsume this candidate; §1.3(a), §8, §9, §10.4, §10.9), consult record docs/notes/consult/opus-consult-2026-05-30-config-schema-refactor.md. Related context: chess-clone (open/active/both) is the prior second-consumer framing.",
 "priorityHint": "drop"
}
````

### §4 · new-exist:split-keybindings-catalog-from-substrate

**Commission** (= `[SHARED: GEN-DISCIPLINE]` (+ `[SHARED: FORK-CONSTRAINT]` for fit) + the following + the candidate's JSON from §4's merge output):

````
You are an adversarial EXISTENCE/EVIDENCE verifier. Try to REFUTE this candidate on factual grounds: does the claimed current state hold at HEAD (read the named files/sections; run git log where commits are cited)? Is the load-bearing evidence real and correctly characterized (read cited docs end to end)? Already fixed? If it survives with details off, refuted=false + sharpenings in corrections. Default to refuted=true only on concrete grounds.
````

**Result (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Every load-bearing claim verified at HEAD. (1) frontend/src/lib/keybindings.ts imports useNavigation (:41), activeBoard+store (:42), analysisService (:43); module-scope useNavigation() at :119; ponder handler dispatches analysisService.isPondering/stopPonderOnBoard/analyzeActiveNode at :201-204. (2) FILES.md:311 tags it [B1] with the 'Keybindings registry substrate' description naming exactly the four helpers; FILES.md:312 covers keybindings-capture.ts ([B1], no domain-service imports — vue ref, store, types, ./keybindings at :40-48); FILES.md:315 timing.ts carries the verbatim 'Structurally B1 — imports nothing domain-specific — though §7 catalogs engine-coupled, band-2/3 timing values' parenthetical, the precedent the candidate cites; keybindings.ts fails that structural half. (3) ADR-0003 read end to end: Band 1 = truly domain-agnostic; engine/katago coupling is Band 3, so the [B1] tag on the fused file is drift under frontend/CLAUDE.md's band-drift rule. (4) The persisted-id risk resolves to confirmed, not hypothetical: keybindings.ts:9-11 declares the id 'the contract with persisted user overrides on AppSettings.keybindings', and keybindings-capture.ts setBinding/resetBinding write store.profile.settings.keybindings keyed by those literal strings — the split must be pure code motion on the ids. (5) Not already fixed or in flight: git log on the file shows only the phase commits (7c69abc, 0f09b70, 1affccb), no split branches. The fusion is real, current, and correctly characterized; only peripheral details need sharpening.",
 "corrections": "(a) currentState's 'domain-free machinery' overcounts by one helper: isActionEnabled (keybindings.ts:306-312) reads activeBoard.value and store.engine.status — it is the in-code embodiment of the enabledWhen seam the proposal names, not substrate-pure today. effectiveKey, normalizeKey, validateKeybindingsRegistry are genuinely domain-free. (b) Consumer count: five src importers, not four — useUserIORegistry.ts:41-47, useAppBootstrap.ts:32, KeybindingsView.vue:18-23, KeybindingRow.vue:35, and the sibling keybindings-capture.ts:43-48 — plus three test files (tests/unit/lib/keybindings.test.ts:28, tests/unit/lib/keybindings-capture.test.ts:23, tests/integration/useUserIORegistry.test.ts:55). Post-split, validateKeybindingsRegistry must take the registry as a parameter (today it closes over KEYBINDINGS_REGISTRY, :323-341), so useAppBootstrap's call site changes shape, not just its import path. (c) The risks section's 'if' resolves to yes: ids ARE threaded through persisted state (keybindings.ts:9-11; keybindings-capture.ts setBinding writes store.profile.settings.keybindings[actionId]). State it as confirmed. (d) Framing sharpening: ADR-0003 is explicitly 'not a refactoring mandate' (its Not-goals section), so the proposal should not lean on the ADR as mandating the split; the live defect at HEAD is FILES.md band-tag drift (frontend/CLAUDE.md 'File map': drift is the named silent-failure mode). The cheap conformant alternative — retag-only to band-mixed — should be named and rebutted explicitly: it forfeits the substrate's B1 discoverability, which is the actual argument for the split, and the open chess-clone work item (todo DB, state open, scope both) grounds the fork motivation concretely. (e) Ride-along fix: keybindings.ts's Phase-1 header (:31-36) still claims useUserIORegistry 'keeps its hardcoded switch'; Phase 2 shipped and the dispatcher consumes the registry (per useUserIORegistry.ts's own header). The stale header should be corrected in the same motion as the split. (f) Trivial: the file lives at frontend/src/lib/keybindings.ts (candidate writes 'lib/keybindings.ts'); its own header self-describes as 'User-configurable keybindings substrate' (:2-3) — the 'Keybindings registry substrate' phrasing is FILES.md:311's.",
 "alreadyFiled": "No. The todo work-status store has no item mentioning keybindings at all (ILIKE search over id/title/description returned zero rows), and no item covering the band fusion or a lib split. The nearest items are chess-clone (open/active, scope both — the fork that motivates the candidate, but it does not name this file) and offload-layout-to-libraries (unrelated). FILES.md:316's 'lib/utils merger flagged separately' note concerns lib/utils.ts, not keybindings.",
 "priorityHint": "keep"
}
````

### §4 · new-exist:review-scoring-extraction-named-seam

**Commission** (= `[SHARED: GEN-DISCIPLINE]` (+ `[SHARED: FORK-CONSTRAINT]` for fit) + the following + the candidate's JSON from §4's merge output):

````
You are an adversarial EXISTENCE/EVIDENCE verifier. Try to REFUTE this candidate on factual grounds: does the claimed current state hold at HEAD (read the named files/sections; run git log where commits are cited)? Is the load-bearing evidence real and correctly characterized (read cited docs end to end)? Already fixed? If it survives with details off, refuted=false + sharpenings in corrections. Default to refuted=true only on concrete grounds.
````

**Result (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Adversarial check at HEAD found every load-bearing claim accurate. (1) frontend/src/composables/review/useReviewSession.ts is exactly 656 lines; sgf.parse(card.sgf) is at :251; the per-color extra.{color}.deltas indexing with the s_1-fast-path-then-path-scan lookup spans :474-508; the loud-failure branch opens at :510 with the ADR-0002 comment at :511-519 explicitly documenting the prior silent-0.5 substitution that \"corrupted the Ebisu recall update\". (2) frontend/FILES.md:229 tags the file [B3]. (3) docs/adr/0003-frontend-portability-and-domain-boundaries.md (369 lines, read end to end): the Band 2 inventory entry is at :149-152, the band-mixed flag at :193-196 with the exact quoted sentence \"The orchestration is portable; the scoring extraction is not\", the named-isolated-function prescription at :230-234 (\"The Go-specific gating lives in a named, isolated function\"), and Revisit #3 at :325-329 names useReviewSession.ts explicitly. The ADR-vs-FILES.md band disagreement is real at HEAD. (4) Not already fixed: no review-scoring.ts or analogous extraction exists anywhere under frontend/src (engine/analysis/ holds only clustering.ts, filters.ts, stability-extractors.ts); git log on the file shows no extraction. (5) The distiller's tier claim holds: no tier-1 test touches the per-color indexing; coverage is tests/integration/useReviewSession.test.ts (fast-path test :334, s_0 path-scan test :394, loud-failure test :446) plus tests/e2e/review-session-harness.test.ts. (6) Work-status DB (todo, queried via psql): no item covers the scoring seam (searched id/title/description for useReviewSession, scoring, review); gradingparameter-opacity-typing is open/future as stated; refactoring-queue-adr0007 is open/active and mentions useReviewSession only as an over-budget file in an \"illustrative snapshot\". Unexpected bonus support: ADR-0003:270-273 independently sizes \"useReviewSession.ts's scoring extraction\" as the canonical Band-2 refactor-with-reuse case. Details that are off are sharpenings, not refutations (see corrections).",
 "corrections": "Four sharpenings. (a) The miner-attributed \"bare `as any` at :266\" is actually at :262 (`parsedBoard.id = bId as any; // Retain the tab ID`), it sits in loadCard — the card-load path, not the processUserMove scoring path the extraction targets — and it carries a (thin) inline comment, so \"bare\" overstates; drop or relocate this evidence item. (b) Line ranges: cite the indexing/lookup as :471-508 (the userColor/colorKey derivation at :471-472 belongs to what gets extracted) and the loud-failure branch as :510-529. (c) The proposed signature takes \"ledgerKeys\" as an input, implying the engine-band function calls ledger.getEnrichment itself — but engine/ currently has zero imports from services/ (verified by grep), so that would introduce a new engine→services dependency edge. Pass an enrichment accessor ((nodeId) => per-color extra entry) as a parameter instead; this also mirrors ADR-0003:230-234's own prescription (\"the persistence service takes the predicate as a parameter; it doesn't import it\") and keeps tier-1 testability without faking the ledger. (d) ADR-0003 names the file at its pre-move path `composables/useReviewSession.ts`; the actual path is `composables/review/useReviewSession.ts` — worth noting in the same PR that resolves the band disagreement, since the ADR's inventory path is stale too. Minor: \"reachable only through tier-3 integration tests\" should read \"tier-3 integration plus the e2e harness; nothing at tier 1\" — tests/e2e/review-session-harness.test.ts also exercises the path.",
 "alreadyFiled": "No work-status item covers the scoring-seam extraction (searched the todo DB items table over id/title/description for useReviewSession/scoring/review/seam). Adjacent open items, both correctly characterized by the candidate: gradingparameter-opacity-typing (open/future — typing of gradingParameter.data the SR composable reads; distinct concern) and refactoring-queue-adr0007 (open/active — names useReviewSession only as an ADR-0007 over-budget file, \"~664\" in an illustrative snapshot; the proposed extraction would incidentally shrink it, so the candidate's plan to add an evidence ref there is apt).",
 "priorityHint": "keep"
}
````

### §4 · new-fit:split-keybindings-catalog-from-substrate

**Commission** (= `[SHARED: GEN-DISCIPLINE]` (+ `[SHARED: FORK-CONSTRAINT]` for fit) + the following + the candidate's JSON from §4's merge output):

````
You are an adversarial FIT/VALUE verifier. Try to REFUTE this candidate on judgment grounds: (1) does it fight a binding tenet or by-design discipline (read the ADRs it touches end to end) without naming the deviation — including ADR-0003's own caution against premature Port extraction (a planned fork justifies seam-NAMING; does it justify this candidate's full proposal)? (2) duplicate of a filed work-status item (psql -h 192.168.122.1 -d todo, read-only) without new signal? (3) honest cost/benefit for a correctness/auditability/generalization gain? (4) shaped to this codebase's vocabulary? Default to refuted=true only on concrete grounds.
````

**Result (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "(1) ADR-0003 (read end to end): not fought. The ADR's \"Not goals\" says it is not a refactoring mandate, but its Revisit-when #1 explicitly names a second domain adopter as the trigger that \"flips the cost-benefit\" — the binding maintainer constraint (generic flash-card fork) instantiates exactly that trigger. More importantly, the proposal is lighter than Port extraction: a file split (code motion of ACTIONS + KEYBINDINGS_REGISTRY + the module-scope nav closure), parameterizing validateKeybindingsRegistry, and one deliberate seam (enabledWhen). That seam's predicate-function option matches ADR-0003's own sanctioned worked example verbatim (\"the persistence service takes the predicate as a parameter; it doesn't import it\" — §What this means for the analysis-recording feature), and avoids the shapes the ADR rejects (no strategy registry, no multi-implementation Port). The in-file comment at keybindings.ts:50-53 already plans promoting enabledWhen to a predicate function when a fourth case arrives, so the seam composes with recorded authoring intent. The design note (docs/archive/notes/design/keybindings-plan.md, status `design-note: implemented`, read end to end) deliberately co-locates decl-with-handler (§Dispatch architecture) but takes no position on a substrate-vs-catalog file boundary — no by-design fusion is being fought, and the candidate's split preserves the decl/handler co-location the plan does commit to. The Band-2 definition in ADR-0003 explicitly notes language flashcards lie outside the game-tree class, which strengthens the candidate: the nav.* handlers are tree-coupled and a flash-card fork likely replaces the whole catalog, keeping only the substrate. (2) No duplicate: zero rows in the todo store for keybind across title/description; nearest neighbors are chess-clone (Band-3 chess port, gated on an external contributor — different scope) and refactoring-queue-adr0007 (file-size backlog — keybindings.ts at 341 lines isn't even in it). The band-fusion observation is new signal. (3) Cost/benefit honest: 5 importers total, 4 need re-pointing (useUserIORegistry.ts:47, KeybindingsView.vue:23-29, keybindings-capture.ts:43-48, useAppBootstrap.ts:32 via the parameterized validator; KeybindingRow.vue:35 imports only substrate exports and stays put) — the candidate's \"four consumer modules to re-point\" is exactly right. The persisted-id risk is real and correctly framed: AppSettings.keybindings is keyed by the literal id strings (keybindings.ts:26-29; plan §Storage shape), so code motion must preserve them — no migration needed. No perf claims made (ADR-0009 moot; the direct-method-reference handler shape at keybindings.ts:124-135 survives the move unchanged). (4) Vocabulary fits: bands, FILES.md retag discipline (frontend/CLAUDE.md §Updating FILES.md names band-drift as exactly the silent failure this surfaces), substrate/catalog, seam-design. Verified facts: imports at keybindings.ts:41-43, module-scope useNavigation at :119, ponder handler at :198-206 (analysisService calls at :201-204), store writes at :217 etc.; FILES.md:311 ([B1] \"Keybindings registry substrate: declarative ... catalog\"), :312, :315 (the timing.ts \"structurally B1\" precedent the candidate correctly invokes — keybindings.ts cannot claim that parenthetical because its imports ARE domain-specific).",
 "corrections": "Three refinements, none refuting. (a) The currentState sentence calling the FILES.md-named helpers \"domain-free machinery\" is overstated for two of the four: isActionEnabled reads activeBoard.value and store.engine.status directly (keybindings.ts:306-312), and the KeybindingEnabledWhen union (:55-58) bakes 'activeBoardExists'/'engineConnected' into the substrate's type vocabulary; validateKeybindingsRegistry closes over KEYBINDINGS_REGISTRY (:326) rather than taking it as a parameter. So the store import at :42 is load-bearing for the substrate half too, not only the catalog handlers — this strengthens the candidate's own \"the enabledWhen seam done lazily would re-create the fusion one level down\" point, but the proposal should be read as: only effectiveKey and normalizeKey are pure today; the substrate needs the predicate seam AND the validator parameterization, not just catalog code motion. (b) Incidental staleness worth folding in: keybindings.ts's header (:31) and keybindings-capture.ts's header (:5) cite docs/notes/keybindings-plan.md, but the note now lives at docs/archive/notes/design/keybindings-plan.md (FILES.md:311 has the correct path); types.ts:110 also carries a prose cross-reference to lib/keybindings.ts that the split should keep accurate. (c) Band suggestion precision: the catalog is band-mixed, not uniformly one tag — nav.* handlers are Band 2 (tree-coupled), the ponder and ownership-overlay toggles Band 3-flavored values writing B1 store paths; either the candidate's [B2]/[B3] hedge or a single tag with a values-note mirroring the timing.ts parenthetical (FILES.md:315) is defensible. Consumer count for the record: five importers total, four to re-point, KeybindingRow.vue stays — the candidate's count is correct under the re-point reading.",
 "alreadyFiled": "No. The todo store has zero items mentioning keybindings (title or description). Nearest filed neighbors: chess-clone (open/active, \"Chess clone (domain port)\") — Band-3 wholesale-replacement port gated on an external contributor, does not cover the substrate/catalog file boundary; refactoring-queue-adr0007 — ADR-0007 file-size backlog, keybindings.ts (341 lines) is not in it and the motivation here is band coherence, not size.",
 "priorityHint": "keep"
}
````

### §4 · new-fit:review-scoring-extraction-named-seam

**Commission** (= `[SHARED: GEN-DISCIPLINE]` (+ `[SHARED: FORK-CONSTRAINT]` for fit) + the following + the candidate's JSON from §4's merge output):

````
You are an adversarial FIT/VALUE verifier. Try to REFUTE this candidate on judgment grounds: (1) does it fight a binding tenet or by-design discipline (read the ADRs it touches end to end) without naming the deviation — including ADR-0003's own caution against premature Port extraction (a planned fork justifies seam-NAMING; does it justify this candidate's full proposal)? (2) duplicate of a filed work-status item (psql -h 192.168.122.1 -d todo, read-only) without new signal? (3) honest cost/benefit for a correctness/auditability/generalization gain? (4) shaped to this codebase's vocabulary? Default to refuted=true only on concrete grounds.
````

**Result (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Attempted refutation on all four grounds; none holds. (1) Tenet fight: ADR-0003 (read end to end, 369 lines) does contain three clauses that superficially oppose this candidate — \"Do not preemptively extract Port abstractions\" (:51-52), \"No code change today... Existing code is not refactored against it\" (:309-311), and \"Not a refactoring mandate. Existing code stays put\" (:358-359). But the proposal is not a Port extraction: it is a named, isolated Go-bound function with the consumer keeping the orchestration — exactly the seam shape ADR-0003 itself prescribes for the analogous analysis-recording case (:230-234, \"The Go-specific gating lives in a named, isolated function\") and its \"seam is a property of the code's shape, not a documented interface contract\" framing (:251-257). The stays-put posture was written 2026-04-24 under the explicit condition \"no concrete second-domain consumer exists\" (:97); Revisit #1 (:317-321) names the second adopter as the trigger that \"flips the cost-benefit\" and makes \"the seams documented here... the natural extraction points,\" and the binding maintainer constraint states the flash-card fork is that second consumer. Revisit #3 (:325-329) names useReviewSession as the canary, and the trigger evidence is real and verified: ADR-0003 inventories the composable Band 2 (:149-152) and band-mixed (:193-196) while FILES.md:229 tags it [B3]. So the candidate asks for the conservative end of what the ADR's own revisit machinery now licenses, and it names the machinery it invokes. (2) Duplicate: DB probed read-only (psql todo, items/refs schema). No item covers the scoring seam. refactoring-queue-adr0007 (open/active) lists useReviewSession (~664) only as ADR-0007 size backlog with a handled-on-touch posture; gradingparameter-opacity-typing (open/future) covers only the Record<string,any> typing of the gradingParameter read at useReviewSession.ts:324-325. The band-disagreement signal is new. (3) Cost/benefit: honest. The regression-net claim is substantiated — tests/integration/useReviewSession.test.ts explicitly covers the delta path-scan, including \"finds the delta on the s_0 packet via the path-scan when s_1 lacks it\" (:394) — and the risk section correctly names the load-bearing scan order (the inline comments at useReviewSession.ts:489-498 confirm s_1-fast-path-then-path-scan is deliberate). The proposed home matches an existing pattern: src/engine/analysis/ already holds pure [B3] utilities (clustering.ts, filters.ts, stability-extractors.ts; FILES.md:250-253), and the engine band is currently services-clean (grep: no engine→services imports). (4) Vocabulary: bands, seams, named-function-not-Port, tier-1/tier-3, FILES.md retag cadence — all native to this codebase. Tree claims verified: sgf.parse at :251, per-color indexing at :474-487, lookup at :499-508, ADR-0002-citing loud-failure branch at :510-529, 656 lines, FILES.md:229 [B3]. The candidate survives with corrections (overstatement of what one extraction buys, and a tier-1 design condition) recorded below.",
 "corrections": "Four corrections, none disqualifying. (a) Scope of the band claim: extracting the scoring alone does not make the composable [B2]. The residual file keeps several Go-bound touchpoints beyond scoring — loadCard's sgf.parse + loadSgf (:251-252), applyGoMove answer-capture (:302, :541), the gtpToBoard best-move follow-through (:537-544), and the two-leg analyzeRange/waitForAnalysis shape (:376-418). Per ADR-0003's own Band 2 definition (:135-141), Band 2 explicitly does NOT survive a port to the flashcard class (\"no game tree and no 'turn'\"), so \"the SR orchestration the fork shares\" should be framed as the queue/state-machine/abort/submission skeleton specifically, and the extraction as naming the FIRST (and ADR-flagged) of roughly four Go seams in the file — the proposal's \"retag or annotate\" hedge should resolve to annotate (band-mixed reality), not a clean retag. (b) Tier-1 condition: the lookup reads ledger.getEnrichment (services layer); the engine band is currently services-clean. The extracted function must take the enrichment read as an injected accessor (matching ADR-0003's takes-the-predicate-as-a-parameter idiom, :233-234) rather than importing the ledger, or the tier-1 claim weakens to tier-2-with-fakes. The sketched signature should carry the accessor explicitly, not just ledgerKeys. (c) The item should explicitly name ADR-0003 :309-311 / :358-359 (\"existing code stays put\" / \"not a refactoring mandate\") as the posture being superseded by Revisit #1 + #3, and the arc should consider touching ADR-0003's inventory itself — the ADR's own Negative consequence (:302-305) anticipates inventory refresh, and a FILES.md-side fix alone leaves the ADR's side of the disagreement on the record. (d) Minor line/attribution fixes: the `as any` is at :262 not :266, and it carries a terse justifying comment (\"Retain the tab ID\"), so \"bare\" slightly overstates; the \"symmetric read\" the inline comment at :478-480 attributes to useEnrichedData.ts actually lives at composables/analysis/enriched-accumulator.ts:174-175 — the implementer should verify that mirror reference rather than trust the comment.",
 "alreadyFiled": "No duplicate. Probed the todo DB read-only: no item names the useReviewSession scoring seam or the ADR-0003-vs-FILES.md band disagreement. Adjacent items confirmed as the candidate describes: refactoring-queue-adr0007 (open/active) — ADR-0007 size backlog listing useReviewSession ~664, handled-on-touch, no named targets; an evidence ref is appropriate. gradingparameter-opacity-typing (open/future) — typing of the gradingParameter read at :324-325, distinct concern. One adjacent item the candidate's likelyAlreadyFiled missed: chess-clone (open/active) — the ADR-0003 domain-port item (\"replace the ~30-40% Band-3 frontend surface\"); it does not enumerate this seam, but the new item should cross-ref it, since a named scoring seam serves both that port and the flash-card fork.",
 "priorityHint": "keep"
}
````

### §4 · new-exist:mechanize-band-conformance-check

**Commission** (= `[SHARED: GEN-DISCIPLINE]` (+ `[SHARED: FORK-CONSTRAINT]` for fit) + the following + the candidate's JSON from §4's merge output):

````
You are an adversarial EXISTENCE/EVIDENCE verifier. Try to REFUTE this candidate on factual grounds: does the claimed current state hold at HEAD (read the named files/sections; run git log where commits are cited)? Is the load-bearing evidence real and correctly characterized (read cited docs end to end)? Already fixed? If it survives with details off, refuted=false + sharpenings in corrections. Default to refuted=true only on concrete grounds.
````

**Result (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Attempted refutation on five fronts; the candidate held on all of them. (1) The ADR quote: docs/adr/0003-frontend-portability-and-domain-boundaries.md read end to end; lines 299-301 are verbatim \"The principle is policy, not mechanism. A contributor who doesn't ask the question won't have the type system catch them. Like ADR-0002, the discipline lives in code review.\" (2) The 8-file baseline: every cited file:line verified exactly at HEAD — main.ts:10 (engine/sgf-writer), components/charts/ColorDebugStrip.vue:9 (engine/suggestion-colors), components/chrome/SidebarWidget.vue:13 (import type BoardSnapshot from engine/board-geometry), components/editors/RegistryEditor.vue:9 (engine/katago/types), composables/useQeubo.ts:87 (engine/util), composables/auth-app/useAppBootstrap.ts:30 (engine/suggestion-colors), services/resource-service.ts:18 (engine/suggestion-colors), store/archived-migrations.ts:29 (engine/util). I independently re-ran the sweep mechanically over all ~70 [B1]-tagged rows in frontend/FILES.md (read in full): exactly those 8 files import from engine/* — no misses. All cited FILES.md rows confirmed at the cited lines (44, 81, 97, 111, 148, 175, 278, 283; timing.ts split note at 315). (3) Already mechanized? No: frontend/eslint.config.js read end to end (365 lines) — import boundaries cover wire types (backend.ts) and four effectful service singletons in components; no band-aware rule; .github/workflows/ has only frontend-ci, doc-graph-ci, cochange-advisory-ci. The tools/doc-graph precedent is real (tools/doc-graph/generate.mjs + doc-graph-ci.yml). (4) Already filed? The todo work-status store has no item matching band/FILES.md/conformance/lint/cast/deny-by-default/eslint/ADR-0003; the chess-clone item (open, active, scope=both) confirms the \"fork makes B1 contractual\" trigger context. (5) Tag grammar: one bracketed tag per file row across the whole map, no [B?] rows today — regex-stable as claimed (parser must skip the legend and prose mentions like \"(B1)\" at line 287, which the candidate's fail-loud-on-ambiguity risk already anticipates). The substantive finding that did NOT refute but does reshape the cost section: the literal proposed rule band(file) >= band(import) over ALL imports fires far beyond the 8-file engine slice — I measured 49 import sites across ~37 [B1] files importing store/index.ts ([B3] at FILES.md:286) or types.ts ([B2] at FILES.md:46), e.g. main.ts:9, components/chrome/RootErrorBoundary.vue:21, lib/keybindings.ts:42, services/sync-service.ts:25. The candidate conceded B2-vs-B3 was unmeasured but not that B1-vs-B2/B3 outside engine/* dominates the count. That is a scoping/cost correction, not a refutation — the lesson, evidence, and the engine-slice baseline all stand.",
 "corrections": "1) Burn-down scope/cost is understated. \"~8 findings\" is the B1→engine/* slice only. Under the literal rule band(file) >= band(import), ~37 additional [B1] files (49 import sites, measured at HEAD) violate via the two hub nodes: store/index.ts is tagged [B3] (FILES.md:286) and types.ts [B2] with self-declared \"B3 leakage\" (FILES.md:46); nearly every B1 component/composable/lib file imports one or both (main.ts:9, RootErrorBoundary.vue:21, lib/keybindings.ts:42, services/sync-service.ts:25, composables/useQeubo.ts:89, ...). The proposal must settle at design time: scope the check to B3-leaf modules / engine/* (matching the measured baseline), or give band-mixed hub nodes an explicit exemption list alongside archived-migrations. Otherwise the warn baseline drowns. 2) The checker would force adjudication of FILES.md-vs-ADR-0003 tag disagreements on exactly those hubs: ADR-0003 places store/index.ts (lines 116-118), analysis-ledger.ts (121-125), and wait-for-analysis.ts (126-129) in Band 1 where FILES.md tags them [B3] (lines 286, 270, 172), and ADR-0003 puts engine/helper.ts's big_table in Band 3 (167-170) where FILES.md tags helper.ts [B1] (line 241). This is ADR-0003's own predicted \"inventory will drift\" negative (lines 302-306); the proposal should name FILES.md as the sole parse source and the drift as a known input. 3) Stale-row handling is a real, live requirement, not hypothetical: FILES.md:45 still lists jquery-bridge.ts [B1] but the file was deleted 2026-06-01 (commit 9949b28, \"drop dead jQuery\"). The checker must fail loudly on rows resolving to no file; FILES.md:348-351 already flags a \"future scripted check\" for coverage/existence as out-of-scope follow-up — uncited prior art the proposal composes with. 4) \"Imports from engine/*\" is a directory proxy, not a band predicate: engine/helper.ts is itself [B1] (FILES.md:241; only importer is suggestion-colors.ts:26, B3→B1, conformant). The real check keys on tags, not paths — the 8-file baseline happens to be unaffected today. 5) Minor cite fixes: proposal's \"SidebarWidget.ts:13\" should be SidebarWidget.vue:13; archived-migrations.ts has a second engine import (block from engine/analysis-config-curation ending at :28) besides :29; FILES.md:315 says timing.ts catalogs \"band-2/3\" timing values, not only B3. 6) Supporting color: eslint.config.js:129-133 names a deferred eslint-plugin-functional \"band convention\" purity-audit leg (explicitly not pre-committed) — adjacent naming precedent for a band-keyed lint scope, distinct from import conformance.",
 "alreadyFiled": "No. The work-status store (todo DB) has no item covering band conformance, FILES.md checking, or band-aware linting (searched title/id/description for band, FILES.md, conformance, lint, eslint, cast, deny, mechanize, ADR-0003). Nearest items: chess-clone (open/active — the fork trigger the lesson cites) and polymorphic-chart-renderer (open). Nearest in-repo precursor is FILES.md:348-351's flagged-as-follow-up \"future scripted check\" for file coverage/existence — adjacent, not band conformance.",
 "priorityHint": "keep"
}
````

### §4 · new-fit:mechanize-band-conformance-check

**Commission** (= `[SHARED: GEN-DISCIPLINE]` (+ `[SHARED: FORK-CONSTRAINT]` for fit) + the following + the candidate's JSON from §4's merge output):

````
You are an adversarial FIT/VALUE verifier. Try to REFUTE this candidate on judgment grounds: (1) does it fight a binding tenet or by-design discipline (read the ADRs it touches end to end) without naming the deviation — including ADR-0003's own caution against premature Port extraction (a planned fork justifies seam-NAMING; does it justify this candidate's full proposal)? (2) duplicate of a filed work-status item (psql -h 192.168.122.1 -d todo, read-only) without new signal? (3) honest cost/benefit for a correctness/auditability/generalization gain? (4) shaped to this codebase's vocabulary? Default to refuted=true only on concrete grounds.
````

**Result (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Attempted refutation on all four grounds; the candidate survives each.\n\n(1) Tenet fit — ADR-0003 read end to end (docs/adr/0003-frontend-portability-and-domain-boundaries.md, 369 lines). The quoted weakness is verbatim at lines 299-301. The probe \"does a planned fork justify the full proposal, not just seam-naming?\" resolves in the candidate's favor because the proposal is not Port extraction at all: it declares no interface, adds no generic parameter, imposes no TypeScript ergonomic cost (the cost argument at 0003:80-89 is about SPA code, not a Node CI script). It mechanizes conformance to the already-documented band map, with a retag escape hatch that makes it tag-honesty enforcement rather than a refactoring mandate — the ADR-0001 \"annotations must match reality\" philosophy that ADR-0003:348-351 explicitly extends to abstractions. Stronger still: ADR-0002 (read end to end) Rule 7 at 0002:185-189 explicitly names \"an ADR-0003 band tag\" among the closed vocabularies whose honest selection fail-loudly governs — the checker is the mechanization of that rule for the band-tag register (a citation the candidate missed; it strengthens the case). ADR-0003's own Negatives name both gaps the checker closes (policy-not-mechanism, 299-301; inventory drift, 302-305), and Revisit-when triggers #1 (second adopter — the binding flash-card-fork constraint; note ADR-0003:107-108 names \"language flashcards\" in the Band 1 survival test, so the fork makes B1 precisely the contractual boundary) and #2 (drift — the verified 8-file baseline is evidence) are both live. One real tension exists and is named in corrections rather than refuting: \"Not a refactoring mandate. Existing code stays put\" (0003:358-359) — the delivery should carry an ADR-0003 amendment, which the candidate omits.\n\n(2) Duplicate — work-status store queried read-only (psql -h 192.168.122.1 -d todo). No item matches band/conformance/lint/mechanize searches. Adjacent: adr-effectiveness-audits (open/active) carries the same \"ADRs are policy, not mechanism\" theme but is a periodic per-tenet audit-cadence item with no CI mechanism, no band content, no baseline — the candidate is a concrete mechanization instance with new signal (the verified 8-file baseline), not a duplicate. chess-clone (open/active) is the game-class port item; the generic-flashcard fork constraint is new signal beyond it.\n\n(3) Cost/benefit — honest. Every factual claim verified at the cited line: main.ts:10 (engine/sgf-writer; dev-only console exposure), ColorDebugStrip.vue:9 (engine/suggestion-colors; [B1] at FILES.md:81 — the mis-tag suspicion is plausible since the strip visualizes a KataGo-tuned gradient), SidebarWidget.vue:13 (type-only BoardSnapshot from engine/board-geometry; [B1] at FILES.md:97), RegistryEditor.vue:9 (WINRATE_FRAMINGS from engine/katago/types — genuine erosion in a \"[B1] Generic managed-registry editor\", FILES.md:111), useQeubo.ts:87 and archived-migrations.ts:29 (generateUUID from engine/util — the util re-home sibling), useAppBootstrap.ts:30 and resource-service.ts:18 (engine/suggestion-colors), timing.ts split note at FILES.md:315. Tag grammar mechanically confirmed: 215 [B1|B2|B3|B?] rows, zero malformed or doubled tags. The candidate is honest about unknowns (B2-vs-B3 unmeasured, type-only policy unsettled, intra-file mixing out of scope).\n\n(4) Vocabulary — fits exactly. The doc-graph precedent exists (tools/doc-graph/generate.mjs + cochange-advisory.mjs; committed, CI-gated artifact). The adoption arc (measured baseline → warn → ratchet to error, annotated exceptions with rationale) is the established shape in frontend/eslint.config.js: RCA-minted guards (the error-message reparse rule, RCA G1), \"0 hits at adoption → error\", and the honest-debt pattern for the four known layering violations. The fail-loud strict-grammar parser matches the work-status store's own ACL posture (tools/work-status/schema.sql header).",
 "corrections": "1. ADR amendment is part of the delivery, and the candidate omits it. Adoption satisfies ADR-0003 Revisit-when #1 (second adopter) and #2 (inventory drift), and partially retires the Negative at 0003:299-301 while contradicting \"Not a refactoring mandate / Existing code stays put\" (0003:309-311, 358-359) in letter. Per ADR-0002 Rule 6, append an amendment to ADR-0003 recording that the structural (import) half of band conformance is now mechanism, the content half stays review judgment, and the retag path preserves the no-mandate posture. Without naming this deviation the proposal would fight the ADR silently.\n\n2. Cite ADR-0002 Rule 7 (0002:185-189) in the filing: it already names \"an ADR-0003 band tag\" as a fail-loudly-governed closed-vocabulary choice. The checker is that rule's mechanization for this register — this is the strongest tenet anchor available and the candidate missed it.\n\n3. \"Both resolutions are wins, loudly\" is too neat. FILES.md:37-38 documents that borderline files are tagged for the dominant concern, so some [B1]-with-lower-band-import cases are legitimate under the existing legend, and a forced retag of a dominantly-agnostic file degrades the map's fork-sizing value. The annotated-exception list should be framed as encoding dominant-concern judgments (citing FILES.md's legend rule, not only the timing.ts value-vs-structure split at FILES.md:315), and the checker's docs should say the exception list is expected to be non-empty by design.\n\n4. New signal from verification — the FILES.md band legend itself needs tightening at adoption: FILES.md:26-27 defines [B1] as \"Would survive a chess port without code change\", but B2 also survives a chess port (FILES.md:30-32), so the legend's B1 test fails to discriminate B1 from B2. ADR-0003:105-111 defines Band 1 against any knowledge domain (\"language flashcards, music theory drills\") — the test that matters for the flash-card fork. If the tags become load-bearing, the legend's B1 line should state the any-domain test, or taggers will under-classify against the weaker chess criterion.\n\n5. The \"confirmed services-boundary-deny-by-default and cast-lint items\" are not in the work-status store (verified by query); their \"confirmed\" status is internal to this mining batch. If they are filed, this item should declare refs/deps on them, since the burn-down list is largely those siblings.\n\n6. Trivial: the proposal field writes \"SidebarWidget.ts:13\"; the file is components/chrome/SidebarWidget.vue:13 (correct elsewhere in the candidate). Also note main.ts:10's import is consumed only inside an `import.meta.env.DEV` block (window.Writer exposure) — relevant to the type-only/dev-only policy question the candidate already flags for settlement at adoption.\n\n7. Filing should ref adr-effectiveness-audits (open, active) — this candidate is the first concrete per-tenet mechanization under that item's theme, and the relationship should be visible in the store rather than discovered later.",
 "alreadyFiled": "No. Work-status store queried read-only (psql -h 192.168.122.1 -d todo): no item covers band conformance, band-aware lint, or FILES.md tag checking. Nearest items, neither a duplicate: adr-effectiveness-audits (open/active — periodic per-tenet audits, same policy-not-mechanism theme, no mechanism or baseline; should be a ref) and chess-clone (open/active — the game-class port; the generic-flashcard fork is new signal beyond it). The sibling \"services-boundary-deny-by-default\" and \"cast-lint\" candidates the evidence cites are not filed either.",
 "priorityHint": "keep"
}
````

### §4 · new-exist:reviewcard-canonical-content-vocabulary

**Commission** (= `[SHARED: GEN-DISCIPLINE]` (+ `[SHARED: FORK-CONSTRAINT]` for fit) + the following + the candidate's JSON from §4's merge output):

````
You are an adversarial EXISTENCE/EVIDENCE verifier. Try to REFUTE this candidate on factual grounds: does the claimed current state hold at HEAD (read the named files/sections; run git log where commits are cited)? Is the load-bearing evidence real and correctly characterized (read cited docs end to end)? Already fixed? If it survives with details off, refuted=false + sharpenings in corrections. Default to refuted=true only on concrete grounds.
````

**Result (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Every load-bearing claim verified at HEAD. (1) Core three sites exact: frontend/src/types.ts:1685 `readonly sgf: string` on ReviewCard (interface at 1683, under the \"Value Objects — SR domain\" section header at 1640); frontend/src/services/backend-service.ts:125 `sgf: raw.canonical_content` inside mapToReviewCard (signature at line 92, typed `raw: CardFromWire`); frontend/src/types/backend.ts:1133 `canonical_content: string` on CardWithRecall. (2) The post-34b comment is real: backend.ts:1095-1104 (\"Post-34b-Commit-3b: emits only canonical field names. The stale-client compat shims that synthesized `normalized_sgf` and top-level `default_visits` ... have been removed\"). (3) All three consumer sites exact: useReviewSession.ts:251 (`sgf.parse(card.sgf)`), useDirtyBoardGuard.ts:122 (`loadOrLog(targetBoardId, card.sgf, ...)`), card-tree-echarts.ts:249 (`getCardThumbnailSync(card.id, card.sgf)`). An exhaustive grep of src/ finds no other consumers — declaration + ACL + these three are the complete src footprint. (4) ADR-0003 read end to end: the quoted principle \"name its abstractions for the problem class, not the instance\" sits exactly at docs/adr/0003-frontend-portability-and-domain-boundaries.md:62-63 (the agnostic-band branch); the items-30a/30b/34 backend-generalization framing is ADR-0003's own Context (lines 16-22), corroborated by docs/archive/TODO-completed-2026-05-06.md lines 120 (34a) and 202 (34b: `normalized_sgf`→`canonical_content`, frontend fallback legs retired in commit 41a9c5d). (5) The risks paragraph correctly pre-names the ADR-0003 \"existing code stays put\" tension (stated three times: line 68, lines 309-311, lines 358-359) and conditions the rename on Revisit #1. (6) Todo DB probed: zero items match canonical/vocab/ReviewCard/canonical_content; refactoring-queue-adr0007 confirmed file-size-scoped (ADR-0007 arc); semantic-clarity-refactors-effect-typing covers a packet multiplexer and error-encoding unification, unrelated. Survives with three sharpenings (site count, one band tag, the Revisit-#1 premise being frame-supplied, not at HEAD) — none refuting.",
 "corrections": "Three sharpenings. (a) Cost undercount: the rename footprint is 5 src sites (types.ts:1685 declaration + backend-service.ts:125 + the three consumers), plus a test-tree tail the candidate omits: fixtures constructing `sgf:` in at least 5 test files (tests/integration/useReviewSession.test.ts:131, tests/integration/autonomous-srs.test.ts:104, tests/unit/composables/autonomous-srs-policies.test.ts:37, tests/e2e/review-session-harness.test.ts:221, tests/e2e/autonomous-srs-loop.test.ts:225) and tests/e2e/seed.ts (own `readonly sgf: string` at line 32 plus an outbound `raw_content: opts.sgf` mapping at line 48 — itself a nice corroborating instance of the asymmetry). Still mechanical and compiler/test-caught, but \"~4 code sites\" should read \"~5 src sites + ~6 test files\". (b) \"already engine-band call sites\" is two-thirds exact: per frontend/FILES.md, useDirtyBoardGuard.ts is [B3] and useReviewSession.ts is [B3], but card-tree-echarts.ts is [B2] — its Go interpretation is delegated to getCardThumbnailSync (src/composables/cards/useCardThumbnail.ts:46), whose parameter is named `cardSgf: string`; that helper signature is one more instance-vocabulary site the rename could touch or deliberately leave at the Go interpreter. (c) The Revisit-#1 justification is supplied by the fork-planning frame, not by anything at HEAD: ADR-0003's principle (lines 56-65) is scoped to \"When designing a new module\", and the repo records no second-domain adopter (docs/handoff-current.md:337 mentions the Chess-port question only as the authoring-time thought experiment). The candidate's fallback (types.ts comment + `future` work-status item) is the correct hedge if the fork premise weakens. Doc-sweep pointer: grep shows docs/adr/0002-fail-loudly.md:346 contains the string `raw.canonical_content ?? raw.normalized_sgf ?? raw.sgf` (not read end to end here — flagged as a grep pointer only), so the proposed pre-landing docs/ grep should cover prose citing the ACL mapping line, not just `ReviewCard.sgf`; the docs/archive/34b-* files also cite the mapping but are frozen archive and should be left untouched.",
 "alreadyFiled": "No. Items table probed directly (psql -h 192.168.122.1 -d todo): zero rows for id/title/description matching canonical%, vocab%, reviewcard%, canonical_content, or ReviewCard.sgf. The two adjacent open items are confirmed non-overlapping: refactoring-queue-adr0007 is the ADR-0007 file-size queue (App.vue contraction arc), and semantic-clarity-refactors-effect-typing covers the analysis-service packet multiplexer and error-encoding unification — neither touches field vocabulary.",
 "priorityHint": "keep"
}
````

### §4 · new-fit:reviewcard-canonical-content-vocabulary

**Commission** (= `[SHARED: GEN-DISCIPLINE]` (+ `[SHARED: FORK-CONSTRAINT]` for fit) + the following + the candidate's JSON from §4's merge output):

````
You are an adversarial FIT/VALUE verifier. Try to REFUTE this candidate on judgment grounds: (1) does it fight a binding tenet or by-design discipline (read the ADRs it touches end to end) without naming the deviation — including ADR-0003's own caution against premature Port extraction (a planned fork justifies seam-NAMING; does it justify this candidate's full proposal)? (2) duplicate of a filed work-status item (psql -h 192.168.122.1 -d todo, read-only) without new signal? (3) honest cost/benefit for a correctness/auditability/generalization gain? (4) shaped to this codebase's vocabulary? Default to refuted=true only on concrete grounds.
````

**Result (verbatim):**

````json
{
 "refuted": false,
 "reasoning": "Tested four refutation angles; the proposal survives on substance, but one finding requires a mandatory reframe. (1) By-design discipline: the strongest refutation candidate is not ADR-0003 but the 34b record itself. Keeping `ReviewCard.sgf` was an explicit, recorded design decision, not an accident: docs/archive/34b-frontend-brief.md:7-10 (\"Internal TypeScript type names (`ReviewCard.sgf`, `ReviewCard.defaultVisits`, etc.) **stay the same**\"), :26-28 (\"The frontend is NOT going domain-agnostic — you're still a Go app. We're just renaming the seam where you talk to the backend\"), :251-254; docs/archive/34b-complete-status.md:150-154 (\"your internal TypeScript type names ... are unchanged, as intended\"). The frontend half of 34b WAS finished as designed (Commit 2 plus the 34b-cleanup in commit 41a9c5d per docs/archive/TODO-completed-2026-05-06.md:202). So the candidate's title/lesson framing (\"finish the rename\", \"the instance name was reintroduced\") is historically wrong — nothing was reintroduced; retention was the intended end state. This does not refute the proposal, because the recorded decision's explicit premise (\"you're still a Go app\"; no second-domain consumer) is precisely what the binding fork constraint has invalidated — i.e., ADR-0003's Revisit #1 trigger (docs/adr/0003 lines 317-321: the second use case \"flips the cost-benefit\") has fired. A decision whose recorded premise no longer holds is legitimately revisitable; but the candidate must cite the decision it is overturning, not present design as accident. (2) ADR-0003 read end to end: the quoted principle at lines 62-63 is accurate; a field rename is seam-naming, not Port extraction, so the \"why not extract Ports preemptively\" caution (lines 71-97) is not implicated; the \"existing code stays put\" / \"Not a refactoring mandate\" posture (lines 67-69, 357-359) is the real tension and the candidate names it in risks with the correct Revisit-#1 justification. The types.ts exclusion from band classification (lines 213-215) excludes band-tagging, not field renames. (3) Duplicate: DB probe of items (title/id/description across canonical/sgf/vocabulary/rename/fork/domain/ReviewCard/canonical_content) returns no match; nearest are chess-clone (open/active — full domain port: PGN normalizer, Band-3 replacement, engine bridge; different scope and trigger) and refactoring-queue-adr0007 (file size only). Not filed. (4) Cost/benefit: benefit is real and fork-aligned — the wire is already generic and the domain type re-specializes exactly the field whose wire name the backend paid to generalize; the rename is compiler-driven and small. Cost is understated (~8 forced sites, not ~4; see corrections). All cited code claims verified: types.ts:1685, backend-service.ts:125, types/backend.ts:1133 (with the post-34b shim-removal comment at 1095-1104), useReviewSession.ts:251, useDirtyBoardGuard.ts:122, card-tree-echarts.ts:249. Vocabulary fit is good (ACL/domain-type/wire/seam framing; `canonicalContent` follows the existing camelCase wire-projection convention, cf. numMoves ← num_moves).",
 "corrections": "(1) MANDATORY reframe of title and lesson: drop \"finish the rename the backend already executed\" and \"the instance name was reintroduced\". The accurate frame: 34b deliberately retained `ReviewCard.sgf` as recorded design (docs/archive/34b-frontend-brief.md:7-10, 26-28, 251-254; docs/archive/34b-complete-status.md:150-154 — \"unchanged, as intended\"), premised explicitly on the frontend remaining a Go-only app with no second-domain consumer; the binding generic-fork constraint invalidates that premise, which is ADR-0003 Revisit #1 firing, making the retained name a now-stale decision to revisit rather than an unfinished rename. This frame is stronger for this codebase (recorded decision + invalidated premise + named revisit trigger) and is required for honesty — as filed, the candidate overturns a recorded by-design decision without knowing it exists. (2) Cost correction: ~8 forced sites, not ~4. Verified: 5 in src (types.ts:1685 declaration, backend-service.ts:125 ACL, useReviewSession.ts:251, useDirtyBoardGuard.ts:122, card-tree-echarts.ts:249) plus at least 3 ReviewCard-typed test fixtures (tests/unit/composables/autonomous-srs-policies.test.ts:37 — verified `makeStubCard(): ReviewCard`; tests/integration/useReviewSession.test.ts:131; tests/integration/autonomous-srs.test.ts:104). The two e2e sites (tests/e2e/review-session-harness.test.ts:221, tests/e2e/autonomous-srs-loop.test.ts:225) construct the local `SeedTestCardOptions` (tests/e2e/seed.ts:31-38), which maps to wire `raw_content` at seed.ts:48 — outside the forced blast radius, optional consistency rename only. Still small and mechanical under vue-tsc. (3) Docs note: the only prose citing `ReviewCard.sgf` lives in frozen archives (34b-frontend-brief.md, 34b-complete-status.md, TODO-completed-2026-05-06.md); archives record history and should not be edited — the rename PR should instead carry the supersession note in the types.ts doc comment. Field rename is a content-only doc change: no doc-graph structural regeneration, no FILES.md/IDENTIFIERS.md/FEATURES.md impact. (4) Add a scope sentence: ReviewCard also carries `defaultVisits`/`gamma` — instance vocabulary the 34b record names in the same breath — but those are ACL extractions from the intentionally domain-specific opaque `grading_parameter.data` blob (backend-service.ts:45-76, 137-138), so the generic-wire-field-respecialized asymmetry is crisp for `sgf` alone; stating this makes the sgf-only scope read as principled rather than arbitrary. (5) Minor: if a work-status item is filed, cross-reference `chess-clone` (the other ADR-0003-portability item) so the two fork-adjacent threads are linked.",
 "alreadyFiled": "No. Read-only probe of the todo DB items table (title/id/description ILIKE over canonical, sgf, vocabulary, rename, fork, generic, domain, canonical_content, ReviewCard) returned only: chess-clone (open/active — full domain port with engine bridge, different scope and trigger), rename-tag (unrelated UI feature), spa-board-scope-consistency-audit (unrelated). refactoring-queue-adr0007 tracks file size, not vocabulary. No item covers domain-type vocabulary alignment with the 34b wire rename.",
 "priorityHint": "keep"
}
````

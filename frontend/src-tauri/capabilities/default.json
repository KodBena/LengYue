{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for LengYue's main window: default core permissions plus the narrow shell-execute scope needed to spawn (and only spawn) the declared backend sidecar binary.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "shell:allow-execute",
      "allow": [
        {
          "name": "binaries/lengyue-backend",
          "sidecar": true
        }
      ]
    }
  ]
}
